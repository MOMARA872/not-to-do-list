// Phase 2 Wave 0 stub. Assertions land in Plan 02-07.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('HomeScreen unified list (LIST-06)', () {
    test(
      'TODO: Apps + Habits render in a single list sorted by recent '
      'activity (max of last_edit_time, last_pause_event_time, '
      'last_check_in_time); only icon source distinguishes the two kinds',
      () {
        // Implemented in Plan 02-07.
      },
      skip: 'Wave 0 stub — see Plan 02-07',
    );
  });
}
