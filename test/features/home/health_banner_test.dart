// Phase 2 Wave 0 stub. Assertions land in Plan 02-09.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('HealthCheckBanner (ONBD-06, REL-02)', () {
    test(
      'TODO: subdued amber MaterialBanner shows verbatim copy '
      '"Tracking is offline — tap to fix" when permissionHealthProvider '
      'reports any missing permission; tap routes to relevant funnel step',
      () {
        // Implemented in Plan 02-09.
      },
      skip: 'Wave 0 stub — see Plan 02-09',
    );
  });
}
