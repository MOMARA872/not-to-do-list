// Phase 2 Wave 0 stub. Assertions land in Plan 02-09.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'Build.FINGERPRINT change detection (ONBD-07)',
    () {
      test(
        'TODO: shared_preferences "last_known_fingerprint" mismatch with '
        'Build.FINGERPRINT triggers permission re-verification on next '
        'launch; matching fingerprint is a no-op',
        () {
          // Implemented in Plan 02-09.
        },
        skip: 'Wave 0 stub — see Plan 02-09',
      );
    },
  );
}
