// Phase 2 Wave 0 stub. Assertions land in Plan 02-07.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ScheduleEditor (LIST-09)', () {
    test(
      'TODO: start/end time pickers + 7 weekday chips; '
      '"Set schedule" toggle hides the controls when off (always-on default)',
      () {
        // Implemented in Plan 02-07.
      },
      skip: 'Wave 0 stub — see Plan 02-07',
    );
  });
}
