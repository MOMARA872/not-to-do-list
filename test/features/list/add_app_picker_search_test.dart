// Phase 2 Wave 0 stub. Assertions land in Plan 02-06.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AddAppPickerScreen search (LIST-01)', () {
    test(
      'TODO: case-insensitive substring match on display name; '
      'debounced; package name is NOT searched',
      () {
        // Implemented in Plan 02-06.
      },
      skip: 'Wave 0 stub — see Plan 02-06',
    );
  });
}
