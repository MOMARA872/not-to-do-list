// Phase 2 Wave 0 stub. Assertions land in Plan 02-07.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'EditEntryScreen (LIST-04, LIST-05, LIST-08, LIST-09)',
    () {
      test(
        'TODO: edit name/reason/category, soft|hard segmented control '
        '(Apps only), schedule editor toggle, delete cascades',
        () {
          // Implemented in Plan 02-07.
        },
        skip: 'Wave 0 stub — see Plan 02-07',
      );
    },
  );
}
