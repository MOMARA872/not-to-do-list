// Phase 6 Wave 0 RED stub — theme_segmented_button_test.dart
// Implementation in 06-03.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 6 / ThemeTile SegmentedButton (SETT-04)', () {
    test(
      '3 segments labelled Light, Dark, System',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-03',
        );
      },
    );

    test(
      'tap segment writes themeModeProvider',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-03',
        );
      },
    );

    test(
      'selected set has exactly one element at all times',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-03',
        );
      },
    );
  });
}
