// Phase 6 Wave 0 RED stub — export_json_envelope_test.dart
// Implementation in 06-04. Tests data.json envelope shape.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 6 / Export JSON envelope (SETT-01, D-11)', () {
    test(
      'envelope keys exactly: schemaVersion, exportedAt, appVersion, tables',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );

    test(
      'tables keys exactly the 5 table names',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );

    test(
      'appVersion format is "1.0.0+1" (versionName+versionCode)',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );
  });
}
