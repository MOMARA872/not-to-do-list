// Phase 6 Wave 0 RED stub — about_tile_test.dart
// Implementation in 06-03. Pitfall 7: format "1.0.0 (1)" not "1.0.0+1".
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 6 / AboutTile (PLAY-08)', () {
    test(
      'renders version in "1.0.0 (1)" format from PackageInfo (Pitfall 7)',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-03',
        );
      },
    );
  });
}
