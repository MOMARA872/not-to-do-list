// Phase 6 Wave 0 RED stub — reset_dialog_test.dart
// Implementation in 06-05. Tests AlertDialog body copy and button order.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 6 / Reset AlertDialog (SETT-02, D-13)', () {
    test(
      'AlertDialog body text equals verbatim D-13 copy',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-05',
        );
        // Verbatim copy from D-13:
        // "This deletes every entry, streak day, pause event, and check-in.
        //  This cannot be undone."
      },
    );

    test(
      'Cancel button is first in actions list (default-focused)',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-05',
        );
      },
    );

    test(
      'calm-tone source-grep finds no ! or ? in dialog body',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-05',
        );
      },
    );
  });
}
