// Phase 6 Wave 0 RED stub — settings_screen_test.dart
// Implementation in 06-03. Unskipped during GREEN flip in 06-03.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 6 / Settings screen (SETT-04, SETT-05, PLAY-08)', () {
    test(
      'tile order: Reminder → Appearance → Data → Privacy → About (D-04)',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-03',
        );
      },
    );

    test(
      'gear icon from HomeScreen routes to /settings',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-03',
        );
      },
    );

    test(
      'Phase 5 D-08 inline Streak section closure above Appearance',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-03',
        );
      },
    );
  });
}
