// Phase 6 Wave 0 RED stub — file_save_port_test.dart
// Implementation in 06-04. Tests FlutterFileDialogSavePort delegation.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 6 / FileSavePort (SETT-01)', () {
    test(
      'FlutterFileDialogSavePort delegates to FlutterFileDialog.saveFile '
      'with mimeTypesFilter=[application/zip]',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );

    test(
      'null return from saveFile treated as silent cancel (Pitfall 3)',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );
  });
}
