// Phase 6 Wave 0 RED stub — export_controller_test.dart
// Implementation in 06-04. Tests export ZIP round-trip.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Phase 6 / ExportController (SETT-01)', () {
    test(
      'ZIP contains 5 CSVs + data.json',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );

    test(
      'filename matches not-to-do-list-export-YYYYMMDD-HHMM.zip regex',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );

    test(
      'data.json schemaVersion equals 1',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );

    test(
      'data.json exportedAt is ISO-8601 UTC ending in Z',
      () {
        markTestSkipped(
          'RED stub — Phase 6 Wave 0; implementation in 06-04',
        );
      },
    );
  });
}
