// Phase 2 Wave 0 stub. Assertions land in Plan 02-08.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('OEM reactive fallback (ONBD-04)', () {
    test(
      'TODO: Build.MANUFACTURER ∈ {xiaomi, huawei, samsung, oppo, vivo, '
      'oneplus} surfaces vendor-specific guidance + dontkillmyapp.com '
      'link AFTER standard intent fails or user returns ungranted',
      () {
        // Implemented in Plan 02-08.
      },
      skip: 'Wave 0 stub — see Plan 02-08',
    );
  });
}
