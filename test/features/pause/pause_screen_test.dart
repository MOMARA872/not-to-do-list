// Phase 4 Plan 04-01 — Wave 0 stub for PauseScreen widget tests.
// Implementation lands in Plan 04-07.
//
// Note: This test file does NOT import any package:not_to_do_list/features/pause/
// symbol — those modules do not exist until Plan 04-07. All tests are skipped.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('PAUS-02 reason hero (D-02)', () {
    test(
      'placeholder',
      () {},
      skip: 'Plan 04-07 fills (PAUS-02 reason hero — italic/serif quote-card)',
    );
  });

  group('PAUS-02 empty-reason app-name hero (D-03)', () {
    test(
      'placeholder',
      () {},
      skip: 'Plan 04-07 fills (PAUS-02 empty-reason — blocked-app display '
          'name in M3 DisplayMedium, no quote-card chrome)',
    );
  });

  group('PAUS-09 hard entry omits Use anyway (D-06)', () {
    test(
      'placeholder',
      () {},
      skip: 'Plan 04-07 fills (PAUS-09 hard entry — Use anyway never '
          'rendered in widget tree, not just hidden)',
    );
  });

  group('PAUS-03 cooldown chips [1m,3m,5m,10m] (D-04)', () {
    test(
      'placeholder',
      () {},
      skip: 'Plan 04-07 fills (PAUS-03 cooldown SegmentedButton chips)',
    );
  });
}
