// Phase 2 Wave 0 stub. Assertions land in Plan 02-03.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AppPickerApi Pigeon channel (LIST-01)', () {
    test(
      'TODO: AppPickerApi.queryLaunchableApps() returns a List<AppInfo> '
      'filtered by AndroidManifest <queries> + LAUNCHER intent filter; '
      'channel decodes Pigeon-typed payload without MissingPluginException',
      () {
        // Implemented in Plan 02-03.
      },
      skip: 'Wave 0 stub — see Plan 02-03',
    );
  });
}
