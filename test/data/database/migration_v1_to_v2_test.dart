// Phase 2 Wave 0 stub. Assertions land in Plan 02-02.
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AppDatabase v1 → v2 migration (LIST-08, LIST-09)', () {
    test(
      'TODO: round-trip from drift_schemas/drift_schema_v1.json adds '
      'block_mode + schedule_* columns and preserves Phase 1 rows',
      () {
        // Implemented in Plan 02-02.
      },
      skip: 'Wave 0 stub — see Plan 02-02',
    );
  });
}
