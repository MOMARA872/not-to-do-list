<?xml version="1.0" encoding="utf-8"?>
<!--
    Phase 1 configuration. Policy-correct attributes only. Phase 4 implements the
    matching Kotlin service body, which MUST honor PLAY-02 (no autonomous-action
    AccessibilityService calls — see docs/play-declaration.md section 2 for the
    full list of forbidden API calls).

    EXPLICITLY OMITTED attributes (do NOT add): see docs/play-declaration.md
    section 6, "Manifest and configuration cross-reference", for the canonical
    list. The forbidden attribute names are kept out of this file so that the
    V9 absence-grep stays exact.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:isAccessibilityTool="false"
    android:accessibilityFlags="flagDefault"
    android:description="@string/a11y_service_description" />
