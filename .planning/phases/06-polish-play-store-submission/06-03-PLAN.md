---
phase: 06-polish-play-store-submission
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - lib/features/settings/pages/settings_screen.dart
  - lib/features/settings/widgets/section_header.dart
  - lib/features/settings/widgets/theme_tile.dart
  - lib/features/settings/widgets/about_tile.dart
  - lib/features/settings/widgets/streak_threshold_tile.dart
  - lib/features/home/pages/home_screen.dart
  - lib/core/router/app_router.dart
autonomous: true
requirements: [SETT-04, SETT-05, PLAY-08]

must_haves:
  truths:
    - "HomeScreen AppBar shows a Settings gear IconButton (Icons.settings, tooltip 'Settings') as the FIRST action, with the existing notifications IconButton preserved as second (D-01 + Phase 5 carry-forward)"
    - "Tapping the gear routes via GoRouter to /settings"
    - "SettingsScreen renders a sectioned ListView in order: Reminder → Streak → Appearance → Data → Privacy → About (Claude's Discretion option a.i adds Streak section closing Phase 5 D-08; D-04 order applies to the remaining 5 sections Reminder/Appearance/Data/Privacy/About)"
    - "Theme tile renders M3 SegmentedButton<ThemeMode> with 3 segments labelled Light/Dark/System; tap writes via themeModeProvider.notifier.set (D-05)"
    - "Streak threshold tile re-uses streakThresholdProvider and the existing Phase 5 _ThresholdDialog flow (closes Phase 5 D-08 promise)"
    - "About tile renders '{version} ({buildNumber})' via package_info_plus (PLAY-08 Pitfall 7 — no '+' glue)"
    - "GoRouter has 4 new routes registered: /settings (SettingsScreen), /settings/export (ExportScreen placeholder — body lands 06-04), /settings/privacy (PrivacyScreen placeholder — body lands 06-06), /settings/disclosure (AccessibilityStep(fromSettings: true) — fromSettings param lands 06-06)"
  artifacts:
    - path: "lib/features/settings/pages/settings_screen.dart"
      provides: "Settings hub ConsumerWidget with 6 sectioned ListView + 8 tiles + Reset AlertDialog handler (body literal in 06-05)"
      contains: "class SettingsScreen extends ConsumerWidget"
      min_lines: 120
    - path: "lib/features/settings/widgets/section_header.dart"
      provides: "M3 section label widget (labelSmall + cs.primary + letterSpacing 0.8)"
      contains: "class SectionHeader"
    - path: "lib/features/settings/widgets/theme_tile.dart"
      provides: "ConsumerWidget tile with inline SegmentedButton<ThemeMode>"
      contains: "SegmentedButton<ThemeMode>"
    - path: "lib/features/settings/widgets/about_tile.dart"
      provides: "FutureBuilder<PackageInfo> tile with em-dash placeholder + 'X.Y.Z (N)' format"
      contains: "FutureBuilder<PackageInfo>"
    - path: "lib/features/settings/widgets/streak_threshold_tile.dart"
      provides: "ConsumerWidget tile re-rendering Phase 5 streak threshold inline (closes Phase 5 D-08)"
      contains: "streakThresholdProvider"
    - path: "lib/features/home/pages/home_screen.dart"
      provides: "AppBar.actions list with Settings gear inserted as first IconButton"
      contains: "Icons.settings"
    - path: "lib/core/router/app_router.dart"
      provides: "4 new GoRoute entries registered"
      contains: "/settings"
  key_links:
    - from: "lib/features/home/pages/home_screen.dart"
      to: "/settings via GoRouter"
      via: "IconButton.onPressed → context.go('/settings')"
      pattern: "context\\.go\\('/settings'\\)"
    - from: "lib/features/settings/widgets/theme_tile.dart"
      to: "lib/features/settings/providers/theme_mode_provider.dart"
      via: "ref.watch + ref.read(themeModeProvider.notifier).set"
      pattern: "themeModeProvider"
    - from: "lib/features/settings/pages/settings_screen.dart"
      to: "/settings/export, /settings/privacy, /settings/disclosure, /settings/reminder"
      via: "ListTile.onTap → context.go(route)"
      pattern: "context\\.go\\('/settings/"
---

<objective>
Wave 2: Settings hub UI + Home AppBar gear icon + GoRouter route registration. Includes the Phase 5 D-08 closure (inline Streak threshold section per Claude's Discretion option a.i — recommended by RESEARCH §Streak Threshold Tile, lines 565-587).

Plan registers ALL 4 new `/settings/*` routes here so downstream plans 06-04, 06-05, 06-06 do NOT have to touch `app_router.dart` (eliminates Wave 2/3 file conflicts on the router). Export/Privacy screens are placeholder ConsumerWidgets with a single `Center(child: CircularProgressIndicator())` body until 06-04 / 06-06 replace them.

Purpose: Deliver the Settings IA + entry point. After this plan, a user can navigate Home → gear → see all tiles in the locked D-04 order; theme switching works end-to-end; Streak threshold UI honors the soft Phase 5 D-08 promise.
Output: 5 new feature files, 2 surgical edits (HomeScreen AppBar, app_router), 2 placeholder screens for downstream wire-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-play-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-play-store-submission/06-UI-SPEC.md
@.planning/phases/06-polish-play-store-submission/06-PATTERNS.md
@.planning/phases/06-polish-play-store-submission/06-RESEARCH.md
@.planning/phases/06-polish-play-store-submission/06-02-SUMMARY.md

<interfaces>
<!-- Canonical analogs the executor uses; read these files to see the existing patterns. -->

From lib/features/reminder/pages/reminder_settings_screen.dart (canonical ConsumerWidget Scaffold + ListView + ListTile shape, lines 14-57; also the Phase 5 _ThresholdDialog body that we're extracting/re-rendering inline in StreakThresholdTile):
```dart
class ReminderSettingsScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final reminderAsync = ref.watch(reminderTimeProvider);
    final thresholdAsync = ref.watch(streakThresholdProvider);
    final hm = reminderAsync.maybeWhen(data: (v) => v, orElse: () => 1260);
    final thresholdValue = thresholdAsync.maybeWhen(data: (v) => v, orElse: () => 5);
    final reminderDisplay = DateFormat.jm().format(DateTime(2000, 1, 1, hm ~/ 60, hm % 60));
    return Scaffold(
      appBar: AppBar(title: const Text('Daily reminder'), centerTitle: false),
      body: ListView(children: [ ListTile(...), ListTile(...) ]),
    );
  }
}
```

From lib/features/list/widgets/block_mode_segmented.dart (canonical SegmentedButton triad pattern):
```dart
SegmentedButton<String>(
  segments: const <ButtonSegment<String>>[
    ButtonSegment<String>(value: 'soft', label: Text('Soft')),
    ButtonSegment<String>(value: 'hard', label: Text('Hard')),
  ],
  selected: <String>{value},
  onSelectionChanged: (sel) => onChanged(sel.first),
);
```

From lib/features/home/pages/home_screen.dart (existing AppBar actions at lines 53-62 per 06-PATTERNS.md):
```dart
actions: [
  Semantics(
    label: 'Daily reminder settings',
    child: IconButton(
      icon: const Icon(Icons.notifications_outlined),
      tooltip: 'Daily reminder',
      onPressed: () => context.go('/settings/reminder'),
    ),
  ),
],
```

From lib/core/router/app_router.dart (existing GoRoute entries; APPEND 4 new routes after line 60):
```dart
GoRoute(path: '/settings/reminder', builder: (_, __) => const ReminderSettingsScreen()),
```

From lib/features/streak/providers/streak_threshold_provider.dart (read+write API the StreakThresholdTile binds to):
```dart
ref.watch(streakThresholdProvider) // AsyncValue<int>
ref.read(streakThresholdProvider.notifier).set(int minutes)
```

From lib/features/settings/providers/theme_mode_provider.dart (created in 06-02):
```dart
ref.watch(themeModeProvider) // AsyncValue<ThemeMode>
ref.read(themeModeProvider.notifier).set(ThemeMode mode)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SectionHeader + ThemeTile + AboutTile + StreakThresholdTile widgets</name>
  <files>lib/features/settings/widgets/section_header.dart, lib/features/settings/widgets/theme_tile.dart, lib/features/settings/widgets/about_tile.dart, lib/features/settings/widgets/streak_threshold_tile.dart</files>
  <read_first>
    - lib/features/list/widgets/block_mode_segmented.dart (SegmentedButton + small-widget convention)
    - lib/features/reminder/pages/reminder_settings_screen.dart (Streak threshold tile body + _ThresholdDialog at lines 47-53 + 100-135 — analog for the Streak section UI)
    - lib/features/settings/providers/theme_mode_provider.dart (created in 06-02)
    - lib/features/streak/providers/streak_threshold_provider.dart (Phase 5 provider; reused by StreakThresholdTile)
    - test/features/settings/theme_segmented_button_test.dart (RED stub from 06-01)
    - test/features/settings/about_tile_test.dart (RED stub from 06-01)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 270-395 — exact widget shapes for section_header, theme_tile, about_tile)
    - .planning/phases/06-polish-play-store-submission/06-UI-SPEC.md (§Component Inventory + §Typography + §Spacing)
    - .planning/phases/06-polish-play-store-submission/06-RESEARCH.md (§Streak Threshold Tile, lines 565-587 — option a.i recommended)
  </read_first>
  <behavior>
    - ThemeTile: renders SegmentedButton with 3 segments Light/Dark/System; reading provider state determines `selected:` set; selecting fires `set(sel.first)`; widget wrapped in a ListTile with title `Text('Theme')`
    - AboutTile: FutureBuilder<PackageInfo>; while loading shows em-dash `—` subtitle; resolved shows `'${version} (${buildNumber})'` (no `+` glue per Pitfall 7); title `Text('About')`; leading Icons.info_outline; no onTap (display-only)
    - SectionHeader: Padding(EdgeInsets.fromLTRB(16, 16, 16, 4)) wrapping Text(label) using theme.textTheme.labelSmall.copyWith(color: cs.primary, letterSpacing: 0.8)
    - StreakThresholdTile: ConsumerWidget reading `ref.watch(streakThresholdProvider)`; ListTile with title 'Streak threshold', subtitle `'Streak breaks after $thresholdValue min/day'`, trailing Icon(Icons.chevron_right); onTap opens a Phase 5-style threshold picker dialog (re-render the same Slider 1..60 UI from `reminder_settings_screen.dart` lines 100-135 inline OR extract to a shared `_ThresholdDialog` widget in this file)
  </behavior>
  <action>
    Create 4 widget files under `lib/features/settings/widgets/`.

    `section_header.dart`: pure StatelessWidget, single `final String label` field, `const` constructor with `required this.label, super.key`. Build returns `Padding(padding: const EdgeInsets.fromLTRB(16, 16, 16, 4), child: Text(label, style: theme.textTheme.labelSmall?.copyWith(color: theme.colorScheme.primary, letterSpacing: 0.8)))`. Matches UI-SPEC §Screen-Level Layout Specs §SettingsScreen and PATTERNS lines 270-313.

    `theme_tile.dart`: ConsumerWidget that reads `themeModeProvider` via `.maybeWhen(data: (m) => m, orElse: () => ThemeMode.system)`. Returns a `ListTile(title: const Text('Theme'), trailing: SegmentedButton<ThemeMode>(...))`. SegmentedButton uses `ButtonSegment<ThemeMode>(value: ThemeMode.light, label: Text('Light'))` plus dark and system in that order; `selected: <ThemeMode>{currentMode}`; `onSelectionChanged: (sel) => ref.read(themeModeProvider.notifier).set(sel.first)`. No `icon:` on segments (calm tone, label-only per UI-SPEC).

    `about_tile.dart`: StatelessWidget per RESEARCH §Example 5, lines 797-819. Imports `package:package_info_plus/package_info_plus.dart`. Build returns `FutureBuilder<PackageInfo>(future: PackageInfo.fromPlatform(), builder: ...)`. Subtitle string is em-dash `—` when not resolved; `'${snap.data!.version} (${snap.data!.buildNumber})'` when resolved (note parens, NOT `+` per Pitfall 7). ListTile leading Icons.info_outline, title Text('About'), no onTap.

    `streak_threshold_tile.dart`: ConsumerWidget closing Phase 5 D-08 promise per RESEARCH §Streak Threshold Tile option a.i (lines 567-585). Reads `ref.watch(streakThresholdProvider).maybeWhen(data: (v) => v, orElse: () => 5)`. Returns `ListTile(leading: Icon(Icons.timer_outlined), title: const Text('Streak threshold'), subtitle: Text('Streak breaks after $thresholdValue min/day'), trailing: const Icon(Icons.chevron_right), onTap: () => _showPicker(context, ref, thresholdValue))`. Implement `_showPicker` as a private helper on the widget that opens an AlertDialog containing a Slider(min: 1, max: 60, divisions: 59, value: ...) — mirror the existing _ThresholdDialog body from `reminder_settings_screen.dart` lines 100-135. On confirm, call `ref.read(streakThresholdProvider.notifier).set(value)`. Use `if (!context.mounted) return;` after the await.

    Unskip and implement the 3 RED widget tests (theme_segmented_button_test.dart, about_tile_test.dart, and the part of settings_screen_test.dart that exercises StreakThresholdTile rendering — full settings_screen_test.dart unskip lands in Task 3).
  </action>
  <verify>
    <automated>flutter test test/features/settings/theme_segmented_button_test.dart test/features/settings/about_tile_test.dart &amp;&amp; dart analyze lib/features/settings/widgets/</automated>
  </verify>
  <acceptance_criteria>
    - 4 widget files exist under `lib/features/settings/widgets/`
    - `section_header.dart` source contains `EdgeInsets.fromLTRB(16, 16, 16, 4)` and `labelSmall`
    - `theme_tile.dart` source contains `SegmentedButton<ThemeMode>` AND `ButtonSegment<ThemeMode>(value: ThemeMode.light` AND `ref.read(themeModeProvider.notifier).set(`
    - `about_tile.dart` source contains `FutureBuilder<PackageInfo>` AND the format `'${snap.data!.version} (${snap.data!.buildNumber})'` (parens, no `+` glue per Pitfall 7)
    - `streak_threshold_tile.dart` source contains `streakThresholdProvider` AND `Slider(`
    - `flutter test test/features/settings/theme_segmented_button_test.dart` exits 0 with tests passing
    - `flutter test test/features/settings/about_tile_test.dart` exits 0 with tests passing
    - `dart analyze lib/features/settings/widgets/` reports 0 errors/warnings
  </acceptance_criteria>
  <done>4 widgets compose-ready for the SettingsScreen ListView; theme + about + streak tile widget tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettingsScreen + GoRouter route registration (4 new routes) + placeholder Export/Privacy screens</name>
  <files>lib/features/settings/pages/settings_screen.dart, lib/features/settings/pages/export_screen.dart, lib/features/settings/pages/privacy_screen.dart, lib/core/router/app_router.dart</files>
  <read_first>
    - lib/features/reminder/pages/reminder_settings_screen.dart (canonical sectioned ListView analog)
    - lib/features/settings/widgets/section_header.dart (just created in Task 1)
    - lib/features/settings/widgets/theme_tile.dart (Task 1)
    - lib/features/settings/widgets/about_tile.dart (Task 1)
    - lib/features/settings/widgets/streak_threshold_tile.dart (Task 1)
    - lib/core/router/app_router.dart (existing routes, lines 46-60)
    - lib/features/reminder/providers/reminder_providers.dart (reminderTimeProvider — for Reminder tile subtitle)
    - test/features/settings/settings_screen_test.dart (RED stub from 06-01)
    - .planning/phases/06-polish-play-store-submission/06-UI-SPEC.md (§Screen-Level Layout Specs + §Copywriting Contract + §Route Inventory)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 103-194 + lines 556-587 — settings_screen and app_router patterns)
    - .planning/phases/06-polish-play-store-submission/06-CONTEXT.md (D-04 tile order + Claude's Discretion #1)
  </read_first>
  <behavior>
    - Test 1: SettingsScreen ListView contains 6 SectionHeader widgets with labels exactly Reminder, Streak, Appearance, Data, Privacy, About (in that order — Claude's Discretion option a.i adds Streak section closing Phase 5 D-08; D-04 order applies to the remaining 5 sections Reminder/Appearance/Data/Privacy/About)
    - Test 2: SettingsScreen contains 8 ListTile destinations: reminder tile (onTap → /settings/reminder, subtitle formatted via DateFormat.jm()), StreakThresholdTile, ThemeTile, export tile (onTap → /settings/export), reset tile (onTap → AlertDialog — body literal land in 06-05), privacy tile (onTap → /settings/privacy), accessibility disclosure tile (onTap → /settings/disclosure), AboutTile
    - Test 3: Reset tile uses `cs.error` for `iconColor` and `textColor`; the reset AlertDialog in this plan can be a thin placeholder that calls `Navigator.pop(context)` on both buttons — full ResetController wiring is 06-05's job
    - Test 4: GoRouter resolves /settings to SettingsScreen, /settings/export to ExportScreen, /settings/privacy to PrivacyScreen, /settings/disclosure to AccessibilityStep (with `fromSettings: true` constructor arg — the param itself lands in 06-06; the route registration uses it now, which will be a temporary `const AccessibilityStep()` until 06-06)
  </behavior>
  <action>
    Create `lib/features/settings/pages/settings_screen.dart` as `class SettingsScreen extends ConsumerWidget`. Reads `reminderTimeProvider` for the Reminder tile subtitle. Body is a `ListView` with `padding: EdgeInsets.symmetric(vertical: 8)`. Children in EXACT order (Claude's Discretion option a.i adds Streak section closing Phase 5 D-08; D-04 order applies to the remaining 5 sections):

    1. `SectionHeader(label: 'Reminder')`
    2. ListTile (Daily reminder) — leading Icons.notifications_outlined, title 'Daily reminder', subtitle formatted reminder display via `DateFormat.jm().format(...)`, trailing chevron, onTap → `context.go('/settings/reminder')`
    3. `SectionHeader(label: 'Streak')`
    4. `const StreakThresholdTile()`
    5. `SectionHeader(label: 'Appearance')`
    6. `const ThemeTile()`
    7. `SectionHeader(label: 'Data')`
    8. ListTile (Export data) — leading Icons.download_outlined, title 'Export data', subtitle 'Saves a ZIP file to your chosen location' (UI-SPEC copy lock), trailing chevron, onTap → `context.go('/settings/export')`
    9. ListTile (Reset all data) — leading Icons.delete_outline with `iconColor: cs.error`, title 'Reset all data' with `textColor: cs.error`, subtitle 'Permanently delete all entries and history', onTap → `_showResetDialog(context, ref)` (private helper; placeholder AlertDialog in this task — body literal + actual reset flow lands in 06-05)
    10. `SectionHeader(label: 'Privacy')`
    11. ListTile (Privacy Policy) — leading Icons.privacy_tip_outlined, title 'Privacy Policy', trailing chevron, onTap → `context.go('/settings/privacy')`
    12. ListTile (Accessibility disclosure) — leading Icons.accessibility_outlined, title 'Accessibility disclosure', trailing chevron, onTap → `context.go('/settings/disclosure')`
    13. `SectionHeader(label: 'About')`
    14. `const AboutTile()`

    Scaffold AppBar: `AppBar(title: const Text('Settings'), centerTitle: false)` (UI-SPEC §Copywriting).

    `_showResetDialog` placeholder: opens an `AlertDialog(title: Text('Reset all data'), content: Text('TODO(06-05): body copy literal'), actions: [TextButton(Cancel → Navigator.pop), FilledButton(Reset → Navigator.pop)])`. Mark with `// TODO(06-05): wire ResetController + verbatim body copy per D-13` so 06-05 can locate the seam. The literal body copy + ResetController invocation is 06-05's deliverable.

    Create `lib/features/settings/pages/export_screen.dart` and `lib/features/settings/pages/privacy_screen.dart` as placeholder `class XxxScreen extends StatelessWidget` returning `Scaffold(appBar: AppBar(title: Text(title)), body: const Center(child: CircularProgressIndicator()))` plus `// TODO(06-04): body lands in export plan` and `// TODO(06-06): body lands in privacy plan` comments respectively. This locks route registrations in this wave without forcing 06-04 / 06-06 to touch app_router.dart.

    Edit `lib/core/router/app_router.dart`:
    - Add imports: `lib/features/settings/pages/settings_screen.dart`, `lib/features/settings/pages/export_screen.dart`, `lib/features/settings/pages/privacy_screen.dart`
    - APPEND 4 GoRoute entries after the existing `/settings/reminder` entry (line 58):
      - `GoRoute(path: '/settings', builder: (_, __) => const SettingsScreen())`
      - `GoRoute(path: '/settings/export', builder: (_, __) => const ExportScreen())`
      - `GoRoute(path: '/settings/privacy', builder: (_, __) => const PrivacyScreen())`
      - `GoRoute(path: '/settings/disclosure', builder: (_, __) => const AccessibilityStep())` — 06-06 will modify this to pass `fromSettings: true` once that constructor param lands. Mark with `// TODO(06-06): pass fromSettings: true after param lands`.
    - Use `// ignore: lines_longer_than_80_chars` ABOVE any line that exceeds 80 chars (existing convention).
    - Do NOT modify the redirect guard at lines 26-43 (RESEARCH §Pattern 6 — guard handles /settings/* automatically as non-onboarding routes).

    Unskip and implement `test/features/settings/settings_screen_test.dart` covering Test 1-4. Use `tester.pumpWidget(ProviderScope(child: MaterialApp.router(routerConfig: router)))` with controllable provider overrides.
  </action>
  <verify>
    <automated>flutter test test/features/settings/settings_screen_test.dart &amp;&amp; dart analyze lib/features/settings/pages/ lib/core/router/app_router.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/settings/pages/settings_screen.dart` exists with `class SettingsScreen extends ConsumerWidget`
    - File source contains exactly 6 `SectionHeader(label: '` instances with labels in the order: Reminder, Streak, Appearance, Data, Privacy, About (Claude's Discretion option a.i adds Streak section closing Phase 5 D-08; D-04 order applies to the remaining 5 sections Reminder/Appearance/Data/Privacy/About)
    - File source contains `context.go('/settings/reminder')`, `context.go('/settings/export')`, `context.go('/settings/privacy')`, `context.go('/settings/disclosure')`
    - File source contains `cs.error` (Reset tile icon/text color)
    - `lib/features/settings/pages/export_screen.dart` and `privacy_screen.dart` exist as placeholders with TODO markers
    - `lib/core/router/app_router.dart` contains the 4 new GoRoute paths: `/settings`, `/settings/export`, `/settings/privacy`, `/settings/disclosure`
    - `flutter test test/features/settings/settings_screen_test.dart` exits 0 with at least 4 tests passing
    - `dart analyze lib/features/settings/pages/ lib/core/router/app_router.dart` reports 0 errors and 0 new warnings
  </acceptance_criteria>
  <done>Settings hub renders all 8 tiles in the locked order; 4 routes registered; theme switching works end-to-end (Settings → Appearance → tap segment → MaterialApp.router rebuilds via 06-02 wire-up).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HomeScreen AppBar gear icon insertion</name>
  <files>lib/features/home/pages/home_screen.dart</files>
  <read_first>
    - lib/features/home/pages/home_screen.dart (existing AppBar actions at lines 53-62)
    - test/features/home/home_appbar_settings_action_test.dart (RED stub from 06-01)
    - .planning/phases/06-polish-play-store-submission/06-UI-SPEC.md (§Screen-Level Layout Specs §HomeScreen AppBar change)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 624-670 — exact insertion pattern)
  </read_first>
  <behavior>
    - Test 1: AppBar.actions list has length 2 (gear + notifications, in that order)
    - Test 2: First action contains `Icons.settings` icon + tooltip 'Settings' + Semantics label 'Settings'
    - Test 3: Tapping the gear navigates to /settings (via context.go); existing notifications button still navigates to /settings/reminder
  </behavior>
  <action>
    Surgical INSERT in `lib/features/home/pages/home_screen.dart` AppBar.actions list. Add a Semantics-wrapped IconButton as the FIRST entry of the existing `actions: [...]` list (preserve the existing notifications IconButton as the SECOND entry — UI-SPEC §HomeScreen AppBar change locks the order):

    ```
    Semantics(
      label: 'Settings',
      child: IconButton(
        icon: const Icon(Icons.settings),
        tooltip: 'Settings',
        onPressed: () => context.go('/settings'),
      ),
    ),
    ```

    DO NOT modify any other widget in home_screen.dart (Phase 2/3/5 card stack untouched). DO NOT change the existing notifications IconButton — leave its onPressed pointing to /settings/reminder.

    Unskip `test/features/home/home_appbar_settings_action_test.dart` and implement Test 1-3 behavior assertions.
  </action>
  <verify>
    <automated>flutter test test/features/home/home_appbar_settings_action_test.dart &amp;&amp; dart analyze lib/features/home/pages/home_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/home/pages/home_screen.dart` source contains `Icons.settings` AND `tooltip: 'Settings'` AND `context.go('/settings')`
    - Existing `Icons.notifications_outlined` action remains in the file (not replaced)
    - `flutter test test/features/home/home_appbar_settings_action_test.dart` exits 0 with 3 tests passing
    - `dart analyze lib/features/home/pages/home_screen.dart` reports 0 errors and 0 new warnings
    - `flutter test test/features/home/` (full home test suite) exits 0 — no regression in Phase 2/3/5 home tests
  </acceptance_criteria>
  <done>Home AppBar has gear icon as first action; tap routes to Settings hub; existing notifications icon preserved.</done>
</task>

</tasks>

<verification>
- `flutter test test/features/settings/ test/features/home/` exits 0 — settings_screen, theme_segmented_button, about_tile, home_appbar_settings_action tests all green
- `flutter test` (full suite) exits 0 — no regression
- `dart analyze lib/features/settings/ lib/features/home/pages/home_screen.dart lib/core/router/app_router.dart` clean
- Manual smoke (developer): cold-launch app, tap gear, see tiles in order, switch theme, see app rebuild brightness, tap each navigation tile (export+privacy show placeholders; reminder + disclosure routes work; reset opens placeholder dialog)
</verification>

<success_criteria>
- Settings hub IA fully wired: gear icon → /settings → 6 sections × 8 tiles in locked D-04 + Claude's-Discretion-a.i order
- Theme switching works end-to-end (driven by 06-02 provider through 06-03 ThemeTile)
- Phase 5 D-08 soft-lock closed (Streak threshold inline section)
- All 4 new GoRouter routes registered, eliminating downstream plans' need to edit app_router.dart
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-play-store-submission/06-03-SUMMARY.md`
</output>
</output>
