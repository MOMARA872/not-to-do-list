---
phase: 06-polish-play-store-submission
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pubspec.yaml
  - docs/PRIVACY.md
  - test/features/settings/settings_screen_test.dart
  - test/features/settings/theme_mode_provider_test.dart
  - test/features/settings/theme_segmented_button_test.dart
  - test/features/settings/about_tile_test.dart
  - test/features/settings/export_controller_test.dart
  - test/features/settings/export_csv_format_test.dart
  - test/features/settings/export_json_envelope_test.dart
  - test/features/settings/file_save_port_test.dart
  - test/features/settings/reset_controller_test.dart
  - test/features/settings/reset_dialog_test.dart
  - test/features/settings/privacy_screen_test.dart
  - test/features/settings/disclosure_route_test.dart
  - test/features/settings/export_screen_test.dart
  - test/features/home/home_appbar_settings_action_test.dart
  - test/app/theme_rebuild_test.dart
  - test/policy/apk_telemetry_strings_test.dart
  - test/policy/privacy_policy_url_present_test.dart
  - integration_test/phase6_happy_path_test.dart
  - lib/data/database/daos/daily_checkins_dao.dart
  - lib/data/database/daos/daily_streak_dao.dart
  - lib/data/database/daos/pause_event_dao.dart
  - lib/data/database/daos/daily_usage_summary_dao.dart
  - test/_fixtures/file_save_port_mock.dart
autonomous: true
requirements: [SETT-01, SETT-02, SETT-04, SETT-05, PLAY-08]

must_haves:
  truths:
    - "All 19 Phase 6 RED test files exist and fail (or are pending) before any production code lands"
    - "4 new pubspec deps (archive ^4.0.9, flutter_file_dialog ^3.0.3, flutter_markdown_plus ^1.0.7, package_info_plus ^10.1.0) installed"
    - "docs/PRIVACY.md exists as bundled asset stub; pubspec.yaml assets list includes it"
    - "All 5 DAOs (BlockListDao, DailyCheckinsDao, DailyStreakDao, PauseEventDao, DailyUsageSummaryDao) expose Future<List<XxxData>> getAll()"
    - "FileSavePort mock fixture exists at test/_fixtures/file_save_port_mock.dart, mirroring the permission_status_mock.dart convention"
    - "flutter test exit code 0 (RED stubs use markTestSkipped or pending expects per project convention); no analyzer errors introduced"
  artifacts:
    - path: "pubspec.yaml"
      provides: "4 new deps + docs/PRIVACY.md asset entry + version line documented (bump applied in 06-07)"
      contains: "archive: ^4.0.9"
    - path: "docs/PRIVACY.md"
      provides: "Privacy Policy SoT stub (final copy written in 06-08)"
      contains: "# Privacy Policy"
    - path: "test/features/settings/theme_mode_provider_test.dart"
      provides: "RED stub for SETT-04 persistence"
    - path: "test/features/settings/export_controller_test.dart"
      provides: "RED stub for SETT-01 ZIP/CSV/JSON round-trip"
    - path: "test/features/settings/reset_controller_test.dart"
      provides: "RED stub for SETT-02 transactional wipe"
    - path: "test/app/theme_rebuild_test.dart"
      provides: "RED stub for SETT-04 MaterialApp.router themeMode rebuild (unskipped in 06-02)"
    - path: "test/features/settings/export_screen_test.dart"
      provides: "RED stub for SETT-01 ExportScreen widget render + save flow (unskipped in 06-04)"
    - path: "test/_fixtures/file_save_port_mock.dart"
      provides: "Mocktail mock for FileSavePort interface (implementation lands in 06-04)"
    - path: "lib/data/database/daos/daily_checkins_dao.dart"
      provides: "Future<List<DailyCheckinData>> getAll() addition"
      contains: "Future<List<"
  key_links:
    - from: "test/_fixtures/file_save_port_mock.dart"
      to: "lib/features/settings/services/file_save_port.dart (lands in 06-04)"
      via: "implements/mocks the FileSavePort abstract class"
      pattern: "class MockFileSavePort"
    - from: "pubspec.yaml"
      to: "all Phase 6 production files"
      via: "transitive package resolution + bundled asset registration"
      pattern: "archive: \\^4\\.0\\.9"
---

<objective>
Wave 0 RED test scaffold for Phase 6. Creates all 19 failing test files referenced in 06-VALIDATION.md, installs the 4 new pubspec dependencies (archive, flutter_file_dialog, flutter_markdown_plus, package_info_plus), audits the 5 Drift DAOs and adds `getAll()` to the 4 that lack it, writes a stub `docs/PRIVACY.md` bundled asset, and lays down the FileSavePort mock fixture.

Purpose: Establish the failing-test floor so every downstream Phase 6 plan can prove its task by flipping at least one previously RED test green. Mirrors Phase 4 Plan 04-01 and Phase 5 Plan 05-01 Wave 0 patterns.
Output: 19 test files (mostly markTestSkipped or pending expects), 4 DAO edits, 1 fixture, pubspec.yaml dep additions, docs/PRIVACY.md stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-play-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-play-store-submission/06-VALIDATION.md
@.planning/phases/06-polish-play-store-submission/06-PATTERNS.md

<interfaces>
<!-- Key contracts the executor will reference. Implementations land in later plans. -->
<!-- Source: 06-RESEARCH.md §Validation Architecture + §Pattern 1 + §Code Examples -->

From lib/features/streak/providers/streak_threshold_provider.dart (canonical analog for theme_mode_provider — RED stub mirrors this shape):
```dart
class StreakThresholdNotifier extends AsyncNotifier<int> { ... }
final AsyncNotifierProvider<StreakThresholdNotifier, int> streakThresholdProvider = ...;
```

From lib/platform/notification_api.g.dart (already exists; Reset controller will call cancelDailyReminder in 06-05):
```dart
Future<void> scheduleDailyReminder(int hour, int minute);
Future<void> cancelDailyReminder();
```

From lib/data/database/daos/block_list_dao.dart (canonical getAll pattern to clone into the other 4 DAOs):
```dart
/// All entries sorted by `updatedAt` desc.
Future<List<BlockListData>> getAll() {
  return (select(blockList)..orderBy([(t) => OrderingTerm.desc(t.updatedAt)])).get();
}
```

FileSavePort contract (implementation lands in 06-04; mock fixture lands here so RED tests can compile):
```dart
abstract class FileSavePort {
  Future<String?> save({
    required Uint8List bytes,
    required String fileName,
    required List<String> mimeTypes,
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pubspec deps + DAO getAll() audit + docs/PRIVACY.md stub</name>
  <files>pubspec.yaml, docs/PRIVACY.md, lib/data/database/daos/daily_checkins_dao.dart, lib/data/database/daos/daily_streak_dao.dart, lib/data/database/daos/pause_event_dao.dart, lib/data/database/daos/daily_usage_summary_dao.dart</files>
  <read_first>
    - pubspec.yaml (current deps + assets block)
    - lib/data/database/daos/block_list_dao.dart (canonical getAll() pattern at lines 25-30)
    - lib/data/database/daos/daily_checkins_dao.dart (verify if getAll exists)
    - lib/data/database/daos/daily_streak_dao.dart
    - lib/data/database/daos/pause_event_dao.dart
    - lib/data/database/daos/daily_usage_summary_dao.dart
    - .planning/phases/06-polish-play-store-submission/06-RESEARCH.md (§Standard Stack + §Pitfall 9)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 405-414 — DAO getAll() canonical reference)
  </read_first>
  <action>
    Per D-16 + 06-RESEARCH.md §Standard Stack (verified 2026-05-24): append to pubspec.yaml `dependencies:` block (alphabetical-ish, after `archive`-area): `archive: ^4.0.9`, `flutter_file_dialog: ^3.0.3`, `flutter_markdown_plus: ^1.0.7`, `package_info_plus: ^10.1.0`. Extend `flutter.assets:` block (currently `assets/onboarding/` + `assets/logos/`) by appending the single-file entry `docs/PRIVACY.md` (NOT a directory). Do NOT bump the version line in this plan — version 1.0.0+1 bump is part of 06-07.

    Create `docs/PRIVACY.md` as a stub (final copy is written in 06-08):
    - Single H1 `# Privacy Policy`
    - One paragraph stating: zero data collection, on-device only, no telemetry, no network calls beyond app launch (calm tone per Phase 4 D-07 lock; no external links per 06-RESEARCH.md Pitfall 6)
    - Footer line: `_Last updated: 2026-05-24_`

    DAO audit (per 06-RESEARCH.md Pitfall 9 verified pre-task — BlockListDao already has getAll at line 26; the 4 others do NOT):
    - daily_checkins_dao.dart: add `Future<List<DailyCheckinData>> getAll() => select(dailyCheckins).get();`
    - daily_streak_dao.dart: add `Future<List<DailyStreakData>> getAll() => select(dailyStreak).get();`
    - pause_event_dao.dart: add `Future<List<PauseEventData>> getAll() => select(pauseEvents).get();`
    - daily_usage_summary_dao.dart: add `Future<List<DailyUsageSummaryData>> getAll() => select(dailyUsageSummary).get();`
    Place each new method after the existing public methods, with a doc comment `/// Phase 6 SETT-01: all rows for export.` No ordering required (export downstream does not depend on order).

    Run `flutter pub get` after pubspec edits. Run `dart format` on the 4 DAO files.
  </action>
  <verify>
    <automated>flutter pub get &amp;&amp; dart analyze pubspec.yaml lib/data/database/daos/ docs/ &amp;&amp; grep -c "Future&lt;List&lt;" lib/data/database/daos/daily_checkins_dao.dart lib/data/database/daos/daily_streak_dao.dart lib/data/database/daos/pause_event_dao.dart lib/data/database/daos/daily_usage_summary_dao.dart | grep -v ":0$" | wc -l | grep -qx "       4"</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` contains literal strings `archive: ^4.0.9`, `flutter_file_dialog: ^3.0.3`, `flutter_markdown_plus: ^1.0.7`, `package_info_plus: ^10.1.0`
    - `pubspec.yaml` `flutter.assets:` block contains literal `- docs/PRIVACY.md`
    - `docs/PRIVACY.md` exists and starts with `# Privacy Policy`
    - Each of the 4 DAO files (daily_checkins_dao.dart, daily_streak_dao.dart, pause_event_dao.dart, daily_usage_summary_dao.dart) contains a `Future<List<` line outside of `.g.dart` files
    - `flutter pub get` exits 0
    - `dart analyze` reports no NEW errors on edited files (existing 18 dart-analyze infos in pigeons/* per STATE.md may remain)
  </acceptance_criteria>
  <done>Pubspec resolves the 4 new deps with no telemetry transitive (`flutter pub deps | grep -iE '(firebase|crashlytics|analytics|fcm|remoteconfig)'` returns no matches); 4 DAOs gained getAll(); docs/PRIVACY.md stub bundled; analyzer clean on edits.</done>
</task>

<task type="auto">
  <name>Task 2: Write 19 RED test stubs + FileSavePort mock fixture</name>
  <files>test/features/settings/settings_screen_test.dart, test/features/settings/theme_mode_provider_test.dart, test/features/settings/theme_segmented_button_test.dart, test/features/settings/about_tile_test.dart, test/features/settings/export_controller_test.dart, test/features/settings/export_csv_format_test.dart, test/features/settings/export_json_envelope_test.dart, test/features/settings/file_save_port_test.dart, test/features/settings/reset_controller_test.dart, test/features/settings/reset_dialog_test.dart, test/features/settings/privacy_screen_test.dart, test/features/settings/disclosure_route_test.dart, test/features/settings/export_screen_test.dart, test/features/home/home_appbar_settings_action_test.dart, test/app/theme_rebuild_test.dart, test/policy/apk_telemetry_strings_test.dart, test/policy/privacy_policy_url_present_test.dart, integration_test/phase6_happy_path_test.dart, test/_fixtures/file_save_port_mock.dart</files>
  <read_first>
    - test/_fixtures/permission_status_mock.dart (canonical mocktail fixture analog)
    - test/policy/play_invariants_test.dart (existing 10 invariants — DO NOT modify in this plan; extension lands in 06-07)
    - test/policy/phase_5_invariants_test.dart (precedent for telemetry absence-grep + comment-stripping idiom)
    - test/features/streak/providers/streak_threshold_provider_test.dart (canonical AsyncNotifier test analog, if it exists; otherwise look at any existing provider test)
    - .planning/phases/06-polish-play-store-submission/06-VALIDATION.md (per-test responsibility list)
    - .planning/phases/06-polish-play-store-submission/06-RESEARCH.md (§Validation Architecture; lines 989-1072 — 17-file mapping; 2 additional files theme_rebuild_test.dart + export_screen_test.dart per checker iteration 1)
  </read_first>
  <action>
    Create each of the 19 test files as a RED stub. Each file:
    - Imports `package:flutter_test/flutter_test.dart` (and `package:flutter/material.dart` / `package:mocktail/mocktail.dart` only where the stub references those types).
    - Wraps the body in `void main() { group('Phase 6 / SETT-XX', () { ... }); }`.
    - Contains ONE pending `test('...', () { markTestSkipped('RED stub — Phase 6 Wave 0; implementation in 06-XX'); });` per behavior listed below. Use `markTestSkipped(...)` to keep `flutter test` exit code 0 while still surfacing the pending count in `--reporter expanded`. Do NOT use bare `expect(false, true)` since that fails CI.

    Test contents per file (one `test(...)` per bullet, all `markTestSkipped`):
    - `test/features/settings/settings_screen_test.dart` → tile order Reminder→Streak→Appearance→Data→Privacy→About per D-04 with Phase 5 D-08 inline Streak section closure; gear icon from Home routes here (implementation in 06-03).
    - `test/features/settings/theme_mode_provider_test.dart` → default ThemeMode.system; persists int across notifier rebuild; encode/decode round-trip 0/1/2 (implementation in 06-02).
    - `test/features/settings/theme_segmented_button_test.dart` → 3 segments labelled Light/Dark/System; tap writes provider; selected set has exactly one element (implementation in 06-03).
    - `test/features/settings/about_tile_test.dart` → renders `"1.0.0 (1)"` format from PackageInfo per Pitfall 7 (implementation in 06-03).
    - `test/features/settings/export_controller_test.dart` → ZIP contains 5 CSVs + data.json; filename matches `not-to-do-list-export-YYYYMMDD-HHMM.zip` regex; schemaVersion=1; exportedAt is ISO-8601 UTC ending in Z (implementation in 06-04).
    - `test/features/settings/export_csv_format_test.dart` → header row = Drift column names verbatim per D-10; CSV escaping wraps fields with `,`/`"`/`\n` in quotes; CSV-injection prefix on `=`/`+`/`-`/`@` per Security Domain V5 (implementation in 06-04).
    - `test/features/settings/export_json_envelope_test.dart` → envelope keys exactly {schemaVersion, exportedAt, appVersion, tables}; tables keys exactly the 5 table names; appVersion format `1.0.0+1` (implementation in 06-04).
    - `test/features/settings/file_save_port_test.dart` → FlutterFileDialogSavePort delegates to FlutterFileDialog.saveFile with mimeTypesFilter=['application/zip']; null return treated as silent cancel per Pitfall 3 (implementation in 06-04).
    - `test/features/settings/reset_controller_test.dart` → Drift transaction deletes child tables before block_list; SharedPreferences.clear() called; cancelDailyReminder() called; 5 providers invalidated; context.go('/onboarding/welcome') called per D-14 + Pitfall 2 (implementation in 06-05).
    - `test/features/settings/reset_dialog_test.dart` → AlertDialog body text equals verbatim `"This deletes every entry, streak day, pause event, and check-in. This cannot be undone."`; Cancel button is first in actions list (default-focused); calm-tone source-grep finds no `!`/`?` in the dialog body (implementation in 06-05).
    - `test/features/settings/privacy_screen_test.dart` → loads `docs/PRIVACY.md` via rootBundle; flutter_markdown_plus Markdown widget present; no exception during render (implementation in 06-06).
    - `test/features/settings/disclosure_route_test.dart` → `/settings/disclosure` mounts AccessibilityStep with `fromSettings: true`; tapping skip/done pops instead of pushing /onboarding/permissions/battery-opt per RESEARCH §Pattern 5 (implementation in 06-06).
    - `test/features/settings/export_screen_test.dart` → ExportScreen widget render (AppBar title 'Export data', single 'Export all data' ListTile with UI-SPEC subtitle copy), save-success SnackBar 'Export saved', save-cancel silent (no SnackBar), save-failure SnackBar 'Export failed. Check available storage and try again.', context.mounted guard after await. TODO: 06-04 Task 3 unskips during GREEN flip.
    - `test/features/home/home_appbar_settings_action_test.dart` → HomeScreen AppBar actions contains an IconButton with `Icons.settings` + tooltip "Settings"; tap routes to /settings; existing notifications IconButton preserved (implementation in 06-03).
    - `test/app/theme_rebuild_test.dart` → MaterialApp.router themeMode rebuild on themeModeProvider change: Test 1 (provider AsyncLoading → themeMode resolves to ThemeMode.system via maybeWhen orElse), Test 2 (provider AsyncValue.data(ThemeMode.dark) → MaterialApp.themeMode == ThemeMode.dark on next pump), Test 3 (themeModeProvider.notifier.set(ThemeMode.light) → MaterialApp.themeMode flips to light on next pump). TODO: 06-02 Task 2 unskips during GREEN flip.
    - `test/policy/apk_telemetry_strings_test.dart` → `Process.runSync('sh', ['-c', "unzip -p ... 'classes*.dex' | strings | grep -iE '(firebase|crashlytics|analytics|fcm|remoteconfig)' | head -5"])` stdout is empty; gracefully skips when APK absent per RESEARCH Example 6 (implementation in 06-07).
    - `test/policy/privacy_policy_url_present_test.dart` → `docs/PRIVACY.md` exists and is non-empty; SettingsScreen source contains a Privacy Policy ListTile entry pointing to `/settings/privacy` (implementation in 06-06/06-08).
    - `integration_test/phase6_happy_path_test.dart` → manual checklist gate; body wraps the 9-step OEM-survival overnight protocol in a single `testWidgets('PHASE-6 OEM PASS — manual', ...)` that markTestSkipped with the literal reason `"manual gate — run on real Xiaomi AND Samsung overnight per 06-VERIFICATION.md"` (template lives in 06-08).

    Create `test/_fixtures/file_save_port_mock.dart`:
    - Mirror `test/_fixtures/permission_status_mock.dart` shape (mocktail Mock + Fake registrations).
    - Declares `class MockFileSavePort extends Mock implements FileSavePort {}` — since FileSavePort class is not yet defined, place the import as `// ignore: uri_does_not_exist` + comment `// TODO(06-04): uncomment after FileSavePort lands` AND wrap the class body in a top-level commented block. Activation happens in 06-04 Task 1.
    - Alternative if executor prefers compiling cleanly NOW: define a local `abstract class FileSavePort { ... }` placeholder inside this fixture file with `@Deprecated('placeholder — superseded by lib/features/settings/services/file_save_port.dart in 06-04')` annotation, then have 06-04 delete the placeholder. Either approach is acceptable; the success criterion is `flutter test` exit 0 in Wave 0.
  </action>
  <verify>
    <automated>flutter test test/features/settings/ test/features/home/home_appbar_settings_action_test.dart test/app/theme_rebuild_test.dart test/policy/apk_telemetry_strings_test.dart test/policy/privacy_policy_url_present_test.dart integration_test/phase6_happy_path_test.dart 2>&amp;1 | grep -E "All tests passed|Some tests failed" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - 19 test files exist at the paths listed in `files_modified`
    - Each test file contains at least one `markTestSkipped(` call (RED stub convention)
    - `flutter test test/features/settings/` exits 0 (skipped is not a failure)
    - `flutter test test/features/home/home_appbar_settings_action_test.dart test/app/theme_rebuild_test.dart test/policy/apk_telemetry_strings_test.dart test/policy/privacy_policy_url_present_test.dart` exits 0
    - `test/_fixtures/file_save_port_mock.dart` exists and either imports FileSavePort with a TODO marker for 06-04 OR contains a placeholder abstract class
    - `dart analyze test/features/settings/ test/features/home/home_appbar_settings_action_test.dart test/app/theme_rebuild_test.dart test/policy/apk_telemetry_strings_test.dart test/policy/privacy_policy_url_present_test.dart integration_test/phase6_happy_path_test.dart test/_fixtures/file_save_port_mock.dart` reports no NEW errors
  </acceptance_criteria>
  <done>All 19 Phase 6 test stubs exist; `flutter test` is green (with skipped tests visible in expanded reporter); FileSavePort mock fixture in place for 06-04 to wire against; theme_rebuild_test.dart + export_screen_test.dart RED stubs ready for 06-02 + 06-04 GREEN flip.</done>
</task>

</tasks>

<verification>
- 19 test files exist; `flutter test` exit 0 with skipped count = 19 Phase 6 stubs (existing tests stay passing — Phase 5 487 passing / 5 skipped baseline preserved + Phase 6 adds ~19 more skipped)
- pubspec.lock has the 4 new deps resolved; running `flutter pub deps --no-dev | grep -iE '(firebase|crashlytics|analytics|fcm|remoteconfig)'` returns no matches (extends Phase 5 D-09 pattern; final test enforcement lands in 06-07)
- 4 DAOs gained Future<List<...>> getAll() (BlockListDao already had it)
- docs/PRIVACY.md exists as bundled asset
- FileSavePort mock fixture exists
</verification>

<success_criteria>
- All 19 RED test files committed; sampling continuity established for Wave 1+
- Pubspec resolves cleanly; new deps do not pull telemetry transitive
- 5/5 DAOs ready for export reads
- Bundled docs/PRIVACY.md asset declaration in place for 06-06
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-play-store-submission/06-01-SUMMARY.md`
</output>
</output>
