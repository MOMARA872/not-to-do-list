---
phase: 06-polish-play-store-submission
plan: 05
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - lib/features/settings/services/reset_controller.dart
  - lib/features/settings/pages/settings_screen.dart
autonomous: true
requirements: [SETT-02]

must_haves:
  truths:
    - "Tap Reset tile → AlertDialog with body verbatim 'This deletes every entry, streak day, pause event, and check-in. This cannot be undone.' (D-13)"
    - "Cancel button listed first in actions list (default-focused per D-13 + UI-SPEC §Accessibility); FilledButton with cs.error / cs.onError styling for Reset"
    - "Tapping Reset runs: cancelDailyReminder() → Drift transaction wiping 5 tables in FK-safe child-first order → SharedPreferences.clear() → invalidate 5+ providers → context.go('/onboarding/welcome') (D-14 + Pitfall 2)"
    - "No flash of pre-reset state between dialog dismiss and onboarding load (Pitfall 2 — provider invalidation precedes context.go)"
    - "Pending daily reminder alarm cancelled BEFORE prefs.clear() so it cannot re-fire after reset (RESEARCH §Runtime State Inventory)"
  artifacts:
    - path: "lib/features/settings/services/reset_controller.dart"
      provides: "ResetController(AppDatabase, NotificationApi, Ref) with Future<void> resetAll(BuildContext)"
      contains: "Future<void> resetAll"
      min_lines: 50
    - path: "lib/features/settings/pages/settings_screen.dart"
      provides: "_showResetDialog wired to ResetController + verbatim D-13 body copy + cs.error styled FilledButton (replaces 06-03 placeholder)"
      contains: "This deletes every entry"
  key_links:
    - from: "lib/features/settings/pages/settings_screen.dart"
      to: "lib/features/settings/services/reset_controller.dart"
      via: "controller.resetAll(context) inside FilledButton.onPressed"
      pattern: "resetAll\\("
    - from: "lib/features/settings/services/reset_controller.dart"
      to: "lib/platform/notification_api.g.dart"
      via: "NotificationApi.cancelDailyReminder()"
      pattern: "cancelDailyReminder"
    - from: "lib/features/settings/services/reset_controller.dart"
      to: "lib/core/router/app_router.dart redirect guard"
      via: "context.go('/onboarding/welcome') after prefs.clear() + invalidate"
      pattern: "context\\.go\\('/onboarding/welcome'\\)"
---

<objective>
Wave 3 (parallel with 06-04 + 06-06): Reset feature end-to-end. Builds ResetController that (1) cancels the pending daily reminder alarm, (2) runs a single Drift transaction wiping all 5 tables in FK-safe child-first order, (3) clears SharedPreferences, (4) invalidates all prefs-backed Riverpod providers, and (5) navigates to /onboarding/welcome for true clean-slate re-walk. Replaces the placeholder AlertDialog from 06-03 with the verbatim D-13 body copy + cs.error-styled FilledButton.

Purpose: Deliver SETT-02 with all the Pitfall-2 (no pre-reset flash) and Runtime-State (cancel pending alarm) edge cases handled, plus the calm-tone destructive UX per D-13.
Output: 1 new service file, 1 surgical SettingsScreen edit (replaces placeholder dialog).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-play-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-play-store-submission/06-PATTERNS.md
@.planning/phases/06-polish-play-store-submission/06-RESEARCH.md
@.planning/phases/06-polish-play-store-submission/06-UI-SPEC.md
@.planning/phases/06-polish-play-store-submission/06-02-SUMMARY.md
@.planning/phases/06-polish-play-store-submission/06-03-SUMMARY.md

<interfaces>
<!-- Source-of-truth contracts the executor implements against. -->

From lib/data/database/app_database.dart (DAO list — Drift table names are camelCase getters on AppDatabase):
```dart
// db.blockList, db.dailyCheckins, db.dailyStreak, db.pauseEvents, db.dailyUsageSummary
// PRAGMA foreign_keys = ON set in beforeOpen (lines 50-52)
```

Transactional reset pattern (RESEARCH §Pattern 3 + §Example 3, lines 754-767 — verified):
```dart
await db.transaction(() async {
  // Child tables FIRST (defensive even though CASCADE enabled)
  await db.delete(db.dailyCheckins).go();
  await db.delete(db.pauseEvents).go();
  await db.delete(db.dailyStreak).go();
  await db.delete(db.dailyUsageSummary).go();
  // Parent LAST
  await db.delete(db.blockList).go();
});
```

From lib/platform/notification_api.g.dart (Pigeon method confirmed at line 95):
```dart
Future<void> cancelDailyReminder();
```

Post-transaction sequence (RESEARCH §Pattern 3 + Pitfall 2 — preserves single-flush ordering):
```dart
final prefs = await SharedPreferences.getInstance();
await prefs.clear();
ref.invalidate(onboardingCompleteProvider);
ref.invalidate(themeModeProvider);
ref.invalidate(reminderTimeProvider);
ref.invalidate(streakThresholdProvider);
ref.invalidate(postNotificationsGrantedProvider);
if (context.mounted) context.go('/onboarding/welcome');
```

Reset dialog literal copy (D-13 + UI-SPEC §Copywriting Contract):
- title: 'Reset all data'
- body: 'This deletes every entry, streak day, pause event, and check-in. This cannot be undone.'
- Cancel button (first, default-focused)
- Reset button (second, FilledButton.styleFrom(backgroundColor: cs.error, foregroundColor: cs.onError))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResetController with transactional wipe + alarm cancel + provider invalidation</name>
  <files>lib/features/settings/services/reset_controller.dart</files>
  <read_first>
    - lib/data/database/app_database.dart (Drift DB + PRAGMA foreign_keys + table getters)
    - lib/features/reminder/providers/reminder_providers.dart (canonical sequenced side-effect chain at lines 24-31 — reminderTimeProvider invalidation source)
    - lib/features/onboarding/providers/onboarding_complete_provider.dart (provider to invalidate)
    - lib/features/streak/providers/streak_threshold_provider.dart (provider to invalidate)
    - lib/features/settings/providers/theme_mode_provider.dart (created 06-02 — provider to invalidate)
    - lib/domain/providers/notification_api_provider.dart (notificationApiProvider Riverpod accessor)
    - lib/platform/notification_api.g.dart (cancelDailyReminder method)
    - test/features/settings/reset_controller_test.dart (RED stub from 06-01)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 448-509 — reset_controller pattern)
    - .planning/phases/06-polish-play-store-submission/06-RESEARCH.md (§Pattern 3 + §Example 3 + §Pitfall 2 + §Runtime State Inventory)
  </read_first>
  <behavior>
    - Test 1: cancelDailyReminder() called BEFORE prefs.clear() (verify mocktail call order)
    - Test 2: Drift transaction runs all 5 deletes in child-first order: dailyCheckins → pauseEvents → dailyStreak → dailyUsageSummary → blockList (RESEARCH §Pattern 3 — defensive even with CASCADE)
    - Test 3: All 5 tables row-count == 0 after resetAll
    - Test 4: SharedPreferences.getKeys() is empty after resetAll
    - Test 5: ref.invalidate called for onboardingCompleteProvider, themeModeProvider, reminderTimeProvider, streakThresholdProvider, postNotificationsGrantedProvider (verify via container.read after invalidate — each provider re-enters AsyncLoading state)
    - Test 6: context.go('/onboarding/welcome') invoked when context.mounted (use a Mock NavigatorObserver or test GoRouter to assert)
    - Test 7: When context.unmounted after the async chain, no nav call is attempted (safety guard)
  </behavior>
  <action>
    Create `lib/features/settings/services/reset_controller.dart` per RESEARCH §Pattern 3 + §Example 3 (lines 456-498) + §Runtime State Inventory (the alarm-cancel addition):

    Class signature: `class ResetController { ResetController(this._db, this._notificationApi, this._ref); final AppDatabase _db; final NotificationApi _notificationApi; final Ref _ref; }`

    Method `Future<void> resetAll(BuildContext context) async`:
    1. **Cancel pending reminder alarm FIRST** (RESEARCH §Runtime State Inventory — prevents pre-cleared alarm from firing post-reset): `await _notificationApi.cancelDailyReminder();`
    2. **Drift transaction** (single atomic wipe per Pattern 3): `await _db.transaction(() async { await _db.delete(_db.dailyCheckins).go(); await _db.delete(_db.pauseEvents).go(); await _db.delete(_db.dailyStreak).go(); await _db.delete(_db.dailyUsageSummary).go(); await _db.delete(_db.blockList).go(); });` — child tables first per Pattern 3.
    3. **SharedPreferences.clear()** per D-14: `final prefs = await SharedPreferences.getInstance(); await prefs.clear();` (NOT per-key remove — D-14 wants ALL keys gone including future-added ones).
    4. **Invalidate 5+ providers** per Pitfall 2 (prevents pre-reset flash): `_ref.invalidate(onboardingCompleteProvider); _ref.invalidate(themeModeProvider); _ref.invalidate(reminderTimeProvider); _ref.invalidate(streakThresholdProvider); _ref.invalidate(postNotificationsGrantedProvider);`
    5. **Navigate** with mounted guard: `if (context.mounted) context.go('/onboarding/welcome');` per Pattern 6 (explicit go() avoids refresh-listener race).

    Doc comment header references SETT-02, D-13, D-14, Pitfall 2, and Runtime-State alarm-cancel rationale.

    Unskip `test/features/settings/reset_controller_test.dart` and implement Tests 1-7. Use in-memory Drift fixture seeded with rows in each of the 5 tables; mocktail MockNotificationApi to capture cancelDailyReminder call order; ProviderContainer to read provider states pre/post-invalidate; mock GoRouter or capture context.go via a NavigatorObserver.

    Note: The order assertion (Test 1) is verifiable via `verifyInOrder([() => mockNotifApi.cancelDailyReminder(), () => mockDb.transaction(any())])` if you choose to inject AppDatabase as a mocktail mock — easier alternative is to use the real in-memory Drift DB and only mock NotificationApi, asserting cancelDailyReminder was called once before assertion on Drift state.
  </action>
  <verify>
    <automated>flutter test test/features/settings/reset_controller_test.dart &amp;&amp; dart analyze lib/features/settings/services/reset_controller.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/settings/services/reset_controller.dart` exists with `class ResetController` and `Future<void> resetAll(BuildContext`
    - Source contains `cancelDailyReminder()` call BEFORE `transaction(` (verified by source-grep line order OR by test 1)
    - Source contains all 5 table delete calls in correct order: dailyCheckins, pauseEvents, dailyStreak, dailyUsageSummary, blockList (FK-safe child-first)
    - Source contains `prefs.clear()` (NOT `prefs.remove`)
    - Source contains 5 distinct `_ref.invalidate(` lines for: onboardingCompleteProvider, themeModeProvider, reminderTimeProvider, streakThresholdProvider, postNotificationsGrantedProvider
    - Source contains `if (context.mounted) context.go('/onboarding/welcome')`
    - `flutter test test/features/settings/reset_controller_test.dart` exits 0 with 7 tests passing
    - `dart analyze` reports 0 errors
  </acceptance_criteria>
  <done>ResetController atomically cancels alarm + wipes Drift + clears prefs + invalidates providers + navigates to onboarding/welcome with no pre-reset flash possibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Reset AlertDialog in SettingsScreen — verbatim D-13 copy + cs.error FilledButton + ResetController invocation</name>
  <files>lib/features/settings/pages/settings_screen.dart</files>
  <read_first>
    - lib/features/settings/pages/settings_screen.dart (current placeholder `_showResetDialog` from 06-03)
    - lib/features/settings/services/reset_controller.dart (just created Task 1)
    - lib/domain/providers/database_provider.dart (databaseProvider accessor)
    - lib/domain/providers/notification_api_provider.dart (notificationApiProvider accessor)
    - test/features/settings/reset_dialog_test.dart (RED stub from 06-01)
    - .planning/phases/06-polish-play-store-submission/06-UI-SPEC.md (§Screen-Level Layout Specs §Reset AlertDialog + §Copywriting Contract Reset rows)
    - .planning/phases/06-polish-play-store-submission/06-CONTEXT.md (D-13 literal body copy)
  </read_first>
  <behavior>
    - Test 1: AlertDialog content Text equals literal `"This deletes every entry, streak day, pause event, and check-in. This cannot be undone."` (D-13 verbatim)
    - Test 2: AlertDialog title Text equals `"Reset all data"` (UI-SPEC)
    - Test 3: AlertDialog actions list: first is TextButton with text `"Cancel"` (default-focused per D-13); second is FilledButton with text `"Reset"` and `FilledButton.styleFrom(backgroundColor: cs.error, foregroundColor: cs.onError)`
    - Test 4: Calm-tone source-grep — the dialog body string has zero `!` and zero `?` characters
    - Test 5: Tap Cancel → Navigator.pop, dialog closes, no ResetController invocation
    - Test 6: Tap Reset → Navigator.pop (close dialog), then `ResetController.resetAll(context)` invoked with the surrounding ScreenContext (not the dialog's context — important so the post-reset context.go uses the surviving widget context)
  </behavior>
  <action>
    Replace the placeholder `_showResetDialog` helper in `lib/features/settings/pages/settings_screen.dart` (created in 06-03 with TODO marker) with the real implementation:

    Imports: `lib/features/settings/services/reset_controller.dart`, `lib/domain/providers/database_provider.dart`, `lib/domain/providers/notification_api_provider.dart`.

    Helper body:
    ```
    Future<void> _showResetDialog(BuildContext context, WidgetRef ref) async {
      final cs = Theme.of(context).colorScheme;
      final confirmed = await showDialog<bool>(
        context: context,
        builder: (dialogCtx) => AlertDialog(
          title: const Text('Reset all data'),
          content: const Text(
            'This deletes every entry, streak day, pause event, and check-in. This cannot be undone.',
          ),
          actions: [
            TextButton(
              onPressed: () => Navigator.of(dialogCtx).pop(false),
              child: const Text('Cancel'),
            ),
            FilledButton(
              style: FilledButton.styleFrom(
                backgroundColor: cs.error,
                foregroundColor: cs.onError,
              ),
              onPressed: () => Navigator.of(dialogCtx).pop(true),
              child: const Text('Reset'),
            ),
          ],
        ),
      );
      if (confirmed != true) return;
      if (!context.mounted) return;
      final controller = ResetController(
        ref.read(databaseProvider),
        ref.read(notificationApiProvider),
        ref,
      );
      await controller.resetAll(context);
    }
    ```

    The split between "show dialog" → "if confirmed, then run controller" cleanly separates UI from side effects + ensures the controller's context is the SettingsScreen context, not the dialog's (which is popped before resetAll runs). The FilledButton onPressed pops with `true` rather than invoking the controller directly inside the dialog, eliminating the "use Reset button context after pop" footgun.

    DELETE the `// TODO(06-05):` markers added in 06-03.

    Unskip `test/features/settings/reset_dialog_test.dart` and implement Tests 1-6. Use a wrapped `MaterialApp` with the Settings screen, mocktail MockResetController via Riverpod override OR a real ResetController with in-memory Drift fixture; tap the reset tile, assert dialog text/buttons, tap Cancel/Reset, verify outcomes.
  </action>
  <verify>
    <automated>flutter test test/features/settings/reset_dialog_test.dart test/features/settings/reset_controller_test.dart &amp;&amp; dart analyze lib/features/settings/pages/settings_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/settings/pages/settings_screen.dart` no longer contains `TODO(06-05)` markers
    - Source contains literal string `'This deletes every entry, streak day, pause event, and check-in. This cannot be undone.'` (exact verbatim per D-13)
    - Source contains `FilledButton.styleFrom(` with `backgroundColor: cs.error` AND `foregroundColor: cs.onError`
    - Source contains Cancel button BEFORE Reset button in the actions list (default-focused per D-13)
    - Source contains `ResetController(` instantiation with 3 args (db, notifApi, ref)
    - `grep -E '[!?]' lib/features/settings/pages/settings_screen.dart | grep -F 'This deletes every entry'` returns empty (calm-tone source-grep — body literal has no ! or ?)
    - `flutter test test/features/settings/reset_dialog_test.dart` exits 0 with 6 tests passing
    - `dart analyze lib/features/settings/pages/settings_screen.dart` reports 0 errors/warnings
  </acceptance_criteria>
  <done>Reset tile → dialog with verbatim copy + cs.error FilledButton → confirm → ResetController.resetAll → onboarding/welcome with no pre-reset flash.</done>
</task>

</tasks>

<verification>
- `flutter test test/features/settings/reset_controller_test.dart test/features/settings/reset_dialog_test.dart` exits 0
- `flutter test` (full suite) exits 0 — no regression in Phase 1-5 baseline
- `dart analyze lib/features/settings/services/reset_controller.dart lib/features/settings/pages/settings_screen.dart` clean
- Manual smoke (developer): add an entry, set a reminder time, tap Reset → dialog appears with literal copy → Cancel returns to Settings unchanged; Reset → onboarding/welcome with empty DB (no flash); reboot device — no spurious reminder alarm fires
</verification>

<success_criteria>
- SETT-02 software-complete: tap Reset → dialog with verbatim D-13 body → tap Reset → cancelDailyReminder + Drift wipe (child-first) + prefs.clear + 5 provider invalidations + nav to /onboarding/welcome
- No pre-reset flash possible (Pitfall 2 mitigated by invalidate-before-go ordering)
- Pending alarm cancelled BEFORE prefs.clear (Runtime State Inventory ordering)
- Calm-tone copy locked (no `!` or `?` in body)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-play-store-submission/06-05-SUMMARY.md`
</output>
