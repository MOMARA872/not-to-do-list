---
phase: 06-polish-play-store-submission
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - lib/features/settings/services/file_save_port.dart
  - lib/features/settings/services/export_controller.dart
  - lib/features/settings/pages/export_screen.dart
  - lib/domain/providers/file_save_port_provider.dart
  - test/_fixtures/file_save_port_mock.dart
autonomous: true
requirements: [SETT-01]

must_haves:
  truths:
    - "User taps Export tile → ExportScreen → 'Export all data' action launches Storage Access Framework via flutter_file_dialog and writes one ZIP archive named not-to-do-list-export-YYYYMMDD-HHMM.zip to the user-chosen location (D-09)"
    - "Archive contents: 5 per-table CSVs (block_list.csv, daily_checkins.csv, daily_streak.csv, pause_events.csv, daily_usage_summary.csv) — header row = Drift column names verbatim — plus data.json envelope (D-10 + D-11)"
    - "data.json envelope shape exactly {schemaVersion: 1, exportedAt: ISO-8601 UTC, appVersion, tables: {...}} (D-11 + D-12)"
    - "All timestamps in CSV string columns + JSON formatted as ISO-8601 UTC with trailing Z (D-12)"
    - "FileSavePort interface abstracts FlutterFileDialog.saveFile so widget tests inject MockFileSavePort via Riverpod override (RESEARCH §Anti-Patterns + Pigeon-port project convention)"
    - "Null return from FlutterFileDialog.saveFile treated as silent cancel (no SnackBar); thrown exception triggers 'Export failed. Check available storage and try again.' SnackBar (Pitfall 3)"
    - "CSV fields starting with =, +, -, @ prefixed with a single quote to mitigate CSV-injection (Security Domain V5)"
  artifacts:
    - path: "lib/features/settings/services/file_save_port.dart"
      provides: "Abstract FileSavePort port + concrete FlutterFileDialogSavePort implementation"
      contains: "abstract class FileSavePort"
    - path: "lib/features/settings/services/export_controller.dart"
      provides: "ExportController(AppDatabase, PackageInfo, FileSavePort) with exportAll() → Future<String?>"
      contains: "Future<String?> exportAll"
      min_lines: 100
    - path: "lib/features/settings/pages/export_screen.dart"
      provides: "ConsumerWidget with single Export action tile + SnackBar feedback (replaces 06-03 placeholder)"
      contains: "ExportController"
    - path: "lib/domain/providers/file_save_port_provider.dart"
      provides: "Provider<FileSavePort> Riverpod binding (mirrors notification_api_provider convention)"
      contains: "fileSavePortProvider"
  key_links:
    - from: "lib/features/settings/services/export_controller.dart"
      to: "lib/data/database/daos/*.dart (5 DAOs.getAll())"
      via: "Future.wait([dao.getAll() × 5])"
      pattern: "blockListDao\\.getAll\\(\\)"
    - from: "lib/features/settings/services/export_controller.dart"
      to: "lib/features/settings/services/file_save_port.dart"
      via: "FileSavePort.save(bytes, fileName, mimeTypes)"
      pattern: "FileSavePort"
    - from: "lib/features/settings/pages/export_screen.dart"
      to: "lib/features/settings/services/export_controller.dart"
      via: "controller.exportAll() inside ListTile.onTap try/catch"
      pattern: "exportAll\\(\\)"
---

<objective>
Wave 3 (parallel with 06-05 + 06-06): Export feature end-to-end. Builds FileSavePort port abstraction (Pigeon-port style for plugin-backed I/O), ExportController that reads all 5 DAOs in parallel and produces a verified ZIP archive (5 CSVs + data.json), and an ExportScreen wired to trigger the flow with SnackBar feedback. Replaces the 06-03 placeholder body.

Purpose: Deliver SETT-01 (export all data to CSV+JSON via ACTION_CREATE_DOCUMENT) with full unit + widget test coverage and a Pigeon-port-style mock that prevents real SAF intents during widget tests.
Output: 4 new feature files, 1 fixture activation, 1 placeholder screen replacement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-play-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-play-store-submission/06-PATTERNS.md
@.planning/phases/06-polish-play-store-submission/06-RESEARCH.md
@.planning/phases/06-polish-play-store-submission/06-UI-SPEC.md
@.planning/phases/06-polish-play-store-submission/06-03-SUMMARY.md

<interfaces>
<!-- Source-of-truth contracts the executor implements against. -->

FileSavePort (defined here; 06-01 fixture pre-declared a placeholder mock):
```dart
abstract class FileSavePort {
  /// Returns saved URI/path, or null on user cancel.
  /// Throws on I/O failure.
  Future<String?> save({
    required Uint8List bytes,
    required String fileName,
    required List<String> mimeTypes,
  });
}
```

From lib/domain/providers/notification_api_provider.dart (canonical Pigeon-port Provider binding analog):
```dart
final notificationApiProvider = Provider<NotificationApi>((ref) {
  return NotificationApi();
});
```

ZIP build pattern (RESEARCH §Pattern 2 + §Example 2, lines 731-752 — verified):
```dart
final archive = Archive()
  ..addFile(ArchiveFile('block_list.csv', csvBytes.length, csvBytes))
  ..addFile(ArchiveFile('data.json', jsonBytes.length, jsonBytes));
final zipBytes = ZipEncoder().encode(archive); // List<int>
final saved = await FlutterFileDialog.saveFile(
  params: SaveFileDialogParams(
    data: Uint8List.fromList(zipBytes),
    fileName: filename,
    mimeTypesFilter: <String>['application/zip'],
  ),
);
```

From lib/data/database/daos/*.dart (all 5 DAOs expose getAll() per 06-01 audit):
```dart
Future<List<BlockListData>> getAll();
Future<List<DailyCheckinData>> getAll();
Future<List<DailyStreakData>> getAll();
Future<List<PauseEventData>> getAll();
Future<List<DailyUsageSummaryData>> getAll();
```

CSV escape contract (15-line hand-roll per RESEARCH §Don't Hand-Roll + Security Domain V5):
```dart
String _escape(String s) {
  // 1. CSV-injection prefix: =, +, -, @ at field start → prefix with single quote
  // 2. RFC 4180: if field contains ',', '"', or '\n', wrap in quotes and double internal "
}
```

Filename pattern (D-09 — local-time date for human clarity; intl DateFormat):
```dart
final stamp = DateFormat('yyyyMMdd-HHmm').format(DateTime.now());
final filename = 'not-to-do-list-export-$stamp.zip';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FileSavePort interface + FlutterFileDialogSavePort impl + Riverpod provider + mock fixture activation</name>
  <files>lib/features/settings/services/file_save_port.dart, lib/domain/providers/file_save_port_provider.dart, test/_fixtures/file_save_port_mock.dart</files>
  <read_first>
    - lib/domain/providers/notification_api_provider.dart (canonical Provider binding analog)
    - test/_fixtures/permission_status_mock.dart (canonical mocktail fixture analog)
    - test/_fixtures/file_save_port_mock.dart (06-01 placeholder — update to real FileSavePort import)
    - test/features/settings/file_save_port_test.dart (RED stub from 06-01)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 513-553 — file_save_port pattern)
    - .planning/phases/06-polish-play-store-submission/06-RESEARCH.md (§Validation Architecture — FileSavePort sketch lines 1051-1071)
  </read_first>
  <behavior>
    - Test 1: FlutterFileDialogSavePort.save delegates to FlutterFileDialog.saveFile with SaveFileDialogParams whose mimeTypesFilter equals the input list
    - Test 2: When FlutterFileDialog.saveFile returns null, FileSavePort.save returns null (silent cancel — caller treats as no-op per Pitfall 3)
    - Test 3: When FlutterFileDialog.saveFile throws, FileSavePort.save rethrows (no swallowing; caller decides SnackBar)
    - Test 4: MockFileSavePort can be registered as Riverpod override in widget tests
  </behavior>
  <action>
    Create `lib/features/settings/services/file_save_port.dart` with two classes:
    - `abstract class FileSavePort { Future<String?> save({required Uint8List bytes, required String fileName, required List<String> mimeTypes}); }`
    - `class FlutterFileDialogSavePort implements FileSavePort` whose `save` method delegates: `return FlutterFileDialog.saveFile(params: SaveFileDialogParams(data: bytes, fileName: fileName, mimeTypesFilter: mimeTypes));` (note: `mimeTypesFilter` field name — plural per RESEARCH Pitfall hint about field-name spelling, lines 446-448).
    - Doc comments referencing SETT-01 + RESEARCH §Anti-Patterns ("wrap in port for test stubbing").

    Create `lib/domain/providers/file_save_port_provider.dart` mirroring `notification_api_provider.dart` shape:
    - `final Provider<FileSavePort> fileSavePortProvider = Provider<FileSavePort>((ref) => FlutterFileDialogSavePort());`

    Update `test/_fixtures/file_save_port_mock.dart` (placeholder from 06-01):
    - Remove any placeholder abstract class declaration
    - Import the real `FileSavePort` from `lib/features/settings/services/file_save_port.dart`
    - Define `class MockFileSavePort extends Mock implements FileSavePort {}` + `class FakeUint8List extends Fake implements Uint8List {}` if needed for mocktail registerFallbackValue
    - Mirror `permission_status_mock.dart` setUp pattern: registerFallbackValue calls in a `void setUpFileSavePortMock()` helper, exposed for test files to call in their setUpAll.

    Unskip `test/features/settings/file_save_port_test.dart` and implement Tests 1-4. Use mocktail `when(() => mock.save(...)).thenAnswer((_) async => 'path')` etc.
  </action>
  <verify>
    <automated>flutter test test/features/settings/file_save_port_test.dart &amp;&amp; dart analyze lib/features/settings/services/file_save_port.dart lib/domain/providers/file_save_port_provider.dart test/_fixtures/file_save_port_mock.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/settings/services/file_save_port.dart` contains `abstract class FileSavePort` AND `class FlutterFileDialogSavePort implements FileSavePort`
    - Source contains `mimeTypesFilter:` (plural — package field name)
    - `lib/domain/providers/file_save_port_provider.dart` contains `Provider<FileSavePort> fileSavePortProvider`
    - `test/_fixtures/file_save_port_mock.dart` contains `class MockFileSavePort extends Mock implements FileSavePort`
    - `flutter test test/features/settings/file_save_port_test.dart` exits 0 with 4 tests passing
    - `dart analyze` reports 0 errors on the 3 files
  </acceptance_criteria>
  <done>FileSavePort contract locked; mock fixture activated; widget tests can stub SAF safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExportController (DAO reads → CSV escape → JSON envelope → ZIP → SAF)</name>
  <files>lib/features/settings/services/export_controller.dart</files>
  <read_first>
    - lib/data/database/daos/block_list_dao.dart (existing getAll lines 25-30)
    - lib/data/database/daos/daily_checkins_dao.dart (getAll added in 06-01)
    - lib/data/database/daos/daily_streak_dao.dart (getAll added in 06-01)
    - lib/data/database/daos/pause_event_dao.dart (getAll added in 06-01)
    - lib/data/database/daos/daily_usage_summary_dao.dart (getAll added in 06-01)
    - lib/features/settings/services/file_save_port.dart (just created Task 1)
    - lib/data/database/app_database.dart (DAO access pattern)
    - test/features/settings/export_controller_test.dart (RED stub from 06-01)
    - test/features/settings/export_csv_format_test.dart (RED stub from 06-01)
    - test/features/settings/export_json_envelope_test.dart (RED stub from 06-01)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 398-446 — export_controller pattern)
    - .planning/phases/06-polish-play-store-submission/06-RESEARCH.md (§Pattern 2 + §Example 2 + §Don't Hand-Roll + Pitfalls 3,4 + §Security Domain V5)
  </read_first>
  <behavior>
    - Test 1: exportAll() reads all 5 DAOs in parallel via Future.wait; builds 5 CSVs whose header rows match Drift column names verbatim (D-10)
    - Test 2: CSV escape: fields with `,`, `"`, or `\n` wrapped in quotes with internal `"` doubled (RFC 4180); fields starting with `=`, `+`, `-`, `@` prefixed with single-quote (CSV-injection mitigation, Security Domain V5)
    - Test 3: data.json envelope shape: top-level keys exactly {schemaVersion, exportedAt, appVersion, tables}; schemaVersion == 1 (D-11); exportedAt parses as ISO-8601 UTC ending in Z (D-12); appVersion format `${pi.version}+${pi.buildNumber}` (e.g., '1.0.0+1')
    - Test 4: tables key has exactly the 5 table names (block_list, daily_checkins, daily_streak, pause_events, daily_usage_summary) as keys
    - Test 5: Filename matches regex `^not-to-do-list-export-\d{8}-\d{4}\.zip$` (D-09 local-time YYYYMMDD-HHMM)
    - Test 6: ZIP bytes decode round-trip via ArchiveDecoder to 6 files (5 CSVs + data.json)
    - Test 7: exportAll returns the FileSavePort.save() return value (passes through null cancel + real path)
  </behavior>
  <action>
    Create `lib/features/settings/services/export_controller.dart` per RESEARCH §Pattern 2 + §Example 2 (verified). File contents:

    Imports: `dart:convert`, `dart:typed_data`, `package:archive/archive.dart`, `package:intl/intl.dart`, `package:package_info_plus/package_info_plus.dart`, the 5 DAO/data classes, AppDatabase, FileSavePort.

    Class signature: `class ExportController { ExportController(this._db, this._packageInfo, this._saver); final AppDatabase _db; final PackageInfo _packageInfo; final FileSavePort _saver; }`

    Method `Future<String?> exportAll()`:
    1. Parallel DAO reads: `final futures = await Future.wait([_db.blockListDao.getAll(), _db.dailyCheckinsDao.getAll(), _db.dailyStreakDao.getAll(), _db.pauseEventDao.getAll(), _db.dailyUsageSummaryDao.getAll()]);` (each DAO has getAll per 06-01 audit).
    2. Build 5 CSV byte buffers via private helper `List<int> _toCsv(String tableName, List<dynamic> rows)`. Helper signature: takes table name (used to look up Drift column order) and row list (each row .toJson() gives a Map<String, dynamic> with Drift column names as keys). Header row = sorted column-name keys from first row OR a static per-table column list (use the Drift Data class's known field order). Wrap each row value via `_escape(String)` helper. Timestamp values formatted via `DateTime.toUtc().toIso8601String()` for DateTime fields; the .toJson() output from drift gives milliseconds since epoch for DateTime — convert to UTC ISO-8601 inside `_toCsv` when the field type indicates a timestamp (or detect by column-name convention — `createdAt`, `updatedAt`, etc.).
    3. Build envelope map: `{'schemaVersion': 1, 'exportedAt': DateTime.now().toUtc().toIso8601String(), 'appVersion': '${_packageInfo.version}+${_packageInfo.buildNumber}', 'tables': {'block_list': futures[0].map((r) => r.toJson()).toList(), 'daily_checkins': futures[1].map((r) => r.toJson()).toList(), ...}}`. JSON-encode with `jsonEncode(envelope)`; convert to bytes via `utf8.encode(...)`.
    4. Build archive: `final archive = Archive(); archive.addFile(ArchiveFile('block_list.csv', csv0.length, csv0)); ...; archive.addFile(ArchiveFile('data.json', jsonBytes.length, jsonBytes));` (5 CSVs + 1 JSON = 6 entries).
    5. Encode: `final zipBytes = ZipEncoder().encode(archive); // List<int>` — wrap via `Uint8List.fromList(zipBytes ?? <int>[])` per Pitfall 4; assert `zipBytes != null` first.
    6. Filename: `final stamp = DateFormat('yyyyMMdd-HHmm').format(DateTime.now()); final filename = 'not-to-do-list-export-$stamp.zip';` per D-09.
    7. Save: `return _saver.save(bytes: Uint8List.fromList(zipBytes!), fileName: filename, mimeTypes: const <String>['application/zip']);` Returns null on user cancel (passes through from FileSavePort).

    Private helpers in same file:
    - `String _escape(String s)`: 15 lines max per RESEARCH §Don't Hand-Roll. Body: (a) if `s` starts with `=`, `+`, `-`, or `@`, prefix with `'` (CSV-injection per OWASP); (b) if escaped `s` contains `,`, `"`, or `\n`, wrap with `"..."` and replace internal `"` with `""`; otherwise return as-is.
    - `String _formatField(dynamic value)`: helper that handles DateTime → UTC ISO-8601 + null → empty string + others → `_escape(value.toString())`.
    - `List<int> _toCsv(String tableName, List<dynamic> rows)`: builds header row from a hard-coded per-table column list (or pulls from `rows.first.toJson().keys` if rows non-empty), then emits one row per record using `_formatField`. Join with `\n`, encode UTF-8.

    Unskip and implement all 7 behaviors across `test/features/settings/export_controller_test.dart`, `export_csv_format_test.dart`, `export_json_envelope_test.dart`. Use an in-memory Drift database (existing test fixture pattern) seeded with 1-2 rows per table; use a mocktail `MockFileSavePort` that returns a fixed path; decode the zip bytes via `ArchiveDecoder` to assert the 6 entries are present.
  </action>
  <verify>
    <automated>flutter test test/features/settings/export_controller_test.dart test/features/settings/export_csv_format_test.dart test/features/settings/export_json_envelope_test.dart &amp;&amp; dart analyze lib/features/settings/services/export_controller.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/settings/services/export_controller.dart` exists with `class ExportController` and `Future<String?> exportAll()`
    - Source contains `ZipEncoder().encode(` AND `ArchiveFile(` AND `Future.wait(`
    - Source contains the literal `'schemaVersion': 1`
    - Source contains `DateFormat('yyyyMMdd-HHmm')`
    - Source contains `.toUtc().toIso8601String()` (for envelope exportedAt)
    - Source contains CSV-injection guard against `=`, `+`, `-`, `@` (regex grep or visual check)
    - All 3 test files exit 0 with at least 7 total tests passing (no skipped Phase 6 tests in these files)
    - `dart analyze` reports 0 errors on the file
  </acceptance_criteria>
  <done>Export builds verified ZIP with 5 CSVs (verbatim Drift column names, RFC 4180 + CSV-injection-safe escape) + envelope JSON (schemaVersion=1, ISO-8601 UTC); handed to FileSavePort; round-trip decode of zip bytes confirms 6 files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExportScreen widget (replaces 06-03 placeholder) + SAF cancel/error handling</name>
  <files>lib/features/settings/pages/export_screen.dart</files>
  <read_first>
    - lib/features/settings/pages/export_screen.dart (06-03 placeholder body)
    - lib/features/settings/services/export_controller.dart (Task 2)
    - lib/features/settings/services/file_save_port.dart (Task 1)
    - lib/features/onboarding/pages/post_notifications_earned_step.dart (lines 72-83 — _onContinue analog for async action handler)
    - lib/features/reminder/pages/reminder_settings_screen.dart (Scaffold + ListView + ListTile shell)
    - test/features/settings/export_screen_test.dart (Wave 0 RED stub in 06-01 — unskip during GREEN flip in this task)
    - .planning/phases/06-polish-play-store-submission/06-PATTERNS.md (lines 196-229 — export_screen pattern)
    - .planning/phases/06-polish-play-store-submission/06-UI-SPEC.md (§Component Inventory ExportScreen + §Copywriting Contract Export rows)
  </read_first>
  <behavior>
    - Test 1: ExportScreen renders an AppBar with title 'Export data' (UI-SPEC copy), and a single ListTile 'Export all data' with subtitle 'ZIP includes all entries, streaks, pause events, and check-ins' (UI-SPEC copy)
    - Test 2: Tapping the action calls `ref.read(exportControllerProvider).exportAll()` (or equivalent direct construction with injected deps); on non-null return shows SnackBar 'Export saved' (UI-SPEC literal)
    - Test 3: On null return (user cancel), no SnackBar is shown (silent — Pitfall 3)
    - Test 4: On thrown exception, shows SnackBar 'Export failed. Check available storage and try again.' (UI-SPEC literal)
    - Test 5: `if (!mounted) return;` / `if (context.mounted)` guard after the await (Phase 4/5 convention)
  </behavior>
  <action>
    Replace the placeholder body of `lib/features/settings/pages/export_screen.dart` (created in 06-03) with the real implementation:

    Imports: ExportController, fileSavePortProvider, databaseProvider (or whatever the AppDatabase Riverpod accessor is — check existing `lib/domain/providers/database_provider.dart`), `package:package_info_plus/package_info_plus.dart`, flutter material, flutter_riverpod, go_router.

    Class signature: `class ExportScreen extends ConsumerStatefulWidget` (ConsumerStatefulWidget because we need a `mounted` guard around the async + SnackBar). Or use `ConsumerWidget` with `context.mounted` guard — both acceptable. Prefer ConsumerWidget for consistency with the rest of Settings (per PATTERNS line 218).

    Body:
    - Scaffold(AppBar(title: const Text('Export data'), centerTitle: false), body: ListView(children: [ListTile(leading: Icon(Icons.archive_outlined), title: Text('Export all data'), subtitle: Text('ZIP includes all entries, streaks, pause events, and check-ins'), onTap: () => _onExport(context, ref))])).
    - `Future<void> _onExport(BuildContext context, WidgetRef ref) async { try { final db = ref.read(databaseProvider); final saver = ref.read(fileSavePortProvider); final pi = await PackageInfo.fromPlatform(); final controller = ExportController(db, pi, saver); final path = await controller.exportAll(); if (!context.mounted) return; if (path == null) return; ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Export saved'))); } catch (_) { if (!context.mounted) return; ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text('Export failed. Check available storage and try again.'))); } }`

    Unskip `test/features/settings/export_screen_test.dart` (Wave 0 RED stub in 06-01) and implement the 5 behaviors above. Tests cover the 5 behaviors using ProviderScope overrides for `fileSavePortProvider` (MockFileSavePort) + `databaseProvider` (in-memory AppDatabase fixture). Use `tester.tap(find.text('Export all data'))` then `await tester.pumpAndSettle()`, then `expect(find.text('Export saved'), findsOneWidget)` etc. Replace the `markTestSkipped(...)` lines from the 06-01 stub with real test bodies.
  </action>
  <verify>
    <automated>flutter test test/features/settings/ &amp;&amp; dart analyze lib/features/settings/pages/export_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/settings/pages/export_screen.dart` no longer contains the 06-03 TODO marker / CircularProgressIndicator placeholder
    - Source contains literal strings `'Export data'`, `'Export all data'`, `'ZIP includes all entries, streaks, pause events, and check-ins'`, `'Export saved'`, `'Export failed. Check available storage and try again.'` (UI-SPEC copy locks)
    - Source contains `ExportController(` instantiation OR a Riverpod provider that constructs one
    - Source contains `if (!context.mounted) return;` or `if (context.mounted)` guard
    - `test/features/settings/export_screen_test.dart` exits 0 with 5+ passing tests (no markTestSkipped remaining)
    - `flutter test test/features/settings/` exits 0 — no Phase 6 SETT-01 tests skipped
    - `dart analyze lib/features/settings/pages/export_screen.dart` reports 0 errors/warnings
  </acceptance_criteria>
  <done>Settings → Export tile → Export screen → tap action → SAF picker; on success SnackBar 'Export saved'; on cancel silent; on error SnackBar 'Export failed.…'; widget tests green.</done>
</task>

</tasks>

<verification>
- `flutter test test/features/settings/` — all SETT-01 tests pass (file_save_port, export_controller, export_csv_format, export_json_envelope, export_screen)
- `flutter test` (full suite) exits 0 — no regression
- `dart analyze lib/features/settings/services/ lib/features/settings/pages/export_screen.dart lib/domain/providers/file_save_port_provider.dart test/_fixtures/file_save_port_mock.dart` clean
- Smoke test (developer-run on real device): Settings → Export → tap "Export all data" → SAF picker opens → save → SnackBar "Export saved" → adb pull the file → unzip locally → confirm 6 entries + open block_list.csv in spreadsheet app (final cross-app round-trip is Manual-Only per 06-VALIDATION.md)
</verification>

<success_criteria>
- SETT-01 software-complete: ZIP with 5 CSVs (verbatim Drift column names + RFC 4180 + CSV-injection-safe) + data.json envelope (schemaVersion=1 + ISO-8601 UTC) handed to user via single ACTION_CREATE_DOCUMENT pick
- FileSavePort port abstraction in place — widget tests inject MockFileSavePort, never trigger real Android intents
- Cancel + error paths handled per Pitfall 3 (cancel = silent, exception = SnackBar)
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-play-store-submission/06-04-SUMMARY.md`
</output>
</output>
