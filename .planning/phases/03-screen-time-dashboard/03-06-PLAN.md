---
plan_id: 03-06
phase: 3
plan: 06
type: execute
wave: 4
objective: "Wave 4: ship the two home cards (AvoidedTodayCard + CumulativeTotalsCard) inserted between HealthCheckBanner and the entries ListView in HomeScreen + the DASH-07 perf gate. Closes DASH-05/DASH-06/DASH-07. Final phase exit: full flutter test green, dart analyze 0/0, flutter build apk --debug green, play_invariants_test.dart 8/8."
depends_on:
  - 03-05
files_modified:
  - lib/features/home/widgets/avoided_today_card.dart
  - lib/features/home/widgets/cumulative_totals_card.dart
  - lib/features/home/pages/home_screen.dart
  - test/features/home/widgets/avoided_today_card_test.dart
  - test/features/home/widgets/cumulative_totals_card_test.dart
  - test/perf/dashboard_render_test.dart
requirements:
  - DASH-05
  - DASH-06
  - DASH-07
autonomous: true
must_haves:
  truths:
    - "HomeScreen body renders, in this order: AnimatedSwitcher(banner) -> AvoidedTodayCard -> CumulativeTotalsCard -> Expanded(entries ListView). Cards are NOT animated-in (D-17 — they belong to home identity)."
    - "AvoidedTodayCard renders headline 'Avoided today' + subtitle '{X} of {Y} entries succeeded today' from avoidedTodayProvider; if usageAccess=false, replaces subtitle with 'Tracking is offline — tap to fix'; if Y=0, replaces subtitle with 'No entries yet' (D-09)."
    - "AvoidedTodayCard taps route to /dashboard when entries > 0 + usageAccess granted; route to /list/add-app when Y=0; route to /onboarding/permissions/usage-access when usageAccess=false (D-09)."
    - "CumulativeTotalsCard renders headline 'Total avoided' + subtitle '{N} launches blocked · {H}h {M}m saved' from cumulativeTotalsProvider; with empty pause_events renders '0 launches blocked · 0 m saved' correctly (D-11)."
    - "CumulativeTotalsCard tap routes to /dashboard."
    - "Both cards use Material 3 Card.outlined (D-17)."
    - "DASH-07 perf test: tester.pumpWidget(DashboardScreen against 30d x 20-app fixture) measured by Stopwatch reports elapsedMilliseconds < 300."
    - "All 8 of test/policy/play_invariants_test.dart invariants stay green."
    - "flutter test reports 0 failures, 0 errors. dart analyze reports 0 errors / 0 warnings (excluding 18 grandfathered pigeons/* infos)."
    - "flutter build apk --debug succeeds."
  artifacts:
    - path: "lib/features/home/widgets/avoided_today_card.dart"
      provides: "AvoidedTodayCard — DASH-05 home card"
      contains: "avoidedTodayProvider"
    - path: "lib/features/home/widgets/cumulative_totals_card.dart"
      provides: "CumulativeTotalsCard — DASH-06 home card"
      contains: "cumulativeTotalsProvider"
    - path: "lib/features/home/pages/home_screen.dart"
      provides: "HomeScreen with two cards inserted between banner and entries"
      contains: "AvoidedTodayCard"
    - path: "test/perf/dashboard_render_test.dart"
      provides: "DASH-07 first-frame budget — Stopwatch + tester.pumpWidget against 30d x 20-app fixture"
      contains: "Stopwatch"
  key_links:
    - from: "lib/features/home/widgets/avoided_today_card.dart"
      to: "lib/features/dashboard/providers/avoided_today_provider.dart"
      via: "ref.watch(avoidedTodayProvider)"
      pattern: "ref\\.watch\\(avoidedTodayProvider\\)"
    - from: "lib/features/home/widgets/cumulative_totals_card.dart"
      to: "lib/features/dashboard/providers/cumulative_totals_provider.dart"
      via: "ref.watch(cumulativeTotalsProvider)"
      pattern: "ref\\.watch\\(cumulativeTotalsProvider\\)"
    - from: "lib/features/home/pages/home_screen.dart"
      to: "lib/features/home/widgets/{avoided_today,cumulative_totals}_card.dart"
      via: "Column children — between AnimatedSwitcher(banner) and Expanded(entries)"
      pattern: "AvoidedTodayCard\\(\\)"
---

# Plan 03-06 — Wave 4: Home cards + DASH-07 perf gate + phase exit

<objective>
Final Phase 3 plan. Three deliverables:

1. Two new Material 3 `Card.outlined` widgets — `AvoidedTodayCard` (DASH-05) and `CumulativeTotalsCard` (DASH-06) — inserted into `HomeScreen` between the existing `HealthCheckBanner` and the entries `ListView`.
2. The DASH-07 perf gate (`test/perf/dashboard_render_test.dart`): `Stopwatch + tester.pumpWidget(DashboardScreen)` against the 30-day × 20-app seeded Drift fixture asserts `elapsedMilliseconds < 300`. CI-runnable, no real device. Real-device validation deferred to Phase 4 first task per D-20.
3. Phase exit gate — `flutter test && dart analyze && flutter build apk --debug && play_invariants_test.dart 8/8` all green.

Per Karpathy "Surgical Changes": HomeScreen gets ONLY two `const Padding(...AvoidedTodayCard...)` + `const Padding(...CumulativeTotalsCard...)` lines inserted in the existing `Column.children` list — `AnimatedSwitcher(banner)` and `Expanded(entries)` are byte-identical pre/post.

Per CONTEXT D-11: `pause_events` is empty in Phase 3 (Phase 4 writes it); the cumulative card MUST render `"0 launches blocked · 0 m saved"` correctly on day 1.

Per CONTEXT D-09: AvoidedTodayCard offline state reuses the literal `"Tracking is offline — tap to fix"` copy + routing to `/onboarding/permissions/usage-access`.

Per CONTEXT D-17: cards are NOT wrapped in `AnimatedSwitcher` (they belong to home identity, not transient state).

Output: 3 modified/new lib files + 3 modified test files; phase exits with all gates green; PHASE 3 COMPLETE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-screen-time-dashboard/03-CONTEXT.md
@.planning/phases/03-screen-time-dashboard/03-RESEARCH.md
@.planning/phases/03-screen-time-dashboard/03-PATTERNS.md
@.planning/phases/03-screen-time-dashboard/03-05-PLAN.md
@./CLAUDE.md
@lib/features/home/pages/home_screen.dart
@lib/features/health/widgets/health_check_banner.dart
@lib/features/dashboard/providers/avoided_today_provider.dart
@lib/features/dashboard/providers/cumulative_totals_provider.dart
@lib/features/dashboard/models/avoided_today_summary.dart
@lib/features/dashboard/models/cumulative_totals_summary.dart
@test/_fixtures/usage_summary_fixture.dart
@test/policy/play_invariants_test.dart

<interfaces>
<!-- Wave 2 + Phase 2 contracts the home cards bind to. -->

avoidedTodayProvider (Plan 03-04):
```dart
final StreamProvider<AvoidedTodaySummary> avoidedTodayProvider = ...;
class AvoidedTodaySummary {
  final int total;
  final int succeeded;
  final int pending;
  final int failed;
}
```

cumulativeTotalsProvider (Plan 03-04):
```dart
final StreamProvider<CumulativeTotalsSummary> cumulativeTotalsProvider = ...;
class CumulativeTotalsSummary {
  final int launchesBlocked;
  final int timeAvoidedSeconds;
}
```

permissionHealthProvider (Phase 2):
```dart
final permissionHealthProvider = ...;
class PermissionHealth { final bool usageAccess; final bool allHealthy; ... }
```

HomeScreen current body (lib/features/home/pages/home_screen.dart lines 39-64):
```dart
body: Column(
  children: [
    AnimatedSwitcher(/* banner */),
    Expanded(/* entries list */),
  ],
),
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-06-01 — Create AvoidedTodayCard + CumulativeTotalsCard + flip Wave 0 home-card stubs</name>
  <files>lib/features/home/widgets/avoided_today_card.dart, lib/features/home/widgets/cumulative_totals_card.dart, test/features/home/widgets/avoided_today_card_test.dart, test/features/home/widgets/cumulative_totals_card_test.dart</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/health/widgets/health_check_banner.dart
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/home/pages/home_screen.dart
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/dashboard/providers/avoided_today_provider.dart (Plan 03-04)
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/dashboard/providers/cumulative_totals_provider.dart (Plan 03-04)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/03-screen-time-dashboard/03-PATTERNS.md (lines 1062-1156)
- /Users/jintanakhomwong/projects/not-to-do-list/test/features/home/health_banner_test.dart (test harness pattern)
  </read_first>
  <behavior>
- AvoidedTodayCard:
  - Watches `avoidedTodayProvider` AND `permissionHealthProvider`.
  - When usageAccess=false: subtitle = literal `"Tracking is offline — tap to fix"`; tap routes to `/onboarding/permissions/usage-access`.
  - When AvoidedTodaySummary.total = 0: subtitle = `"No entries yet"`; tap routes to `/list/add-app`.
  - Otherwise: subtitle = `"{succeeded} of {total} entries succeeded today"`; tap routes to `/dashboard`.
  - Headline = `"Avoided today"` (always).
  - Card.outlined; tap target wraps the card body in InkWell.
- CumulativeTotalsCard:
  - Watches `cumulativeTotalsProvider`.
  - Headline = `"Total avoided"`.
  - Subtitle = `"{N} launches blocked · {H}h {M}m saved"` formatted from `launchesBlocked` + `timeAvoidedSeconds`.
  - Phase 3 starting state: `pause_events` empty -> "0 launches blocked · 0 m saved".
  - Tap routes to `/dashboard`.
  - Card.outlined.
- Both card test files: replace Wave 0 skip stubs with passing tests using `StreamProvider.overrideWith((ref) => Stream.value(...))` to inject summaries.
  </behavior>
  <action>
**1) `lib/features/home/widgets/avoided_today_card.dart`**:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:not_to_do_list/features/dashboard/providers/avoided_today_provider.dart';
import 'package:not_to_do_list/features/health/permission_health_provider.dart';

/// Home-screen card showing today's avoidance progress (DASH-05 / D-09).
/// Tap routes by priority:
/// - usageAccess=false -> /onboarding/permissions/usage-access
/// - total=0           -> /list/add-app
/// - otherwise         -> /dashboard
///
/// Material 3 Card.outlined per D-17 (low visual weight; does not compete
/// with the entries list below it).
class AvoidedTodayCard extends ConsumerWidget {
  const AvoidedTodayCard({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final summaryAsync = ref.watch(avoidedTodayProvider);
    final healthAsync = ref.watch(permissionHealthProvider);
    final tt = Theme.of(context).textTheme;

    final usageAccess =
        healthAsync.maybeWhen(data: (h) => h.usageAccess, orElse: () => true);

    String subtitle;
    VoidCallback onTap;

    if (!usageAccess) {
      subtitle = 'Tracking is offline — tap to fix';
      onTap = () => context.go('/onboarding/permissions/usage-access');
    } else {
      final summary = summaryAsync.valueOrNull;
      final total = summary?.total ?? 0;
      final succeeded = summary?.succeeded ?? 0;
      if (total == 0) {
        subtitle = 'No entries yet';
        onTap = () => context.go('/list/add-app');
      } else {
        subtitle = '$succeeded of $total entries succeeded today';
        onTap = () => context.go('/dashboard');
      }
    }

    return Card.outlined(
      margin: EdgeInsets.zero,
      child: InkWell(
        onTap: onTap,
        child: Padding(
          padding: const EdgeInsets.all(16),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text('Avoided today', style: tt.titleMedium),
              const SizedBox(height: 4),
              Text(
                subtitle,
                style: tt.bodyMedium?.copyWith(
                  color: Theme.of(context).colorScheme.onSurfaceVariant,
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
```

**2) `lib/features/home/widgets/cumulative_totals_card.dart`**:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';
import 'package:not_to_do_list/features/dashboard/providers/cumulative_totals_provider.dart';

/// Home-screen card showing all-time cumulative avoidance (DASH-06 / D-11).
/// Aggregates pause_events (outcome IN (0, 1)). Phase 3 starts at 0/0
/// because pause_events is empty until Phase 4 ships PauseActivity.
class CumulativeTotalsCard extends ConsumerWidget {
  const CumulativeTotalsCard({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final summaryAsync = ref.watch(cumulativeTotalsProvider);
    final tt = Theme.of(context).textTheme;
    final summary = summaryAsync.valueOrNull;
    final n = summary?.launchesBlocked ?? 0;
    final s = summary?.timeAvoidedSeconds ?? 0;
    final h = s ~/ 3600;
    final m = (s % 3600) ~/ 60;
    final timeText = h == 0 ? '${m}m' : '${h}h ${m}m';

    return Card.outlined(
      margin: EdgeInsets.zero,
      child: InkWell(
        onTap: () => context.go('/dashboard'),
        child: Padding(
          padding: const EdgeInsets.all(16),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text('Total avoided', style: tt.titleMedium),
              const SizedBox(height: 4),
              Text(
                '$n launches blocked · $timeText saved',
                style: tt.bodyMedium?.copyWith(
                  color: Theme.of(context).colorScheme.onSurfaceVariant,
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
```

**3) `test/features/home/widgets/avoided_today_card_test.dart`** (replaces Wave 0 stub):

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:go_router/go_router.dart';
import 'package:not_to_do_list/features/dashboard/models/avoided_today_summary.dart';
import 'package:not_to_do_list/features/dashboard/providers/avoided_today_provider.dart';
import 'package:not_to_do_list/features/health/permission_health_provider.dart';
import 'package:not_to_do_list/features/home/widgets/avoided_today_card.dart';

class _PermissionHealthFake extends PermissionHealthNotifier {
  _PermissionHealthFake(this._initial);
  final PermissionHealth _initial;
  @override
  Future<PermissionHealth> build() async => _initial;
}

Widget _wrap({
  required AvoidedTodaySummary summary,
  required PermissionHealth health,
}) {
  final router = GoRouter(
    initialLocation: '/',
    routes: [
      GoRoute(
        path: '/',
        builder: (_, __) => const Scaffold(body: AvoidedTodayCard()),
      ),
      GoRoute(
        path: '/dashboard',
        builder: (_, __) => const Scaffold(body: Text('dashboard-stub')),
      ),
      GoRoute(
        path: '/list/add-app',
        builder: (_, __) => const Scaffold(body: Text('add-app-stub')),
      ),
      GoRoute(
        path: '/onboarding/permissions/usage-access',
        builder: (_, __) => const Scaffold(body: Text('usage-access-stub')),
      ),
    ],
  );
  return ProviderScope(
    overrides: [
      avoidedTodayProvider.overrideWith((ref) => Stream.value(summary)),
      permissionHealthProvider
          .overrideWith(() => _PermissionHealthFake(health)),
    ],
    child: MaterialApp.router(routerConfig: router),
  );
}

const _healthOk = PermissionHealth(
  usageAccess: true,
  accessibilityService: true,
  batteryOptExempt: true,
  fingerprintChanged: false,
);

const _healthOffline = PermissionHealth(
  usageAccess: false,
  accessibilityService: true,
  batteryOptExempt: true,
  fingerprintChanged: false,
);

void main() {
  group('AvoidedTodayCard (DASH-05 / D-08 / D-09)', () {
    testWidgets('renders "{X} of {Y} entries succeeded today" when Y > 0', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const AvoidedTodaySummary(total: 5, succeeded: 3, pending: 1, failed: 1),
        health: _healthOk,
      ));
      await tester.pump();
      expect(find.text('Avoided today'), findsOneWidget);
      expect(find.text('3 of 5 entries succeeded today'), findsOneWidget);
    });

    testWidgets('renders "No entries yet" when total = 0', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const AvoidedTodaySummary(total: 0, succeeded: 0, pending: 0, failed: 0),
        health: _healthOk,
      ));
      await tester.pump();
      expect(find.text('No entries yet'), findsOneWidget);
    });

    testWidgets('renders "Tracking is offline — tap to fix" when usageAccess = false (D-13)', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const AvoidedTodaySummary(total: 5, succeeded: 3, pending: 0, failed: 2),
        health: _healthOffline,
      ));
      await tester.pump();
      expect(find.text('Tracking is offline — tap to fix'), findsOneWidget);
    });

    testWidgets('tap routes to /dashboard when entries > 0 + usageAccess granted', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const AvoidedTodaySummary(total: 3, succeeded: 2, pending: 0, failed: 1),
        health: _healthOk,
      ));
      await tester.pump();
      await tester.tap(find.byType(AvoidedTodayCard));
      await tester.pumpAndSettle();
      expect(find.text('dashboard-stub'), findsOneWidget);
    });

    testWidgets('tap routes to /list/add-app when total = 0', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const AvoidedTodaySummary(total: 0, succeeded: 0, pending: 0, failed: 0),
        health: _healthOk,
      ));
      await tester.pump();
      await tester.tap(find.byType(AvoidedTodayCard));
      await tester.pumpAndSettle();
      expect(find.text('add-app-stub'), findsOneWidget);
    });

    testWidgets('tap routes to /onboarding/permissions/usage-access when offline', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const AvoidedTodaySummary(total: 5, succeeded: 0, pending: 0, failed: 5),
        health: _healthOffline,
      ));
      await tester.pump();
      await tester.tap(find.byType(AvoidedTodayCard));
      await tester.pumpAndSettle();
      expect(find.text('usage-access-stub'), findsOneWidget);
    });
  });
}
```

**4) `test/features/home/widgets/cumulative_totals_card_test.dart`** (replaces Wave 0 stub):

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:go_router/go_router.dart';
import 'package:not_to_do_list/features/dashboard/models/cumulative_totals_summary.dart';
import 'package:not_to_do_list/features/dashboard/providers/cumulative_totals_provider.dart';
import 'package:not_to_do_list/features/home/widgets/cumulative_totals_card.dart';

Widget _wrap({required CumulativeTotalsSummary summary}) {
  final router = GoRouter(
    initialLocation: '/',
    routes: [
      GoRoute(
        path: '/',
        builder: (_, __) => const Scaffold(body: CumulativeTotalsCard()),
      ),
      GoRoute(
        path: '/dashboard',
        builder: (_, __) => const Scaffold(body: Text('dashboard-stub')),
      ),
    ],
  );
  return ProviderScope(
    overrides: [
      cumulativeTotalsProvider.overrideWith((ref) => Stream.value(summary)),
    ],
    child: MaterialApp.router(routerConfig: router),
  );
}

void main() {
  group('CumulativeTotalsCard (DASH-06 / D-10 / D-11)', () {
    testWidgets('renders "0 launches blocked · 0m saved" with empty pause_events', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const CumulativeTotalsSummary(launchesBlocked: 0, timeAvoidedSeconds: 0),
      ));
      await tester.pump();
      expect(find.text('Total avoided'), findsOneWidget);
      expect(find.text('0 launches blocked · 0m saved'), findsOneWidget);
    });

    testWidgets('formats time as "{H}h {M}m saved" for large totals', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const CumulativeTotalsSummary(launchesBlocked: 42, timeAvoidedSeconds: 5025),
      ));
      await tester.pump();
      // 5025s = 1h + 23m + 45s -> "1h 23m saved"
      expect(find.text('42 launches blocked · 1h 23m saved'), findsOneWidget);
    });

    testWidgets('tap routes to /dashboard', (tester) async {
      await tester.pumpWidget(_wrap(
        summary: const CumulativeTotalsSummary(launchesBlocked: 1, timeAvoidedSeconds: 60),
      ));
      await tester.pump();
      await tester.tap(find.byType(CumulativeTotalsCard));
      await tester.pumpAndSettle();
      expect(find.text('dashboard-stub'), findsOneWidget);
    });
  });
}
```
  </action>
  <verify>
    <automated>flutter test test/features/home/widgets/avoided_today_card_test.dart test/features/home/widgets/cumulative_totals_card_test.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- Files `lib/features/home/widgets/avoided_today_card.dart`, `lib/features/home/widgets/cumulative_totals_card.dart` exist.
- Both files contain `Card.outlined(` (verify: `grep -c 'Card.outlined(' lib/features/home/widgets/avoided_today_card.dart lib/features/home/widgets/cumulative_totals_card.dart` returns >= 2).
- `avoided_today_card.dart` contains the literal `'Tracking is offline — tap to fix'` (D-09 + D-13 reuse) — verify: `grep -F "'Tracking is offline — tap to fix'" lib/features/home/widgets/avoided_today_card.dart`.
- `avoided_today_card.dart` contains the literal `'Avoided today'` headline.
- `cumulative_totals_card.dart` contains the literal `'Total avoided'` headline AND `'launches blocked'` AND `' saved'`.
- `flutter test test/features/home/widgets/avoided_today_card_test.dart` exits 0 with 6 tests passed.
- `flutter test test/features/home/widgets/cumulative_totals_card_test.dart` exits 0 with 3 tests passed.
- No forbidden tokens (verify: `grep -v '^\s*///\|^\s*//\|^\s*\*' lib/features/home/widgets/avoided_today_card.dart lib/features/home/widgets/cumulative_totals_card.dart | grep -cE 'performAction\(|performGlobalAction\(|dispatchGesture\(|QUERY_ALL_PACKAGES|SYSTEM_ALERT_WINDOW|BIND_DEVICE_ADMIN'` returns 0).
- `flutter test test/policy/play_invariants_test.dart` 8/8 green — the new "Tracking is offline — tap to fix" string in `avoided_today_card.dart` does NOT trip any invariant (it's an allowed copy reuse — Phase 2 already shipped it in `health_check_banner.dart`).
  </acceptance_criteria>
  <done>Two home-card widgets land; Wave 0 home-card stubs flipped to passing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-06-02 — Insert two cards into HomeScreen between banner and entries</name>
  <files>lib/features/home/pages/home_screen.dart</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/home/pages/home_screen.dart
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/03-screen-time-dashboard/03-PATTERNS.md (lines 1163-1218 — HomeScreen insertion guide)
- /Users/jintanakhomwong/projects/not-to-do-list/test/features/home/home_screen_unified_list_test.dart
  </read_first>
  <behavior>
- HomeScreen body's `Column.children` grows from 2 children (`AnimatedSwitcher` + `Expanded`) to 4 children (`AnimatedSwitcher` + `Padding(AvoidedTodayCard)` + `Padding(CumulativeTotalsCard)` + `Expanded`).
- Cards each have `EdgeInsets.fromLTRB(16, 8, 16, 0)` and `EdgeInsets.fromLTRB(16, 8, 16, 8)` respectively (subtle vertical rhythm).
- Cards are NOT wrapped in AnimatedSwitcher (D-17 — they are home-identity, not transient state).
- All other parts of HomeScreen (AppBar, FloatingActionButton column, _homeEntriesProvider, BlockListRow, etc.) UNCHANGED.
- `home_screen_unified_list_test.dart` (Phase 2) MUST stay green — the test injects entries and expects them to render; new cards above are visible but the test asserts on entry rows, which still appear.
  </behavior>
  <action>
Edit `lib/features/home/pages/home_screen.dart`:

**Step 1 — Add imports** (alphabetically among the existing `features/home/widgets/...` imports):
```dart
import 'package:not_to_do_list/features/home/widgets/avoided_today_card.dart';
import 'package:not_to_do_list/features/home/widgets/cumulative_totals_card.dart';
```

**Step 2 — Insert the two cards.** Find the body `Column.children` block (lines 39-63 of current file):

```dart
      body: Column(
        children: [
          AnimatedSwitcher(
            duration: const Duration(milliseconds: 200),
            child: healthAsync.maybeWhen(
              data: (h) => h.allHealthy
                  ? const SizedBox.shrink()
                  : HealthCheckBanner(health: h),
              orElse: () => const SizedBox.shrink(),
            ),
          ),
          Expanded(
            child: entriesAsync.when(
              loading: () => const SizedBox.shrink(),
              error: (e, _) => Center(child: Text('$e')),
              data: (entries) {
                if (entries.isEmpty) return const EmptyHomeState();
                return ListView.builder(
                  itemCount: entries.length,
                  itemBuilder: (ctx, i) => BlockListRow(entry: entries[i]),
                );
              },
            ),
          ),
        ],
      ),
```

Insert two `const Padding(...)` children BETWEEN `AnimatedSwitcher(...)` and `Expanded(...)`:

```dart
      body: Column(
        children: [
          AnimatedSwitcher(
            duration: const Duration(milliseconds: 200),
            child: healthAsync.maybeWhen(
              data: (h) => h.allHealthy
                  ? const SizedBox.shrink()
                  : HealthCheckBanner(health: h),
              orElse: () => const SizedBox.shrink(),
            ),
          ),
          // Phase 3 D-17 — two home cards above the unified list. NOT
          // wrapped in AnimatedSwitcher: they belong to the home identity,
          // not transient state.
          const Padding(
            padding: EdgeInsets.fromLTRB(16, 8, 16, 0),
            child: AvoidedTodayCard(),
          ),
          const Padding(
            padding: EdgeInsets.fromLTRB(16, 8, 16, 8),
            child: CumulativeTotalsCard(),
          ),
          Expanded(
            child: entriesAsync.when(
              loading: () => const SizedBox.shrink(),
              error: (e, _) => Center(child: Text('$e')),
              data: (entries) {
                if (entries.isEmpty) return const EmptyHomeState();
                return ListView.builder(
                  itemCount: entries.length,
                  itemBuilder: (ctx, i) => BlockListRow(entry: entries[i]),
                );
              },
            ),
          ),
        ],
      ),
```

DO NOT touch:
- The `class HomeScreen extends ConsumerWidget` declaration.
- The `_homeEntriesProvider` declaration (lines 14-18).
- The `AppBar`, `floatingActionButton` block, `FloatingActionButtonLocation`.
- The `entriesAsync.when(...)` body (one indentation level inside Expanded).
- The `_homeEntriesProvider` import.

**Step 3 — Verify Phase 2 home_screen_unified_list_test.dart still passes.** It uses `_wrap({required BlockListRepository repo})` and overrides `blockListRepoProvider` + `appIconBytesProvider`. It does NOT override `avoidedTodayProvider` / `cumulativeTotalsProvider` / `permissionHealthProvider` — when those are not overridden, the Stream provider yields `loading()` indefinitely (which renders empty cards above) but the test asserts on entry rows further down the tree, which are unaffected. If the test fails because of new providers timing out, add the appropriate overrides to the test's `_wrap` helper:

```dart
overrides: [
  blockListRepoProvider.overrideWithValue(repo),
  appIconBytesProvider.overrideWith((ref, pkg) async => null),
  avoidedTodayProvider.overrideWith(
    (ref) => Stream.value(const AvoidedTodaySummary(total: 0, succeeded: 0, pending: 0, failed: 0)),
  ),
  cumulativeTotalsProvider.overrideWith(
    (ref) => Stream.value(const CumulativeTotalsSummary(launchesBlocked: 0, timeAvoidedSeconds: 0)),
  ),
],
```

Make the test edit ONLY if the test fails after the HomeScreen edit; otherwise leave it alone (Karpathy "Surgical Changes").
  </action>
  <verify>
    <automated>flutter test test/features/home/ test/policy/play_invariants_test.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
- `lib/features/home/pages/home_screen.dart` contains `import 'package:not_to_do_list/features/home/widgets/avoided_today_card.dart';` (verify: grep returns 1).
- `lib/features/home/pages/home_screen.dart` contains `import 'package:not_to_do_list/features/home/widgets/cumulative_totals_card.dart';`.
- `lib/features/home/pages/home_screen.dart` contains the literal `child: AvoidedTodayCard(),` (verify: grep returns 1).
- `lib/features/home/pages/home_screen.dart` contains the literal `child: CumulativeTotalsCard(),`.
- `lib/features/home/pages/home_screen.dart` cards are NOT inside an `AnimatedSwitcher` (verify: `grep -B2 'AvoidedTodayCard()' lib/features/home/pages/home_screen.dart` shows `Padding`, NOT `AnimatedSwitcher`).
- The `_homeEntriesProvider` declaration is unchanged (verify: `grep -A4 'final StreamProvider<List<BlockListData>> _homeEntriesProvider' lib/features/home/pages/home_screen.dart` matches the pre-edit form).
- `flutter test test/features/home/` exits 0 with all Phase 2 + Phase 3 home tests passing.
- `flutter test test/policy/play_invariants_test.dart` 8/8 green.
- No forbidden tokens (verify: `grep -v '^\s*///\|^\s*//\|^\s*\*' lib/features/home/pages/home_screen.dart | grep -cE 'performAction\(|performGlobalAction\(|dispatchGesture\(|QUERY_ALL_PACKAGES|SYSTEM_ALERT_WINDOW|BIND_DEVICE_ADMIN'` returns 0).
- `dart analyze lib/features/home/` reports 0 errors / 0 warnings.
  </acceptance_criteria>
  <done>HomeScreen body has two cards inserted; Phase 2 home tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-03 — DASH-07 perf gate + final phase exit</name>
  <files>test/perf/dashboard_render_test.dart</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/test/_fixtures/usage_summary_fixture.dart (Plan 03-01)
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/dashboard/pages/dashboard_screen.dart (Plan 03-05)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/03-screen-time-dashboard/03-RESEARCH.md (lines 940-991 — perf-test harness verbatim; lines 1218-1238 — A3 host-vs-Android proxy caveat)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/03-screen-time-dashboard/03-PATTERNS.md (lines 1301-1364 — perf test sketch)
- /Users/jintanakhomwong/projects/not-to-do-list/test/features/dashboard/dashboard_screen_test.dart (Plan 03-05 harness)
  </read_first>
  <behavior>
- Replace the Wave 0 skip stub with a real perf-test that:
  1. Seeds an in-memory Drift DB via `seedUsageSummary(db, days: 30, packages: 20)`.
  2. Wraps `DashboardScreen` in `ProviderScope` with `databaseProvider.overrideWithValue(db)` + `usageDaoProvider/usageRepositoryProvider/usageApiProvider/appIconBytesProvider` overrides.
  3. Starts a `Stopwatch`, calls `await tester.pumpWidget(...)`, stops the stopwatch.
  4. Asserts `stopwatch.elapsedMilliseconds < 300` with a clear `reason:` documenting the host wall-clock proxy caveat (research A3).
  5. Exits 0 with 1 test passed.
- Phase exit gate: `flutter test && dart analyze && flutter build apk --debug` all green; `play_invariants_test.dart` 8/8 green; record metrics in the SUMMARY.
  </behavior>
  <action>
**Step 1 — Replace `test/perf/dashboard_render_test.dart`** with the real harness:

```dart
import 'package:drift/native.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:go_router/go_router.dart';
import 'package:mocktail/mocktail.dart';
import 'package:not_to_do_list/data/database/app_database.dart';
import 'package:not_to_do_list/data/database/daos/daily_usage_summary_dao.dart';
import 'package:not_to_do_list/data/repositories/usage_repository.dart';
import 'package:not_to_do_list/domain/providers/database_provider.dart';
import 'package:not_to_do_list/domain/providers/usage_api_provider.dart';
import 'package:not_to_do_list/domain/providers/usage_dao_provider.dart';
import 'package:not_to_do_list/domain/providers/usage_repo_provider.dart';
import 'package:not_to_do_list/features/dashboard/pages/dashboard_screen.dart';
import 'package:not_to_do_list/features/list/providers/app_icon_cache_provider.dart';

import '../_fixtures/usage_summary_fixture.dart';
import '../platform/usage_api_test.dart' show MockUsageApi;

/// DASH-07: dashboard first frame must render in < 300 ms with a 30-day x
/// 20-app seeded fixture.
///
/// Caveat (research A3 / CONTEXT D-20): `flutter test` runs on the host
/// (Linux/macOS), not on Android. The 300 ms budget is a host wall-clock
/// proxy for mid-range Android — real-device validation is deferred to
/// Phase 4's first task per CONTEXT D-20 (re-measure on the Pixel emulator
/// used for Phase 2 UAT).
void main() {
  setUpAll(() => registerFallbackValue(0));

  testWidgets('DASH-07: DashboardScreen first frame < 300 ms with 30d x 20-app fixture',
      (tester) async {
    final db = AppDatabase(NativeDatabase.memory());
    addTearDown(db.close);
    await seedUsageSummary(db, days: 30, packages: 20);

    final api = MockUsageApi();
    when(() => api.queryRange(any(), any())).thenAnswer((_) async => const []);

    final router = GoRouter(
      initialLocation: '/dashboard',
      routes: [
        GoRoute(
          path: '/dashboard',
          builder: (_, __) => const DashboardScreen(),
        ),
        GoRoute(
          path: '/onboarding/permissions/usage-access',
          builder: (_, __) => const Scaffold(body: Text('usage-access-stub')),
        ),
      ],
    );

    final harness = ProviderScope(
      overrides: [
        databaseProvider.overrideWithValue(db),
        usageDaoProvider.overrideWithValue(DailyUsageSummaryDao(db)),
        usageApiProvider.overrideWithValue(api),
        usageRepositoryProvider.overrideWithValue(
          UsageRepository(DailyUsageSummaryDao(db), api),
        ),
        appIconBytesProvider.overrideWith((ref, pkg) async => null),
      ],
      child: MaterialApp.router(routerConfig: router),
    );

    final sw = Stopwatch()..start();
    await tester.pumpWidget(harness);
    sw.stop();

    expect(
      sw.elapsedMilliseconds,
      lessThan(300),
      reason:
          'DASH-07: dashboard first frame must render in < 300 ms; '
          'measured ${sw.elapsedMilliseconds} ms with 30d x 20-app fixture. '
          'Host wall-clock is a proxy for mid-range Android (research A3); '
          'real-device validation deferred to Phase 4 first task per D-20.',
    );

    // Drain stream timers so the test runner does not flag pending timers.
    await tester.runAsync(() async {
      await tester.pumpWidget(const SizedBox.shrink());
      await Future<void>.delayed(Duration.zero);
    });
  });
}
```

**Step 2 — Run the full phase exit gate** (commands in order):

```bash
# 1. Full test suite must be green.
flutter test 2>&1 | tail -5

# 2. dart analyze must be 0 errors / 0 warnings (excluding 18 grandfathered pigeons/* infos).
dart analyze 2>&1 | tail -5

# 3. APK build must succeed.
flutter build apk --debug 2>&1 | tail -5

# 4. Cross-tree policy invariants must be 8/8 green.
flutter test test/policy/play_invariants_test.dart 2>&1 | tail -5
```

If any of the four gates fails, diagnose and fix — DO NOT mark the task done with red gates.

**Step 3 — If `flutter test` fails on `test/perf/dashboard_render_test.dart` because the wall-clock measurement exceeds 300 ms on a slow CI runner:**
- First, investigate whether this is genuinely too slow (likely an excessive number of widget builds or stream re-bindings).
- Common culprit: `dashboardRowsProvider`'s `_join` runs over 600 daily_usage_summary rows on every emission. If perf is tight, consider memoizing the `totals` map (Karpathy: only optimize if the perf test fails — don't pre-optimize).
- DO NOT relax the budget below 300 ms without explicit user approval (CLAUDE.md "No destructive scope expansion").
- If diagnosing reveals the proxy is unrealistic for CI, document the observation in the SUMMARY and consider raising the budget WITH user sign-off (this is a checkpoint candidate, not auto-mode).

**Step 4 — Update `.planning/phases/03-screen-time-dashboard/03-VALIDATION.md`** frontmatter when Phase 3 exit succeeds:
- Flip `nyquist_compliant: false` -> `nyquist_compliant: true`.
- Flip `wave_0_complete: false` -> `wave_0_complete: true` (already done in Plan 03-01 SUMMARY but reaffirm here).
- Flip `status: draft` -> `status: complete`.
  </action>
  <verify>
    <automated>flutter test 2>&1 | tail -3 && echo "---" && dart analyze 2>&1 | tail -3 && echo "---" && flutter build apk --debug 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
- `test/perf/dashboard_render_test.dart` no longer contains `skip:` (verify: `grep -F 'skip:' test/perf/dashboard_render_test.dart` returns nothing).
- Test contains `Stopwatch()` AND `lessThan(300)` (verify: both grep return >= 1).
- `flutter test test/perf/dashboard_render_test.dart` exits 0 with 1 test passed.
- `flutter test` exits 0; the final summary shows passed = (Phase 2 baseline 124) + (Phase 3 new tests, ~25) and failed = 0 (verify: `flutter test 2>&1 | tail -1 | grep -F 'failed'` returns nothing OR the summary shows `0 failed`).
- `dart analyze` exits 0 errors / 0 warnings (excluding 18 grandfathered pigeons/* infos — those should NOT have grown).
- `flutter build apk --debug` exits 0 and `build/app/outputs/flutter-apk/app-debug.apk` exists.
- `flutter test test/policy/play_invariants_test.dart` reports `8/8` invariants green (verify all 8 individual tests pass).
- `.planning/phases/03-screen-time-dashboard/03-VALIDATION.md` frontmatter contains `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`.
- ROADMAP.md updated: Phase 3 row shows `6/6` plans complete.
- REQUIREMENTS.md DASH-01..07 statuses flipped from `Pending` to `Complete (Plan 03-NN)` for each requirement.
  </acceptance_criteria>
  <done>Phase 3 exits with all gates green: full flutter test + dart analyze + apk build + 8/8 policy invariants. PHASE 3 COMPLETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Perf test ↔ Drift in-memory DB | seedUsageSummary inserts deterministic rows; no real SQLite file. |
| Home cards ↔ Riverpod streams | autoDispose providers; widget tests use `Stream.value(...)` overrides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information disclosure | AvoidedTodayCard offline path | mitigate | Reuses the literal "Tracking is offline — tap to fix" copy that Phase 2 locked in `health_check_banner.dart`. play_invariants_test.dart's verbatim copy invariant for that string is unchanged. |
| T-3-03 | Tampering | cumulativeTotalsProvider SQL | mitigate | Already enforced in Plan 03-04. Acceptance criterion in Plan 03-04 grepped `outcome IN (0, 1)` — Pitfall #6 mitigation. |
| T-3-05 | Compliance | New UI files | mitigate | Both card files MUST NOT contain forbidden tokens. Acceptance criteria grep + play_invariants_test.dart enforces. |
| T-3-07 | Privacy | Home-card data | mitigate | Both cards consume Drift streams that read on-device data only. No telemetry. No FCM. allowBackup=false. |
| T-3-08 | Resource (DoS) | Stream subscriptions on home | mitigate | Both providers are `.autoDispose`. The home_screen_unified_list_test.dart `drainStreamTimers` helper covers stream-timer leakage if test failures surface. |
| Pitfall #6 (use-anyway counted) | Tampering | CumulativeTotalsCard | mitigate | Plan 03-04 enforces `outcome IN (0, 1)` literal SQL. Plan 03-06's card test verifies the expected zero-state and a positive-state by overriding the provider's stream value — does not regress the SQL invariant. |
</threat_model>

<verification>
- `flutter test` exits 0 with full suite green: Phase 2 baseline 124 + Phase 3 (~25 new tests including DAO + repo + range + dashboard widgets + home cards + perf gate). 0 failures.
- `dart analyze` exits 0 errors / 0 warnings (18 grandfathered pigeons/* infos unchanged).
- `flutter build apk --debug` exits 0; APK at `build/app/outputs/flutter-apk/app-debug.apk`.
- `flutter test test/policy/play_invariants_test.dart` reports `8/8` invariants green.
- `.planning/phases/03-screen-time-dashboard/03-VALIDATION.md` frontmatter flipped to `status: complete`.
- ROADMAP.md Phase 3 row checked off; REQUIREMENTS.md DASH-01..07 marked Complete.
- (Manual / Phase 4 first task) Real-device first-frame measurement deferred to Phase 4 per D-20.
</verification>

<success_criteria>
- DASH-05 (AvoidedTodayCard) shipped with all 3 D-09 copy variants and routing.
- DASH-06 (CumulativeTotalsCard) shipped with literal "0 launches blocked · 0 m saved" empty-state.
- DASH-07 perf gate (Stopwatch + pumpWidget against 30d × 20-app fixture) passes < 300 ms in CI.
- HomeScreen modified surgically — two cards inserted, no Phase 2 widgets touched.
- ALL Phase 3 exit gates green: flutter test, dart analyze, flutter build apk --debug, play_invariants_test.dart 8/8.
- Phase 3 VALIDATION.md flipped to complete.
- Phase 4 prerequisites (Pigeon channel pattern validated on a low-risk surface) satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-screen-time-dashboard/03-06-SUMMARY.md` per `templates/summary.md`. Include:
- Final perf measurement: actual `elapsedMilliseconds` reported by the perf test on this dev machine.
- Final test counts: `flutter test 2>&1 | tail -1` baseline.
- APK size delta vs Phase 2 (171.6 MB).
- 03-VERIFICATION.md template — populate per Phase 2 02-VERIFICATION.md precedent (cross-cutting evidence: schemaVersion still 2, manifest unchanged, AccessibilityApi/NotificationApi stubs untouched, Phase 4 / Phase 5 prerequisite signoff).
- ROADMAP.md update: Phase 3 row checked off, 6/6 plans complete.
- REQUIREMENTS.md: DASH-01..07 flipped from Pending to Complete with plan IDs.
</output>
