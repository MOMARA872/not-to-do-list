---
phase: 01-foundation-play-declaration
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/res/xml/notTodo_a11y_config.xml
  - android/app/src/main/res/xml/data_extraction_rules.xml
  - android/app/src/main/res/values/strings.xml
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt
  - docs/play-declaration.md
  - docs/data-safety.md
autonomous: true
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-04
  - PLAY-05
  - PLAY-07
  - PLAY-09
  - SETT-03
must_haves:
  truths:
    - "AndroidManifest declares <queries> with LAUNCHER intent filter"
    - "AndroidManifest does NOT declare QUERY_ALL_PACKAGES"
    - "AndroidManifest does NOT declare SYSTEM_ALERT_WINDOW"
    - "AndroidManifest does NOT declare USE_EXACT_ALARM (uses SCHEDULE_EXACT_ALARM)"
    - "AndroidManifest does NOT declare android.permission.INTERNET"
    - "AndroidManifest sets android:allowBackup=\"false\" on <application>"
    - "notTodo_a11y_config.xml sets isAccessibilityTool=\"false\""
    - "notTodo_a11y_config.xml sets accessibilityEventTypes=\"typeWindowStateChanged\" (and only that one)"
    - "notTodo_a11y_config.xml does NOT set canPerformGestures, canRetrieveWindowContent, or flagRequestFilterKeyEvents"
    - "docs/play-declaration.md exists and is the verbatim mechanical declaration source-of-truth"
    - "docs/data-safety.md exists and declares zero data collected"
    - "NotToDoAccessibilityService.kt stub class exists with empty onAccessibilityEvent so manifest entry compiles"
    - "PauseActivity.kt stub class exists (empty FlutterActivity) so manifest entry compiles"
    - "`flutter analyze` still passes"
  artifacts:
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Permission declarations + <queries> + service/activity entries"
      contains: "<queries>"
    - path: "android/app/src/main/res/xml/notTodo_a11y_config.xml"
      provides: "AccessibilityServiceInfo XML (PLAY-01, PLAY-03)"
      contains: "isAccessibilityTool=\"false\""
    - path: "android/app/src/main/res/xml/data_extraction_rules.xml"
      provides: "Backup-exclusion rules consistent with allowBackup=false"
      contains: "<data-extraction-rules>"
    - path: "android/app/src/main/res/values/strings.xml"
      provides: "Plain-language a11y service description"
      contains: "a11y_service_description"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt"
      provides: "Phase-1 stub class so manifest entry compiles; Phase 4 fills onAccessibilityEvent body"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt"
      provides: "Empty FlutterActivity stub so manifest entry compiles"
    - path: "docs/play-declaration.md"
      provides: "Source of truth for Play Console Permission Declaration form (PLAY-07)"
      min_lines: 60
    - path: "docs/data-safety.md"
      provides: "Source of truth for Play Console Data Safety form (PLAY-09)"
      min_lines: 40
  key_links:
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android/app/src/main/res/xml/notTodo_a11y_config.xml"
      via: "<meta-data android:resource=\"@xml/notTodo_a11y_config\"/>"
      pattern: "@xml/notTodo_a11y_config"
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android/app/src/main/res/xml/data_extraction_rules.xml"
      via: "android:dataExtractionRules"
      pattern: "@xml/data_extraction_rules"
    - from: "android/app/src/main/res/xml/notTodo_a11y_config.xml"
      to: "android/app/src/main/res/values/strings.xml"
      via: "android:description=\"@string/a11y_service_description\""
      pattern: "@string/a11y_service_description"
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt"
      via: "<service android:name=\".service.NotToDoAccessibilityService\">"
      pattern: ".service.NotToDoAccessibilityService"
    - from: "docs/play-declaration.md"
      to: "android/app/src/main/res/values/strings.xml"
      via: "a11y_service_description string mirrors §2 of the declaration"
      pattern: "Never automates clicks, gestures"
---

<objective>
Write the AndroidManifest, AccessibilityService config XML, data-extraction rules, strings.xml, the Phase-1 Kotlin stubs (NotToDoAccessibilityService + PauseActivity), and the two canonical documentation files (play-declaration.md, data-safety.md). All seven artifacts are reproduced verbatim in RESEARCH §4, §5, §8, §9 — this plan is a verbatim copy-from-research operation.

Purpose: Locks every Play-policy-relevant manifest declaration and config XML attribute BEFORE any service code is written (success criterion #2, #3, #5). The two docs files become the single source of truth that the Play Console form, the in-app prominent disclosure (Phase 2), and the Privacy Policy (Phase 6) all key off.

Output: A manifest-and-docs-complete Phase 1, with `flutter analyze` still able to compile (the Kotlin stubs ensure manifest entries point to real classes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-play-declaration/01-RESEARCH.md
@.planning/phases/01-foundation-play-declaration/01-01-SUMMARY.md

# Verbatim source of truth — copy-paste from these sections, do NOT re-derive
RESEARCH §4 — full AndroidManifest.xml (~90 lines)
RESEARCH §4 — data_extraction_rules.xml (~15 lines)
RESEARCH §4 — strings.xml (~5 lines)
RESEARCH §5 — notTodo_a11y_config.xml (full)
RESEARCH §8 — docs/play-declaration.md content (verbatim)
RESEARCH §9 — docs/data-safety.md content (verbatim)
RESEARCH Open Question #1 — Phase-1 NotToDoAccessibilityService.kt stub recommendation (ship empty onAccessibilityEvent override)
RESEARCH Pitfalls A (a11y config policy correctness), B (do NOT re-run flutter create)

# Existing files
- android/app/src/main/AndroidManifest.xml (Flutter-template default — to be REPLACED)
- android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt (Flutter-template default — leave alone, Plan 01-04 owns it)
</context>

<interfaces>
<!-- Resource references that the manifest, config XML, and docs all share. -->

`a11y_service_description` (from RESEARCH §4):

  Reads the foreground app's package name and shows your reflection screen when you open
  an app on your Not-To-Do list. Never automates clicks, gestures, or any UI action on your behalf.

This wording mirrors `docs/play-declaration.md` §2. Any change to the wording requires updating BOTH `strings.xml` and `docs/play-declaration.md` in the same commit.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write AndroidManifest.xml + data_extraction_rules.xml + strings.xml</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§4 — full verbatim AndroidManifest.xml + data_extraction_rules.xml + strings.xml)
    - /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/AndroidManifest.xml (current Flutter-template default — to be REPLACED)
  </read_first>
  <action>
    Replace `android/app/src/main/AndroidManifest.xml` with the EXACT content from RESEARCH §4 (lines 670-790 in 01-RESEARCH.md). Do NOT re-derive. Critical declarations:
    - 8 `<uses-permission>` entries: PACKAGE_USAGE_STATS, BIND_ACCESSIBILITY_SERVICE, POST_NOTIFICATIONS, RECEIVE_BOOT_COMPLETED, SCHEDULE_EXACT_ALARM, FOREGROUND_SERVICE, FOREGROUND_SERVICE_SPECIAL_USE, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS
    - DELIBERATELY ABSENT: SYSTEM_ALERT_WINDOW, QUERY_ALL_PACKAGES, USE_EXACT_ALARM, android.permission.INTERNET
    - `<queries>` block: one `<intent>` with action MAIN + category LAUNCHER (PLAY-04)
    - `<application>` attributes: `android:allowBackup="false"`, `android:fullBackupContent="false"`, `android:dataExtractionRules="@xml/data_extraction_rules"`, `tools:replace="android:allowBackup"`
    - 3 application components:
      1. `.MainActivity` (existing — declared with intent-filter MAIN + LAUNCHER)
      2. `.PauseActivity` (`exported="false"`, `launchMode="singleInstance"`, `excludeFromRecents="true"`, `taskAffinity=""`, `showOnLockScreen="true"`)
      3. `.service.NotToDoAccessibilityService` (`exported="false"`, `permission="android.permission.BIND_ACCESSIBILITY_SERVICE"`, intent-filter `android.accessibilityservice.AccessibilityService`, meta-data resource `@xml/notTodo_a11y_config`)
    - `<meta-data android:name="flutterEmbedding" android:value="2"/>` preserved
    - The XML namespaces on `<manifest>`: `xmlns:android="http://schemas.android.com/apk/res/android"` AND `xmlns:tools="http://schemas.android.com/tools"`

    Replace `android/app/src/main/res/values/strings.xml` (or merge if Flutter-template wrote one) with EXACTLY 3 string entries from RESEARCH §4:
      - `app_name` = "Not To-Do List"
      - `a11y_service_label` = "Not To-Do List"
      - `a11y_service_description` = "Reads the foreground app\'s package name and shows your reflection screen when you open an app on your Not-To-Do list. Never automates clicks, gestures, or any UI action on your behalf."

    The escaped apostrophe `\'` in `app\'s` is required by Android XML.

    Create `android/app/src/main/res/xml/data_extraction_rules.xml` with the EXACT content from RESEARCH §4: a `<data-extraction-rules>` root with both `<cloud-backup>` and `<device-transfer>` children, each excluding domains `root`, `file`, `database`, `sharedpref`.

    Per CLAUDE.md "Surgical Changes": replace these three files completely. Do NOT preserve any auto-generated `<application>` body that `flutter create` produced — the manifest in RESEARCH §4 IS the new authoritative content.

    Per CLAUDE.md "Simplicity First": do NOT add any `<provider>`, `<receiver>`, or other manifest entries beyond §4. PauseActivity, MainActivity, and NotToDoAccessibilityService are the only three application components in Phase 1.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; M=android/app/src/main/AndroidManifest.xml &amp;&amp; D=android/app/src/main/res/xml/data_extraction_rules.xml &amp;&amp; S=android/app/src/main/res/values/strings.xml &amp;&amp; grep -q '<queries>' "$M" &amp;&amp; grep -A2 '<queries>' "$M" | grep -q 'android.intent.category.LAUNCHER' &amp;&amp; ! grep -q 'QUERY_ALL_PACKAGES' "$M" &amp;&amp; ! grep -q 'SYSTEM_ALERT_WINDOW' "$M" &amp;&amp; ! grep -q 'USE_EXACT_ALARM' "$M" &amp;&amp; ! grep -q 'android.permission.INTERNET' "$M" &amp;&amp; grep -q 'android:allowBackup="false"' "$M" &amp;&amp; grep -q 'android:dataExtractionRules="@xml/data_extraction_rules"' "$M" &amp;&amp; grep -q 'SCHEDULE_EXACT_ALARM' "$M" &amp;&amp; grep -q '\.PauseActivity' "$M" &amp;&amp; grep -q '\.service\.NotToDoAccessibilityService' "$M" &amp;&amp; grep -q '@xml/notTodo_a11y_config' "$M" &amp;&amp; grep -q '@string/a11y_service_label' "$M" &amp;&amp; [ -f "$D" ] &amp;&amp; grep -q 'data-extraction-rules' "$D" &amp;&amp; grep -q 'a11y_service_description' "$S" &amp;&amp; grep -q 'Never automates clicks' "$S"</automated>
  </verify>
  <acceptance_criteria>
    - V11: grep '<queries>' AND grep -A2 '<queries>' contains 'android.intent.category.LAUNCHER' in AndroidManifest.xml — exits 0
    - V5: `! grep -q "QUERY_ALL_PACKAGES" android/app/src/main/AndroidManifest.xml` exits 0
    - V6: `! grep -q "SYSTEM_ALERT_WINDOW" android/app/src/main/AndroidManifest.xml` exits 0
    - V7: `! grep -q "USE_EXACT_ALARM" android/app/src/main/AndroidManifest.xml` exits 0
    - SETT-03 manifest gate: `! grep -q "android.permission.INTERNET" android/app/src/main/AndroidManifest.xml` exits 0
    - V12: `grep -q 'android:allowBackup="false"' android/app/src/main/AndroidManifest.xml` exits 0
    - `grep -q "SCHEDULE_EXACT_ALARM" android/app/src/main/AndroidManifest.xml` exits 0
    - `grep -q "\.PauseActivity" android/app/src/main/AndroidManifest.xml` exits 0
    - `grep -q "\.service\.NotToDoAccessibilityService" android/app/src/main/AndroidManifest.xml` exits 0
    - `grep -q "@xml/notTodo_a11y_config" android/app/src/main/AndroidManifest.xml` exits 0
    - `[ -f android/app/src/main/res/xml/data_extraction_rules.xml ]` exits 0
    - `grep -q "a11y_service_description" android/app/src/main/res/values/strings.xml` exits 0
    - `grep -q "Never automates clicks" android/app/src/main/res/values/strings.xml` exits 0
  </acceptance_criteria>
  <done>Manifest declares queries+LAUNCHER, allowBackup=false, SCHEDULE_EXACT_ALARM, PauseActivity, AccessibilityService stub. INTERNET, SYSTEM_ALERT_WINDOW, QUERY_ALL_PACKAGES, USE_EXACT_ALARM all absent. Data-extraction rules and strings present.</done>
</task>

<task type="auto">
  <name>Task 2: Write notTodo_a11y_config.xml + Kotlin stubs (NotToDoAccessibilityService, PauseActivity)</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§5 — full verbatim notTodo_a11y_config.xml; Open Question #1 — empty-stub recommendation)
    - /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/AndroidManifest.xml (just written by Task 1 — references the files we are writing here)
  </read_first>
  <action>
    Create `android/app/src/main/res/xml/notTodo_a11y_config.xml` with the EXACT content from RESEARCH §5. Required attributes on the single `<accessibility-service>` element:
      - `xmlns:android="http://schemas.android.com/apk/res/android"`
      - `android:accessibilityEventTypes="typeWindowStateChanged"` (single value, no `|`-OR with other types)
      - `android:accessibilityFeedbackType="feedbackGeneric"`
      - `android:notificationTimeout="100"`
      - `android:isAccessibilityTool="false"` (PLAY-01)
      - `android:accessibilityFlags="flagDefault"` (PLAY-03 — no `flagRequestFilterKeyEvents`)
      - `android:description="@string/a11y_service_description"`

    EXPLICITLY OMITTED attributes (do NOT add):
      - `canPerformGestures`
      - `canRetrieveWindowContent`
      - `flagRequestFilterKeyEvents`

    Include the full XML comment block from RESEARCH §5 explaining the omissions — it is part of the source-of-truth contract for future Phase-4 work.

    Create the Phase-1 stub Kotlin AccessibilityService class. Per RESEARCH Open Question #1, ship an empty `onAccessibilityEvent` override so the manifest entry compiles. File: `android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt`. The class must:
      - have `package com.nottodo.not_to_do_list.service`
      - import `android.accessibilityservice.AccessibilityService` and `android.view.accessibility.AccessibilityEvent`
      - declare `class NotToDoAccessibilityService : AccessibilityService()`
      - override `onAccessibilityEvent(event: AccessibilityEvent?)` with an empty body
      - override `onInterrupt()` with an empty body
      - have a KDoc on the class explaining: (a) Phase-1 stub, (b) Phase-4 will implement filter+debounce+match+launch, (c) PLAY-02 requires NEVER calling performAction/performGlobalAction/dispatchGesture
      - NOT import or reference any of: SQLite, Drift, SQLDelight, Room, SQLiteOpenHelper (RESEARCH §13 anti-pattern lockdown — DB access in service is forbidden)
      - NOT contain any of the strings `performAction`, `performGlobalAction`, or `dispatchGesture` (PLAY-02 enforcement-by-absence)

    Create the Phase-1 PauseActivity stub. File: `android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt`. The class must:
      - have `package com.nottodo.not_to_do_list`
      - import `io.flutter.embedding.android.FlutterActivity`
      - declare `class PauseActivity : FlutterActivity()` (empty body)
      - have a KDoc explaining: (a) Phase-1 stub, (b) Phase-4 will implement setShowWhenLocked/setTurnScreenOn/Intent-extras-handling/Flutter-route

    Run `flutter analyze` to confirm the Kotlin files don't break Dart analysis. Do NOT run `flutter build apk --debug` here — Plan 01-05 owns the APK build (this plan must remain parallel-safe with 01-03 and 01-04).

    Per CLAUDE.md "Surgical Changes": leave `MainActivity.kt` untouched. Plan 01-04 owns it (Pigeon stub registrations need to land AFTER `dart run pigeon` produces the generated Kotlin files).
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; A=android/app/src/main/res/xml/notTodo_a11y_config.xml &amp;&amp; SVC=android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt &amp;&amp; PA=android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt &amp;&amp; [ -f "$A" ] &amp;&amp; grep -q 'isAccessibilityTool="false"' "$A" &amp;&amp; grep -q 'accessibilityEventTypes="typeWindowStateChanged"' "$A" &amp;&amp; ! grep -qE 'canPerformGestures|canRetrieveWindowContent|flagRequestFilterKeyEvents|typeAllMask|typeViewClicked|typeViewFocused' "$A" &amp;&amp; [ -f "$SVC" ] &amp;&amp; grep -q 'class NotToDoAccessibilityService : AccessibilityService' "$SVC" &amp;&amp; ! grep -qiE 'sqlite|drift|sqldelight|room|sqliteopenhelper' "$SVC" &amp;&amp; ! grep -qE 'performAction|performGlobalAction|dispatchGesture' "$SVC" &amp;&amp; [ -f "$PA" ] &amp;&amp; grep -q 'class PauseActivity : FlutterActivity' "$PA" &amp;&amp; flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - V8: `grep -q 'isAccessibilityTool="false"' android/app/src/main/res/xml/notTodo_a11y_config.xml` exits 0
    - PLAY-03: `grep -q 'accessibilityEventTypes="typeWindowStateChanged"' android/app/src/main/res/xml/notTodo_a11y_config.xml` exits 0
    - V9: `! grep -qE 'canPerformGestures|canRetrieveWindowContent|flagRequestFilterKeyEvents' android/app/src/main/res/xml/notTodo_a11y_config.xml` exits 0
    - PLAY-03 (additional): `! grep -qE 'typeAllMask|typeViewClicked|typeViewFocused' android/app/src/main/res/xml/notTodo_a11y_config.xml` exits 0
    - `[ -f android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt ]` exits 0
    - `grep -q 'class NotToDoAccessibilityService : AccessibilityService' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt` exits 0
    - PLAY-02 by absence: `! grep -qE 'performAction|performGlobalAction|dispatchGesture' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt` exits 0
    - RESEARCH §13 anti-pattern: `! grep -qiE 'sqlite|drift|sqldelight|room|sqliteopenhelper' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt` exits 0
    - `[ -f android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt ]` exits 0
    - `grep -q 'class PauseActivity : FlutterActivity' android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt` exits 0
    - `flutter analyze` exits 0
  </acceptance_criteria>
  <done>A11y config XML is policy-correct. NotToDoAccessibilityService and PauseActivity stubs compile. flutter analyze passes.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/play-declaration.md and docs/data-safety.md (verbatim from RESEARCH §8 and §9)</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§8 — full verbatim docs/play-declaration.md content; §9 — full verbatim docs/data-safety.md content)
  </read_first>
  <action>
    Create `docs/play-declaration.md` with the EXACT content from RESEARCH §8 (lines ~1145-1234 of 01-RESEARCH.md). The content is the literal mechanical text that lands in the Play Console Permission Declaration form. Required headings (verbatim):
      - `# Play Console Permission Declaration — Not To-Do List`
      - Bold metadata block: "Source of truth for:" with 6 bulleted destinations (Play Console form, a11y_config XML description, strings.xml, in-app prominent disclosure (Phase 2 PLAY-06), Privacy Policy (Phase 6), data-safety.md cross-reference)
      - `**Last updated:** 2026-04-27 (Phase 1 commit)`
      - `**Policy version this matches:** Use of the AccessibilityService API, post Jan-28-2026 + Apr-15-2026 Play Console policy.`
      - `## 1. App description and core function`
      - `## 2. Why Not To-Do List uses the AccessibilityService API` — must contain the literal sentence "Never automates clicks, gestures, or any UI action on your behalf." somewhere in the section, and must contain bullet lists titled "What the service does, mechanically:" and "What the service NEVER does:"
      - `## 3. Why no narrower API works` — explains UsageStatsManager lag and getRunningTasks deprecation
      - `## 4. Disclosure and consent`
      - `## 5. Data Safety form alignment` — must include the line "Data collected: NONE."
      - `## 6. Manifest and configuration cross-reference` — must contain a verification table mapping claims to files

    Required tokens that the V13 grep / acceptance criteria below check:
      - The string "no data collected" OR "Data Collected: None" must appear (data-safety.md cross-reference grep — RESEARCH validates "no data collected")
      - The phrase "performAction" must appear (in §2 listing what the service NEVER does)
      - The phrase "performGlobalAction" must appear
      - The phrase "dispatchGesture" must appear
      - The phrase "isAccessibilityTool" must appear
      - The phrase "QUERY_ALL_PACKAGES" must appear (in §6 cross-reference)
      - The phrase "SYSTEM_ALERT_WINDOW" must appear (in §6 cross-reference)

    Create `docs/data-safety.md` with the EXACT content from RESEARCH §9 (lines ~1240-1304 of 01-RESEARCH.md). Required headings:
      - `# Play Console Data Safety Form — Not To-Do List`
      - `**Source of truth for:** the Play Console Data Safety form (PLAY-09).`
      - `**Last updated:** 2026-04-27 (Phase 1 commit)`
      - `## Section 1. Data collection and sharing` — must contain the answer "No." to "Does your app collect or share any of the required user data types?"
      - `## Section 2. Data types — declaration` — 13 data categories all declared "NOT COLLECTED"
      - `## Section 3. Verification — actual code matches declaration` — includes the dependency audit grep command
      - `## Section 4. User controls`
      - `## Section 5. Cross-reference` — points back to play-declaration.md

    Required tokens that V14 / VALIDATION acceptance criteria check:
      - The phrase "no data collected" OR "Data Collected: None" must appear (the validation matrix grep for `"no data collected\|Data Collected: None"` must succeed)
      - The phrase "NOT COLLECTED" must appear (the 13 data categories)
      - The phrase "100% on-device" or "fully on-device" or equivalent must appear
      - The exact dependency-audit grep command `flutter pub deps | grep -iE "firebase|fcm|analytics|crashlytics|amplitude|mixpanel|segment|sentry|datadog|appsflyer|adjust"` must appear (Phase 6 reuses it)

    Per CLAUDE.md "Think Before Coding" + RESEARCH §8 drafting note: "The Phase 1 task is to commit this file, NOT to edit the prose." If the executor finds prose they would edit, they MUST stop and surface the suggested edit as a follow-up task — they MUST NOT silently rewrite the declaration. The text in RESEARCH §8 was reviewed against the Use-of-AccessibilityService policy lane and any edit triggers a new GSD discuss → research → plan cycle.

    Per CLAUDE.md "Simplicity First": do NOT add a `docs/README.md` index, a TOC, or any auxiliary docs file. Phase 1 ships these two files only. Phase 6 may add a Privacy Policy markdown later.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; PD=docs/play-declaration.md &amp;&amp; DS=docs/data-safety.md &amp;&amp; [ -s "$PD" ] &amp;&amp; [ -s "$DS" ] &amp;&amp; grep -q '^# Play Console Permission Declaration' "$PD" &amp;&amp; grep -q 'performAction' "$PD" &amp;&amp; grep -q 'performGlobalAction' "$PD" &amp;&amp; grep -q 'dispatchGesture' "$PD" &amp;&amp; grep -q 'isAccessibilityTool' "$PD" &amp;&amp; grep -q 'QUERY_ALL_PACKAGES' "$PD" &amp;&amp; grep -q 'SYSTEM_ALERT_WINDOW' "$PD" &amp;&amp; grep -q '^# Play Console Data Safety Form' "$DS" &amp;&amp; grep -qE 'no data collected|Data Collected: None|NONE' "$DS" &amp;&amp; grep -q 'NOT COLLECTED' "$DS" &amp;&amp; grep -qE 'firebase|fcm|analytics|crashlytics' "$DS" &amp;&amp; [ "$(wc -l < "$PD")" -ge 60 ] &amp;&amp; [ "$(wc -l < "$DS")" -ge 40 ]</automated>
  </verify>
  <acceptance_criteria>
    - V13 (file presence + non-empty): `[ -s docs/play-declaration.md ] && [ -s docs/data-safety.md ]` exits 0
    - PLAY-07: `grep -q "^# Play Console Permission Declaration" docs/play-declaration.md` exits 0
    - PLAY-07 (PLAY-02 cross-ref): `grep -q "performAction" docs/play-declaration.md && grep -q "performGlobalAction" docs/play-declaration.md && grep -q "dispatchGesture" docs/play-declaration.md` exits 0
    - PLAY-07 (cross-ref to manifest): `grep -q "isAccessibilityTool" docs/play-declaration.md && grep -q "QUERY_ALL_PACKAGES" docs/play-declaration.md && grep -q "SYSTEM_ALERT_WINDOW" docs/play-declaration.md` exits 0
    - PLAY-09: `grep -q "^# Play Console Data Safety Form" docs/data-safety.md` exits 0
    - PLAY-09 (zero-collection claim): `grep -qE "no data collected|Data Collected: None|NONE" docs/data-safety.md` exits 0
    - PLAY-09 (13-categories declaration): `grep -q "NOT COLLECTED" docs/data-safety.md` exits 0
    - PLAY-09 (dep-audit reference): `grep -qE "firebase|fcm|analytics|crashlytics" docs/data-safety.md` exits 0
    - Min length: docs/play-declaration.md has ≥60 lines AND docs/data-safety.md has ≥40 lines
  </acceptance_criteria>
  <done>Both docs files exist verbatim from RESEARCH §8 and §9. They are the single source of truth for Play Console form text and the in-app prominent disclosure (Phase 2).</done>
</task>

<task type="auto">
  <name>Task 4: Run full Phase-1 manifest+docs verification gate (V5–V13 from RESEARCH §10)</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§10 — full verification commands V1–V14)
  </read_first>
  <action>
    Run all manifest+docs verification commands from RESEARCH §10 (V5–V13) to confirm this plan's artifacts are policy-correct. Each command's expected output is "exit 0" (or as documented in §10). Run sequentially; the first failure halts the gate.

    V5 — QUERY_ALL_PACKAGES absent in manifest:
      `! grep -q "QUERY_ALL_PACKAGES" android/app/src/main/AndroidManifest.xml`

    V6 — SYSTEM_ALERT_WINDOW absent in manifest:
      `! grep -q "SYSTEM_ALERT_WINDOW" android/app/src/main/AndroidManifest.xml`

    V7 — USE_EXACT_ALARM absent (we use SCHEDULE_EXACT_ALARM instead):
      `! grep -q "USE_EXACT_ALARM" android/app/src/main/AndroidManifest.xml`

    V8 — isAccessibilityTool="false" present in a11y config:
      `grep -q 'isAccessibilityTool="false"' android/app/src/main/res/xml/notTodo_a11y_config.xml`

    V9 — privileged a11y flags absent:
      `! grep -qE "canPerformGestures|canRetrieveWindowContent|flagRequestFilterKeyEvents" android/app/src/main/res/xml/notTodo_a11y_config.xml`

    V10 — dep-tree audit shows zero telemetry SDKs (re-running the check from Plan 01-01 Task 3 step 6):
      `flutter pub deps | grep -iE "firebase|fcm|analytics|crashlytics|amplitude|mixpanel|segment|sentry|datadog|appsflyer|adjust" || echo "OK: no telemetry deps"`
      Expected: `OK: no telemetry deps`

    V11 — `<queries>` element + LAUNCHER intent filter present:
      `grep -q "<queries>" android/app/src/main/AndroidManifest.xml && grep -A2 "<queries>" android/app/src/main/AndroidManifest.xml | grep -q "android.intent.category.LAUNCHER"`

    V12 — allowBackup="false" present:
      `grep -q 'android:allowBackup="false"' android/app/src/main/AndroidManifest.xml`

    V13 — both docs present and non-empty:
      `test -s docs/play-declaration.md && test -s docs/data-safety.md`

    Additional SETT-03 check (RESEARCH §12 validation matrix):
      `! grep -q "android.permission.INTERNET" android/app/src/main/AndroidManifest.xml`

    Record the exact pass/fail status of each in the task SUMMARY. Do not modify any artifact in this task — this is a read-only verification gate.

    Per CLAUDE.md "Goal-Driven Execution": this task IS the verification loop. If any command fails, the task fails — go fix the upstream task that produced the bad artifact, then re-run this gate. Do NOT mask failures.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; M=android/app/src/main/AndroidManifest.xml &amp;&amp; A=android/app/src/main/res/xml/notTodo_a11y_config.xml &amp;&amp; ! grep -q "QUERY_ALL_PACKAGES" "$M" &amp;&amp; ! grep -q "SYSTEM_ALERT_WINDOW" "$M" &amp;&amp; ! grep -q "USE_EXACT_ALARM" "$M" &amp;&amp; ! grep -q "android.permission.INTERNET" "$M" &amp;&amp; grep -q 'isAccessibilityTool="false"' "$A" &amp;&amp; ! grep -qE "canPerformGestures|canRetrieveWindowContent|flagRequestFilterKeyEvents" "$A" &amp;&amp; (flutter pub deps 2>/dev/null | grep -v '^#' | grep -iE "firebase|fcm|analytics|crashlytics|amplitude|mixpanel|segment|sentry|datadog|appsflyer|adjust"; [ $? -ne 0 ]) &amp;&amp; grep -q "<queries>" "$M" &amp;&amp; grep -A2 "<queries>" "$M" | grep -q "android.intent.category.LAUNCHER" &amp;&amp; grep -q 'android:allowBackup="false"' "$M" &amp;&amp; test -s docs/play-declaration.md &amp;&amp; test -s docs/data-safety.md &amp;&amp; echo "OK: V5-V13 + SETT-03 manifest gate green"</automated>
  </verify>
  <acceptance_criteria>
    - V5, V6, V7, V8, V9, V10, V11, V12, V13 all exit 0
    - Additional SETT-03 manifest check (`! grep -q android.permission.INTERNET …`) exits 0
    - The combined verify command above prints "OK: V5-V13 + SETT-03 manifest gate green"
  </acceptance_criteria>
  <done>All 9 manifest/docs verification commands green. Phase 1 manifest and Play Console source-of-truth docs are policy-correct. Plan 01-05 will run the remaining verifications V1–V4 + V14 + APK build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Play Store reviewer ↔ AndroidManifest.xml + a11y config XML | Reviewers literally grep these files for policy violations; copy-pasted Stack Overflow snippets are the single biggest risk |
| Play Store reviewer ↔ docs/play-declaration.md | The file IS the form text the reviewer reads — wording precision is load-bearing |
| User ↔ AccessibilityService stub | Phase-1 stub does nothing; risk is OEM auto-disable if granted (acknowledged in RESEARCH Assumption A7; mitigated by Phase 1 onboarding NOT deep-linking to a11y settings) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information Disclosure | notTodo_a11y_config.xml | mitigate | RESEARCH Pitfall A: a stray `canPerformGestures="true"` from a copy-pasted screen-reader snippet would imply autonomous action. Task 2 acceptance criteria grep V9 catches all three privileged flags by absence. |
| T-1-04 | Tampering | notTodo_a11y_config.xml event scope | mitigate | PLAY-03: subscribing to `typeAllMask` is an automatic Play review red flag. Task 2 acceptance criteria grep verifies single-event subscription (`typeWindowStateChanged` only) AND absence of `typeAllMask|typeViewClicked|typeViewFocused`. |
| T-1-05 | Spoofing | docs/play-declaration.md vs strings.xml | mitigate | The strings.xml `a11y_service_description` mirrors §2 of the declaration. RESEARCH §8 drafting note forbids silent prose edits — Task 3 action explicitly disallows them and routes any edit through a new GSD cycle. |
| T-1-08 | Information Disclosure | AndroidManifest INTERNET permission | mitigate | SETT-03: `! grep -q "android.permission.INTERNET" AndroidManifest.xml` is a hard gate in Task 1, Task 4, AND Plan 01-05's V14. If a future phase needs network, this gate forces an explicit doc + Data Safety form revision in the same commit. |
| T-1-09 | Information Disclosure | NotToDoAccessibilityService DB access | mitigate | RESEARCH §13 anti-pattern: service writing to SQLite. Task 2 acceptance criteria forbids `sqlite|drift|sqldelight|room|sqliteopenhelper` imports in the .kt file. Phase 4 implementation MUST keep this property. |
| T-1-10 | Elevation of Privilege | NotToDoAccessibilityService autonomous action | mitigate | PLAY-02: service must NEVER call `performAction|performGlobalAction|dispatchGesture`. Task 2 acceptance criteria forbids these strings in the .kt file. Enforcement-by-absence at Phase 1; enforcement-by-code-review at Phase 4. |
</threat_model>

<verification>
After all four tasks complete, the following commands collectively constitute the Phase-1 manifest+docs verification gate (V5–V13 from RESEARCH §10 + the SETT-03 INTERNET-absence check):

```bash
cd /Users/jintanakhomwong/projects/not-to-do-list
M=android/app/src/main/AndroidManifest.xml
A=android/app/src/main/res/xml/notTodo_a11y_config.xml
! grep -q "QUERY_ALL_PACKAGES" "$M"        # V5
! grep -q "SYSTEM_ALERT_WINDOW" "$M"       # V6
! grep -q "USE_EXACT_ALARM" "$M"           # V7
! grep -q "android.permission.INTERNET" "$M"  # SETT-03 manifest gate
grep -q 'isAccessibilityTool="false"' "$A" # V8
! grep -qE "canPerformGestures|canRetrieveWindowContent|flagRequestFilterKeyEvents" "$A"  # V9
flutter pub deps | grep -v '^#' | grep -iE "firebase|fcm|analytics|crashlytics|amplitude|mixpanel|segment|sentry|datadog|appsflyer|adjust" \
  || echo "OK: no telemetry deps"          # V10
grep -q "<queries>" "$M" \
  && grep -A2 "<queries>" "$M" | grep -q "android.intent.category.LAUNCHER"  # V11
grep -q 'android:allowBackup="false"' "$M" # V12
test -s docs/play-declaration.md && test -s docs/data-safety.md  # V13
flutter analyze                            # still clean
```
</verification>

<success_criteria>
- AndroidManifest.xml is the verbatim RESEARCH §4 content with all 4 forbidden permissions absent (QUERY_ALL_PACKAGES, SYSTEM_ALERT_WINDOW, USE_EXACT_ALARM, INTERNET)
- `<queries>` block declares LAUNCHER intent filter (PLAY-04)
- `allowBackup="false"` and `dataExtractionRules="@xml/data_extraction_rules"` set on `<application>`
- 3 application components declared: MainActivity, PauseActivity, NotToDoAccessibilityService — each with policy-correct attributes
- notTodo_a11y_config.xml sets `isAccessibilityTool="false"` and `accessibilityEventTypes="typeWindowStateChanged"` only (PLAY-01, PLAY-03)
- notTodo_a11y_config.xml does NOT set canPerformGestures, canRetrieveWindowContent, or flagRequestFilterKeyEvents
- NotToDoAccessibilityService.kt stub class compiles, has empty onAccessibilityEvent body, and contains no SQLite/Drift/Room imports and no performAction/performGlobalAction/dispatchGesture references (PLAY-02 enforcement-by-absence)
- PauseActivity.kt empty FlutterActivity stub compiles
- docs/play-declaration.md exists, ≥60 lines, references performAction/performGlobalAction/dispatchGesture/isAccessibilityTool/QUERY_ALL_PACKAGES/SYSTEM_ALERT_WINDOW (PLAY-07)
- docs/data-safety.md exists, ≥40 lines, declares zero data collected via "NOT COLLECTED" + the dep-audit grep command (PLAY-09)
- `flutter analyze` still passes
- V5–V13 all green; SETT-03 INTERNET-absence gate green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-play-declaration/01-02-SUMMARY.md` with:
- The exact output of running V5–V13 verification commands (paste each command's stdout+exit-code)
- The line count of docs/play-declaration.md and docs/data-safety.md
- Confirmation that NotToDoAccessibilityService.kt has zero SQLite/Drift/Room imports (paste the negative grep result)
- Confirmation that NotToDoAccessibilityService.kt contains no performAction/performGlobalAction/dispatchGesture references (paste the negative grep result)
- The exact list of `<uses-permission>` declarations in AndroidManifest.xml (paste `grep "<uses-permission" android/app/src/main/AndroidManifest.xml`)
- Note any prose deviation in docs/play-declaration.md from RESEARCH §8 (should be zero — verbatim copy)
</output>
