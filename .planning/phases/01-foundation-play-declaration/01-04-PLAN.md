---
phase: 01-foundation-play-declaration
plan: 04
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - pigeons/usage_api.dart
  - pigeons/accessibility_api.dart
  - pigeons/notification_api.dart
  - tool/pigeon.sh
  - lib/platform/usage_api.g.dart
  - lib/platform/accessibility_api.g.dart
  - lib/platform/notification_api.g.dart
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/UsageApi.g.kt
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/AccessibilityApi.g.kt
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApi.g.kt
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt
  - lib/domain/blocked_app_detector.dart
  - lib/data/detectors/accessibility_blocked_app_detector.dart
  - lib/data/detectors/usage_stats_polling_blocked_app_detector.dart
  - lib/domain/providers/blocked_app_detector_provider.dart
  - lib/domain/providers/blocked_app_detector_provider.g.dart
  - test/domain/providers/blocked_app_detector_provider_test.dart
autonomous: true
requirements:
  - PLAY-02
  - REL-05
must_haves:
  truths:
    - "pigeons/ directory at project root contains 3 input files: usage_api.dart, accessibility_api.dart, notification_api.dart"
    - "pigeons/ directory is NOT inside lib/ (RESEARCH Pitfall D)"
    - "Each pigeon input declares one @HostApi abstract class with @ConfigurePigeon annotation"
    - "pigeon codegen produced 3 Dart outputs in lib/platform/ AND 3 Kotlin outputs in android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/"
    - "tool/pigeon.sh exists and is executable"
    - "MainActivity.kt registers all 3 Pigeon APIs with stub implementations that throw UnimplementedError or return safe defaults"
    - "BlockedAppDetector interface exists with no `performAction`, `performGlobalAction`, or `dispatchGesture` methods (PLAY-02 enforcement-by-absence)"
    - "AccessibilityBlockedAppDetector and UsageStatsPollingBlockedAppDetector concrete stubs both throw UnimplementedError on initialize/updateBlockList"
    - "blockedAppDetectorProvider uses @riverpod and reads useAccessibilityServiceProvider flag"
    - "Toggling useAccessibilityServiceProvider in a test container switches the concrete BlockedAppDetector type (REL-05 contract)"
    - "`flutter test test/domain/providers/blocked_app_detector_provider_test.dart` passes"
    - "`flutter analyze` passes"
  artifacts:
    - path: "pigeons/usage_api.dart"
      provides: "Pigeon @HostApi UsageApi (Dart→Kotlin queryRange interface for Phase 3)"
      contains: "@HostApi()"
    - path: "pigeons/accessibility_api.dart"
      provides: "Pigeon @HostApi AccessibilityApi (isServiceEnabled, openAccessibilitySettings)"
      contains: "@HostApi()"
    - path: "pigeons/notification_api.dart"
      provides: "Pigeon @HostApi NotificationApi (scheduleDailyReminder, cancelDailyReminder for Phase 5)"
      contains: "@HostApi()"
    - path: "tool/pigeon.sh"
      provides: "Codegen helper script that iterates pigeons/*.dart"
      contains: "dart run pigeon"
    - path: "lib/platform/usage_api.g.dart"
      provides: "Pigeon-generated Dart shim for UsageApi"
    - path: "lib/platform/accessibility_api.g.dart"
      provides: "Pigeon-generated Dart shim for AccessibilityApi"
    - path: "lib/platform/notification_api.g.dart"
      provides: "Pigeon-generated Dart shim for NotificationApi"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/UsageApi.g.kt"
      provides: "Pigeon-generated Kotlin abstract class"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/AccessibilityApi.g.kt"
      provides: "Pigeon-generated Kotlin abstract class"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApi.g.kt"
      provides: "Pigeon-generated Kotlin abstract class"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt"
      provides: "Pigeon stub registrations so app compiles & runs"
      contains: "UsageApi.setUp"
    - path: "lib/domain/blocked_app_detector.dart"
      provides: "REL-05 interface; no autonomous-action methods (PLAY-02)"
      contains: "abstract class BlockedAppDetector"
    - path: "lib/data/detectors/accessibility_blocked_app_detector.dart"
      provides: "Phase-1 stub; Phase 4 fills body"
      contains: "class AccessibilityBlockedAppDetector"
    - path: "lib/data/detectors/usage_stats_polling_blocked_app_detector.dart"
      provides: "Phase-1 stub; activated only as kill-switch fallback"
      contains: "class UsageStatsPollingBlockedAppDetector"
    - path: "lib/domain/providers/blocked_app_detector_provider.dart"
      provides: "Riverpod selector keyed on useAccessibilityServiceProvider flag (REL-05)"
      contains: "blockedAppDetector"
    - path: "test/domain/providers/blocked_app_detector_provider_test.dart"
      provides: "REL-05 unit test — toggling flag changes concrete type"
  key_links:
    - from: "lib/domain/providers/blocked_app_detector_provider.dart"
      to: "lib/domain/blocked_app_detector.dart"
      via: "imports BlockedAppDetector abstract class"
      pattern: "package:not_to_do_list/domain/blocked_app_detector.dart"
    - from: "lib/domain/providers/blocked_app_detector_provider.dart"
      to: "lib/data/detectors/accessibility_blocked_app_detector.dart"
      via: "imports AccessibilityBlockedAppDetector and instantiates when flag=true"
      pattern: "AccessibilityBlockedAppDetector"
    - from: "lib/domain/providers/blocked_app_detector_provider.dart"
      to: "lib/data/detectors/usage_stats_polling_blocked_app_detector.dart"
      via: "imports UsageStatsPollingBlockedAppDetector and instantiates when flag=false"
      pattern: "UsageStatsPollingBlockedAppDetector"
    - from: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt"
      to: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/*.g.kt"
      via: "import + UsageApi.setUp / AccessibilityApi.setUp / NotificationApi.setUp"
      pattern: "com.nottodo.not_to_do_list.platform"
---

<objective>
Define Pigeon-typed Dart→Kotlin channel contracts for Phase 3 (UsageStatsManager), Phase 4 (AccessibilityService state), and Phase 5 (Notifications) — INTERFACES ONLY, no implementations. Run pigeon codegen to produce Dart and Kotlin shims. Hand-write MainActivity.kt to register all three with stub implementations so the app compiles. Then ship the load-bearing REL-05 artifact: a `BlockedAppDetector` interface (with NO autonomous-action methods, satisfying PLAY-02 by absence) backed by two concrete stub implementations (AccessibilityBlockedAppDetector, UsageStatsPollingBlockedAppDetector) that throw UnimplementedError, switched by a Riverpod provider keyed on a flag.

Purpose: This plan locks the Pigeon channel pattern (RESEARCH §13 architectural commitment) and ships REL-05 — the swappable provider that lets a UsageStats-polling fallback ship without rework if Play rejects the AccessibilityService. The unit test in test/domain/providers/blocked_app_detector_provider_test.dart proves the switch works.

Output: A working Pigeon scaffold + the REL-05 abstraction + a passing unit test that verifies toggling the `useAccessibilityServiceProvider` flag returns a different concrete `BlockedAppDetector` instance type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-play-declaration/01-RESEARCH.md
@.planning/phases/01-foundation-play-declaration/01-01-SUMMARY.md

# Verbatim source of truth
RESEARCH §3 — full verbatim Pigeon input files (pigeons/usage_api.dart, accessibility_api.dart, notification_api.dart) + tool/pigeon.sh + MainActivity.kt stub registrations
RESEARCH §6 — full verbatim BlockedAppDetector interface + AccessibilityBlockedAppDetector stub + UsageStatsPollingBlockedAppDetector stub + Riverpod selector
RESEARCH §13 — Pigeon-for-synchronous-Dart→Native-calls pattern; service NEVER calls performAction/performGlobalAction/dispatchGesture
RESEARCH §10 V14 — pigeons/ at project root (NOT under lib/)
RESEARCH Pitfall B — do NOT re-run flutter create (would wipe MainActivity.kt registrations)
RESEARCH Pitfall D — pigeons/ at project root, NOT under lib/
RESEARCH Open Question #2 — Phase 1 home screen MUST NOT subscribe to blockedAppDetectorProvider (test exercises it instead)
RESEARCH Assumption A1 — generated Kotlin signatures use Long for epoch-ms; mirror after codegen produces output

# Existing files (created by Plan 01-01)
- pigeons/                    (empty dir)
- tool/                       (empty dir)
- lib/platform/               (empty dir)
- lib/data/detectors/         (empty dir)
- lib/domain/                 (empty dir except providers/database_provider.dart from Plan 01-03)
- test/domain/providers/      (empty dir)
- android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/  (empty dir)
- android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt  (Flutter-template default — to be REPLACED by Task 3)
</context>

<interfaces>
<!-- Contracts this plan creates that downstream phases (3, 4, 5) will implement. -->

`pigeons/usage_api.dart`:
  @ConfigurePigeon(PigeonOptions(
    dartOut: 'lib/platform/usage_api.g.dart',
    kotlinOut: 'android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/UsageApi.g.kt',
    kotlinOptions: KotlinOptions(package: 'com.nottodo.not_to_do_list.platform'),
    dartPackageName: 'not_to_do_list',
  ))
  class UsagePackageStat {
    UsagePackageStat({required this.packageName, required this.foregroundSeconds, required this.launchCount});
    final String packageName;
    final int foregroundSeconds;
    final int launchCount;
  }
  @HostApi()
  abstract class UsageApi {
    @async
    List<UsagePackageStat> queryRange(int startEpochMs, int endEpochMs);
  }

`pigeons/accessibility_api.dart`:
  @HostApi()
  abstract class AccessibilityApi {
    @async
    bool isServiceEnabled();
    void openAccessibilitySettings();
  }
  // (No performAction / performGlobalAction / dispatchGesture — PLAY-02)

`pigeons/notification_api.dart`:
  @HostApi()
  abstract class NotificationApi {
    @async
    void scheduleDailyReminder(int hour, int minute);
    @async
    void cancelDailyReminder();
  }

`lib/domain/blocked_app_detector.dart`:
  abstract class BlockedAppDetector {
    Future<void> initialize(Set<String> blockedPackageNames);
    Future<void> updateBlockList(Set<String> blockedPackageNames);
    Stream<BlockedAppDetection> get detections;
    Future<bool> get isHealthy;
    Future<void> dispose();
  }
  class BlockedAppDetection {
    const BlockedAppDetection({required this.packageName, required this.detectedAt, required this.source});
    final String packageName;
    final DateTime detectedAt;
    final BlockedAppDetectionSource source;
  }
  enum BlockedAppDetectionSource { accessibilityService, usageStatsPolling }
  // NO performAction / performGlobalAction / dispatchGesture methods (PLAY-02 by absence)

`lib/domain/providers/blocked_app_detector_provider.dart`:
  @riverpod
  bool useAccessibilityService(UseAccessibilityServiceRef ref) => true;
  @riverpod
  BlockedAppDetector blockedAppDetector(BlockedAppDetectorRef ref) {
    final useA11y = ref.watch(useAccessibilityServiceProvider);
    return useA11y ? AccessibilityBlockedAppDetector() : UsageStatsPollingBlockedAppDetector();
  }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write 3 pigeon input files + tool/pigeon.sh; run codegen</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§3 — full verbatim pigeons/*.dart files + tool/pigeon.sh)
  </read_first>
  <action>
    Create 3 Pigeon input files at the PROJECT ROOT under `pigeons/` (NOT under `lib/` — RESEARCH Pitfall D, V14 enforces this). Each file uses `@ConfigurePigeon(PigeonOptions(...))` (Pigeon 26.x convention) to drive codegen — no CLI flags. Use the EXACT content from RESEARCH §3 (lines 495-581).

    `pigeons/usage_api.dart` — declares `UsagePackageStat` data class + `@HostApi() abstract class UsageApi { @async List<UsagePackageStat> queryRange(int startEpochMs, int endEpochMs); }`

    `pigeons/accessibility_api.dart` — `@HostApi() abstract class AccessibilityApi { @async bool isServiceEnabled(); void openAccessibilitySettings(); }`. The class MUST NOT declare any method named `performAction`, `performGlobalAction`, `dispatchGesture`, `triggerPauseScreen`, or anything that would represent autonomous action — PLAY-02 enforcement-by-absence; the Pigeon channel is read-only-state and deep-link-to-Settings only.

    `pigeons/notification_api.dart` — `@HostApi() abstract class NotificationApi { @async void scheduleDailyReminder(int hour, int minute); @async void cancelDailyReminder(); }`

    Each file's `@ConfigurePigeon` block specifies:
      - `dartOut: 'lib/platform/<name>.g.dart'`
      - `kotlinOut: 'android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/<Name>.g.kt'`
      - `kotlinOptions: KotlinOptions(package: 'com.nottodo.not_to_do_list.platform')`
      - `dartPackageName: 'not_to_do_list'`

    Create `tool/pigeon.sh` (RESEARCH §3) — a bash helper that iterates `pigeons/*.dart` and runs `dart run pigeon --input <file>` for each. Make it executable (`chmod +x tool/pigeon.sh`). The script's exact body:

      #!/usr/bin/env bash
      set -euo pipefail
      for f in pigeons/*.dart; do
        echo "Generating $f..."
        dart run pigeon --input "$f"
      done
      echo "Done. Run 'dart format lib/platform/' to format generated Dart."

    Run codegen:
      cd /Users/jintanakhomwong/projects/not-to-do-list
      flutter pub get      # Plan 01-01 already did this, but re-run for safety
      tool/pigeon.sh

    Expected outputs:
      - lib/platform/usage_api.g.dart
      - lib/platform/accessibility_api.g.dart
      - lib/platform/notification_api.g.dart
      - android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/UsageApi.g.kt
      - android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/AccessibilityApi.g.kt
      - android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApi.g.kt

    Format generated Dart:
      dart format lib/platform/

    Per CLAUDE.md "Surgical Changes": commit the generated .g.dart and .g.kt files; future tasks read them but do NOT hand-edit them. RESEARCH Pitfall B applies: do NOT re-run `flutter create` — it would wipe the to-be-written MainActivity.kt registrations.

    Per RESEARCH Assumption A1: the exact generated Kotlin signature for `Long` epoch-ms parameters cannot be predicted exactly without running codegen. Task 3 reads the actually-generated `UsageApi.g.kt` to mirror the exact signature in MainActivity.kt — do not pre-write the registration.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; for f in pigeons/usage_api.dart pigeons/accessibility_api.dart pigeons/notification_api.dart tool/pigeon.sh lib/platform/usage_api.g.dart lib/platform/accessibility_api.g.dart lib/platform/notification_api.g.dart android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/UsageApi.g.kt android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/AccessibilityApi.g.kt android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApi.g.kt; do [ -f "$f" ] || { echo "MISSING: $f"; exit 1; }; done &amp;&amp; [ -x tool/pigeon.sh ] &amp;&amp; grep -q '@HostApi' pigeons/usage_api.dart &amp;&amp; grep -q '@HostApi' pigeons/accessibility_api.dart &amp;&amp; grep -q '@HostApi' pigeons/notification_api.dart &amp;&amp; ! grep -qE 'performAction|performGlobalAction|dispatchGesture|triggerPauseScreen' pigeons/accessibility_api.dart &amp;&amp; ! grep -qE 'performAction|performGlobalAction|dispatchGesture' pigeons/usage_api.dart &amp;&amp; ! grep -qE 'performAction|performGlobalAction|dispatchGesture' pigeons/notification_api.dart &amp;&amp; [ ! -d lib/pigeons ]</automated>
  </verify>
  <acceptance_criteria>
    - All 3 pigeon input files exist at PROJECT ROOT under pigeons/ (NOT lib/pigeons/)
    - V14 (RESEARCH §10): `[ ! -d lib/pigeons ]` exits 0 (Pitfall D enforcement)
    - Each pigeon input contains `@HostApi()`
    - PLAY-02: pigeons/accessibility_api.dart contains NONE of `performAction|performGlobalAction|dispatchGesture|triggerPauseScreen`
    - PLAY-02 (extended): pigeons/usage_api.dart and pigeons/notification_api.dart contain NONE of `performAction|performGlobalAction|dispatchGesture`
    - tool/pigeon.sh exists and is executable (`-x`)
    - All 3 lib/platform/*.g.dart and 3 android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/*.g.kt files exist (codegen ran)
  </acceptance_criteria>
  <done>Pigeon inputs and codegen outputs exist; pigeons/ correctly placed at project root; PLAY-02 absence-enforcement holds across all 3 channel definitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write BlockedAppDetector interface + 2 concrete stub implementations + Riverpod selector</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§6 — full verbatim BlockedAppDetector interface, both detector stubs, Riverpod selector)
    - /Users/jintanakhomwong/projects/not-to-do-list/lib/domain/providers/database_provider.dart (Plan 01-03 wrote this — match the @riverpod conventions)
  </read_first>
  <behavior>
    - Test 1: BlockedAppDetector is an abstract class
    - Test 2: BlockedAppDetector has the 5 expected methods/getters and ZERO methods named performAction/performGlobalAction/dispatchGesture
    - Test 3: AccessibilityBlockedAppDetector implements BlockedAppDetector with all 5 members; initialize/updateBlockList throw UnimplementedError; detections returns empty Stream; isHealthy returns false
    - Test 4: UsageStatsPollingBlockedAppDetector ditto
    - Test 5: blockedAppDetectorProvider reads useAccessibilityServiceProvider; when flag=true returns AccessibilityBlockedAppDetector; when flag=false returns UsageStatsPollingBlockedAppDetector
  </behavior>
  <action>
    Create `lib/domain/blocked_app_detector.dart` with the EXACT content from RESEARCH §6 (lines 875-925). Required:
      - `abstract class BlockedAppDetector` with 5 members:
          1. `Future<void> initialize(Set<String> blockedPackageNames);`
          2. `Future<void> updateBlockList(Set<String> blockedPackageNames);`
          3. `Stream<BlockedAppDetection> get detections;`
          4. `Future<bool> get isHealthy;`
          5. `Future<void> dispose();`
      - `class BlockedAppDetection` (immutable; const constructor; 3 final fields: packageName, detectedAt, source)
      - `enum BlockedAppDetectionSource { accessibilityService, usageStatsPolling }`
      - KDoc on the abstract class explaining REL-05 / PLAY-02: "deliberately exposes ONLY the read-side; does not expose performAction / dispatchGesture / performGlobalAction"
      - The file MUST NOT contain any of the strings `performAction`, `performGlobalAction`, `dispatchGesture` — PLAY-02 enforcement-by-absence (the KDoc may MENTION them as "deliberately not exposed" — Task 1 acceptance forbids this in the actual API surface, NOT in commentary; for grep simplicity here the EXTERNAL CONTRACT must match the negative grep `! grep -E 'performAction\(|performGlobalAction\(|dispatchGesture\(' lib/domain/blocked_app_detector.dart` — i.e., no method/function declarations with these names. Method-mention in comments is fine.)

    For the verification gate to be unambiguous: in this file, the KDoc may use the words "performAction" etc. only in negation comments. The grep test in `<verify>` looks for `( ` after the name (method-like) which excludes comment text. To stay safe, executor SHOULD phrase the KDoc as: "...does not expose performAction-style autonomous APIs" (no parentheses).

    Create `lib/data/detectors/accessibility_blocked_app_detector.dart` with the EXACT content from RESEARCH §6 (lines 929-955):
      - `import 'package:not_to_do_list/domain/blocked_app_detector.dart';`
      - `class AccessibilityBlockedAppDetector implements BlockedAppDetector { ... }`
      - `initialize` throws `UnimplementedError('Phase 4 will implement.')`
      - `updateBlockList` throws `UnimplementedError('Phase 4 will implement.')`
      - `detections` returns `const Stream<BlockedAppDetection>.empty()`
      - `isHealthy` returns `false`
      - `dispose` is a no-op

    Create `lib/data/detectors/usage_stats_polling_blocked_app_detector.dart` with analogous structure (RESEARCH §6 lines 959-985). Throws `UnimplementedError('Activated only as kill-switch fallback.')` instead of "Phase 4 will implement."

    Create `lib/domain/providers/blocked_app_detector_provider.dart` with the EXACT content from RESEARCH §6 (lines 990-1015):
      - `import 'package:flutter_riverpod/flutter_riverpod.dart';`
      - `import 'package:riverpod_annotation/riverpod_annotation.dart';`
      - `import 'package:not_to_do_list/domain/blocked_app_detector.dart';`
      - `import 'package:not_to_do_list/data/detectors/accessibility_blocked_app_detector.dart';`
      - `import 'package:not_to_do_list/data/detectors/usage_stats_polling_blocked_app_detector.dart';`
      - `part 'blocked_app_detector_provider.g.dart';`
      - `@riverpod bool useAccessibilityService(UseAccessibilityServiceRef ref) => true;`
      - `@riverpod BlockedAppDetector blockedAppDetector(BlockedAppDetectorRef ref) { final useA11y = ref.watch(useAccessibilityServiceProvider); return useA11y ? AccessibilityBlockedAppDetector() : UsageStatsPollingBlockedAppDetector(); }`

    Run Riverpod codegen to produce blocked_app_detector_provider.g.dart:
      cd /Users/jintanakhomwong/projects/not-to-do-list
      dart run build_runner build --delete-conflicting-outputs

    Run analyzer:
      flutter analyze

    Expected: "No issues found!"

    Per CLAUDE.md "Surgical Changes": do NOT modify lib/main.dart or lib/app.dart in this task — Plan 01-05 owns them. The Riverpod providers are exercised by the test in Task 4, NOT by the home screen (RESEARCH Open Question #2).

    Per CLAUDE.md "Simplicity First": stub implementations should NOT include logging, retry logic, or any extra behavior beyond `throw UnimplementedError(…)`. Phase 4 fills the body with the actual broadcast wiring; over-engineering the stub creates dead code that needs deletion later.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; B=lib/domain/blocked_app_detector.dart &amp;&amp; A=lib/data/detectors/accessibility_blocked_app_detector.dart &amp;&amp; U=lib/data/detectors/usage_stats_polling_blocked_app_detector.dart &amp;&amp; P=lib/domain/providers/blocked_app_detector_provider.dart &amp;&amp; [ -f "$B" ] &amp;&amp; [ -f "$A" ] &amp;&amp; [ -f "$U" ] &amp;&amp; [ -f "$P" ] &amp;&amp; grep -q 'abstract class BlockedAppDetector' "$B" &amp;&amp; ! grep -qE 'performAction\(|performGlobalAction\(|dispatchGesture\(' "$B" &amp;&amp; grep -q 'class AccessibilityBlockedAppDetector' "$A" &amp;&amp; grep -q 'UnimplementedError' "$A" &amp;&amp; grep -q 'class UsageStatsPollingBlockedAppDetector' "$U" &amp;&amp; grep -q 'UnimplementedError' "$U" &amp;&amp; grep -q '@riverpod' "$P" &amp;&amp; grep -q 'useAccessibilityService' "$P" &amp;&amp; grep -q 'blockedAppDetector' "$P" &amp;&amp; dart run build_runner build --delete-conflicting-outputs &amp;&amp; [ -f lib/domain/providers/blocked_app_detector_provider.g.dart ] &amp;&amp; flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - lib/domain/blocked_app_detector.dart contains `abstract class BlockedAppDetector`
    - REL-05 ↔ PLAY-02 enforcement-by-absence: `! grep -qE 'performAction\(|performGlobalAction\(|dispatchGesture\(' lib/domain/blocked_app_detector.dart` exits 0 (no method declarations of those names; KDoc mentions are allowed)
    - 3-task1 pigeon files PLAY-02 holds (Task 1 already verified)
    - lib/data/detectors/accessibility_blocked_app_detector.dart declares `class AccessibilityBlockedAppDetector` and contains `UnimplementedError`
    - lib/data/detectors/usage_stats_polling_blocked_app_detector.dart declares `class UsageStatsPollingBlockedAppDetector` and contains `UnimplementedError`
    - lib/domain/providers/blocked_app_detector_provider.dart contains `@riverpod`, `useAccessibilityService`, `blockedAppDetector`
    - lib/domain/providers/blocked_app_detector_provider.g.dart exists (Riverpod codegen output)
    - `flutter analyze` exits 0
  </acceptance_criteria>
  <done>BlockedAppDetector interface + 2 stub impls + Riverpod selector compile; PLAY-02 absence-enforcement holds in the interface file.</done>
</task>

<task type="auto">
  <name>Task 3: Hand-write MainActivity.kt with Pigeon stub registrations</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§3 lines 614-662 — verbatim MainActivity.kt stub registrations; Assumption A1 — actual signature comes from codegen output, not the doc)
    - /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/UsageApi.g.kt (just generated by Task 1 — read to determine actual Kotlin method signatures)
    - /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/AccessibilityApi.g.kt (just generated)
    - /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApi.g.kt (just generated)
    - /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt (Flutter-template default — to be REPLACED)
  </read_first>
  <action>
    Replace `android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt` with a hand-written stub registration file based on the EXACT generated signatures from `*.g.kt` produced by Task 1. Use RESEARCH §3 lines 614-662 as the template, BUT mirror the EXACT signature from each `UsageApi.g.kt` / `AccessibilityApi.g.kt` / `NotificationApi.g.kt` (RESEARCH Assumption A1 — Pigeon 26.x may produce `Long` instead of `Int` for epoch-ms, callback-style instead of suspend, etc.).

    Required structure:
      - `package com.nottodo.not_to_do_list`
      - imports:
          - `io.flutter.embedding.android.FlutterActivity`
          - `io.flutter.embedding.engine.FlutterEngine`
          - `com.nottodo.not_to_do_list.platform.UsageApi`
          - `com.nottodo.not_to_do_list.platform.AccessibilityApi`
          - `com.nottodo.not_to_do_list.platform.NotificationApi`
          - `com.nottodo.not_to_do_list.platform.UsagePackageStat`
      - `class MainActivity : FlutterActivity()` overrides `configureFlutterEngine(flutterEngine: FlutterEngine)`
      - In `configureFlutterEngine`, after `super.configureFlutterEngine(flutterEngine)`:
          - `UsageApi.setUp(flutterEngine.dartExecutor.binaryMessenger, object : UsageApi { ... })` — `queryRange` callback signals `Result.failure(NotImplementedError("UsageApi: implemented in Phase 3"))`
          - `AccessibilityApi.setUp(...)` — `isServiceEnabled` returns `Result.success(false)` (safe stub: "off" until Phase 4 implements it); `openAccessibilitySettings` is a no-op stub (Phase 2 implements)
          - `NotificationApi.setUp(...)` — `scheduleDailyReminder` returns `Result.failure(NotImplementedError("NotificationApi: implemented in Phase 5"))`; `cancelDailyReminder` returns `Result.success(Unit)`

    The exact callback parameter types (`Long` vs `Int`, `Result<Unit>` vs no result for void, suspend vs callback) MUST match what each `*.g.kt` generated. Read each `*.g.kt` first; if the signature is `fun queryRange(startEpochMs: Long, endEpochMs: Long, callback: (Result<List<UsagePackageStat>>) -> Unit)`, use exactly that. Do NOT invent the signature from RESEARCH §3 — the doc warns this may differ from actual codegen.

    Build to verify the stub compiles (this also exercises V3 from RESEARCH §10):
      cd /Users/jintanakhomwong/projects/not-to-do-list
      flutter analyze
      flutter build apk --debug

    Expected: APK builds successfully (`build/app/outputs/flutter-apk/app-debug.apk` exists). If the build fails because the registration signature doesn't match generated Kotlin, fix the registration to match the .g.kt — do NOT edit the .g.kt.

    Per CLAUDE.md "Think Before Coding": if the generated Kotlin uses suspend functions instead of callbacks (Pigeon 26.x may emit either depending on Kotlin coroutines support), the registration must use the matching style. The compiler error will tell you which style is expected.

    Per RESEARCH Pitfall B: do NOT re-run `flutter create` to "regenerate" MainActivity.kt. This is the only place MainActivity.kt is hand-edited; future tasks must not re-invoke `flutter create`.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; M=android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt &amp;&amp; [ -f "$M" ] &amp;&amp; grep -q 'class MainActivity : FlutterActivity' "$M" &amp;&amp; grep -q 'UsageApi.setUp' "$M" &amp;&amp; grep -q 'AccessibilityApi.setUp' "$M" &amp;&amp; grep -q 'NotificationApi.setUp' "$M" &amp;&amp; grep -q 'com.nottodo.not_to_do_list.platform' "$M" &amp;&amp; ! grep -qE 'performAction|performGlobalAction|dispatchGesture' "$M" &amp;&amp; flutter analyze &amp;&amp; flutter build apk --debug</automated>
  </verify>
  <acceptance_criteria>
    - MainActivity.kt declares `class MainActivity : FlutterActivity` and registers all 3 Pigeon APIs (UsageApi.setUp, AccessibilityApi.setUp, NotificationApi.setUp)
    - PLAY-02: MainActivity.kt contains NONE of `performAction|performGlobalAction|dispatchGesture`
    - `flutter analyze` exits 0
    - `flutter build apk --debug` exits 0; `build/app/outputs/flutter-apk/app-debug.apk` is produced (this also covers V3 from RESEARCH §10)
  </acceptance_criteria>
  <done>MainActivity.kt registers all 3 Pigeon APIs with stub implementations. APK builds successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write blocked_app_detector_provider_test.dart (REL-05 unit test) + run flutter test</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§6 — Riverpod selector contract; §10 V2 — flutter test passes)
    - /Users/jintanakhomwong/projects/not-to-do-list/lib/domain/providers/blocked_app_detector_provider.dart (just written by Task 2)
    - /Users/jintanakhomwong/projects/not-to-do-list/lib/domain/blocked_app_detector.dart (Task 2)
    - /Users/jintanakhomwong/projects/not-to-do-list/lib/data/detectors/accessibility_blocked_app_detector.dart (Task 2)
    - /Users/jintanakhomwong/projects/not-to-do-list/lib/data/detectors/usage_stats_polling_blocked_app_detector.dart (Task 2)
  </read_first>
  <behavior>
    - Test 1: With default `useAccessibilityServiceProvider=true`, `blockedAppDetectorProvider` returns an `AccessibilityBlockedAppDetector` instance (verified via `isA<AccessibilityBlockedAppDetector>()`)
    - Test 2: When `useAccessibilityServiceProvider` is overridden to return `false`, `blockedAppDetectorProvider` returns a `UsageStatsPollingBlockedAppDetector` instance
    - Test 3: BlockedAppDetector interface contains NO method named performAction/performGlobalAction/dispatchGesture (smoke test asserting the source file does not declare them — done via test reading the file via dart:io)
    - Test 4 (optional): The interface has the 5 expected members (initialize, updateBlockList, detections, isHealthy, dispose) — soft check
  </behavior>
  <action>
    Create `test/domain/providers/blocked_app_detector_provider_test.dart`. Use the Riverpod 3.x test pattern with `ProviderContainer` to read the provider. Required structure:

      import 'dart:io';
      import 'package:flutter_riverpod/flutter_riverpod.dart';
      import 'package:flutter_test/flutter_test.dart';
      import 'package:not_to_do_list/data/detectors/accessibility_blocked_app_detector.dart';
      import 'package:not_to_do_list/data/detectors/usage_stats_polling_blocked_app_detector.dart';
      import 'package:not_to_do_list/domain/blocked_app_detector.dart';
      import 'package:not_to_do_list/domain/providers/blocked_app_detector_provider.dart';

      void main() {
        group('blockedAppDetectorProvider (REL-05)', () {
          test('default flag (true) returns AccessibilityBlockedAppDetector', () {
            final container = ProviderContainer();
            addTearDown(container.dispose);
            final detector = container.read(blockedAppDetectorProvider);
            expect(detector, isA<AccessibilityBlockedAppDetector>());
            expect(detector, isA<BlockedAppDetector>());
          });

          test('flag overridden to false returns UsageStatsPollingBlockedAppDetector', () {
            final container = ProviderContainer(
              overrides: [
                useAccessibilityServiceProvider.overrideWith((ref) => false),
              ],
            );
            addTearDown(container.dispose);
            final detector = container.read(blockedAppDetectorProvider);
            expect(detector, isA<UsageStatsPollingBlockedAppDetector>());
            expect(detector, isA<BlockedAppDetector>());
          });
        });

        group('PLAY-02 enforcement-by-absence in BlockedAppDetector', () {
          test('blocked_app_detector.dart declares no autonomous-action methods', () {
            final source = File('lib/domain/blocked_app_detector.dart').readAsStringSync();
            // Match a method/function DECLARATION (name followed by parens),
            // not commentary in a KDoc.
            expect(
              RegExp(r'\bperformAction\s*\(').hasMatch(source),
              isFalse,
              reason: 'BlockedAppDetector must not declare performAction (PLAY-02)',
            );
            expect(
              RegExp(r'\bperformGlobalAction\s*\(').hasMatch(source),
              isFalse,
              reason: 'BlockedAppDetector must not declare performGlobalAction (PLAY-02)',
            );
            expect(
              RegExp(r'\bdispatchGesture\s*\(').hasMatch(source),
              isFalse,
              reason: 'BlockedAppDetector must not declare dispatchGesture (PLAY-02)',
            );
          });
        });
      }

    The `useAccessibilityServiceProvider.overrideWith((ref) => false)` syntax matches Riverpod 3.x code-gen conventions. If the actual generated provider name differs (RESEARCH Assumption A2 — the `*.g.dart` may name them slightly differently), read `lib/domain/providers/blocked_app_detector_provider.g.dart` first and use the actual exported provider symbol.

    Run the test:
      cd /Users/jintanakhomwong/projects/not-to-do-list
      flutter test test/domain/providers/blocked_app_detector_provider_test.dart -r expanded

    Expected: 3 tests pass.

    Then run the full suite to confirm no regressions across both this test and the Drift round-trip test from Plan 01-03:
      flutter test

    Expected: All tests pass (Drift schema test from Plan 01-03 + this provider test).

    Per CLAUDE.md "Goal-Driven Execution": REL-05's contract is "swapping the implementation requires changing one line." The test EXERCISES that contract by overriding a single provider and asserting the swap. If the test fails, fix the provider — do NOT loosen the assertion.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; T=test/domain/providers/blocked_app_detector_provider_test.dart &amp;&amp; [ -f "$T" ] &amp;&amp; grep -q 'blockedAppDetectorProvider' "$T" &amp;&amp; grep -q 'AccessibilityBlockedAppDetector' "$T" &amp;&amp; grep -q 'UsageStatsPollingBlockedAppDetector' "$T" &amp;&amp; grep -q 'overrideWith' "$T" &amp;&amp; grep -q 'performAction' "$T" &amp;&amp; flutter test test/domain/providers/blocked_app_detector_provider_test.dart -r expanded &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - test/domain/providers/blocked_app_detector_provider_test.dart exists
    - Test references both concrete types AccessibilityBlockedAppDetector and UsageStatsPollingBlockedAppDetector (verified by grep)
    - Test uses `overrideWith` to flip useAccessibilityServiceProvider (REL-05 swap contract)
    - Test contains the PLAY-02 absence-enforcement smoke test (verified by `grep -q performAction` finding the test assertion)
    - `flutter test test/domain/providers/blocked_app_detector_provider_test.dart -r expanded` exits 0
    - `flutter test` (full suite) exits 0
  </acceptance_criteria>
  <done>REL-05 test passes; flag-toggle proves swap contract; PLAY-02 absence-enforcement holds via runtime test that reads the source file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart ↔ Kotlin (Pigeon channel) | All Dart→Native calls flow through type-checked Pigeon shims; no MethodChannel string keys |
| BlockedAppDetector interface ↔ implementations | The interface IS the contract — anything not exposed by the interface cannot be called by the rest of the app |
| Generated .g.dart / .g.kt ↔ pigeon input files | Codegen output must match input; running pigeon twice with same input is idempotent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-14 | Elevation of Privilege | BlockedAppDetector interface | mitigate | PLAY-02 enforcement-by-absence: the interface deliberately omits performAction/performGlobalAction/dispatchGesture. Task 4's test reads the source file at runtime and asserts these strings (as method declarations) are absent. Phase-4 implementation cannot expose them without modifying this file, which would fail the test. |
| T-1-15 | Spoofing | Pigeon channel registration | mitigate | Pigeon 26.x catches type mismatches at build time. RESEARCH Assumption A1 risk (Long vs Int) is caught by `flutter build apk --debug` (Task 3); developer mirrors actual generated signature, not the documented one. |
| T-1-16 | Tampering | pigeons/ directory placement | mitigate | RESEARCH Pitfall D: pigeons/ at project root. V14 (Plan 01-05) and Task 1 acceptance criterion both enforce `[ ! -d lib/pigeons ]`. |
| T-1-17 | Tampering | MainActivity.kt regeneration | mitigate | RESEARCH Pitfall B: never re-run `flutter create`. This task hand-edits MainActivity.kt; future plans must not re-invoke flutter create. |
| T-1-18 | Repudiation | Riverpod swap contract (REL-05) | mitigate | The unit test in Task 4 proves the swap works. If a future change accidentally hard-codes one detector, the test fails. |
</threat_model>

<verification>
After all four tasks complete:

```bash
cd /Users/jintanakhomwong/projects/not-to-do-list

# V14 file presence (RESEARCH §10)
for f in pigeons/usage_api.dart pigeons/accessibility_api.dart pigeons/notification_api.dart \
         lib/domain/blocked_app_detector.dart lib/domain/providers/blocked_app_detector_provider.dart; do
  test -f "$f" || { echo "MISSING: $f"; exit 1; }
done
[ ! -d lib/pigeons ]   # Pitfall D

# PLAY-02 absence-enforcement
! grep -qE 'performAction\(|performGlobalAction\(|dispatchGesture\(' \
  lib/domain/blocked_app_detector.dart
! grep -qE 'performAction|performGlobalAction|dispatchGesture' \
  pigeons/accessibility_api.dart
! grep -qE 'performAction|performGlobalAction|dispatchGesture' \
  android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt
! grep -qE 'performAction|performGlobalAction|dispatchGesture' \
  android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt

# Tests pass
flutter test test/domain/providers/blocked_app_detector_provider_test.dart -r expanded
flutter test                              # V2

# Build passes
flutter analyze                           # V1
flutter build apk --debug                 # V3
```
</verification>

<success_criteria>
- 3 pigeon input files at PROJECT ROOT under pigeons/ (NOT lib/pigeons/) — V14
- Each pigeon input declares one @HostApi class; none of the 3 contain performAction/performGlobalAction/dispatchGesture
- Pigeon codegen produced 3 lib/platform/*.g.dart and 3 android/.../platform/*.g.kt files
- tool/pigeon.sh exists and is executable
- MainActivity.kt registers all 3 Pigeon APIs with stub implementations; APK builds successfully (V3)
- BlockedAppDetector interface declared with 5 members (initialize, updateBlockList, detections, isHealthy, dispose); contains NO method declarations of performAction/performGlobalAction/dispatchGesture (PLAY-02 by absence)
- 2 concrete stubs (AccessibilityBlockedAppDetector, UsageStatsPollingBlockedAppDetector) throw UnimplementedError on initialize/updateBlockList
- blockedAppDetectorProvider uses @riverpod and reads useAccessibilityServiceProvider flag (REL-05)
- Unit test proves toggling the flag swaps the concrete type (REL-05 contract); also asserts PLAY-02 absence at runtime
- `flutter test` and `flutter analyze` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-play-declaration/01-04-SUMMARY.md` with:
- Pinned pigeon version from pubspec.lock (must match ^26.3.4)
- Output of `tool/pigeon.sh` (success messages for each of 3 inputs)
- The exact Kotlin signature emitted by Pigeon for `UsageApi.queryRange` (paste from UsageApi.g.kt) — important because RESEARCH Assumption A1 said this might differ
- Output of `flutter test test/domain/providers/blocked_app_detector_provider_test.dart -r expanded` (full test report)
- Confirmation that `flutter build apk --debug` produced `build/app/outputs/flutter-apk/app-debug.apk` (paste path + size)
- Confirmation that `! grep -qE 'performAction|performGlobalAction|dispatchGesture' MainActivity.kt` and the analogous greps on pigeons/ and BlockedAppDetector all exited 0 (PLAY-02 absence-enforcement evidence)
</output>
