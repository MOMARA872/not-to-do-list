---
phase: 01-foundation-play-declaration
plan: 03
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - lib/data/database/app_database.dart
  - lib/data/database/app_database.g.dart
  - lib/data/database/tables/block_list_table.dart
  - lib/data/database/tables/daily_streak_table.dart
  - lib/data/database/tables/pause_events_table.dart
  - lib/data/database/tables/daily_checkins_table.dart
  - lib/data/database/tables/daily_usage_summary_table.dart
  - lib/domain/providers/database_provider.dart
  - lib/domain/providers/database_provider.g.dart
  - test/data/database/app_database_test.dart
autonomous: true
requirements: []
must_haves:
  truths:
    - "AppDatabase compiles with @DriftDatabase annotation listing 5 tables"
    - "Five tables (block_list, daily_streak, pause_events, daily_checkins, daily_usage_summary) are created by m.createAll() at schemaVersion 1"
    - "build_runner produces lib/data/database/app_database.g.dart without errors"
    - "Round-trip insert+select test on block_list passes"
    - "Schema-presence test (querying sqlite_master) confirms all 5 expected table names exist"
    - "schemaVersion is exactly 1"
    - "Riverpod databaseProvider returns a singleton AppDatabase"
    - "`flutter analyze` passes"
    - "`flutter test test/data/database/app_database_test.dart -r expanded` passes"
  artifacts:
    - path: "lib/data/database/app_database.dart"
      provides: "@DriftDatabase class AppDatabase + MigrationStrategy"
      contains: "@DriftDatabase"
    - path: "lib/data/database/app_database.g.dart"
      provides: "Drift codegen output (table data classes, companion classes, query helpers)"
    - path: "lib/data/database/tables/block_list_table.dart"
      provides: "BlockList table — apps + habits with kind discriminator"
      contains: "class BlockList extends Table"
    - path: "lib/data/database/tables/daily_streak_table.dart"
      provides: "DailyStreak table — one row per (entry, day)"
      contains: "class DailyStreak extends Table"
    - path: "lib/data/database/tables/pause_events_table.dart"
      provides: "PauseEvents table — one row per pause-screen interaction"
      contains: "class PauseEvents extends Table"
    - path: "lib/data/database/tables/daily_checkins_table.dart"
      provides: "DailyCheckins table — self-report per entry per day"
      contains: "class DailyCheckins extends Table"
    - path: "lib/data/database/tables/daily_usage_summary_table.dart"
      provides: "DailyUsageSummary table — pre-aggregated per-package per-day totals"
      contains: "class DailyUsageSummary extends Table"
    - path: "lib/domain/providers/database_provider.dart"
      provides: "Riverpod singleton AppDatabase provider"
      contains: "@riverpod"
    - path: "test/data/database/app_database_test.dart"
      provides: "Schema round-trip + table presence test"
      contains: "AppDatabase schema v1 round-trip"
  key_links:
    - from: "lib/data/database/app_database.dart"
      to: "lib/data/database/tables/*.dart"
      via: "@DriftDatabase tables: [BlockList, DailyStreak, PauseEvents, DailyCheckins, DailyUsageSummary]"
      pattern: "tables:"
    - from: "lib/data/database/app_database.dart"
      to: "lib/data/database/app_database.g.dart"
      via: "part 'app_database.g.dart';"
      pattern: "part 'app_database.g.dart';"
    - from: "lib/domain/providers/database_provider.dart"
      to: "lib/data/database/app_database.dart"
      via: "AppDatabase() constructor invoked from @riverpod factory"
      pattern: "AppDatabase()"
    - from: "test/data/database/app_database_test.dart"
      to: "lib/data/database/app_database.dart"
      via: "import 'package:not_to_do_list/data/database/app_database.dart';"
      pattern: "package:not_to_do_list/data/database/app_database.dart"
---

<objective>
Define the five Drift tables required by Phase 1 success criterion #1 (`block_list`, `daily_streak`, `pause_events`, `daily_checkins`, `daily_usage_summary`), wire them into a single `AppDatabase` class at schemaVersion 1, run drift_dev codegen, write a Riverpod singleton provider, and write the round-trip integration test that confirms `m.createAll()` produces all five tables and that an insert+select round-trip on `block_list` works.

Purpose: Locks the on-device data model BEFORE any feature work. Schema versioning starts at 1; every later phase that adds a table or column bumps the version and supplies a migration step. The single-writer-Dart contract from RESEARCH §13 is structurally enforced by this database living entirely in lib/data/, never in android/.

Output: A typed, codegen-verified Drift schema that compiles, has its codegen output committed (so clean checkouts pass `flutter test` without running build_runner first), and proves itself with a round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-play-declaration/01-RESEARCH.md
@.planning/phases/01-foundation-play-declaration/01-01-SUMMARY.md
@.planning/research/STACK.md

# Verbatim source of truth
RESEARCH §2 — full verbatim Drift table classes, AppDatabase class, round-trip test
RESEARCH §7 — folder layout (lib/data/database/tables/, lib/domain/providers/database_provider.dart)
RESEARCH Pitfall C — always pass `--delete-conflicting-outputs` to build_runner
RESEARCH Assumption A2 — Riverpod 3.x @riverpod annotation produces *.g.dart sibling

# Existing files
- lib/data/database/tables/  (empty dir, created by Plan 01-01 Task 4)
- lib/domain/providers/      (empty dir, created by Plan 01-01 Task 4)
- test/data/database/        (empty dir, created by Plan 01-01 Task 4)
</context>

<interfaces>
<!-- Drift table contracts these tasks must produce. After Plan 01-03 lands, these are the public types
     the rest of the app (Phase 2+) will import. -->

From `lib/data/database/tables/block_list_table.dart`:
  class BlockList extends Table {
    IntColumn get id => integer().autoIncrement()();
    IntColumn get kind => integer()();           // 0 = app, 1 = habit
    TextColumn get packageName => text().nullable()();
    TextColumn get displayName => text()();
    TextColumn get reasonNote => text().withDefault(const Constant(''))();
    IntColumn get streakBreakThresholdMinutes => integer().withDefault(const Constant(5))();
    DateTimeColumn get createdAt => dateTime()();
    DateTimeColumn get updatedAt => dateTime()();
    @override List<Set<Column>> get uniqueKeys => [{kind, packageName}];
  }

From `lib/data/database/tables/daily_streak_table.dart`:
  class DailyStreak extends Table {
    IntColumn get id => integer().autoIncrement()();
    IntColumn get entryId => integer().references(BlockList, #id, onDelete: KeyAction.cascade)();
    DateTimeColumn get day => dateTime()();
    IntColumn get status => integer()();         // 0 success, 1 broken, 2 incomplete-data, 3 pending
    IntColumn get source => integer()();         // 0 system-confirmed, 1 self-reported-only
    IntColumn get usageMinutesObserved => integer().withDefault(const Constant(0))();
    DateTimeColumn get evaluatedAt => dateTime()();
    @override List<Set<Column>> get uniqueKeys => [{entryId, day}];
  }

From `lib/data/database/tables/pause_events_table.dart`:
  class PauseEvents extends Table {
    IntColumn get id => integer().autoIncrement()();
    IntColumn get entryId => integer().references(BlockList, #id, onDelete: KeyAction.cascade)();
    TextColumn get packageName => text()();
    DateTimeColumn get triggeredAt => dateTime()();
    IntColumn get cooldownChosenSeconds => integer().nullable()();
    IntColumn get outcome => integer()();        // 0 cooldown-completed, 1 cancel, 2 use-anyway
  }

From `lib/data/database/tables/daily_checkins_table.dart`:
  class DailyCheckins extends Table {
    IntColumn get id => integer().autoIncrement()();
    IntColumn get entryId => integer().references(BlockList, #id, onDelete: KeyAction.cascade)();
    DateTimeColumn get day => dateTime()();
    BoolColumn get avoided => boolean()();
    DateTimeColumn get answeredAt => dateTime()();
    @override List<Set<Column>> get uniqueKeys => [{entryId, day}];
  }

From `lib/data/database/tables/daily_usage_summary_table.dart`:
  class DailyUsageSummary extends Table {
    IntColumn get id => integer().autoIncrement()();
    TextColumn get packageName => text()();
    DateTimeColumn get day => dateTime()();
    IntColumn get foregroundSeconds => integer()();
    IntColumn get launchCount => integer().withDefault(const Constant(0))();
    DateTimeColumn get aggregatedAt => dateTime()();
    @override List<Set<Column>> get uniqueKeys => [{packageName, day}];
  }

From `lib/data/database/app_database.dart`:
  @DriftDatabase(tables: [BlockList, DailyStreak, PauseEvents, DailyCheckins, DailyUsageSummary])
  class AppDatabase extends _$AppDatabase {
    AppDatabase([QueryExecutor? executor]) : super(executor ?? _openConnection());
    @override int get schemaVersion => 1;
    @override MigrationStrategy get migration => MigrationStrategy(
      onCreate: (m) => m.createAll(),
      onUpgrade: (m, from, to) async { /* no upgrades yet */ },
    );
    static QueryExecutor _openConnection() => driftDatabase(name: 'not_to_do_list');
  }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define five Drift table classes + AppDatabase class</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§2 — full verbatim Drift table classes + AppDatabase class)
    - /Users/jintanakhomwong/projects/not-to-do-list/pubspec.yaml (confirm drift ^2.32.1, drift_flutter ^0.2.0 are pinned by Plan 01-01)
  </read_first>
  <behavior>
    - Test 1: Drift codegen succeeds and produces app_database.g.dart
    - Test 2: AppDatabase compiles when imported from a test
    - Test 3: schemaVersion == 1
    - Test 4: After m.createAll(), querying sqlite_master returns rows for all 5 expected table names
  </behavior>
  <action>
    Create the five table-definition files in `lib/data/database/tables/`. Each file imports `package:drift/drift.dart` and (where it has a foreign key) imports the referenced table file. Use the EXACT class definitions from RESEARCH §2. The `<interfaces>` section above reproduces the contracts; the executor must copy the verbatim class bodies from RESEARCH §2 lines 263-372.

    Specifically:

    1. `lib/data/database/tables/block_list_table.dart` — `class BlockList extends Table` with the 8 columns listed in `<interfaces>` and `uniqueKeys = [{kind, packageName}]`.

    2. `lib/data/database/tables/daily_streak_table.dart` — `class DailyStreak extends Table` with FK to `BlockList` (`onDelete: KeyAction.cascade`), `uniqueKeys = [{entryId, day}]`.

    3. `lib/data/database/tables/pause_events_table.dart` — `class PauseEvents extends Table` with FK to `BlockList` (cascade), no uniqueKeys (events repeat).

    4. `lib/data/database/tables/daily_checkins_table.dart` — `class DailyCheckins extends Table` with FK to `BlockList` (cascade), `uniqueKeys = [{entryId, day}]`.

    5. `lib/data/database/tables/daily_usage_summary_table.dart` — `class DailyUsageSummary extends Table` (no FK; package name is a string, not a FK target), `uniqueKeys = [{packageName, day}]`.

    Then create `lib/data/database/app_database.dart` with the EXACT content from RESEARCH §2 (lines 374-417):
      - Import `package:drift/drift.dart` and `package:drift_flutter/drift_flutter.dart`
      - Import all 5 table files
      - `part 'app_database.g.dart';`
      - `@DriftDatabase(tables: [BlockList, DailyStreak, PauseEvents, DailyCheckins, DailyUsageSummary])`
      - `class AppDatabase extends _$AppDatabase`
      - Constructor `AppDatabase([QueryExecutor? executor]) : super(executor ?? _openConnection());`
      - `@override int get schemaVersion => 1;`
      - `@override MigrationStrategy get migration => MigrationStrategy(onCreate: (m) => m.createAll(), onUpgrade: (m, from, to) async { /* no upgrades yet */ });`
      - `static QueryExecutor _openConnection() => driftDatabase(name: 'not_to_do_list');`

    Per CLAUDE.md "Simplicity First": do NOT add custom DAO classes, queries, or DataClass overrides yet. Phase 2 fills repositories. Drift's codegen produces companion + data classes for free.

    Per RESEARCH Pitfall C: do NOT run `dart run build_runner build` yet — Task 2 owns codegen. Order matters because codegen uses the table classes as input.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; for f in lib/data/database/tables/block_list_table.dart lib/data/database/tables/daily_streak_table.dart lib/data/database/tables/pause_events_table.dart lib/data/database/tables/daily_checkins_table.dart lib/data/database/tables/daily_usage_summary_table.dart lib/data/database/app_database.dart; do [ -f "$f" ] || { echo "MISSING: $f"; exit 1; }; done &amp;&amp; grep -q 'class BlockList extends Table' lib/data/database/tables/block_list_table.dart &amp;&amp; grep -q 'class DailyStreak extends Table' lib/data/database/tables/daily_streak_table.dart &amp;&amp; grep -q 'class PauseEvents extends Table' lib/data/database/tables/pause_events_table.dart &amp;&amp; grep -q 'class DailyCheckins extends Table' lib/data/database/tables/daily_checkins_table.dart &amp;&amp; grep -q 'class DailyUsageSummary extends Table' lib/data/database/tables/daily_usage_summary_table.dart &amp;&amp; grep -q '@DriftDatabase' lib/data/database/app_database.dart &amp;&amp; grep -q 'schemaVersion => 1' lib/data/database/app_database.dart &amp;&amp; grep -q "part 'app_database.g.dart';" lib/data/database/app_database.dart</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist (5 table files + app_database.dart)
    - Each table file declares the expected `class X extends Table`
    - app_database.dart contains `@DriftDatabase`, `schemaVersion => 1`, and the part directive `part 'app_database.g.dart';`
    - app_database.dart imports `package:drift/drift.dart` and `package:drift_flutter/drift_flutter.dart`
    - app_database.dart's `@DriftDatabase` annotation lists exactly 5 tables in order: BlockList, DailyStreak, PauseEvents, DailyCheckins, DailyUsageSummary
  </acceptance_criteria>
  <done>Five table classes + AppDatabase class exist, ready for codegen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run drift_dev codegen + write Riverpod database provider + run analyze</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§2 codegen command; §6 + §7 — Riverpod provider pattern)
    - /Users/jintanakhomwong/projects/not-to-do-list/lib/data/database/app_database.dart (just written by Task 1)
  </read_first>
  <behavior>
    - Test 1: build_runner produces app_database.g.dart (committed to git so clean checkouts work without running codegen first)
    - Test 2: `flutter analyze` passes
    - Test 3: databaseProvider is a Riverpod-annotated function returning AppDatabase
  </behavior>
  <action>
    Run drift_dev codegen with the deletion-of-conflicting-outputs flag (RESEARCH Pitfall C):

      cd /Users/jintanakhomwong/projects/not-to-do-list
      dart run build_runner build --delete-conflicting-outputs

    Expected: a new file `lib/data/database/app_database.g.dart` is generated. It contains data classes (`BlockListData`), companion classes (`BlockListCompanion`), and the `_$AppDatabase` mixin. We TRACK this file in git so clean checkouts can run `flutter test` without prerequisite codegen.

    If build_runner fails because `drift_flutter ^0.2.0` does not resolve cleanly with `drift ^2.32.1` (RESEARCH Assumption A6), bump `drift_flutter` to whatever version `flutter pub upgrade --no-precompile drift_flutter` reports, then re-run codegen. Document the bump in the SUMMARY.

    Create `lib/domain/providers/database_provider.dart` — a Riverpod singleton provider that returns the AppDatabase. Use `@riverpod` annotation (Riverpod 3.x code-gen — RESEARCH §6, RESEARCH Assumption A2). Required structure:
      - `import 'package:riverpod_annotation/riverpod_annotation.dart';`
      - `import 'package:not_to_do_list/data/database/app_database.dart';`
      - `part 'database_provider.g.dart';`
      - `@Riverpod(keepAlive: true)` annotation (DB lives for the app lifetime; do not auto-dispose)
      - Function signature: `AppDatabase database(DatabaseRef ref) { final db = AppDatabase(); ref.onDispose(db.close); return db; }`

    Re-run codegen so Riverpod generates `database_provider.g.dart`:
      dart run build_runner build --delete-conflicting-outputs

    Run `flutter analyze` to confirm no analyzer errors:
      flutter analyze

    Expected: "No issues found!"

    Per CLAUDE.md "Surgical Changes": do NOT modify any other Dart file in this task. Plan 01-04 owns Riverpod provider for `BlockedAppDetector`; Plan 01-05 owns `lib/main.dart`. The only generated files this task creates are `app_database.g.dart` and `database_provider.g.dart`.

    Per CLAUDE.md "Think Before Coding": if `flutter analyze` reports lints (not errors) on the generated files, those are usually false positives that `analysis_options.yaml`'s `analyzer.exclude: ["**/*.g.dart"]` (set in Plan 01-01) should silence. If an actual error appears in a hand-written file, surface it before continuing.
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; dart run build_runner build --delete-conflicting-outputs &amp;&amp; [ -f lib/data/database/app_database.g.dart ] &amp;&amp; grep -q '_\$AppDatabase' lib/data/database/app_database.g.dart &amp;&amp; [ -f lib/domain/providers/database_provider.dart ] &amp;&amp; grep -q '@Riverpod' lib/domain/providers/database_provider.dart &amp;&amp; grep -q 'AppDatabase database' lib/domain/providers/database_provider.dart &amp;&amp; [ -f lib/domain/providers/database_provider.g.dart ] &amp;&amp; flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `lib/data/database/app_database.g.dart` exists and contains `_$AppDatabase` (the generated mixin)
    - `lib/domain/providers/database_provider.dart` exists, contains `@Riverpod`, declares `AppDatabase database(DatabaseRef ref)`, and includes `part 'database_provider.g.dart';`
    - `lib/domain/providers/database_provider.g.dart` exists (Riverpod codegen output)
    - `flutter analyze` exits 0 with "No issues found!"
  </acceptance_criteria>
  <done>Codegen complete, generated files committed, Riverpod database provider exists, analyzer is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write round-trip test (test/data/database/app_database_test.dart) + run flutter test</name>
  <read_first>
    - /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/01-foundation-play-declaration/01-RESEARCH.md (§2 — full verbatim round-trip test code)
    - /Users/jintanakhomwong/projects/not-to-do-list/lib/data/database/app_database.g.dart (generated; the test imports from app_database.dart but uses BlockListCompanion which is generated)
  </read_first>
  <behavior>
    - Test 1: After m.createAll() runs (via setUp creating an in-memory NativeDatabase), querying `sqlite_master` returns row for table `block_list`
    - Test 2: Same query returns rows for `daily_streak`, `pause_events`, `daily_checkins`, `daily_usage_summary`
    - Test 3: Inserting a BlockListCompanion (kind=0, packageName="com.instagram.android", displayName="Instagram", reasonNote="Doomscrolling at night.", createdAt+updatedAt timestamps) and selecting back returns exactly 1 row with the right packageName
    - Test 4: db.schemaVersion == 1
  </behavior>
  <action>
    Create `test/data/database/app_database_test.dart` with the EXACT content from RESEARCH §2 lines 432-484. Required structure:
      - `import 'package:drift/native.dart';`
      - `import 'package:flutter_test/flutter_test.dart';`
      - `import 'package:not_to_do_list/data/database/app_database.dart';`
      - `void main() { group('AppDatabase schema v1 round-trip', () { ... }); }`
      - Inside the group: `late AppDatabase db;` declared at group scope
      - `setUp(() { db = AppDatabase(NativeDatabase.memory()); });`
      - `tearDown(() async { await db.close(); });`
      - Test 1: "all 5 tables exist after onCreate" — runs `customSelect("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name;")`, collects names into a Set, asserts `containsAll({block_list, daily_streak, pause_events, daily_checkins, daily_usage_summary})`
      - Test 2: "block_list insert + select round-trips" — inserts a BlockListCompanion (kind=0, packageName='com.instagram.android', displayName='Instagram', reasonNote='Doomscrolling at night.', createdAt=DateTime.utc(2026, 4, 27, 12), updatedAt=DateTime.utc(2026, 4, 27, 12)); selects all rows; asserts length==1 and rows.first.packageName=='com.instagram.android'
      - Test 3: "schemaVersion is 1" — `expect(db.schemaVersion, 1);`

    Run the test:
      flutter test test/data/database/app_database_test.dart -r expanded

    Expected: 3 tests pass, 0 failures.

    Then run the full suite to confirm no regressions:
      flutter test

    Expected: All tests pass (this is the only test file in the repo so far; Plan 01-04 will add the BlockedAppDetector test).

    Per CLAUDE.md "Goal-Driven Execution": if any test fails, fix the upstream artifact (table definition or AppDatabase wiring) — do NOT loosen the test assertions. The 5-table list is non-negotiable per Phase 1 success criterion #1.

    Per CLAUDE.md "Simplicity First": do NOT add migration tests yet. Phase 1 schema is at version 1 with no upgrades; the migration onUpgrade body is `async { /* no upgrades yet */ }`. Migration tests land in Phase 2 (when the schema first changes).
  </action>
  <verify>
    <automated>cd /Users/jintanakhomwong/projects/not-to-do-list &amp;&amp; [ -f test/data/database/app_database_test.dart ] &amp;&amp; grep -q "AppDatabase schema v1 round-trip" test/data/database/app_database_test.dart &amp;&amp; grep -q "block_list" test/data/database/app_database_test.dart &amp;&amp; grep -q "daily_streak" test/data/database/app_database_test.dart &amp;&amp; grep -q "pause_events" test/data/database/app_database_test.dart &amp;&amp; grep -q "daily_checkins" test/data/database/app_database_test.dart &amp;&amp; grep -q "daily_usage_summary" test/data/database/app_database_test.dart &amp;&amp; grep -q "schemaVersion" test/data/database/app_database_test.dart &amp;&amp; flutter test test/data/database/app_database_test.dart -r expanded</automated>
  </verify>
  <acceptance_criteria>
    - `test/data/database/app_database_test.dart` exists
    - Test file references all 5 table names (block_list, daily_streak, pause_events, daily_checkins, daily_usage_summary) — verified by 5 separate greps
    - Test file references `schemaVersion`
    - `flutter test test/data/database/app_database_test.dart -r expanded` exits 0 with all tests passing
    - V2 (RESEARCH §10): `flutter test` passes (full suite)
  </acceptance_criteria>
  <done>Round-trip test passes; schema-presence test confirms all 5 tables exist after onCreate; schemaVersion locked at 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart writer ↔ SQLite file | Single-writer-Dart contract per RESEARCH §13 — only Dart code writes the database; AccessibilityService never opens it |
| Drift codegen ↔ table source files | drift_dev parses table classes; typos at column-definition time become runtime SQL errors if codegen is skipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | Tampering | app_database.g.dart drift | mitigate | RESEARCH Pitfall C: stale codegen output causes runtime type errors. Task 2 always passes `--delete-conflicting-outputs`. RESEARCH §10 V4 (`git diff --exit-code lib/` after re-running codegen) is enforced in Plan 01-05. |
| T-1-12 | Information Disclosure | block_list.packageName | accept | The table stores user-chosen Android package names (e.g., `com.instagram.android`). Per `docs/data-safety.md` §2 note, on-device storage is not "data collection" under Play policy. No off-device transmission per SETT-03 (Plan 01-02 manifest gate). |
| T-1-13 | Denial of Service | NativeDatabase.memory() in tests | accept | In-memory test DB is ephemeral; no persistence between tests via setUp/tearDown ensures isolation. |
</threat_model>

<verification>
After all three tasks complete:

```bash
cd /Users/jintanakhomwong/projects/not-to-do-list

# Codegen output is current
dart run build_runner build --delete-conflicting-outputs
git diff --exit-code lib/data/database/ lib/domain/providers/   # V4 from §10 (partial)

# Tests pass
flutter test test/data/database/app_database_test.dart -r expanded   # V2 (Drift schema)

# Analyzer clean
flutter analyze   # V1
```
</verification>

<success_criteria>
- All 5 Drift table classes (BlockList, DailyStreak, PauseEvents, DailyCheckins, DailyUsageSummary) exist in lib/data/database/tables/
- AppDatabase class declared with @DriftDatabase listing all 5 tables; schemaVersion=1; migration onCreate uses m.createAll()
- app_database.g.dart and database_provider.g.dart are committed (clean checkouts pass tests without prerequisite codegen)
- Riverpod databaseProvider exists at lib/domain/providers/database_provider.dart with @Riverpod(keepAlive: true)
- Round-trip test in test/data/database/app_database_test.dart passes — covers schema-presence (all 5 tables), insert+select on block_list, and schemaVersion=1
- `flutter analyze` passes
- `flutter test test/data/database/app_database_test.dart -r expanded` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-play-declaration/01-03-SUMMARY.md` with:
- Final pinned versions of drift, drift_dev, drift_flutter (from pubspec.lock)
- Confirmation that `dart run build_runner build --delete-conflicting-outputs` produced exactly 2 .g.dart files (paste the exit code and `ls lib/**/*.g.dart`)
- Output of `flutter test test/data/database/app_database_test.dart -r expanded` (full test report)
- Confirmation that `git diff --exit-code lib/` after a fresh codegen run shows no diff (i.e., committed codegen matches sources)
- Note any version bump from Assumption A6 (drift_flutter pin change)
</output>
