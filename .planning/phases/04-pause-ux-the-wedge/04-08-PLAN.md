---
plan_id: 04-08
phase: 4
plan: 08
type: execute
wave: 4
objective: "Wave 4: phase exit gate. Append GoRouter /pause/:entryId route; verify permissionHealthProvider.accessibilityServiceGranted is truth-bearing end-to-end (HealthCheckBanner becomes the first time-of-truth signal); measure PauseActivity cold-start ≤300ms (PAUS-07 software gate); execute REL-04 OEM-survival overnight test with device-acquisition checkpoint per CD-03; flip 04-VERIFICATION.md to status=complete; update REQUIREMENTS.md + ROADMAP.md + STATE.md."
depends_on:
  - 04-06
  - 04-07
files_modified:
  - lib/core/router/app_router.dart
  - test/features/health/permission_health_provider_a11y_live_test.dart
  - .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
requirements:
  - REL-04
autonomous: false
must_haves:
  truths:
    - "lib/core/router/app_router.dart appends ONE GoRoute entry: GoRoute(path: '/pause/:entryId', builder: (ctx, state) => PauseScreen(state: state)). The Phase 2 redirect gate continues to work — but PauseActivity's bound engine bypasses the redirect entirely (the engine starts at /pause/:entryId, not /, so the onboardingComplete check never runs)."
    - "permissionHealthProvider.accessibilityServiceGranted goes live end-to-end: install fresh APK → grant a11y in Settings → return to app → HealthCheckBanner disappears (banner was showing 'Accessibility Service is off' when the underlying value was hardcoded false; now it correctly disappears when the service is enabled). Toggling a11y off in Settings → returning to the app → banner re-appears within one resume cycle."
    - "PauseActivity cold-start ≤300ms on Pixel emulator (stock Android 16) — measured via `adb logcat` timestamp diff from intent broadcast to first-frame draw. Recorded in 04-VERIFICATION.md."
    - "REL-04 OEM-survival overnight gate: real Xiaomi or Samsung device, ≥8h idle, next-morning blocked-app tap triggers PauseActivity within 500ms detect-to-pause. Recorded in 04-VERIFICATION.md."
    - "CD-03 device-acquisition checkpoint: if no Xiaomi or Samsung is on hand at gate execution time, PAUSE the plan and surface to the user — do NOT silently substitute a Pixel + MIUI emulator image. (The plan's checkpoint mechanism is a checkpoint:human-action task on the gate itself.)"
    - "test/policy/play_invariants_test.dart 8/8 stays green — final phase exit assertion. Plus an EXPANDED scope: a NEW invariant test checks that android/.../service/ AND android/.../ (PauseActivity) directories also contain ZERO autonomous-action tokens (the Plan 04-08 PLAY-02 scope expansion mentioned in earlier plans)."
    - "REQUIREMENTS.md: PAUS-01..10 + REL-04 flipped to Complete; REL-01 noted as DEFERRED — CD-01 (no FGS in v1)."
    - "ROADMAP.md Phase 4 row marked ✅ Complete with the date; Plans column updated to 8/8."
    - "STATE.md updated: completed_phases=4, completed_plans=29 (21 Phase 1-3 + 8 Phase 4); current Position becomes Phase 5; key decisions table augmented with the Phase 4 PLAY-02 service-side absence-grep + the CD-01 (no FGS) decision."
  artifacts:
    - path: "lib/core/router/app_router.dart"
      provides: "Appends /pause/:entryId GoRoute (the route is internal to the pause engine — main app's engine never navigates here)"
      contains: "/pause/:entryId"
    - path: ".planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md"
      provides: "Filled REL-04 sign-off + cold-start measurement + REL-01 disposition; status flipped to complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PAUS-01..10 + REL-04 marked Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 row marked ✅ Complete"
    - path: ".planning/STATE.md"
      provides: "Phase 4 closed; current position Phase 5"
  key_links:
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/pause/pages/pause_screen.dart"
      via: "GoRoute(path: '/pause/:entryId', builder: ...)"
      pattern: "/pause/:entryId"
    - from: "Plan 04-03 AccessibilityApiImpl.isServiceEnabled"
      to: "lib/features/health/permission_health_provider.dart accessibilityServiceGranted"
      via: "End-to-end chain — manually verified by toggling a11y in Settings + observing HealthCheckBanner"
      pattern: "accessibilityServiceGranted"
---

# Plan 04-08 — Wave 4: Phase exit gate (router, health-banner live, REL-04 OEM overnight test)

<objective>
Phase 4's exit gate. Three concrete deliverables + four bookkeeping updates:

1. **Router** — append the `/pause/:entryId` GoRoute. Plan 04-07 ships PauseScreen; this plan wires it into the GoRouter. The route is internal to PauseActivity's bound engine (the main MainActivity engine never navigates here), but GoRouter is shared between the two engines via the same Dart code, so the route declaration lives in `app_router.dart`.

2. **HealthCheckBanner truth-bearing** — verify end-to-end that toggling a11y in Settings flips the banner. Plan 04-03 wired the underlying Pigeon side; this plan verifies the chain. Includes flipping the Plan 04-01 stub `permission_health_provider_a11y_live_test.dart` from skip to a real integration test that mocks AccessibilityApi and asserts permissionHealthProvider.refresh() reflects the change.

3. **REL-04 OEM overnight gate** — the load-bearing exit criterion of Phase 4. CD-03: real Xiaomi or Samsung device, ≥8h idle, next-morning blocked-app tap triggers PauseActivity within 500ms. Includes a `checkpoint:human-action` for device acquisition (if no Xiaomi/Samsung is on hand, the plan PAUSES and surfaces the issue — escalation to discuss-phase per CD-03).

Bookkeeping (auto):
- Fill 04-VERIFICATION.md fields with measured values; flip status: complete.
- Mark PAUS-01..10 + REL-04 Complete in REQUIREMENTS.md.
- Mark Phase 4 ✅ Complete in ROADMAP.md.
- Update STATE.md: completed_phases=4, current position Phase 5, augment key decisions table.

Per CLAUDE.md "Surgical Changes": app_router.dart gets ONE new GoRoute entry; the Phase 2 redirect gate is preserved byte-for-byte. Per Karpathy "Goal-Driven Execution": the success criterion for this plan is the REL-04 stopwatch reading + the manual UAT sign-off — not a green flutter test alone.

This plan is `autonomous: false` because of the device-acquisition checkpoint AND the REL-04 manual stopwatch step. Both require human interaction.

Output: 1 router edit + 1 test flip + 5 .planning/ edits + the manual REL-04 sign-off recorded in 04-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pause-ux-the-wedge/04-CONTEXT.md
@.planning/phases/04-pause-ux-the-wedge/04-PLAN-OVERVIEW.md
@.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md
@./CLAUDE.md
@lib/core/router/app_router.dart
@lib/features/pause/pages/pause_screen.dart
@lib/features/health/permission_health_provider.dart
@lib/features/health/widgets/health_check_banner.dart
@test/features/health/permission_health_provider_a11y_live_test.dart
@test/policy/play_invariants_test.dart
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 04-08-01 — Append /pause/:entryId GoRoute + flip permissionHealthProvider live test</name>
  <files>lib/core/router/app_router.dart, test/features/health/permission_health_provider_a11y_live_test.dart</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/lib/core/router/app_router.dart (current 10 routes — append the 11th)
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/pause/pages/pause_screen.dart (Plan 04-07 — the widget the route resolves to)
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/health/permission_health_provider.dart (the chain being asserted)
- /Users/jintanakhomwong/projects/not-to-do-list/test/features/health/permission_health_provider_a11y_live_test.dart (Plan 04-01 stub — flip from skip)
  </read_first>
  <action>
**Step 1 — Edit `lib/core/router/app_router.dart`.** Append ONE new GoRoute to the routes list, AFTER the existing `/dashboard` route:

```dart
GoRoute(
  path: '/pause/:entryId',
  builder: (ctx, state) => PauseScreen(state: state),
),
```

Add the import: `import 'package:not_to_do_list/features/pause/pages/pause_screen.dart';`

**Important:** the Phase 2 onboarding redirect (`if (!completed && !goingToOnboarding) return '/onboarding/welcome';`) RUNS for `/pause/:entryId` if the user hasn't completed onboarding. This is acceptable — a pause launch can only happen if the user has enabled the AccessibilityService, which can only happen via onboarding. So if onboarding isn't complete, the redirect to `/onboarding/welcome` is the correct fallback (the user shouldn't see PauseScreen before onboarding).

DO NOT modify the redirect block. DO NOT touch any of the existing 10 routes.

**Step 2 — Flip `test/features/health/permission_health_provider_a11y_live_test.dart` from skip to real tests.** 2 cases:
1. `permissionHealthProvider observes AccessibilityApi.isServiceEnabled → true` — mock AccessibilityApi to return true; pump the provider; assert PermissionHealth.accessibilityService is true.
2. `permissionHealthProvider observes AccessibilityApi.isServiceEnabled → false` — mock AccessibilityApi to return false; assert PermissionHealth.accessibilityService is false.

Both are unit tests against the Dart provider — they verify the wiring works, NOT the underlying Kotlin impl (Plan 04-03 acceptance covered the Kotlin side; the end-to-end manual check is in Task 04-08-03 below).
  </action>
  <verify>
    <automated>flutter test test/features/health/ test/features/pause/ 2>&1 | tail -10 && flutter analyze lib/core/router/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `lib/core/router/app_router.dart` contains the literal `'/pause/:entryId'` (verify: `grep -F "'/pause/:entryId'" lib/core/router/app_router.dart`).
- `lib/core/router/app_router.dart` contains the literal `import 'package:not_to_do_list/features/pause/pages/pause_screen.dart'`.
- The Phase 2 redirect block is byte-identical (verify: `git diff lib/core/router/app_router.dart` shows ONLY the new GoRoute + import; no changes inside the redirect callback).
- `test/features/health/permission_health_provider_a11y_live_test.dart` no longer contains `skip:` and has ≥2 `test(...)` cases.
- `flutter test test/features/health/` exits 0.
- `flutter test test/features/pause/` exits 0 (Plan 04-07's tests still pass).
- `flutter analyze lib/core/router/` 0 errors / 0 warnings.
- `flutter build apk --debug` exits 0.
  </acceptance_criteria>
  <done>Router wires /pause/:entryId; live a11y test green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 04-08-02 — Expand PLAY-02 absence-grep scope to include android/.../service/ + android/.../ (PauseActivity)</name>
  <files>test/policy/play_invariants_test.dart</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/test/policy/play_invariants_test.dart (existing 8 invariants — augment the PLAY-02 Kotlin block)
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt (Plan 04-05 — must stay clean under the new scope)
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt (Plan 04-06 — must stay clean)
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt (must stay clean)
  </read_first>
  <action>
The existing PLAY-02 Kotlin-side test scans `android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/` only. Phase 4 introduced the service body + PauseActivity body in DIFFERENT directories. The cross-tree absence-grep must catch ANY future regression there.

Edit `test/policy/play_invariants_test.dart`. Add a NEW test block AFTER the existing PLAY-02 Kotlin platform/ test (around line 72):

```dart
// ---- PLAY-02 (Phase 4 expanded scope): service/ + root activity files ----
test(
  'PLAY-02: no Kotlin source under service/ or the root not_to_do_list/ '
  'package declares performAction / performGlobalAction / dispatchGesture',
  () {
    final dirs = <String>[
      'android/app/src/main/kotlin/com/nottodo/not_to_do_list/service',
      'android/app/src/main/kotlin/com/nottodo/not_to_do_list', // root only — recursive=false
    ];
    final forbidden = <String>[
      r'\bperformAction\s*\(',
      r'\bperformGlobalAction\s*\(',
      r'\bdispatchGesture\s*\(',
    ];
    for (final dir in dirs) {
      final d = Directory(dir);
      if (!d.existsSync()) continue;
      // For the root not_to_do_list/ scan, recursive=false so we only catch
      // root files (MainActivity.kt, PauseActivity.kt) — sub-packages
      // (platform/, service/) are scanned by their own dedicated tests above.
      final recursive = !dir.endsWith('/not_to_do_list');
      for (final entity in d.listSync(recursive: recursive).whereType<File>()) {
        if (!entity.path.endsWith('.kt')) continue;
        final src = entity.readAsStringSync();
        // Strip comment lines (// or leading * inside /** */) before scanning
        // to allow doc-comment references to forbidden token NAMES (the Phase 1
        // doc comment header on NotToDoAccessibilityService.kt mentions PLAY-02
        // by name; that's allowed). Code lines containing the literal calls
        // are NOT.
        final codeOnly = src
            .split('\n')
            .where((line) => !RegExp(r'^\s*(//|\*)').hasMatch(line))
            .join('\n');
        for (final pat in forbidden) {
          expect(
            RegExp(pat).hasMatch(codeOnly),
            isFalse,
            reason: 'PLAY-02 (Phase 4 expanded scope): ${entity.path} '
                'contains forbidden symbol matching $pat in CODE line',
          );
        }
      }
    }
  },
);
```

Also augment the v1-scope `BIND_DEVICE_ADMIN` test (already existing) — no change needed; it already scans all of `android/` recursively.

This brings the invariants count from 8 to 9. The plan's acceptance criteria assert `9/9` after this edit.
  </action>
  <verify>
    <automated>flutter test test/policy/play_invariants_test.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
- `test/policy/play_invariants_test.dart` contains the literal phrase `Phase 4 expanded scope` (verify: `grep -F 'Phase 4 expanded scope' test/policy/play_invariants_test.dart`).
- `flutter test test/policy/play_invariants_test.dart` reports 9 passing tests (was 8; added one — verify: count `test(` declarations in the play_invariants file → ≥9).
- The new test passes against the Phase 4 service file (no regression from Plan 04-05).
- The new test passes against the Phase 4 PauseActivity file (no regression from Plan 04-06).
- The new test passes against MainActivity.kt (no regression from Plan 04-03 or 04-04).
- `flutter test` full suite exits 0.
  </acceptance_criteria>
  <done>PLAY-02 invariants expanded to 9/9 covering Phase 4's new Kotlin surfaces.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 04-08-03 — REL-04 OEM device acquisition checkpoint (CD-03)</name>
  <what-to-check>
The REL-04 overnight gate REQUIRES a real Xiaomi or Samsung Android device. CD-03 explicitly forbids silently substituting a Pixel emulator with an MIUI image.

Per CONTEXT line 88: "Solo developer's actual device inventory is unknown to Claude — planner should add a 'REL-04 device acquisition' sub-task if no Xiaomi or Samsung is on-hand; alternatives: borrow a device for the test window, OR escalate to discuss-phase for a one-time gate relaxation."

This task pauses the plan. The user must answer:
  </what-to-check>
  <how-to-verify>
Pick ONE:

**Option A — Have a Xiaomi or Samsung device:** Confirm:
  - Manufacturer: ____________________ (Xiaomi or Samsung)
  - Model: ____________________ (e.g., Redmi Note 12, Galaxy S22)
  - Android version: ____________________ (e.g., Android 14, MIUI 14, OneUI 6)
  - Battery level at test start: ____% (recommend ≥30% so 8h idle doesn't drain to dead)
  - USB cable + adb connection working: yes / no
  → If all confirmed: proceed to Task 04-08-04.

**Option B — Need to borrow a device:** Identify a friend/family/co-working contact who can lend one for ~24 hours.
  - Borrow source: ____________________
  - Pickup date: ____________________
  → Pause this plan; resume at Task 04-08-03 once the device is on hand.

**Option C — Cannot acquire a Xiaomi or Samsung within reasonable time:** Per CD-03, escalate.
  - Run `/gsd-discuss-phase 4 --area rel-04-device` to escalate.
  - Possible escalation outcomes: (1) one-time gate relaxation to "Pixel emulator only; flag REL-04 as PARTIAL", (2) defer Phase 4 sign-off until a device is acquired, (3) use a cloud-device service (Firebase Test Lab, BrowserStack) for a one-time overnight slot.
  - DO NOT silently substitute the emulator path.
  → After discuss-phase: resume per the escalation decision.
  </how-to-verify>
  <resume-signal>Type "device-ready: {manufacturer}/{model}/{Android version}" to proceed to Task 04-08-04, OR "escalating: {reason}" to halt for discuss-phase.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-08-04 — REL-04 overnight test execution + cold-start measurement + manual UAT</name>
  <what-built>
After Plans 04-01..04-07 land, the wedge is functionally complete:
- AccessibilityService observes window changes, debounces 800ms, matches against in-memory map refreshed via LocalBroadcast.
- PauseActivity launches with lock-screen flags + FlutterEngineCache pre-warm.
- PauseScreen renders the user's reason (or app-name-as-hero), cooldown chips, asymmetric buttons; resolves outcome to pause_events; auto-closes via SystemNavigator.pop.
- HealthCheckBanner is truth-bearing for the first time.

This task verifies all of the above on a real device, measures the PAUS-07 cold-start, AND executes the REL-04 overnight gate.
  </what-built>
  <how-to-verify>
**Pre-flight (15 min, on the Xiaomi or Samsung device from Task 04-08-03):**
1. `flutter build apk --debug && adb install -r build/app/outputs/flutter-apk/app-debug.apk`.
2. Launch the app; complete onboarding (3 permission steps + battery-opt; OEM-fallback panel may show on Xiaomi/Samsung — follow the dontkillmyapp.com link if surfaced).
3. Add a benign blocked app (e.g., `com.android.chrome` — already on every device, costs nothing to test against).
4. Leave the BlockList screen; observe HealthCheckBanner is GONE (a11y is enabled, all-healthy → no banner).
5. Toggle a11y service OFF in Settings → return to app → observe banner appears within ~1s. Toggle back ON → banner disappears.
6. Tap Chrome on the launcher → PauseActivity should appear within ~500ms with the cooldown chips.

**Cold-start measurement (PAUS-07 software gate; 5 min, ALSO measure on a Pixel emulator for parity):**
On Pixel emulator:
1. `adb logcat -c` (clear logs).
2. `adb shell am start -n com.nottodo.not_to_do_list/.PauseActivity --es extra_blocked_package "com.android.chrome" --el extra_entry_id 1 --es extra_block_mode "soft" --el extra_triggered_at_ms $(date +%s)000`.
3. `adb logcat -d -e PauseActivity` — find the timestamp of "Displayed com.nottodo.not_to_do_list/.PauseActivity" line. The duration after the `+...` is the cold-start (e.g., `+285ms`).
4. Record value in 04-VERIFICATION.md `## PauseActivity Cold-Start` section.
5. PASS criterion: ≤300ms with pre-warmed engine. If >300ms, check that `adb logcat | grep "pause_engine"` shows the cache hit; if not, the FlutterEngineCache pre-warm regressed (debug Plan 04-03).

**REL-04 overnight gate (CD-03; ~10 min execution + 8h wait):**
1. On the Xiaomi/Samsung device, charge to ≥30%.
2. Open the app once to confirm a11y still enabled; close to launcher.
3. `adb shell dumpsys deviceidle force-idle` — forces Doze NOW.
4. Lock the screen.
5. Wait ≥8 hours (overnight; do NOT use the device).
6. Next morning: unlock the device.
7. Start a stopwatch THE INSTANT you tap the Chrome icon on the launcher.
8. Stop the stopwatch when PauseActivity becomes the foreground window (cooldown chips visible).
9. Record the elapsed time in 04-VERIFICATION.md `## REL-04 OEM-Survival Overnight Gate` section.
10. PASS criterion: ≤500ms.
11. ALSO record: device manufacturer, model, Android version, idle duration (compute from steps 3-7), test date, sign-off date.
12. If FAIL (>500ms or PauseActivity does not appear): escalate to discuss-phase per CD-01 to consider adding the companion FGS. Do NOT auto-add the FGS — the escalation goes through `/gsd-discuss-phase 4 --area rel-01-fgs`.

**Manual UAT — happy path (5 min, on either device):**
- With a11y enabled, add a blocked app, tap it, see PauseScreen with reason hero (if reason set) or app-name hero (if not).
- Tap [3m] chip → LinearProgressIndicator drains; caption shows '2:59 remaining' etc.
- Tap [5m] chip mid-cooldown → countdown restarts at 5:00 (D-04 replace, not add).
- Tap Cancel → '✓ Cooldown complete' card briefly appears? NO — Cancel does NOT show the confirmation card; per D-07 only the cooldown auto-completion shows it. Cancel goes straight to launcher.
- Add a HARD entry (toggle block_mode in entry edit screen). Tap that app on launcher → PauseScreen appears WITHOUT 'Use anyway' button (PAUS-09).
- Add a SCHEDULED entry (set a 2-min schedule window centered on now). Tap the app inside the window → PauseScreen appears. Tap outside the window → app launches with NO PauseScreen interception (PAUS-10).
- After a successful cooldown completion → cumulativeTotalsProvider home card increments by 1 launch + the chosen-seconds. Use anyway does NOT increment.

**Sign-off:**
- All checkboxes pass → mark Task 04-08-04 complete; proceed to Task 04-08-05.
- Any FAIL → STOP, escalate to discuss-phase per the failing criterion's escalation rule (CD-01 for REL-04 fail; CONTEXT.md line 88 for device acquisition fail).
  </how-to-verify>
  <resume-signal>Type "uat-pass: cold-start={ms} rel-04={ms}" with the measurements, OR "uat-fail: {which-criterion} {detail}" to escalate.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 04-08-05 — Bookkeeping: fill 04-VERIFICATION.md, mark requirements Complete, update STATE.md</name>
  <files>.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md (Plan 04-01 template — fill the blank fields with measurements from Task 04-08-04)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/REQUIREMENTS.md (the traceability table to flip)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/ROADMAP.md (the Phase 4 row to mark Complete)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/STATE.md (the running state to update)
  </read_first>
  <action>
**Step 1 — Fill `04-VERIFICATION.md`:**
- Frontmatter: `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, `rel_04_signed_off: true`.
- `## Phase 4 Verification Map`: confirm every PAUS-* test and REL-04 has its filled plan number from Plans 04-01..07 alongside the test file path.
- `## REL-04 OEM-Survival Overnight Gate`: fill device manufacturer/model/Android version, idle duration, detect-to-pause ms, test date, sign-off date, outcome (PASS / FAIL / DEFERRED-FOR-DEVICE per Task 04-08-04 result).
- `## PauseActivity Cold-Start`: fill measured cold-start (ms), pass threshold (300 ms), Pixel emulator Android version.
- `## REL-01 Disposition (CD-01)`: pre-populated from Plan 04-01; confirm "Phase 4 ships WITHOUT companion FGS" still holds (Task 04-08-04 didn't trip the escalation path).
- `## PLAY-02 Forbidden-Token Sweep`: confirm "9/9 invariants green at phase exit" (Task 04-08-02 expanded from 8 to 9).

**Step 2 — Edit `REQUIREMENTS.md`:**
- Lines 198-207 (PAUS-01..10 traceability table rows): change `Pending` → `Complete (Plan 04-NN — brief)`.
  - PAUS-01 → Complete (Plan 04-05 — service Intent launch on TYPE_WINDOW_STATE_CHANGED)
  - PAUS-02 → Complete (Plan 04-07 — ReasonHero D-02 / AppNameHero D-03)
  - PAUS-03 → Complete (Plan 04-07 — CooldownChipRow D-04 SegmentedButton {1,3,5,10}m)
  - PAUS-04 → Complete (Plan 04-07 — auto-close on cooldown=0 via DoneConfirmationCard + SystemNavigator.pop D-07)
  - PAUS-05 → Complete (Plan 04-07 — Cancel writes outcome=1)
  - PAUS-06 → Complete (Plan 04-07 — Use anyway writes outcome=2; CD-02 enabled immediately)
  - PAUS-07 → Complete (Plan 04-06 + 04-08 — measured {N}ms with FlutterEngineCache pre-warm)
  - PAUS-08 → Complete (Plan 04-06 — setShowWhenLocked + setTurnScreenOn before super.onCreate D-15)
  - PAUS-09 → Complete (Plan 04-07 — hard entries omit Use anyway from widget tree)
  - PAUS-10 → Complete (Plan 04-02 + 04-05 — Kotlin schedule_window port D-12 + service gate)
- Line 248 (REL-04): Pending → Complete (Plan 04-08 — {device manufacturer/model} overnight {ms}ms detect-to-pause).
- Line 245 (REL-01): Pending → DEFERRED — CD-01 ships without companion FGS in v1; revisit if REL-04 fails on additional OEMs in M2.

Update line 17-21 (PAUS section): flip the `[ ]` checkboxes to `[x]` for PAUS-01..10. Same for line 102 (REL-04).

**Step 3 — Edit `ROADMAP.md`:**
- Line 19 (Phase 4 row): change `[ ]` → `[x]` and add the completion date suffix.
- Line 76-87 (Phase 4 details — `**Plans:** TBD`): replace `TBD` with `8 plans` and the listing:
  ```
  Plans:
  - [x] 04-01-PLAN.md — Wave 0: test scaffold + REL-04 verification template
  - [x] 04-02-PLAN.md — Wave 1: Kotlin schedule_window port + parity oracle (D-12)
  - [x] 04-03-PLAN.md — Wave 1: AccessibilityApiImpl + MainActivity FlutterEngine pre-warm (D-14)
  - [x] 04-04-PLAN.md — Wave 2: BlocklistBroadcast Pigeon channel + Repository emit + Detector fill
  - [x] 04-05-PLAN.md — Wave 2: AccessibilityService body + Intent launch (D-09..D-12, REL-01 disposition)
  - [x] 04-06-PLAN.md — Wave 3: PauseActivity.kt onCreate body (D-14, D-15)
  - [x] 04-07-PLAN.md — Wave 3: Flutter pause-screen UI + PauseController + pause_events writer (D-01..D-08, D-13)
  - [x] 04-08-PLAN.md — Wave 4: Phase exit gate (router, health-banner live, REL-04 overnight)
  ```
- Line 119-125 (Progress table): update Phase 4 row to `8/8 ✅ Complete {date}`.

**Step 4 — Edit `STATE.md`:**
- Frontmatter `progress.completed_phases`: 3 → 4.
- Frontmatter `progress.total_plans`: 21 → 29 (21 + 8 Phase 4 plans).
- Frontmatter `progress.completed_plans`: 21 → 29.
- `## Current Position`: change "Phase: 3 (Screen-Time Dashboard) — EXECUTING" → "Phase: 4 (Pause UX) — ✅ COMPLETE; Next: Phase 5 (Streak Engine & Daily Reminder)".
- `### Active Todos`: update to "None — Phase 4 closed. Next action: `/gsd-discuss-phase 5` then `/gsd-plan-phase 5`."
- `### Last Session`: append a new bullet: `2026-{MM}-{DD}: Phase 4 closed (Plan 04-08 final exit gate). All 8 plans landed. AccessibilityService body + PauseActivity + Flutter pause UI + LocalBroadcast block-list refresh + Kotlin schedule port + REL-04 overnight gate signed off on {device}. PLAY-02 invariants expanded from 8 → 9 (added service/ + root not_to_do_list/ scope per Plan 04-08 Task 02). HealthCheckBanner truth-bearing for the first time. CD-01 deferred (no FGS); CD-02 immediate Use-anyway shipped; CD-03 device-acquisition was {Option A/B/C}.`
- Augment "Key Decisions" table with two new rows: (a) "Phase 4 expanded PLAY-02 absence-grep to service/ + root activities" → Source: Plan 04-08 → Rationale: catches future autonomous-action regression on the new Phase 4 surfaces. (b) "REL-01 deferred: ship without companion FGS" → Source: Plan 04-05 (CD-01) → Rationale: research recommends against; revisit only if REL-04 OEM gate fails.
- Augment "Performance Metrics Per Plan" table with the 8 Phase 4 row entries (durations TBD — fill from actual execution).

DO NOT touch:
- The PROJECT.md file.
- The PROJECT.md-derived constraints in STATE.md (the v1-scope items).
- The Phase 5 / Phase 6 placeholder sections in ROADMAP.md.
- The Risks Being Tracked table in STATE.md (REL-04 risk's mitigation column gets ONE word added: "passed on {device}" — but the row itself stays).
  </action>
  <verify>
    <automated>grep -c '\[x\]' .planning/REQUIREMENTS.md && grep -F 'Phase 4' .planning/STATE.md | head -3</automated>
  </verify>
  <acceptance_criteria>
- `04-VERIFICATION.md` frontmatter declares `status: complete`, `rel_04_signed_off: true`.
- `04-VERIFICATION.md` REL-04 section has all 6 fields filled (manufacturer, model, Android version, idle duration, detect-to-pause ms, sign-off date) — none remain as blank `__________`.
- `04-VERIFICATION.md` PauseActivity Cold-Start section has the measured value filled.
- `REQUIREMENTS.md`: PAUS-01 through PAUS-10 all show `[x]` AND `Complete (Plan 04-NN — ...)` in the traceability table (verify: `grep -c 'Complete (Plan 04-' .planning/REQUIREMENTS.md` returns ≥10).
- `REQUIREMENTS.md`: REL-04 shows `Complete` in the traceability table.
- `REQUIREMENTS.md`: REL-01 shows `DEFERRED — CD-01 ships without companion FGS in v1`.
- `ROADMAP.md`: Phase 4 row has `[x]` AND `**Plans:** 8 plans` AND a complete listing of 04-01..04-08.
- `ROADMAP.md`: Progress table Phase 4 row shows `8/8 ✅ Complete`.
- `STATE.md`: frontmatter `completed_phases: 4`, `completed_plans: 29`.
- `STATE.md`: Current Position reflects Phase 4 complete + Phase 5 next.
- `STATE.md`: Last Session has a 2026-{MM}-{DD} bullet for the Phase 4 close.
- `flutter test` exits 0 (full suite — Phase 1 + 2 + 3 + 4 all green).
- `flutter test test/policy/play_invariants_test.dart` 9/9 invariants green.
- `flutter build apk --debug` exits 0.
  </acceptance_criteria>
  <done>04-VERIFICATION.md filled; REQUIREMENTS + ROADMAP + STATE updated; Phase 4 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GoRouter / → /pause/:entryId | The route is reachable from the main app's engine but the redirect gate auto-deflects to /onboarding/welcome if onboarding isn't complete. PauseActivity's bound engine starts AT /pause/:entryId (skipping the redirect) — this is intentional per the engine's getInitialRoute override (Plan 04-06). |
| permissionHealthProvider → AccessibilityApi.isServiceEnabled | End-to-end chain spanning Dart provider → Pigeon → Kotlin AccessibilityApiImpl (Plan 04-03) → AccessibilityManager system service. Every link verified individually in earlier plans; this plan asserts the full chain. |
| 04-VERIFICATION.md sign-off → REQUIREMENTS / ROADMAP flips | Manual UAT-driven; the bookkeeping flips happen ONLY after Task 04-08-04's resume signal confirms PASS. If Task 04-08-04 returns "uat-fail", Task 04-08-05 does NOT run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-08-01 | Logic / UX bypass | Onboarding redirect on /pause/:entryId | accept | If user navigates the main app to /pause/:entryId before completing onboarding, the redirect deflects to /onboarding/welcome. This is correct — the user shouldn't see PauseScreen pre-onboarding. PauseActivity's bound engine bypasses this because it sets initial route to /pause/:entryId directly. |
| T-4-08-02 | Compliance / PLAY-02 | Phase 4 expanded scope | mitigate | Task 04-08-02 adds a 9th invariant covering android/.../service/ and android/.../ root files. Catches any future autonomous-action regression. |
| T-4-08-03 | UAT bypass | Silent emulator substitution for REL-04 | mitigate | Task 04-08-03 is a `checkpoint:human-action` per CD-03 — no automatic path. The user must explicitly choose Option A/B/C. Silent substitution is impossible because Task 04-08-04's stopwatch instructions reference the device manufacturer recorded in Task 04-08-03. |
| T-4-08-04 | Information disclosure | Manual UAT logs | accept | The Task 04-08-04 instructions ask for `adb logcat -e PauseActivity` which surfaces the "Displayed" line. No PII. The cold-start measurement is a system metric, not user data. |
| T-4-08-05 | Compliance | REL-01 deferral | accept | CD-01: ship without FGS. STATE.md + 04-VERIFICATION.md document the deferral with the escalation rule. If a future Phase 5 OEM gate fails, the escalation re-opens REL-01 via discuss-phase. |
</threat_model>

<verification>
- `flutter test` exits 0 (full suite, all 4 phases).
- `flutter test test/policy/play_invariants_test.dart` 9/9 invariants green (was 8 → 9 after Task 04-08-02).
- `flutter analyze lib/` 0 errors / 0 warnings.
- `flutter build apk --debug` exits 0.
- `cd android && ./gradlew :app:test` passes (Plan 04-02 ScheduleWindowTest still green).
- `04-VERIFICATION.md` filled with measurements; status=complete.
- `REQUIREMENTS.md` PAUS-01..10 + REL-04 marked Complete.
- `ROADMAP.md` Phase 4 marked ✅ Complete.
- `STATE.md` reflects Phase 4 closed; Phase 5 next.
- `git log --oneline | head` shows the Phase 4 plan commits in order.
</verification>

<success_criteria>
- The wedge ships. AccessibilityService → PauseActivity → cooldown timer → pause_events all work end-to-end on a real device.
- REL-04 OEM overnight gate signed off (PASS at <500ms detect-to-pause).
- PAUS-07 cold-start ≤300ms with FlutterEngineCache pre-warm.
- HealthCheckBanner is truth-bearing for the first time.
- PLAY-02 invariants expanded to 9/9 covering Phase 4's new Kotlin surfaces.
- REL-01 explicitly deferred per CD-01; escalation rule documented.
- All bookkeeping artifacts (REQUIREMENTS, ROADMAP, STATE, 04-VERIFICATION) reflect Phase 4 completion.
- The next session's first command is `/gsd-discuss-phase 5`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pause-ux-the-wedge/04-08-SUMMARY.md` per `templates/summary.md`. Include:
- Final REL-04 stopwatch reading + device used
- PauseActivity cold-start ms (Pixel emulator)
- Whether the CD-01 (no FGS) decision held or was overturned by REL-04 fail
- Total Phase 4 LOC delta (Kotlin + Dart, est. ~1500 lines including tests)
- Confirm PLAY-02 invariants count = 9 (Phase 4 added the service/ + root scope)
</output>
