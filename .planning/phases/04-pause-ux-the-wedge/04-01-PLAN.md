---
plan_id: 04-01
phase: 4
plan: 01
type: execute
wave: 0
objective: "Wave 0: stub every Phase 4 test file (PAUS-01..10 + REL-04 verification template) so the testable surface is locked before any production code lands. Mirrors Phase 2 Plan 02-01 + Phase 3 Plan 03-01."
depends_on: []
files_modified:
  - test/platform/accessibility_api_test.dart
  - test/data/repositories/blocklist_broadcast_test.dart
  - test/data/repositories/pause_event_repository_test.dart
  - test/data/detectors/accessibility_blocked_app_detector_test.dart
  - test/domain/schedule/schedule_window_parity_test.dart
  - test/features/pause/pause_screen_test.dart
  - test/features/pause/widgets/cooldown_chip_row_test.dart
  - test/features/pause/widgets/cooldown_progress_bar_test.dart
  - test/features/pause/widgets/done_confirmation_card_test.dart
  - test/features/pause/pause_controller_test.dart
  - test/features/health/permission_health_provider_a11y_live_test.dart
  - test/_fixtures/pause_intent_fixture.dart
  - .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md
requirements:
  - PAUS-01
  - PAUS-02
  - PAUS-03
  - PAUS-04
  - PAUS-05
  - PAUS-06
  - PAUS-07
  - PAUS-08
  - PAUS-09
  - PAUS-10
  - REL-04
autonomous: true
must_haves:
  truths:
    - "Every Phase 4 REQ-ID (PAUS-01..10 + REL-04) has at least one stubbed test file with a `group('<scope> (<PAUS-NN|REL-04>)', ...)` declaration."
    - "test/_fixtures/pause_intent_fixture.dart exposes `buildPauseIntentExtras({entryId, packageName, blockMode, triggeredAtMs})` returning a Map<String,Object> with the 4 lowercase_snake keys."
    - "test/domain/schedule/schedule_window_parity_test.dart contains a `group('schedule_window Dart parity oracle (D-12 / PAUS-10)', ...)` whose body is stubbed `skip: 'Plan 04-02 fills'`."
    - "04-VERIFICATION.md exists with REL-04 sign-off template (device manufacturer/model, Android version, idle duration, detect-to-pause ms, sign-off date — fields blank until Plan 04-08)."
    - "`flutter test` exits 0 (test files may be marked skip — but no compile errors; suite stays at >=124 Phase 2 + Phase 3 baseline)."
    - "`flutter test test/policy/play_invariants_test.dart` 8/8 invariants stay green (Wave 0 adds no Dart source under lib/, only test/ — invariants are unaffected)."
  artifacts:
    - path: "test/_fixtures/pause_intent_fixture.dart"
      provides: "Shared buildPauseIntentExtras helper for Plan 04-05 (service Intent build) + Plan 04-06 (PauseActivity extras read) widget tests"
      contains: "buildPauseIntentExtras"
    - path: "test/platform/accessibility_api_test.dart"
      provides: "Stub group 'AccessibilityApi.isServiceEnabled (REL-05 health)' with MockAccessibilityApi extends Mock implements AccessibilityApi"
    - path: "test/features/pause/pause_screen_test.dart"
      provides: "Stub groups 'PAUS-02 reason hero (D-02)', 'PAUS-02 empty-reason app-name hero (D-03)', 'PAUS-09 hard entry omits Use anyway', 'PAUS-03 cooldown chips'"
    - path: "test/features/pause/pause_controller_test.dart"
      provides: "Stub groups for cooldown drain (PAUS-04), Cancel writes outcome=1 (PAUS-05), Use anyway writes outcome=2 (PAUS-06), pause_events write seam (D-13)"
    - path: "test/domain/schedule/schedule_window_parity_test.dart"
      provides: "Stub group 'schedule_window Dart parity oracle (D-12 / PAUS-10)' — the Dart side of the byte-for-byte Kotlin parity test"
    - path: "test/features/health/permission_health_provider_a11y_live_test.dart"
      provides: "Stub group 'permissionHealthProvider.accessibilityServiceGranted goes live (HealthCheckBanner truth-bearing)' — exercises the chain after Plan 04-03 ships AccessibilityApiImpl"
    - path: ".planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md"
      provides: "REL-04 OEM-survival overnight gate sign-off template — populated by Plan 04-08"
      contains: "REL-04"
  key_links:
    - from: "test/_fixtures/pause_intent_fixture.dart"
      to: "test/features/pause/* + test/data/repositories/pause_event_repository_test.dart"
      via: "import '../_fixtures/pause_intent_fixture.dart' (relative; depth varies by test path)"
      pattern: "import.*_fixtures/pause_intent_fixture.dart"
    - from: "test/domain/schedule/schedule_window_parity_test.dart"
      to: "lib/domain/schedule/schedule_window.dart (Phase 2 — existing) + android/.../service/ScheduleWindow.kt (Plan 04-02 — pending)"
      via: "isInScheduleWindow Dart import + JVM unit-test companion at android/app/src/test/.../ScheduleWindowTest.kt"
      pattern: "isInScheduleWindow"
---

# Plan 04-01 — Wave 0: Test scaffolding + Pause Intent fixture + REL-04 verification template

<objective>
Stub the 11 Phase 4 test files + the shared `buildPauseIntentExtras` fixture + the 04-VERIFICATION.md REL-04 sign-off template, so the Phase 4 testable surface is locked before any production code lands.

Per CLAUDE.md "Goal-Driven Execution": each later wave becomes "make these tests pass," not "build a feature." Per Karpathy "Simplicity First": every stub minimally compiles — NO speculative test bodies (`skip: 'Plan 04-NN fills'` is the right shape).

Tests that depend on Wave 1+ source are marked `skip:` with a clear `reason:`. The Wave 0 invariant is "the suite stays green" + "every Phase 4 REQ-ID has a named group somewhere."

Purpose: Lock the Phase 4 verification map. Subsequent waves implement against tests, not against vague specs. Mirrors the Phase 2 Plan 02-01 + Phase 3 Plan 03-01 pattern that established the project's RED-stub Wave 0 norm.

Output: 11 stub test files + 1 fixture file + 1 verification template + green `flutter test` suite + 8/8 PLAY-* invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pause-ux-the-wedge/04-CONTEXT.md
@.planning/phases/04-pause-ux-the-wedge/04-PLAN-OVERVIEW.md
@./CLAUDE.md
@test/_fixtures/permission_status_mock.dart
@test/_fixtures/drain_stream_timers.dart
@test/policy/play_invariants_test.dart
@test/data/repositories/block_list_repo_test.dart
@test/platform/usage_api_test.dart
@test/features/home/widgets/cumulative_totals_card_test.dart
@pigeons/accessibility_api.dart
@lib/platform/accessibility_api.g.dart
@lib/data/database/tables/pause_events_table.dart
@lib/data/database/tables/block_list_table.dart
@lib/domain/schedule/schedule_window.dart
@lib/data/detectors/accessibility_blocked_app_detector.dart

<interfaces>
<!-- Pigeon-generated Dart contracts Wave 0 mocks against. Verified from
     lib/platform/accessibility_api.g.dart. -->

AccessibilityApi (Dart):
- Future<bool> isServiceEnabled() — async @HostApi method
- Future<void> openAccessibilitySettings() — fire-and-forget

PauseEvents schema (lib/data/database/tables/pause_events_table.dart):
- id: int autoIncrement
- entryId: int (cascade FK to block_list.id)
- packageName: text
- triggeredAt: DateTime
- cooldownChosenSeconds: int? (nullable — null when Cancel without chip tap)
- outcome: int (0=cooldown-completed, 1=cancel, 2=use-anyway)

Existing Phase 2/3 fixture conventions (test/_fixtures/*):
- Top-level helper function `seed*(db, ...)` shape
- Pure-Dart, no Flutter imports unless the helper is widget-test-scoped
- Re-exports types that test files re-import for terseness

D-11 Intent extras contract (FINAL — Wave 0 fixture encodes this):
- key `extra_blocked_package` → String (e.g., "com.instagram.android")
- key `extra_entry_id` → Long / int (block_list row id)
- key `extra_block_mode` → String ("soft" | "hard")
- key `extra_triggered_at_ms` → Long / int (System.currentTimeMillis())
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 04-01-01 — Create test/_fixtures/pause_intent_fixture.dart + 11 stub test files</name>
  <files>test/_fixtures/pause_intent_fixture.dart, test/platform/accessibility_api_test.dart, test/data/repositories/blocklist_broadcast_test.dart, test/data/repositories/pause_event_repository_test.dart, test/data/detectors/accessibility_blocked_app_detector_test.dart, test/domain/schedule/schedule_window_parity_test.dart, test/features/pause/pause_screen_test.dart, test/features/pause/widgets/cooldown_chip_row_test.dart, test/features/pause/widgets/cooldown_progress_bar_test.dart, test/features/pause/widgets/done_confirmation_card_test.dart, test/features/pause/pause_controller_test.dart, test/features/health/permission_health_provider_a11y_live_test.dart</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/test/_fixtures/permission_status_mock.dart
- /Users/jintanakhomwong/projects/not-to-do-list/test/_fixtures/drain_stream_timers.dart
- /Users/jintanakhomwong/projects/not-to-do-list/test/platform/usage_api_test.dart
- /Users/jintanakhomwong/projects/not-to-do-list/test/data/repositories/block_list_repo_test.dart
- /Users/jintanakhomwong/projects/not-to-do-list/test/features/home/widgets/cumulative_totals_card_test.dart
- /Users/jintanakhomwong/projects/not-to-do-list/lib/platform/accessibility_api.g.dart
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/04-pause-ux-the-wedge/04-CONTEXT.md (the locked-decisions and specifics sections — the test group titles MUST mention the locked D-NN they exercise)
  </read_first>
  <action>
Create one new fixture and 11 new stub test files. Every stub follows the Phase 2/3 Wave 0 idiom: minimal imports, declared groups with named scopes that mention the REQ-ID and the locked decision, body marked `skip: '<reason — fills in Plan 04-NN>'`.

**File 1 — test/_fixtures/pause_intent_fixture.dart** (the only file with real logic; consumed by Plan 04-05 + 04-06 + 04-07 tests):

Top-level helper `buildPauseIntentExtras({required int entryId, required String packageName, String blockMode = 'soft', int? triggeredAtMs})` returning a `Map<String,Object>` with the 4 lowercase_snake keys per D-11. Default `triggeredAtMs` to `DateTime.now().millisecondsSinceEpoch` when null. No Flutter imports — pure Dart so it's reusable from widget tests AND non-widget tests.

Also export two constants: `const intentExtraEntryId = 'extra_entry_id';` and the 3 sibling keys, so Plan 04-05/06 tests don't string-literal the key names. This enforces the D-11 contract at compile time.

**File 2 — test/platform/accessibility_api_test.dart**: import `package:not_to_do_list/platform/accessibility_api.g.dart`, declare `class MockAccessibilityApi extends Mock implements AccessibilityApi {}`, group `'AccessibilityApi (REL-05 health surface) — Plan 04-03 fills'` with a single `test('placeholder', () {}, skip: 'Plan 04-03 fills AccessibilityApiImpl.isServiceEnabled');`.

**File 3 — test/data/repositories/blocklist_broadcast_test.dart**: group `'BlockListRepository broadcast emit (D-10) — Plan 04-04 fills'`; skipped placeholder noting the 'com.nottodo.not_to_do_list.ACTION_BLOCKLIST_UPDATED' action string is the contract.

**File 4 — test/data/repositories/pause_event_repository_test.dart**: group `'PauseEventRepository.insertOutcome (D-13) — Plan 04-07 fills'` with sub-groups stubbing the three outcomes (0=cooldown-completed, 1=cancel, 2=use-anyway).

**File 5 — test/data/detectors/accessibility_blocked_app_detector_test.dart**: group `'AccessibilityBlockedAppDetector (D-16 REL-05 swap) — Plan 04-04 fills'` — note `detections` stream stays `Stream.empty()` (the consumer is the Intent path, not the stream).

**File 6 — test/domain/schedule/schedule_window_parity_test.dart**: group `'schedule_window Dart parity oracle (D-12 / PAUS-10) — Plan 04-02 fills'`. Skipped. Note in a comment that the Kotlin-side companion `android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindowTest.kt` is the partner test that asserts the same 200+ tuples (Plan 04-02 ships both).

**File 7 — test/features/pause/pause_screen_test.dart**: 4 nested groups —
  - `'PAUS-02 reason hero (D-02) — Plan 04-07 fills'`
  - `'PAUS-02 empty-reason app-name hero (D-03) — Plan 04-07 fills'`
  - `'PAUS-09 hard entry omits Use anyway (D-06) — Plan 04-07 fills'`
  - `'PAUS-03 cooldown chips [1m,3m,5m,10m] (D-04) — Plan 04-07 fills'`
All skipped placeholders.

**File 8 — test/features/pause/widgets/cooldown_chip_row_test.dart**: group `'CooldownChipRow (D-04 SegmentedButton clone of BlockModeSegmented) — Plan 04-07 fills'`. Skipped.

**File 9 — test/features/pause/widgets/cooldown_progress_bar_test.dart**: group `'CooldownProgressBar (D-05 LinearProgressIndicator + X:XX remaining caption) — Plan 04-07 fills'`. Skipped.

**File 10 — test/features/pause/widgets/done_confirmation_card_test.dart**: group `'DoneConfirmationCard (D-07 — ✓ Cooldown complete, 1.5s) — Plan 04-07 fills'`. Skipped. Comment that the literal copy is `"✓ Cooldown complete"` per CONTEXT specifics line 195.

**File 11 — test/features/pause/pause_controller_test.dart**: 4 groups (PAUS-04 cooldown drain auto-completes outcome=0, PAUS-05 Cancel writes outcome=1, PAUS-06 Use anyway writes outcome=2, D-13 insert-at-session-end seam). All skipped, Plan 04-07 fills.

**File 12 — test/features/health/permission_health_provider_a11y_live_test.dart**: group `'permissionHealthProvider.accessibilityServiceGranted goes live (HealthCheckBanner truth-bearing) — Plan 04-08 verifies end-to-end'`. Skipped placeholder. Note the underlying Kotlin flip actually happens in Plan 04-03 (when AccessibilityApiImpl replaces the hardcoded-false stub); this test asserts the Dart side observes the change after a refresh().

**Style notes (must preserve, per the Wave 0 idiom in Plan 03-01):**
- Every test file starts with `import 'package:flutter_test/flutter_test.dart';` (or `flutter_test` for widget tests; bare `test` package would also work but the project uses flutter_test universally).
- Mock classes use `class MockX extends Mock implements X {}` from `package:mocktail/mocktail.dart` — NOT mockito (Phase 2 + 3 settled mocktail).
- The `skip:` field is a String stating which Plan fills the test, e.g., `skip: 'Plan 04-07 fills (PAUS-02 reason hero)'`.
- Do NOT add `tearDown` / `setUp` blocks that the stubbed test doesn't use.
- Do NOT introduce `late` variables — fixtures stay top-level functions.
- The fixture in File 1 is the ONLY file with real production-shaped code; everything else is a `group(..., () { test('placeholder', () {}, skip: ...); });` shell.
  </action>
  <verify>
    <automated>flutter test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
- All 12 files exist at the listed paths.
- `test/_fixtures/pause_intent_fixture.dart` exports `buildPauseIntentExtras` AND the four `intentExtra*` String constants (verify: `grep -F 'intentExtraEntryId' test/_fixtures/pause_intent_fixture.dart` returns a line and `grep -F "extra_entry_id" test/_fixtures/pause_intent_fixture.dart` returns a line).
- Every test file declares at least one `group(` whose first argument contains either `PAUS-` or `REL-04` or `D-1` (verify: `grep -rE "group\(.*('PAUS-|'REL-04|'schedule_window|'PauseEventRepo|'AccessibilityApi|'AccessibilityBlockedAppDetector|'permissionHealthProvider)" test/features/pause/ test/data/repositories/pause_event_repository_test.dart test/data/repositories/blocklist_broadcast_test.dart test/data/detectors/accessibility_blocked_app_detector_test.dart test/platform/accessibility_api_test.dart test/domain/schedule/schedule_window_parity_test.dart test/features/health/permission_health_provider_a11y_live_test.dart | grep -c "group(" | awk '$1 > 0'` returns at least 12).
- `flutter test` exits 0 — all skipped tests count as passing; baseline >= Phase 2 + Phase 3 totals.
- `flutter test test/policy/play_invariants_test.dart` reports 8/8 invariants green (Wave 0 adds nothing under `lib/` — invariants unaffected).
- `dart analyze test/` reports 0 errors / 0 warnings (test files compile clean; missing-implementation skipped tests do not produce warnings).
- No test file imports `package:not_to_do_list/features/pause/` (those modules don't exist yet — would fail to compile).
- File 6 (`schedule_window_parity_test.dart`) imports `package:not_to_do_list/domain/schedule/schedule_window.dart` (the Phase 2 helper exists and is the Dart side of the parity contract).
- File 2 (`accessibility_api_test.dart`) imports `package:not_to_do_list/platform/accessibility_api.g.dart` and declares `class MockAccessibilityApi extends Mock implements AccessibilityApi {}`.
  </acceptance_criteria>
  <done>11 stub test files + 1 fixture compile cleanly; flutter test exits 0; 8 PLAY-* invariants green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 04-01-02 — Create 04-VERIFICATION.md REL-04 sign-off template</name>
  <files>.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/04-pause-ux-the-wedge/04-CONTEXT.md (specifics line 201 — REL-04 pass criterion is the contract)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/02-list-crud-onboarding-permissions/02-VERIFICATION.md
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/03-screen-time-dashboard/03-VERIFICATION.md
  </read_first>
  <action>
Create `.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md` as a Markdown file with the REL-04 OEM-survival overnight gate sign-off template. The template has populated section headers + blank fields that Plan 04-08 fills.

Required sections in this exact order:
1. **Frontmatter** with `status: pending`, `nyquist_compliant: false`, `wave_0_complete: true` (toggled when Plan 04-01 lands), `rel_04_signed_off: false`.
2. **`## Phase 4 Verification Map`** — table of every PAUS-* + REL-04 requirement → test file (the 11 stubs from Task 04-01-01) → fill plan (the 04-NN that lands the green test).
3. **`## REL-04 OEM-Survival Overnight Gate (CD-03)`** — sub-sections:
   - `### Device Acquisition` — checklist: real Xiaomi or Samsung (model ____, OS ____), borrowed-or-owned, charge level at test start ____, charge level next morning ____. NOTE the explicit escalation rule per CD-03: "If no Xiaomi or Samsung is on-hand at sign-off time, do NOT silently substitute Pixel + MIUI emulator image. Escalate to discuss-phase for a one-time gate relaxation."
   - `### Test Procedure` — 6-step sequence: (1) install fresh APK + complete onboarding, (2) add 1 not-to-do app (e.g., com.android.chrome to keep cost free), (3) lock device + `adb shell dumpsys deviceidle force-idle`, (4) wait ≥8h (real overnight — not adb-fast-forwarded), (5) next morning unlock + tap the blocked app from launcher + start stopwatch on first tap, (6) stop stopwatch when PauseActivity becomes the foreground window.
   - `### Pass Criterion` — literal copy: "detect-to-pause < 500 ms". Blank fields for: device manufacturer, device model, Android version, idle duration (hours), detect-to-pause (ms), test date, sign-off date.
   - `### Outcome` — blank: PASS / FAIL / DEFERRED-FOR-DEVICE.
4. **`## PauseActivity Cold-Start (PAUS-07 software gate)`** — blank fields for: Pixel emulator Android version (default: stock Android 16 to match Phase 2 + 3 UAT), measured cold-start (ms), pass threshold (300 ms).
5. **`## REL-01 Disposition (CD-01)`** — pre-populated: "Phase 4 ships WITHOUT companion FGS. Manifest FOREGROUND_SERVICE + FOREGROUND_SERVICE_SPECIAL_USE permissions remain declared (declared-but-unused is fine per CD-01). Revisit only if REL-04 fails."
6. **`## PLAY-02 Forbidden-Token Sweep`** — pre-populated: "test/policy/play_invariants_test.dart 8/8 invariants green (verified by Plan 04-08 final gate). NotToDoAccessibilityService.kt source MUST contain ZERO occurrences of `performAction(`, `performGlobalAction(`, `dispatchGesture(` — verified by Plan 04-05 acceptance + the cross-tree absence-grep test."

Footer line: `*Verification template created Plan 04-01; sign-off fields populated by Plan 04-08.*`

Do NOT pre-fill the device fields or stopwatch reading — those are blank lines `__________` for Plan 04-08 to fill manually after the overnight test.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md && grep -c "REL-04" .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
- File exists at `.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md`.
- Contains literal string `detect-to-pause < 500 ms` (CD-03 pass criterion).
- Contains literal string `adb shell dumpsys deviceidle force-idle` (test script per CONTEXT specifics line 201).
- Contains literal section header `## REL-04 OEM-Survival Overnight Gate (CD-03)`.
- Contains literal section header `## PauseActivity Cold-Start (PAUS-07 software gate)`.
- Contains literal section header `## REL-01 Disposition (CD-01)`.
- Frontmatter declares `rel_04_signed_off: false` (verify: `grep -F 'rel_04_signed_off: false' .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md`).
- The escalation rule appears verbatim: "do NOT silently substitute" (per CD-03 + CONTEXT line 88).
- Pre-populated REL-01 disposition mentions `FOREGROUND_SERVICE` AND `FOREGROUND_SERVICE_SPECIAL_USE` (so the declared-but-unused contract is greppable).
  </acceptance_criteria>
  <done>04-VERIFICATION.md exists as the REL-04 sign-off template; Plan 04-08 will fill the blank fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test source ↔ production source | Wave 0 adds NO new production code under `lib/` or `android/app/src/main/`. All new files land under `test/` or `.planning/`. PLAY-02 / PLAY-03 / PLAY-05 / PLAY-06 invariants are unaffected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Compliance | New test sources | mitigate | Wave 0 adds nothing to `android/app/src/main/kotlin/` or `lib/` — only `test/` and `.planning/`. PLAY-02 cross-tree absence-grep is unaffected (the test runs on `android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform` and `lib/` — Wave 0 paths are out of scope). |
| T-4-T05 | PLAY-02 forbidden tokens (PLAY-02 absence) | Wave 0 test sources | accept | The PLAY-02 Dart-side absence-grep scans `lib/`, not `test/`. Wave 0 test files may legitimately mention `performAction` inside skip-reason strings to assert that the production code does NOT contain them — but per the convention established in Phase 2's `play_invariants_test.dart`, these forbidden tokens are described in prose, never as code literals. Wave 0 follows the same convention (the skip-reasons reference the contract by name, not by code symbol). |
| T-4-02 | Information disclosure | 04-VERIFICATION.md | accept | The verification template is a planning artifact, not shipped with the APK. No PII; the only field that could contain device-identifying info (manufacturer + model) is recorded post-test by the solo developer for their own records. |
</threat_model>

<verification>
- `flutter test` exits 0.
- `flutter test test/policy/play_invariants_test.dart` 8/8 invariants green.
- `dart analyze test/` 0 errors / 0 warnings.
- All 12 new test files compile (no missing-import errors).
- `04-VERIFICATION.md` exists with all 6 required sections and the CD-03 escalation rule.
</verification>

<success_criteria>
- Wave 0 lands every Phase 4 test stub + the REL-04 sign-off template.
- Suite stays green; subsequent waves turn skips into passes.
- The Phase 4 testable surface is fully enumerated and traceable to PAUS-01..10 + REL-04.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pause-ux-the-wedge/04-01-SUMMARY.md` per `templates/summary.md`. Note the test count delta (12 new test files; all skipped at this stage).
</output>
