---
plan_id: 04-05
phase: 4
plan: 05
type: execute
wave: 2
objective: "Wave 2: fill NotToDoAccessibilityService body — TYPE_WINDOW_STATE_CHANGED handler with 800ms per-package debounce, in-memory Map<String, ScheduleSlice> refreshed via ACTION_BLOCKLIST_UPDATED LocalBroadcast (RECEIVER_NOT_EXPORTED on API 33+), schedule gate via Plan 04-02's Kotlin port, PauseActivity Intent launch with 4 lowercase_snake extras (D-09..D-12; REL-01 ships WITHOUT companion FGS per CD-01)."
depends_on:
  - 04-02
  - 04-04
files_modified:
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt
  - lib/features/health/widgets/_health_lifecycle_observer.dart
# NOTE: Task 04-05-02 augments the verified-existing _health_lifecycle_observer.dart
# (plan-checker FLAG #4 fix; the prior placeholder `lib/core/app_lifecycle_observer.dart` was wrong).
requirements:
  - PAUS-01
  - PAUS-10
  - REL-01
autonomous: true
must_haves:
  truths:
    - "Service.onAccessibilityEvent observes AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED only — early-returns for null or non-WINDOW_STATE_CHANGED events (PLAY-03 invariant)."
    - "Per-package debounce: 800ms via Map<String, Long> lastFiredAtMs. Events where (now - last < 800) early-return."
    - "In-memory store is Map<String, ScheduleSlice> keyed by packageName; ScheduleSlice carries entryId, blockMode, startMinutes?, endMinutes?, weekdayMask?. The Map is @Volatile and replaced atomically by the receiver."
    - "Service NEVER reads SQLite. The service .kt file imports ZERO Drift/sqlite/Database/Dao symbols — verified by absence-grep in acceptance."
    - "BroadcastReceiver registered in onServiceConnected via LocalBroadcastManager.getInstance(this).registerReceiver(receiver, IntentFilter(BlocklistBroadcastApiImpl.ACTION_BLOCKLIST_UPDATED)). Unregistered in onDestroy. (LocalBroadcastManager is in-process — registration flags like RECEIVER_NOT_EXPORTED are a runtime Android concept on raw Context.registerReceiver; LocalBroadcastManager bypasses that codepath entirely, which is exactly the T-01 mitigation per Plan 04-04 threat model.)"
    - "Schedule gate (PAUS-10): when ALL of slice.startMinutes/endMinutes/weekdayMask are non-null, call isInScheduleWindow(now, start, end, mask); if false, return without launching (outside-window launches pass through). Always-on entries (all-three-null) skip the gate."
    - "On match (in-list + in-window OR in-list + always-on): build Intent(this, PauseActivity::class.java).addFlags(FLAG_ACTIVITY_NEW_TASK) with 4 lowercase_snake extras (extra_blocked_package, extra_entry_id, extra_block_mode, extra_triggered_at_ms) and call startActivity(intent)."
    - "REL-01 disposition (CD-01): NO companion ForegroundService class added. Service body does NOT call startForeground(), does NOT import NotificationCompat, does NOT extend any other Service subclass. Manifest FGS perms remain declared but unused (declared-but-unused is intentional per CD-01 to reduce churn risk)."
    - "Service file body (excluding comment lines) contains ZERO matches for performAction(, performGlobalAction(, dispatchGesture( (PLAY-02 invariant; the cross-tree absence-grep expands scope to android/.../service/ in Plan 04-08)."
    - "MainActivity.onResume calls into the Dart side to re-publish the current snapshot (D-10) so the service's in-memory map is refreshed after a settings round-trip. Achieved by triggering a Pigeon round-trip on resume — see Task 04-05-02 for the exact mechanism."
  artifacts:
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt"
      provides: "Filled service body — event filter, 800ms debounce, in-memory Map, ACTION_BLOCKLIST_UPDATED receiver, schedule gate, PauseActivity Intent launch"
      contains: "AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED"
      forbidden_tokens: ["performAction(", "performGlobalAction(", "dispatchGesture("]
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt"
      provides: "onResume override that triggers a Dart-side republishCurrent() Pigeon round-trip (D-10)"
      contains: "onResume"
  key_links:
    - from: "android/.../service/NotToDoAccessibilityService.kt onAccessibilityEvent"
      to: "android/.../PauseActivity.kt"
      via: "Intent(this, PauseActivity::class.java).addFlags(FLAG_ACTIVITY_NEW_TASK).putExtra(...) + startActivity(intent)"
      pattern: "Intent\\(this,\\s*PauseActivity::class\\.java\\)"
    - from: "android/.../service/NotToDoAccessibilityService.kt onServiceConnected"
      to: "BlocklistBroadcastApiImpl.ACTION_BLOCKLIST_UPDATED"
      via: "LocalBroadcastManager.getInstance(this).registerReceiver(receiver, IntentFilter(BlocklistBroadcastApiImpl.ACTION_BLOCKLIST_UPDATED))"
      pattern: "LocalBroadcastManager\\.getInstance\\(this\\)\\.registerReceiver"
    - from: "android/.../service/NotToDoAccessibilityService.kt schedule gate"
      to: "android/.../service/ScheduleWindow.kt"
      via: "isInScheduleWindow(System.currentTimeMillis(), slice.startMinutes, slice.endMinutes, slice.weekdayMask)"
      pattern: "isInScheduleWindow\\("
---

# Plan 04-05 — Wave 2: AccessibilityService body fill + Intent launch (PAUS-01, PAUS-10, REL-01)

<objective>
Fill the Phase 1 stub at `android/.../service/NotToDoAccessibilityService.kt` with the production body. This is the load-bearing plan of Phase 4 — the wedge fires here.

Architecture per CONTEXT D-09..D-12 + research/ARCHITECTURE.md Pattern 1:
1. **Event filter (D-09):** observe `TYPE_WINDOW_STATE_CHANGED` only; early-return everything else.
2. **Per-package debounce (D-09):** maintain `Map<String, Long> lastFiredAtMs`; skip when `now - last < 800ms`.
3. **In-memory match table (D-10 / Pattern 1):** `Map<String, ScheduleSlice>` keyed by packageName. Populated by a runtime-registered BroadcastReceiver listening for `ACTION_BLOCKLIST_UPDATED`. **Service NEVER reads SQLite.**
4. **Schedule gate (D-12 / PAUS-10):** for snapshots with all-three schedule fields non-null, call Plan 04-02's `isInScheduleWindow(now, start, end, mask)`. False → no Pause. True → continue.
5. **Intent launch (D-11):** `Intent(this, PauseActivity::class.java).addFlags(FLAG_ACTIVITY_NEW_TASK)` + 4 lowercase_snake extras.

REL-01 disposition per CD-01: ship WITHOUT a companion FGS. Manifest's FGS perms remain declared (declared-but-unused — reduces churn risk per CD-01). No FGS class added. Escalation rule: only add an FGS if Plan 04-08's REL-04 overnight gate fails.

Per CLAUDE.md "Surgical Changes": manifest entry, a11y XML config, and Phase 1's `onInterrupt` no-op stay byte-identical. Per Karpathy "Simplicity First": one class with two override methods + one private receiver class + one private data class — ~120 lines including comments.

Per PLAY-02: forbidden tokens are NOT present in the service source — verified by `grep` in acceptance + the cross-tree test.

Depends on Plan 04-02 (Kotlin schedule port) AND Plan 04-04 (broadcast action-string constant). Parallel-safe with Plan 04-04 in Wave 2 (zero file overlap).

Output: 1 filled service file + 1 small MainActivity edit. APK builds; PLAY-02 invariants green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pause-ux-the-wedge/04-CONTEXT.md
@.planning/phases/04-pause-ux-the-wedge/04-PLAN-OVERVIEW.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@./CLAUDE.md
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindow.kt
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/BlocklistBroadcastApiImpl.kt
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt
@android/app/src/main/AndroidManifest.xml
@android/app/src/main/res/xml/not_todo_a11y_config.xml
@test/policy/play_invariants_test.dart

<interfaces>
<!-- Contracts the service consumes — pinned by Plan 04-02 + 04-04. -->

ScheduleWindow.kt (Plan 04-02 — shipped):
```kotlin
fun isInScheduleWindow(
    nowEpochMs: Long,
    startMinutes: Int?,
    endMinutes: Int?,
    weekdayMask: Int?,
    timeZone: TimeZone = TimeZone.getDefault(),
): Boolean
```

BlocklistBroadcastApiImpl.kt (Plan 04-04 — shipped):
- `const val ACTION_BLOCKLIST_UPDATED = "com.nottodo.not_to_do_list.ACTION_BLOCKLIST_UPDATED"`
- `const val EXTRA_BLOCKLIST_SNAPSHOT_JSON = "extra_blocklist_snapshot_json"`
- Wire format: JSONArray string; each element a JSONObject with keys entryId (long), packageName (string), blockMode (string), scheduleStartMinutes (int|absent), scheduleEndMinutes (int|absent), scheduleWeekdayMask (int|absent).

PauseActivity.kt: Phase 1 empty `class PauseActivity : FlutterActivity()`. Plan 04-06 fills the body. This plan only LAUNCHES it with the contract:
- `extra_blocked_package` → String
- `extra_entry_id` → Long
- `extra_block_mode` → String ("soft"|"hard")
- `extra_triggered_at_ms` → Long

Background-launch policy: research/ARCHITECTURE.md L252 — AccessibilityService is documented exempt from Android 10's background-activity-launch restrictions; startActivity from the service is allowed without an FGS.

Manifest service entry (frozen — verified): `android:name=".service.NotToDoAccessibilityService"`, `android:exported="false"`, `android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"`, intent-filter action `android.accessibilityservice.AccessibilityService`, meta-data resource `@xml/not_todo_a11y_config`.

A11y config XML (frozen): `accessibilityEventTypes="typeWindowStateChanged"`, `isAccessibilityTool="false"`, `accessibilityFlags="flagDefault"`.

Phase 1 doc comment at the top of NotToDoAccessibilityService.kt (lines 1-26) MUST be preserved verbatim — it documents the PLAY-02 / Pattern 1 invariants and is the absence-grep contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-05-01 — Fill NotToDoAccessibilityService.kt body</name>
  <files>android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt (Phase 1 stub — preserve the lines 1-26 doc comment verbatim)
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindow.kt (Plan 04-02 — the function being called)
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/BlocklistBroadcastApiImpl.kt (Plan 04-04 — the companion constants the receiver imports)
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/PauseActivity.kt (the target activity class)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/research/ARCHITECTURE.md (lines 182-197 — Pattern 1 verbatim)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/research/PITFALLS.md (lines 152-187 + L334)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/04-pause-ux-the-wedge/04-CONTEXT.md (specifics lines 197-202, D-09..D-12, CD-01)
- /Users/jintanakhomwong/projects/not-to-do-list/test/policy/play_invariants_test.dart (PLAY-02 forbidden-token list)
  </read_first>
  <behavior>
The service has FIVE concerns; each maps to a small concrete code block:

(a) **State:** one private `@Volatile var blockMap: Map<String, ScheduleSlice> = emptyMap()` (atomic replace by receiver; safe to read on event thread). One private `mutableMapOf<String, Long> lastFiredAtMs` (single-threaded event thread; no concurrent map needed). One private constant `DEBOUNCE_MS = 800L`. One private constant `TAG = "NotToDoA11yService"` for a single onServiceConnected log line.

(b) **onServiceConnected:** call super; register `BlockListReceiver` via `LocalBroadcastManager.getInstance(this).registerReceiver(receiver, IntentFilter(BlocklistBroadcastApiImpl.ACTION_BLOCKLIST_UPDATED))`. Log a static "service connected" string (no event data, no PII — T-04). The Map starts empty; Dart re-publishes on MainActivity.onResume (Task 04-05-02) so the service is hydrated within one resume cycle.

(c) **onDestroy:** unregister the receiver; call super.

(d) **onAccessibilityEvent:** the hot path. Order of checks (each early-returns):
  1. `event == null` → return.
  2. `event.eventType != TYPE_WINDOW_STATE_CHANGED` → return.
  3. `pkg = event.packageName?.toString() ?: return`.
  4. `slice = blockMap[pkg] ?: return` (not in block list).
  5. `now = System.currentTimeMillis(); last = lastFiredAtMs[pkg] ?: 0L; if (now - last < DEBOUNCE_MS) return`.
  6. Schedule gate: `hasSchedule = slice.startMinutes != null && slice.endMinutes != null && slice.weekdayMask != null`; if `hasSchedule && !isInScheduleWindow(now, slice.startMinutes!!, slice.endMinutes!!, slice.weekdayMask!!)` → return.
  7. `lastFiredAtMs[pkg] = now`.
  8. Build Intent with the 4 extras and call `startActivity(intent)`. The Intent uses `Intent(this, PauseActivity::class.java).addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)`. Extras: `extra_blocked_package` (String pkg), `extra_entry_id` (Long slice.entryId), `extra_block_mode` (String slice.blockMode), `extra_triggered_at_ms` (Long now).

(e) **onInterrupt:** Phase 1 no-op preserved.

Inner private data class `ScheduleSlice(val entryId: Long, val blockMode: String, val startMinutes: Int?, val endMinutes: Int?, val weekdayMask: Int?)`.

Inner private receiver class `BlockListReceiver : BroadcastReceiver`. `onReceive(context, intent)`: read `intent?.getStringExtra(BlocklistBroadcastApiImpl.EXTRA_BLOCKLIST_SNAPSHOT_JSON) ?: return`. Wrap in `try { ... } catch (e: Throwable) { /* no Log of json — T-04 */ }`. Parse via `JSONArray(json)`, iterate, build a fresh `Map<String, ScheduleSlice>` (skip rows whose packageName is empty), then atomically `blockMap = map`. Null-checks on optional schedule fields via `if (o.has("scheduleStartMinutes")) o.getInt(...) else null` (matches Plan 04-04's putOpt semantics — absent key = null).
  </behavior>
  <action>
Replace the body of `android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt`. PRESERVE the Phase 1 doc comment header (lines 1-26 of current file) byte-for-byte — it is the absence-grep contract.

The class body delivers the 5 concerns in <behavior>. Imports the service file needs:
- `android.accessibilityservice.AccessibilityService` (existing)
- `android.content.BroadcastReceiver`
- `android.content.Context`
- `android.content.Intent`
- `android.content.IntentFilter`
- `android.util.Log`
- `android.view.accessibility.AccessibilityEvent` (existing)
- `androidx.localbroadcastmanager.content.LocalBroadcastManager`
- `com.nottodo.not_to_do_list.PauseActivity`
- `com.nottodo.not_to_do_list.platform.BlocklistBroadcastApiImpl`
- `org.json.JSONArray`

Implementation rules:
- Single `Log.d(TAG, "service connected")` call in onServiceConnected — the ONLY Log call in the file. NO logging of packageName, NO logging of event data, NO logging of the snapshot JSON (T-04 discipline).
- All Intent extras keys are STRING LITERALS, lowercase_snake_case, exactly: `"extra_blocked_package"`, `"extra_entry_id"`, `"extra_block_mode"`, `"extra_triggered_at_ms"` (D-11). Do NOT centralize these into a constants file — Plan 04-06 reads them on the PauseActivity side via `intent.getStringExtra("extra_blocked_package")` etc.; keeping the keys as inline literals on both sides preserves the readability of the wire contract. (If a future plan extracts them, that's a refactor — not Phase 4's job.)
- Use `slice.startMinutes!!` etc. inside the `isInScheduleWindow` call because the `hasSchedule` guard above proved them non-null. Kotlin's smart-cast across the `!=null` chain doesn't carry into a function call argument list, so the `!!` is necessary — but it's safe by construction.
- The receiver is `inner class` so it can access `blockMap` directly. The single assignment to `@Volatile blockMap = map` is the publish point.
- DO NOT add any imports beyond the 11 listed. In particular:
  - NO `android.app.NotificationCompat` (REL-01 / CD-01 — no FGS).
  - NO `android.app.Service` (parent class is enough).
  - NO Drift/sqlite/Room/AppDatabase imports (Pattern 1).
  - NO `androidx.work` (Phase 5 territory).
  - NO `android.accessibilityservice.AccessibilityServiceInfo` (we don't need to call `setServiceInfo` — the manifest XML config is sufficient).
- DO NOT call `disableSelf()`, `getRootInActiveWindow()`, `getWindows()`, `performAction()`, `performGlobalAction()`, or `dispatchGesture()`. The PLAY-02 absence-grep is the contract.

Preserve the Phase 1 doc comment block at the top of the file VERBATIM. The Phase 1 PLAY-02 wording — that the forbidden tokens are deliberately kept out of the file body — is the absence-grep contract. The new body of the class continues to honor that wording.
  </action>
  <verify>
    <automated>flutter build apk --debug 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
- File compiles: `cd android && ./gradlew :app:compileDebugKotlin` exits 0.
- File contains the literal `AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED` (verify: `grep -F 'AccessibilityEvent.TYPE_WINDOW_STATE_CHANGED' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt`).
- File contains the literal `DEBOUNCE_MS = 800L` (D-09 verbatim).
- File contains the literal `LocalBroadcastManager.getInstance(this).registerReceiver` (D-10 receiver pattern).
- File contains the literal `BlocklistBroadcastApiImpl.ACTION_BLOCKLIST_UPDATED` (the action constant import — single source of truth from Plan 04-04).
- File contains the literal `isInScheduleWindow(` AND the call passes `System.currentTimeMillis()` as the first arg (verify: `grep -A 1 'isInScheduleWindow(' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt | head -3 | grep -F 'System.currentTimeMillis()'`).
- File contains all four Intent-extras key literals: `"extra_blocked_package"`, `"extra_entry_id"`, `"extra_block_mode"`, `"extra_triggered_at_ms"` (verify each via `grep -F`).
- File contains the literal `Intent(this, PauseActivity::class.java)` AND `Intent.FLAG_ACTIVITY_NEW_TASK` (D-11).
- File contains a `data class ScheduleSlice` declaration.
- File contains an `@Volatile` annotation on `blockMap` (atomic publish from receiver to event thread).
- File body (non-comment lines) contains ZERO matches for `performAction(|performGlobalAction(|dispatchGesture(` (verify: `grep -v '^\s*\(//\|\*\)' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt | grep -cE 'performAction\(|performGlobalAction\(|dispatchGesture\('` returns 0).
- File body (non-comment lines) contains ZERO matches for `Drift|sqlite|AppDatabase|Database\(|getDao|insertOnConflictUpdate` (Pattern 1 — verify: `grep -v '^\s*\(//\|\*\)' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt | grep -cE 'Drift|\\bsqlite\\b|AppDatabase|getDao\\('` returns 0).
- File body (non-comment lines) contains ZERO matches for `startForeground|NotificationCompat\.Builder|FOREGROUND_SERVICE` (REL-01 / CD-01 — ship without FGS; verify: `grep -v '^\s*\(//\|\*\)' android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt | grep -cE 'startForeground\\(|NotificationCompat\\.Builder|FOREGROUND_SERVICE'` returns 0).
- The Phase 1 doc comment header (lines 1-26 — the PLAY-02 absence-grep contract paragraph) is preserved verbatim (verify: `head -26 android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/NotToDoAccessibilityService.kt | grep -c 'PLAY-02'` returns ≥1).
- `flutter test test/policy/play_invariants_test.dart` 8/8 invariants stay green (existing test scans `lib/` Dart + `android/.../platform/` Kotlin; this file is in `android/.../service/` so not yet in scope — Plan 04-08 expands the scope. This acceptance preemptively satisfies that expansion.)
- `flutter build apk --debug` exits 0; APK lands at `build/app/outputs/flutter-apk/app-debug.apk`.
- `flutter test` exits 0 (Phase 2 + Phase 3 + Phase 4 Wave 0/1 tests all green/skip).
  </acceptance_criteria>
  <done>Service body filled; APK builds; Pattern 1 + PLAY-02 + REL-01 (no FGS) invariants all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-05-02 — MainActivity.onResume triggers republishCurrent() via Dart-side observer</name>
  <files>lib/features/health/widgets/_health_lifecycle_observer.dart</files>
  <read_first>
- /Users/jintanakhomwong/projects/not-to-do-list/android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt (current state — Plan 04-03 just added onCreate; we add onResume now)
- /Users/jintanakhomwong/projects/not-to-do-list/lib/main.dart (the app's entrypoint — where to install a WidgetsBindingObserver)
- /Users/jintanakhomwong/projects/not-to-do-list/lib/data/repositories/block_list_repository.dart (Plan 04-04 added republishCurrent() — the method this task calls)
- /Users/jintanakhomwong/projects/not-to-do-list/.planning/phases/02-list-crud-onboarding-permissions/02-08-PLAN.md (Phase 2 used a WidgetsBindingObserver for ONBD-03 onResume — same pattern, search for "AppLifecycleState.resumed")
- /Users/jintanakhomwong/projects/not-to-do-list/lib/features/health/widgets/_health_lifecycle_observer.dart (verified-existing Phase 2 lifecycle observer — Task 04-05-02 AUGMENTS this file by adding one line in its `AppLifecycleState.resumed` branch. Plan-checker FLAG #4 fix: the verified path includes the `widgets/` subfolder and the leading underscore.)
  </read_first>
  <behavior>
- MainActivity does NOT need a Kotlin-side onResume override. The cleanest path is Dart-side: Flutter's `WidgetsBindingObserver.didChangeAppLifecycleState(AppLifecycleState.resumed)` fires whenever the main app comes to the foreground — same hook Phase 2 uses for ONBD-03 health re-check.
- Add (or augment) a Dart-side AppLifecycleObserver that calls `ref.read(blockListRepositoryProvider).republishCurrent()` on `AppLifecycleState.resumed`. The repository's republishCurrent (added in Plan 04-04 Task 04-04-03) emits the Pigeon broadcast.
- The verified-existing observer is at `lib/features/health/widgets/_health_lifecycle_observer.dart` (plan-checker FLAG #4 — confirmed via `find lib/features/health -type f -name "*lifecycle*"`). Task 04-05-02 AUGMENTS this file — add the `republishCurrent()` call to its existing `AppLifecycleState.resumed` handler. The "create a new observer" branch in Step 3 below is dead code retained only as a safety net; if you reach Step 3, something is wrong with your environment — re-run the find command before creating any new file.
- MainActivity.kt itself stays unchanged in this task — the Plan 04-03 onCreate (FlutterEngineCache pre-warm) remains; no Kotlin onResume needed. (Rationale: the Kotlin Activity's onResume runs BEFORE Flutter's engine has booted the Dart code on cold launch, so a Kotlin-side hook would be redundant with the Dart-side observer that fires every resume; using only the Dart side keeps the layer separation clean.)
  </behavior>
  <action>
**Step 1 — Read the verified-existing observer.** Path: `lib/features/health/widgets/_health_lifecycle_observer.dart` (confirmed via plan-checker FLAG #4 verification). It already implements `WidgetsBindingObserver.didChangeAppLifecycleState` and calls `permissionHealthProvider.refresh()` on `AppLifecycleState.resumed`.

**Step 2 — Augment the existing observer** (preferred — Karpathy Simplicity First: one observer per app, not two). In the observer's `didChangeAppLifecycleState` resumed-branch, add one new line AFTER the existing `permissionHealthProvider.refresh()` call:

```dart
// D-10: re-publish the block-list snapshot to the AccessibilityService's
// in-memory map. Cheap (one Pigeon round-trip + one LocalBroadcast).
await ref.read(blockListRepositoryProvider).republishCurrent();
```

Add the `blockListRepositoryProvider` import to the observer file (verify import path — likely `lib/domain/providers/block_list_repository_provider.dart` per Phase 2 convention).

**Step 3 — If no existing observer is found:** create `lib/core/app_lifecycle_observer.dart` declaring a `class AppLifecycleObserver extends ConsumerStatefulWidget` that wraps its child + installs a WidgetsBindingObserver in initState. In its `didChangeAppLifecycleState(state)`, if `state == AppLifecycleState.resumed`, call `ref.read(blockListRepositoryProvider).republishCurrent()`. Register it in `lib/main.dart` as a top-level wrapper around the existing app widget tree. This branch is unlikely — Phase 2 almost certainly shipped a health observer per CONTEXT line 132. If you create a new file, document why the existing observer wasn't augmented.

**Step 4 — Do NOT touch MainActivity.kt in this task.** The Plan 04-03 FlutterEngineCache pre-warm in onCreate stays as-is. No Kotlin onResume override.

**Step 5 — Verify the Dart-side broadcast emit chain.** After this task, the flow is:
- User puts the app in background.
- User toggles a11y service off in Settings.
- User returns to the app.
- AppLifecycleState.resumed fires in Flutter.
- Observer calls `repo.republishCurrent()`.
- Repository calls `_broadcaster!.publishBlockList(snapshot)` (Plan 04-04's emit hook).
- Kotlin BlocklistBroadcastApiImpl sends LocalBroadcast.
- Service's BlockListReceiver onReceive parses the snapshot, atomically replaces blockMap.
- Service is now in sync with the latest Dart state.

DO NOT add any other republishCurrent call sites — the observer is the only one. Mutating ops in BlockListRepository (add/update/delete/insertMany) already emit per Plan 04-04 — no duplicate emit needed.
  </action>
  <verify>
    <automated>flutter test test/features/health/ 2>&1 | tail -10 && flutter analyze lib/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
- A Dart file under `lib/` declares a `WidgetsBindingObserver` or `ConsumerStatefulWidget` lifecycle hook that calls `republishCurrent()` (verify: `grep -rF 'republishCurrent()' lib/` returns at least one match outside `lib/data/repositories/block_list_repository.dart`).
- The call site is reached via `AppLifecycleState.resumed` (verify: in the same file as the `republishCurrent` call, `grep -F 'AppLifecycleState.resumed'` returns a match).
- No new MainActivity.kt edit in this task (verify: `git diff android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt` shows no changes attributable to this task — Plan 04-03's onCreate + Plan 04-04's BlocklistBroadcastApi.setUp remain).
- `flutter analyze lib/` 0 errors / 0 warnings on the changed Dart file(s).
- `flutter test` exits 0 — the Phase 2 lifecycle-observer tests (if any) still pass; no new test file required (the Plan 04-08 manual UAT verifies the end-to-end resume → service refresh chain).
- `flutter build apk --debug` exits 0.
- `flutter test test/policy/play_invariants_test.dart` 8/8 invariants stay green.
  </acceptance_criteria>
  <done>Dart-side lifecycle observer calls republishCurrent() on resume; the D-10 refresh path closes end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccessibilityService event thread → blockMap read | Single read of `@Volatile` Map reference. Safe by Kotlin/JVM memory model. No lock. |
| BlockListReceiver → blockMap write | Single assignment under `@Volatile` — publish happens-before the next event-thread read. Receiver runs on the binder thread (LocalBroadcastManager delivers synchronously in-process). |
| Service event thread → PauseActivity Intent | startActivity from an active AccessibilityService is exempt from Android 10's background-launch restriction (documented). Intent target is `PauseActivity::class.java` — a compile-time class reference, NOT a string action; cannot be spoofed. |
| BlockListReceiver → Intent extras | Receiver consumes the EXTRA_BLOCKLIST_SNAPSHOT_JSON string. JSON parsing is wrapped in try/catch — malformed snapshot keeps the previous map (fail-safe). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05-01 (T-01 from planning) | Spoofing | ACTION_BLOCKLIST_UPDATED receiver | mitigate | LocalBroadcastManager is in-process by design — broadcasts NEVER leave the app. No `<receiver>` declared in AndroidManifest. Combined with Plan 04-04's `setPackage` narrowing, external apps cannot inject ACTION_BLOCKLIST_UPDATED into this service's BlockListReceiver. |
| T-4-05-02 (T-02 from planning) | Tampering | Intent extras to PauseActivity | mitigate | PauseActivity validates extras in Plan 04-06; missing/invalid extras cause `finish()` without writing pause_events. This plan establishes the contract; Plan 04-06 enforces it. |
| T-4-05-03 (T-03 from planning) | Spoofing | PauseActivity launched externally | mitigate (verify-only) | Plan 04-06 verifies `android:exported="false"` (already set in manifest line 85). This plan's Intent uses the explicit-component constructor `Intent(this, PauseActivity::class.java)` — only callable from in-process. External apps cannot launch PauseActivity. |
| T-4-05-04 (T-04 from planning) | Privacy | reasonNote in logs | mitigate | The service file contains ONE `Log.d` call with a static string ("service connected"). NO logging of packageName, event data, or snapshot JSON. The Phase 2 reasonNote is NEVER read by this service (Pattern 1: service doesn't read SQLite; snapshot also excludes reasonNote per Plan 04-04 DTO design). |
| T-4-05-05 (T-05 from planning) | Compliance / PLAY-02 | NotToDoAccessibilityService.kt | mitigate | Acceptance criteria's `grep -cE 'performAction\(|performGlobalAction\(|dispatchGesture\('` enforces zero non-comment-line matches. Plan 04-08's expanded cross-tree absence-grep will include `android/.../service/` and catch any future regression. The Phase 1 doc comment header explicitly documents this invariant. |
| T-4-05-06 | DoS / Performance | 800ms debounce floods | mitigate | TYPE_WINDOW_STATE_CHANGED can re-fire on rapid window transitions (e.g., a dialog overlay on top of the blocked app fires another event). The 800ms per-package debounce drops these. Anything faster (300ms tested in research) misses legit launches when the system delays the second event slightly; 800ms is the research-anchored optimum (CONTEXT specifics line 197). |
| T-4-05-07 | DoS / Crash | Malformed snapshot | mitigate | BlockListReceiver wraps JSON parsing in try/catch and falls back to the previous map. Service does not crash; service does not log the snapshot (T-04). Worst case: the user adds an app, the broadcast is corrupted, the service uses the stale map and that one new entry doesn't intercept on its first foregrounding — corrected on the next resume (Task 04-05-02 republishCurrent). |
| T-4-05-08 | Information disclosure | Pre-warmed engine + service in same process | accept | Both Plan 04-03's pre-warmed Flutter engine and this service run in the SAME app process (Pixel-based Android allocates one process per app). No process boundary to cross; no IPC reliability concern. The service's blockMap is purely native-heap (Kotlin Map<String, ScheduleSlice>); the engine cannot read it directly without going through Pigeon (which is by design). Phase 1's Pattern 1 trade-off is preserved. |
| T-4-05-09 | REL-01 / CD-01 | No companion FGS | accept (with escalation rule) | Per CD-01: ship without FGS. Manifest FGS perms remain declared (declared-but-unused). If Plan 04-08's REL-04 overnight gate fails on Xiaomi/Samsung, escalate to discuss-phase BEFORE adding an FGS — the rationale is to keep the Play declaration's permission set minimal AND to honor the research recommendation (ARCHITECTURE.md L488-489: a11y service has its own lifecycle, not subject to Doze). Acceptance grep enforces NO `startForeground` in the service file. |
</threat_model>

<verification>
- `flutter build apk --debug` exits 0; APK lands at `build/app/outputs/flutter-apk/app-debug.apk`.
- `flutter test` exits 0 (Phase 2 + Phase 3 baseline + Phase 4 Wave 0/1 all green/skip).
- `flutter test test/policy/play_invariants_test.dart` 8/8 invariants green.
- `dart analyze` 0 errors / 0 warnings on the changed Dart file(s).
- `cd android && ./gradlew :app:compileDebugKotlin` exits 0.
- `cd android && ./gradlew :app:test` passes (no new JVM tests added in this plan; Plan 04-02 ScheduleWindowTest still passes).
- All grep-based acceptance criteria (in each task) pass.
- Manual via `adb logcat -d -s NotToDoA11yService` would show the "service connected" line after a11y service toggle — defer to Plan 04-08 final UAT.
- Manual flow: install APK, complete onboarding, add a blocked app, tap that app on launcher → empty PauseActivity (Plan 04-06 + 04-07 fill the UI) becomes the foreground window within ~1s. Deferred to Plan 04-06 + 04-08.
</verification>

<success_criteria>
- The wedge fires for the first time: tapping a blocked app on the launcher → PauseActivity launches via the Intent path.
- 800ms debounce, schedule gate, and PLAY-02 / Pattern 1 invariants all in place.
- REL-01 disposition (no FGS) is committed; escalation rule is documented in 04-VERIFICATION.md (Plan 04-01) and in this plan's threat model.
- Plan 04-06 + 04-07 can now build the PauseActivity body and the pause-screen UI against a working Intent contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pause-ux-the-wedge/04-05-SUMMARY.md` per `templates/summary.md`. Note the LOC count of the service file (~120-150 lines including the preserved Phase 1 doc comment); flag any deviation from CD-01 (no FGS) as a critical regression.
</output>
