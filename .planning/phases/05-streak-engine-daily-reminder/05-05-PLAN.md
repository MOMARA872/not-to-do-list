---
phase: 05-streak-engine-daily-reminder
plan: 05
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-04]
files_modified:
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/ReminderAlarmReceiver.kt
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/res/values/strings.xml
  - test/features/reminder/deep_link_navigation_test.dart
  - test/policy/phase_5_invariants_test.dart
autonomous: true
requirements:
  - NOTF-02
  - NOTF-03
  - NOTF-04
  - NOTF-05
  - REL-05
must_haves:
  truths:
    - "NotificationApiImpl.scheduleDailyReminder(hour, minute) computes the next wall-clock occurrence of hour:minute via Calendar (today's instance if future; else +1 day), then calls AlarmManager.setExactAndAllowWhileIdle(RTC_WAKEUP, triggerAtMs, pendingIntent). On Android 12+: if !canScheduleExactAlarms() return Result.failure(NotificationApiError(\"EXACT_ALARM_DENIED\")); Dart side surfaces banner state."
    - "NotificationApiImpl.cancelDailyReminder calls AlarmManager.cancel(pendingIntent) — idempotent."
    - "scheduleDailyReminder writes `flutter.reminder_hour_minute = hour * 60 + minute` to the FlutterSharedPreferences file (key without `flutter.` prefix in Dart, with `flutter.` prefix in Kotlin reads per shared_preferences plugin convention) — RESEARCH §10 R-9."
    - "ReminderAlarmReceiver.onReceive (1) creates NotificationChannel('daily_checkin', 'Daily check-in', IMPORTANCE_DEFAULT) idempotently; (2) builds NotificationCompat notification with title=\"Daily check-in\", body=\"How did today go?\", contentIntent = PendingIntent.getActivity(MainActivity with extra `deep_link_to`=\"/checkin\", FLAG_IMMUTABLE | FLAG_UPDATE_CURRENT); (3) nm.notify; (4) re-arms self for next day via setExactAndAllowWhileIdle (or setAndAllowWhileIdle if canScheduleExactAlarms() returns false)."
    - "BootReceiver.onReceive guards `if (intent.action != Intent.ACTION_BOOT_COMPLETED && intent.action != Intent.ACTION_LOCKED_BOOT_COMPLETED) return`; reads `flutter.reminder_hour_minute` from FlutterSharedPreferences file (default 21*60=1260 if absent); re-arms the alarm via the same exact-or-inexact branch as ReminderAlarmReceiver."
    - "MainActivity.configureFlutterEngine replaces the NotImplementedError NotificationApi anonymous stub with `NotificationApi.setUp(messenger, NotificationApiImpl(applicationContext))`."
    - "MainActivity onCreate populates a static weak-ref `MainActivity.weakInstance` for the PermissionStatusApiImpl request-permission flow (Plan 05-04 prerequisite); onDestroy clears it."
    - "MainActivity overrides onNewIntent: extract intent.getStringExtra(\"deep_link_to\"); validate against allow-list {\"/checkin\"} ONLY; on match, write `flutter.pending_deep_link = \"/checkin\"` to FlutterSharedPreferences (consumed by HomeScreen.initState in Plan 05-07). Reject (no-op) every other value — T-2 input-validation lock."
    - "AndroidManifest declares `<receiver android:name=\".receiver.ReminderAlarmReceiver\" android:exported=\"false\" />` AND `<receiver android:name=\".receiver.BootReceiver\" android:exported=\"true\" android:enabled=\"true\"><intent-filter><action android:name=\"android.intent.action.BOOT_COMPLETED\" /><action android:name=\"android.intent.action.LOCKED_BOOT_COMPLETED\" /></intent-filter></receiver>`. POST_NOTIFICATIONS / SCHEDULE_EXACT_ALARM / RECEIVE_BOOT_COMPLETED already declared (no change)."
    - "strings.xml declares <string name=\"notification_channel_daily_checkin\">Daily check-in</string> and <string name=\"notification_body_daily_checkin\">How did today go?</string> — referenced by NotificationCompat builder via getString(R.string.*)."
    - "9 PLAY-02 invariants stay green. phase_5_invariants_test.dart now passes the BootReceiver-declared and notification-body-privacy invariants (previously skipped — flipped to real in this plan)."
  artifacts:
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt"
      provides: "Real Kotlin body for scheduleDailyReminder + cancelDailyReminder + buildPendingIntent + computeNextOccurrenceMs"
      contains: "class NotificationApiImpl"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/ReminderAlarmReceiver.kt"
      provides: "BroadcastReceiver firing daily_checkin notification + self-re-arm"
      contains: "class ReminderAlarmReceiver"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt"
      provides: "BroadcastReceiver re-arming the daily reminder on ACTION_BOOT_COMPLETED + ACTION_LOCKED_BOOT_COMPLETED"
      contains: "class BootReceiver"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Two <receiver> declarations (ReminderAlarmReceiver + BootReceiver with intent-filter)"
      contains: "BootReceiver"
  key_links:
    - from: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt"
      to: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt"
      via: "NotificationApi.setUp(messenger, NotificationApiImpl(applicationContext))"
      pattern: "NotificationApi\\.setUp\\(.*NotificationApiImpl"
    - from: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt"
      to: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt"
      via: "NotificationApiImpl.buildPendingIntent(context) — shared PendingIntent target"
      pattern: "NotificationApiImpl\\.buildPendingIntent"
    - from: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt"
      to: "lib/features/streak/storage/streak_keys.dart (Plan 05-02 — key contract)"
      via: "Cross-process key 'flutter.reminder_hour_minute' on FlutterSharedPreferences"
      pattern: "reminder_hour_minute"
---

# Plan 05-05 — Wave 4: NotificationApiImpl.kt + ReminderAlarmReceiver + BootReceiver + MainActivity wiring + manifest

<objective>
Replace the Phase 1 `NotImplementedError` NotificationApi stub with a real Kotlin body: `AlarmManager.setExactAndAllowWhileIdle()` for NOTF-02/NOTF-04, a `BroadcastReceiver` (ReminderAlarmReceiver) that fires the daily_checkin notification and re-arms itself for the next day, a second `BroadcastReceiver` (BootReceiver) that re-arms the alarm after device reboot (NOTF-05), and MainActivity changes that wire `NotificationApi.setUp(...)` + parse the `deep_link_to` intent extra for NOTF-03.

This is the biggest Kotlin surface in Phase 5. RESEARCH §5 provides the full skeletons (lines 395-557). PATTERNS.md flags these as "no analog in codebase" — first BroadcastReceivers in the project; planner consults RESEARCH §5 directly.

Highest-risk landmine: RESEARCH §10 R-9 — BootReceiver must read `flutter.reminder_hour_minute` from `FlutterSharedPreferences` file. This plan executes the cross-process key read at runtime; REL-05 (Plan 05-09) verifies on real device.

Purpose: deliver the platform body for the daily reminder; close NOTF-02..05 software gates; set up REL-05 manual gate.
Output: 3 Kotlin files (1 impl + 2 receivers) + manifest edit + MainActivity wiring + strings.xml + 1 GREEN deep-link test + 2 unskipped phase_5_invariants tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md
@.planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md
@.planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md
@.planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/PermissionStatusApiImpl.kt
@android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/BlocklistBroadcastApiImpl.kt
@android/app/src/main/AndroidManifest.xml
@pigeons/notification_api.dart
@lib/platform/notification_api.g.dart
@lib/features/streak/storage/streak_keys.dart
@test/policy/phase_4_invariants_test.dart
</context>

<interfaces>
<!-- Kotlin contracts extracted from RESEARCH §5 + PATTERNS.md. Receiver skeletons are first-of-kind in the codebase. -->

From pigeons/notification_api.dart (signature unchanged from Phase 1 stub):
- @async void scheduleDailyReminder(int hour, int minute)
- @async void cancelDailyReminder()
- KotlinOptions package = "com.nottodo.not_to_do_list.platform"
- errorClassName = "NotificationApiError"

From RESEARCH §5 NotificationApiImpl skeleton (lines 395-454):
- companion object { REMINDER_REQUEST_CODE = 5001; fun buildPendingIntent(ctx) -> PendingIntent }
- PendingIntent flags = FLAG_IMMUTABLE | FLAG_UPDATE_CURRENT
- triggerAtMs computed via Calendar (today's hour:minute if future, else add(Calendar.DAY_OF_YEAR, 1))
- Android 12+: check canScheduleExactAlarms() before setExactAndAllowWhileIdle; else Result.failure(NotificationApiError("EXACT_ALARM_DENIED"))
- Persistence of reminder_hour_minute to FlutterSharedPreferences file

From RESEARCH §5 ReminderAlarmReceiver skeleton (lines 459-518):
- NotificationChannel created in-line (idempotent — creating existing channel is a no-op)
- NotificationCompat builder with setContentTitle/setContentText/setSmallIcon/setContentIntent/setAutoCancel(true)
- Deep-link intent puts extra "deep_link_to" = "/checkin"
- Re-arm at end of onReceive: setExactAndAllowWhileIdle (or setAndAllowWhileIdle fallback)
- REMINDER_NOTIFICATION_ID = 5001

From RESEARCH §5 BootReceiver skeleton (lines 524-541):
- Guard `intent.action != ACTION_BOOT_COMPLETED && intent.action != ACTION_LOCKED_BOOT_COMPLETED` → return
- Read flutter.reminder_hour_minute (default 21*60)
- Same exact/inexact branch as ReminderAlarmReceiver
- Manifest receiver android:exported="true" with intent-filter for BOOT_COMPLETED + LOCKED_BOOT_COMPLETED

From RESEARCH §5 manifest snippet (lines 547-557): the exact <receiver> + intent-filter XML

From PATTERNS.md "MainActivity.onNewIntent": V5 input-validation — accept only "/checkin" as deep_link_to extra
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationApiImpl + ReminderAlarmReceiver + BootReceiver + manifest + MainActivity wiring + onNewIntent deep-link parser + strings.xml</name>
  <files>android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt, android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/ReminderAlarmReceiver.kt, android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt, android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt, android/app/src/main/AndroidManifest.xml, android/app/src/main/res/values/strings.xml</files>
  <read_first>
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §5 (full Kotlin skeletons for all three new files — lines 395-557),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §10 R-2 (canScheduleExactAlarms revoke mid-flight), R-3 (Xiaomi BootReceiver kill), R-9 (FlutterSharedPreferences file/key contract — highest-risk landmine),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §16 (security domain — receivers exported/not, V5 input validation),
    .planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md §"New Patterns Introduced by Phase 5" (lines 1041-1083 — all three new Kotlin files spec'd verbatim),
    .planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md §Copywriting Contract (notification title "Daily check-in", body "How did today go?" — referenced from strings.xml),
    .planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md (D-10 notification copy lock — no entry names in body, privacy),
    android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt (current configureFlutterEngine — the NotImplementedError stub at lines 68-80 is replaced),
    android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/PermissionStatusApiImpl.kt (try/catch + launchSettingsOrFallback pattern + companion object placement model),
    android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/BlocklistBroadcastApiImpl.kt (LocalBroadcastManager pattern — used here as the analog for how Receivers integrate with the Pigeon API surface),
    android/app/src/main/AndroidManifest.xml (existing receiver registration patterns, perms already declared),
    pigeons/notification_api.dart (verify signature unchanged — no Pigeon edit in this plan),
    lib/platform/notification_api.g.dart (Dart NotificationApi class generated — referenced by Dart-side notificationApiProvider added in Plan 05-04),
    lib/features/streak/storage/streak_keys.dart (cross-process key contract — Dart writes 'reminder_hour_minute'; Kotlin reads 'flutter.reminder_hour_minute'),
    test/policy/phase_4_invariants_test.dart (cross-tree absence-grep test pattern — to verify PLAY-02 + new Phase 5 invariants stay green)
  </read_first>
  <behavior>
    - NotificationApiImpl.scheduleDailyReminder(hour, minute): computes triggerAtMs via Calendar for today's hour:minute (or +1 day if past); on SDK >= S checks AlarmManager.canScheduleExactAlarms() — if false, Result.failure(NotificationApiError("EXACT_ALARM_DENIED")). Else calls setExactAndAllowWhileIdle(RTC_WAKEUP, triggerAtMs, pendingIntent). Writes `flutter.reminder_hour_minute = hour * 60 + minute` to context.getSharedPreferences("FlutterSharedPreferences", MODE_PRIVATE). Returns Result.success(Unit).
    - NotificationApiImpl.cancelDailyReminder: AlarmManager.cancel(buildPendingIntent(context)). Returns Result.success(Unit) regardless.
    - companion object {`buildPendingIntent(ctx: Context): PendingIntent`} builds explicit Intent(ctx, ReminderAlarmReceiver::class.java).apply { `package` = ctx.packageName } wrapped in PendingIntent.getBroadcast(ctx, 5001, intent, FLAG_IMMUTABLE | FLAG_UPDATE_CURRENT). REMINDER_REQUEST_CODE = 5001.
    - companion object {`computeNextOccurrenceMs(h: Int, m: Int): Long`} returns Calendar set to h:m:0 today; if not after now → add DAY_OF_YEAR=1; return timeInMillis.
    - ReminderAlarmReceiver.onReceive(context, intent):
      1. Acquire NotificationManager service; create channel "daily_checkin" with IMPORTANCE_DEFAULT (idempotent; createNotificationChannel is no-op if exists). Channel name pulled from `context.getString(R.string.notification_channel_daily_checkin)` = "Daily check-in".
      2. Build PendingIntent for MainActivity with extras `deep_link_to` = "/checkin" and flags = FLAG_ACTIVITY_NEW_TASK | FLAG_ACTIVITY_CLEAR_TOP; wrap in PendingIntent.getActivity(ctx, 0, intent, FLAG_IMMUTABLE | FLAG_UPDATE_CURRENT).
      3. NotificationCompat.Builder(ctx, "daily_checkin").setContentTitle("Daily check-in").setContentText("How did today go?").setSmallIcon(R.mipmap.ic_launcher).setContentIntent(pi).setAutoCancel(true).build(). UI-SPEC lock: title + body are EXACT literals, no entry names (privacy).
      4. nm.notify(REMINDER_NOTIFICATION_ID = 5001, notification).
      5. Re-arm self: read flutter.reminder_hour_minute from FlutterSharedPreferences (default 21*60 if absent); compute next occurrence (always tomorrow because we just fired today's); if SDK >= S && !canScheduleExactAlarms() → setAndAllowWhileIdle; else setExactAndAllowWhileIdle.
    - BootReceiver.onReceive(context, intent):
      1. Action guard: `if (intent.action != Intent.ACTION_BOOT_COMPLETED && intent.action != Intent.ACTION_LOCKED_BOOT_COMPLETED) return` (V5 input validation).
      2. Read flutter.reminder_hour_minute from FlutterSharedPreferences (default 21*60 = 1260).
      3. Decompose h = hm / 60, m = hm % 60.
      4. Compute triggerMs via the same NotificationApiImpl.computeNextOccurrenceMs helper (or a private copy if visibility is an issue — prefer reuse via the companion object).
      5. Re-arm via setExactAndAllowWhileIdle (or setAndAllowWhileIdle fallback).
    - MainActivity changes:
      1. Add a private `companion object { @JvmStatic var weakInstance: java.lang.ref.WeakReference<MainActivity>? = null }` — populated in `onCreate(savedInstanceState: Bundle?)` via `weakInstance = WeakReference(this)`; cleared in `onDestroy()` via `weakInstance?.clear(); weakInstance = null`. This enables Plan 05-04's PermissionStatusApiImpl.requestPostNotifications to call ActivityCompat.requestPermissions(activity, ...).
      2. configureFlutterEngine: replace the existing NotImplementedError stub (lines 68-80) with `NotificationApi.setUp(flutterEngine.dartExecutor.binaryMessenger, NotificationApiImpl(applicationContext))`.
      3. Override `onNewIntent(intent: Intent)` AND check `intent` in onCreate for the same extra (cold-launch from notification): extract `deep_link_to` extra, allow-list check {"/checkin"} only, on match write `flutter.pending_deep_link = "/checkin"` to FlutterSharedPreferences (consumed by HomeScreen.initState in Plan 05-07). Reject (no-op) every other extra value — input-validation lock.
    - AndroidManifest.xml additions: append `<receiver android:name=".receiver.ReminderAlarmReceiver" android:exported="false" />` AND `<receiver android:name=".receiver.BootReceiver" android:exported="true" android:enabled="true"><intent-filter><action android:name="android.intent.action.BOOT_COMPLETED" /><action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" /></intent-filter></receiver>`. Place inside the existing <application> element after the PauseActivity declaration. NO permission additions (all three perms already declared in Phase 1).
    - strings.xml additions: `<string name="notification_channel_daily_checkin">Daily check-in</string>` and `<string name="notification_body_daily_checkin">How did today go?</string>`. NotificationCompat builder pulls from these via getString.
    - test/features/reminder/deep_link_navigation_test.dart unskip: tests verifying SharedPreferences-backed `pending_deep_link = /checkin` mechanism. Use mocktail to stub SharedPreferences via `SharedPreferences.setMockInitialValues({...})`. Tests: `test_pending_deep_link_routes_to_checkin` — set prefs.pending_deep_link=/checkin, pump HomeScreen (or a minimal harness that reads + routes), expect router.location to be /checkin. `test_deep_link_cleared_after_consumption` — after consuming, prefs.pending_deep_link is removed.
    - phase_5_invariants_test.dart: unskip the two previously-skipped tests — 'NOTF-05: BootReceiver declared with BOOT_COMPLETED intent-filter' (assert manifest contains both literals) and 'PLAY-Phase5: notification body must not contain entry names' (grep ReminderAlarmReceiver.kt + strings.xml for any pattern matching `getString(R.string.entry_name|app_name_dynamic)` — body MUST be static literal).
  </behavior>
  <action>
Implement Kotlin platform body in three new files + 1 modified file + manifest + strings.

(a) android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt — RESEARCH §5 lines 395-454 verbatim, adjusted to project package. Includes companion object with REMINDER_REQUEST_CODE = 5001 + `buildPendingIntent(ctx)` + `computeNextOccurrenceMs(h, m)`. try/catch wrap on both override methods returning Result.failure(NotificationApiError("EXACT_ALARM_DENIED")) on the Android 12+ canScheduleExactAlarms branch. Imports: android.app.AlarmManager, android.app.PendingIntent, android.content.Context, android.content.Intent, android.os.Build, java.util.Calendar, com.nottodo.not_to_do_list.receiver.ReminderAlarmReceiver, com.nottodo.not_to_do_list.platform.NotificationApi (Pigeon-generated abstract), com.nottodo.not_to_do_list.platform.NotificationApiError.

(b) android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/ReminderAlarmReceiver.kt — RESEARCH §5 lines 459-518 verbatim. Notification channel id = "daily_checkin"; ID = REMINDER_NOTIFICATION_ID = 5001. Builder uses `ctx.getString(R.string.notification_channel_daily_checkin)` for content title and `ctx.getString(R.string.notification_body_daily_checkin)` for body to satisfy the "no entry names in body" privacy lock. SmallIcon `R.mipmap.ic_launcher` (re-use existing icon — Phase 5 does not add new drawables; Plan 06 polish phase may upgrade).

(c) android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt — RESEARCH §5 lines 524-541 verbatim. Action guard at top.

(d) android/app/src/main/AndroidManifest.xml — append the two <receiver> declarations inside <application> block, after the existing PauseActivity / NotToDoAccessibilityService declarations. Use exact XML from interfaces section above.

(e) android/app/src/main/res/values/strings.xml — append the two <string> resources. If the file does not exist or lacks a <resources> wrapper, add the wrapper.

(f) android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt — surgical edits:
- Imports: add `import com.nottodo.not_to_do_list.platform.NotificationApiImpl` and `import java.lang.ref.WeakReference`.
- companion object {`@JvmStatic var weakInstance: WeakReference<MainActivity>? = null`} after class declaration line.
- onCreate: set `weakInstance = WeakReference(this)` after super.onCreate; ALSO check the launch intent for `deep_link_to` via the same parse helper used by onNewIntent.
- onDestroy override: `weakInstance?.clear(); weakInstance = null; super.onDestroy()`.
- onNewIntent override: validate `deep_link_to` extra against allow-list {"/checkin"}; on match write to FlutterSharedPreferences key `flutter.pending_deep_link` value "/checkin"; reject others. Call `super.onNewIntent(intent)`.
- configureFlutterEngine: replace the 13-line NotImplementedError stub (lines 68-80) with the single line `NotificationApi.setUp(flutterEngine.dartExecutor.binaryMessenger, NotificationApiImpl(applicationContext))`. Verify the new import is at top.

(g) test/features/reminder/deep_link_navigation_test.dart — unskip; write tests around the SharedPreferences-backed deep-link mechanism. Helper widget that reads `prefs.getString('pending_deep_link')` in `initState`, calls `context.go(value); prefs.remove('pending_deep_link')`. The test mocks SharedPreferences via `SharedPreferences.setMockInitialValues({'pending_deep_link': '/checkin'})` and asserts the GoRouter navigates to /checkin after pumpAndSettle.

(h) test/policy/phase_5_invariants_test.dart — flip the two skipped tests to real:
- 'NOTF-05: BootReceiver declared with BOOT_COMPLETED intent-filter' — File.readAsStringSync('android/app/src/main/AndroidManifest.xml') and assert it contains both 'android:name=".receiver.BootReceiver"' and 'android.intent.action.BOOT_COMPLETED'.
- 'PLAY-Phase5: notification body must not contain entry names' — File.readAsStringSync over android/.../receiver/ReminderAlarmReceiver.kt + strings.xml; assert no hardcoded entry-name patterns like `Instagram\|TikTok\|YouTube\|Reddit\|X (`; assert body content uses `R.string.notification_body_daily_checkin` (static resource) not dynamic concat. Use grep-style absence + presence checks.

Verify after each file change that `cd android && ./gradlew :app:compileDebugKotlin` succeeds before proceeding. Receiver order in manifest does not matter; placement matters only that they are inside <application>.

PLAY-02 lock: every new Kotlin file (NotificationApiImpl.kt, ReminderAlarmReceiver.kt, BootReceiver.kt, MainActivity.kt updates) MUST NOT contain `performAction(`, `performGlobalAction(`, `dispatchGesture(`. Verified by the 9th PLAY-02 invariant (which already greps android/.../platform/ + android/.../service/ + android/.../not_to_do_list/ root; this plan adds android/.../receiver/ — extend the invariant scope if needed, but receiver/ is sibling-tree and not yet in the grep — leave for Plan 05-09 to expand to 10 invariants).
  </action>
  <verify>
    <automated>pushd android ; ./gradlew :app:compileDebugKotlin ; ./gradlew :app:testDebugUnitTest --tests "*.ScheduleWindowTest" --tests "*.StreakDayAnchoringTest" ; popd ; flutter test test/policy/play_invariants_test.dart test/policy/phase_5_invariants_test.dart test/features/reminder/deep_link_navigation_test.dart ; flutter analyze ; flutter build apk --debug</automated>
  </verify>
  <acceptance_criteria>
    - `cd android && ./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL (all 4 new/modified Kotlin files compile).
    - `flutter build apk --debug` exits 0 (full app builds with the new manifest + Kotlin body).
    - `flutter test test/policy/play_invariants_test.dart` 9/9 invariants green.
    - `flutter test test/policy/phase_5_invariants_test.dart` ALL invariants pass (the 2 previously-skipped ones are now REAL and PASSING).
    - `flutter test test/features/reminder/deep_link_navigation_test.dart` exits 0 with both tests passing (no skip).
    - `flutter analyze` shows 0 errors / 0 warnings (warnings on generated/test code allowed).
    - `grep -E 'class NotificationApiImpl' android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt` returns 1 line.
    - `grep -E 'class ReminderAlarmReceiver' android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/ReminderAlarmReceiver.kt` returns 1 line.
    - `grep -E 'class BootReceiver' android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt` returns 1 line.
    - `grep -c 'setExactAndAllowWhileIdle' android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt` >= 1.
    - `grep -c 'setExactAndAllowWhileIdle' android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/ReminderAlarmReceiver.kt` >= 1 (re-arm).
    - `grep -c 'canScheduleExactAlarms' android/app/src/main/kotlin/com/nottodo/not_to_do_list/platform/NotificationApiImpl.kt` >= 1 (Android 12+ guard).
    - `grep -c 'ACTION_BOOT_COMPLETED' android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt` >= 1.
    - `grep -c 'BootReceiver' android/app/src/main/AndroidManifest.xml` >= 1.
    - `grep -c 'BOOT_COMPLETED' android/app/src/main/AndroidManifest.xml` >= 1.
    - `grep -c 'ReminderAlarmReceiver' android/app/src/main/AndroidManifest.xml` >= 1.
    - `grep -c 'android:exported="false"' android/app/src/main/AndroidManifest.xml` >= 1 (ReminderAlarmReceiver — defence in depth).
    - `grep -E 'android:name="\.receiver\.BootReceiver"\s+android:exported="true"' android/app/src/main/AndroidManifest.xml` returns 1 line (BootReceiver must be exported to receive BOOT_COMPLETED).
    - `grep -c 'USE_EXACT_ALARM' android/app/src/main/AndroidManifest.xml` == 0 (forbidden token per CLAUDE.md tech stack).
    - `grep -c 'NotificationApi\.setUp.*NotificationApiImpl' android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt` >= 1 (Phase 1 stub replaced).
    - `grep -c 'NotImplementedError' android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt` == 0 (stub removed).
    - `grep -c 'WeakReference' android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt` >= 1 (weakInstance for permission request flow).
    - `grep -c 'deep_link_to' android/app/src/main/kotlin/com/nottodo/not_to_do_list/MainActivity.kt` >= 1 (onNewIntent parses).
    - `grep -E 'performAction\(|performGlobalAction\(|dispatchGesture\(' android/app/src/main/kotlin/com/nottodo/not_to_do_list/ -r | grep -v '^[^:]*:[[:space:]]*//' | wc -l` == 0 (PLAY-02 lock across new files).
    - `grep -c 'notification_channel_daily_checkin' android/app/src/main/res/values/strings.xml` >= 1.
    - `grep -c 'notification_body_daily_checkin' android/app/src/main/res/values/strings.xml` >= 1.
  </acceptance_criteria>
  <done>NotificationApiImpl ships AlarmManager.setExactAndAllowWhileIdle; ReminderAlarmReceiver fires daily_checkin + self-re-arms; BootReceiver re-arms after reboot; MainActivity wires Pigeon + parses deep_link_to; manifest declares both receivers with correct exported flags; phase_5_invariants_test BootReceiver + notification-privacy invariants flip to GREEN; APK builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External BroadcastReceiver intent ↔ BootReceiver | OS-sent BOOT_COMPLETED is trusted; userland apps without system perms cannot spoof; receiver action guard provides defence in depth |
| External BroadcastReceiver intent ↔ ReminderAlarmReceiver | exported="false" + Intent.setPackage(ctx.packageName) on PendingIntent prevents external triggers |
| MainActivity launching Intent ↔ deep_link_to extra | T-2 input validation — allow-list {"/checkin"} only; reject all others |
| Cross-process FlutterSharedPreferences ↔ BootReceiver | Same SharedPreferences file the Flutter plugin writes; cross-process read of `flutter.reminder_hour_minute` (RESEARCH §10 R-9 highest-risk landmine) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Spoofing | 3rd-party app sends Intent to our ReminderAlarmReceiver to fire fake notifications | mitigate | android:exported="false" on ReminderAlarmReceiver + PendingIntent uses explicit class + Intent.setPackage(ctx.packageName) |
| T-05-17 | Spoofing | 3rd-party app sends fake ACTION_BOOT_COMPLETED to BootReceiver | mitigate | Platform restricts ACTION_BOOT_COMPLETED to system sender — userland apps cannot send it; receiver action guard rejects any other action |
| T-05-18 | Tampering | Deep-link extra `deep_link_to` is set to a route that bypasses onboarding redirect (e.g., "/list/delete") | mitigate | MainActivity allow-list contains only "/checkin"; all other values rejected (no-op write to pending_deep_link); validated by V5 input-validation lock + future tests |
| T-05-19 | Information disclosure | Notification body leaks entry names to lock-screen observers | mitigate | D-10 lock + strings.xml resource pull — body is the static "How did today go?"; phase_5_invariants_test.dart asserts no dynamic entry-name patterns |
| T-05-20 | Denial of service | canScheduleExactAlarms revoked → SecurityException on setExactAndAllowWhileIdle crashes receiver mid-flight | mitigate | RESEARCH §10 R-2 — both ReminderAlarmReceiver.onReceive and BootReceiver.onReceive branch on canScheduleExactAlarms() before re-arming; fall back to setAndAllowWhileIdle; Pigeon-side scheduleDailyReminder returns Result.failure("EXACT_ALARM_DENIED") which Dart maps to NOTF-07 banner |
| T-05-21 | Elevation of privilege | PendingIntent flag mutability allows hijack | mitigate | FLAG_IMMUTABLE | FLAG_UPDATE_CURRENT on all PendingIntents (RESEARCH §5 explicit) — required since Android 12 |
| T-05-22 | Tampering | BootReceiver reads wrong FlutterSharedPreferences file path → reminder dies after reboot | mitigate | Use exact filename "FlutterSharedPreferences" and key prefix "flutter." per shared_preferences plugin convention; verified by REL-05 manual gate (Plan 05-09); assumption A1 in RESEARCH §13 |
</threat_model>

<verification>
- All Kotlin compiles; APK builds.
- 9 PLAY-02 + 5 Phase-5 invariants all PASS (the 2 previously-skipped Phase 5 invariants flip to REAL and pass).
- Deep-link navigation test passes; existing tests stay green.
- No NotImplementedError remains for NotificationApi.
</verification>

<success_criteria>
- Daily reminder Kotlin path is end-to-end implementable (Dart → Pigeon → AlarmManager → Receiver → Notification → MainActivity deep-link → /checkin).
- BootReceiver re-arms across reboot.
- REL-05 manual gate (Plan 05-09) can now exercise NOTF-02..05 on real device.
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-05-SUMMARY.md`
</output>
