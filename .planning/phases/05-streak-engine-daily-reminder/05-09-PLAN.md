---
phase: 05-streak-engine-daily-reminder
plan: 09
type: execute
wave: 7
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08]
files_modified:
  - test/policy/play_invariants_test.dart
  - test/policy/phase_5_invariants_test.dart
  - .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - STRK-01
  - STRK-02
  - STRK-03
  - STRK-04
  - STRK-05
  - STRK-06
  - STRK-07
  - STRK-08
  - STRK-09
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04
  - NOTF-05
  - NOTF-06
  - NOTF-07
  - REL-05
must_haves:
  truths:
    - "play_invariants_test.dart 9th PLAY-02 invariant scope is expanded to include android/.../receiver/ (Phase 5 BroadcastReceivers) — Plan 04-08 added android/.../service/ + android/.../not_to_do_list/ root; Phase 5 mirrors the pattern by adding the receiver directory. 10th invariant overall."
    - "phase_5_invariants_test.dart confirms — REAL non-skipped — every Phase 5 invariant: no FCM/firebase, no USE_EXACT_ALARM, no PeriodicWorkRequest in android/.../streak/, BootReceiver declared with BOOT_COMPLETED + LOCKED_BOOT_COMPLETED intent-filter, notification body has no entry-name dynamic concat."
    - "05-VERIFICATION.md status flipped from draft to one of: complete (REL-05 PASS) OR pending_overnight_run (test scheduled; documented evidence pending). Frontmatter fields populated with device class/model/Android version/idle duration/alarm-fire delta/BOOT_COMPLETED re-arm outcome/pass-date."
    - "Pre-overnight verification snapshot captured: flutter test (full suite) exits 0 OR documents only pre-existing flakes; flutter analyze 0 errors; flutter build apk --debug succeeds; cd android && ./gradlew :app:testDebugUnitTest BUILD SUCCESSFUL."
    - "REL-05 manual gate is a [BLOCKING] human-action checkpoint task — phase cannot reach VERIFICATION.md status=complete without explicit user sign-off on the 9-step protocol (mirrors Phase 4 REL-04 protocol)."
    - "REQUIREMENTS.md updated — every STRK-01..09 + NOTF-01..07 row flipped to Complete (with plan refs); REL-05 flipped to Complete (if REL-05 PASS) or pending_overnight_run + documented timeline."
    - "ROADMAP.md Phase 5 row updated — 9/9 plans listed [x], Complete date set."
    - "STATE.md updated — completed_phases 4 → 5; completed_plans 29 → 38; v1 requirements complete += 17 (16 Phase 5 reqs + REL-05) → 57/68; Active Todos updated to Phase 6 next; Performance Metrics table appended with Phase 5 plan rows; Last Session entry written; Next Session reads /gsd-plan-phase 6."
  artifacts:
    - path: ".planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md"
      provides: "Final REL-05 sign-off with device record, evidence, and frontmatter status flip"
      contains: "rel_05_status"
    - path: "test/policy/play_invariants_test.dart"
      provides: "10th PLAY-02 invariant (Phase 5 expanded scope to android/.../receiver/)"
      contains: "receiver"
    - path: ".planning/REQUIREMENTS.md"
      provides: "16 STRK/NOTF rows + REL-05 row flipped to Complete (or pending_overnight_run)"
      contains: "Complete"
    - path: ".planning/STATE.md"
      provides: "Phase 5 closed; Phase 6 next"
      contains: "Phase 5"
  key_links:
    - from: "test/policy/play_invariants_test.dart"
      to: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/"
      via: "Cross-tree absence-grep scope expansion (Phase 4 pattern — Plan 04-08 Task 2 model)"
      pattern: "receiver"
    - from: ".planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md"
      to: "real Samsung or Xiaomi device — REL-05 manual gate (CD-equivalent for Phase 5)"
      via: "9-step copy-pasteable protocol mirroring REL-04"
      pattern: "REL-05"
---

# Plan 05-09 — Wave 7: Phase exit gate — PLAY-02 invariant expansion + REL-05 OEM-survival overnight gate + bookkeeping

<objective>
Close Phase 5. Three concerns:

1. **PLAY-02 invariant expansion to 10.** Phase 5 added a new Kotlin sub-tree `android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/` (BroadcastReceivers). Phase 4 set the pattern (Plan 04-08 Task 2) of expanding the cross-tree absence-grep invariant scope with each phase's new surfaces. Plan 05-09 adds a 10th invariant covering the receiver directory.

2. **REL-05 OEM-survival overnight gate (BLOCKING manual checkpoint).** Mirror Phase 4 REL-04: real Samsung Galaxy S20 Ultra (already proven) or Xiaomi device, ≥8h unplugged Doze, alarm fires within 5 min of scheduled time, BOOT_COMPLETED re-arms after reboot, /checkin opens on notification tap. This is the only blocking human-action task in Phase 5. The plan is `autonomous: false` because of this gate.

3. **Bookkeeping flips.** 05-VERIFICATION.md status, REQUIREMENTS.md 17 row flips (16 STRK/NOTF + REL-05), ROADMAP.md Phase 5 row, STATE.md completed_phases / completed_plans / v1 requirements complete / Active Todos / Performance Metrics / Last Session / Next Session. Mirror Plan 04-08 bookkeeping exactly.

Purpose: lock the phase exit and unblock Phase 6 planning.
Output: 2 modified test files + 5 bookkeeping artifact updates + REL-05 sign-off (manual).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md
@.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md
@.planning/phases/04-pause-ux-the-wedge/04-08-PLAN.md
@.planning/phases/04-pause-ux-the-wedge/04-08-SUMMARY.md
@test/policy/play_invariants_test.dart
@test/policy/phase_4_invariants_test.dart
@test/policy/phase_5_invariants_test.dart
</context>

<interfaces>
<!-- Bookkeeping contracts. Phase 4 Plan 04-08 is the exact analog. -->

From .planning/phases/04-pause-ux-the-wedge/04-08-PLAN.md (analog for Phase 5 exit gate):
- 9th PLAY-02 invariant expansion (Phase 4) — covers android/.../service/ + android/.../not_to_do_list/ root
- REL-04 protocol documented (9-step procedure)
- bookkeeping order: VERIFICATION → REQUIREMENTS → ROADMAP → STATE

From .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md (analog for 05-VERIFICATION.md final shape):
- frontmatter fields: phase, status (draft → pending_overnight_run → complete), nyquist_compliant, wave_*_complete, rel_NN_status, rel_NN_device_class, rel_NN_device_model, rel_NN_*_metrics, rel_NN_pass_date
- Device Record table to be filled
- Pre-overnight verification snapshot table

REQUIREMENTS.md mapping in this plan: 16 STRK/NOTF rows + REL-05 row → Complete with plan refs
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand PLAY-02 cross-tree invariant to 10 (add android/.../receiver/ scope) + verify all phase_5_invariants are real-and-green</name>
  <files>test/policy/play_invariants_test.dart, test/policy/phase_5_invariants_test.dart</files>
  <read_first>
    test/policy/play_invariants_test.dart (existing 9 invariants — read every test to identify the 9th PLAY-02 expansion test added by Plan 04-08; mirror that pattern for Phase 5),
    test/policy/phase_4_invariants_test.dart (Phase 4 invariant style — Plan 05-01 mirrored this for phase_5_invariants_test.dart),
    test/policy/phase_5_invariants_test.dart (Plan 05-01 stubs + Plan 05-05 unskipped — confirm all 5 invariants are now REAL and GREEN: no FCM/firebase, no USE_EXACT_ALARM, no PeriodicWorkRequest in android/.../streak/, BootReceiver declared with BOOT_COMPLETED, notification body privacy),
    .planning/phases/04-pause-ux-the-wedge/04-08-PLAN.md (Task 2 — Plan 04-08 invariant expansion pattern verbatim),
    .planning/phases/04-pause-ux-the-wedge/04-08-SUMMARY.md (records "9 → 10 invariants" pattern for Phase 5 to mirror as "9 → 10 invariants")
  </read_first>
  <action>
Mirror Plan 04-08 Task 2's pattern of cross-tree invariant scope expansion.

(a) test/policy/play_invariants_test.dart — add a new top-level test inside the existing test group (after the 9th invariant added by Plan 04-08):
```
test('PLAY-02 Phase 5 expanded scope: no autonomous-action calls in android/.../receiver/', () {
  final dir = Directory('android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver');
  if (!dir.existsSync()) return; // phase order: skip if Phase 5 not yet implemented
  for (final entity in dir.listSync(recursive: true).whereType<File>()) {
    if (!entity.path.endsWith('.kt')) continue;
    final src = entity.readAsStringSync();
    // grep-v hygiene: strip comment lines before counting (Nyquist absence-grep rule)
    final code = src.split('\n').where((l) => !l.trimLeft().startsWith('//')).join('\n');
    expect(code.contains('performAction('), isFalse, reason: 'PLAY-02: receivers must not call performAction');
    expect(code.contains('performGlobalAction('), isFalse, reason: 'PLAY-02: receivers must not call performGlobalAction');
    expect(code.contains('dispatchGesture('), isFalse, reason: 'PLAY-02: receivers must not call dispatchGesture');
  }
});
```
Document in a leading comment: "10th invariant: Phase 5 expanded scope to android/.../receiver/. Mirrors Plan 04-08 Task 2 pattern (Phase 4 expanded to android/.../service/ + android/.../not_to_do_list/ root)."

(b) test/policy/phase_5_invariants_test.dart — verify (read-only check + grep) that every invariant is non-skipped and passing:
- 'NOTF-05: BootReceiver declared with BOOT_COMPLETED intent-filter' — real, passing.
- 'PLAY-Phase5: no USE_EXACT_ALARM permission' — real, passing.
- 'no FCM / firebase_messaging imports' — real, passing.
- 'STRK-05: no WorkManager periodic for streak rollover' — real, passing.
- 'PLAY-Phase5: notification body must not contain entry names' — real, passing (Plan 05-05 flipped this).

If any test is still `skip:`, flip to real here using the same body Plan 05-01/05-05 sketched. Otherwise, no edit needed beyond a documentation comment confirming "Phase 5 invariants finalized 2026-MM-DD by Plan 05-09".
  </action>
  <verify>
    <automated>flutter test test/policy/play_invariants_test.dart test/policy/phase_5_invariants_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/policy/play_invariants_test.dart` 10/10 invariants green (was 9; added 1 Phase 5 scope expansion).
    - `flutter test test/policy/phase_5_invariants_test.dart` ALL invariants REAL (no skip) and PASSING.
    - `grep -c "Phase 5 expanded scope" test/policy/play_invariants_test.dart` >= 1.
    - `grep -c "receiver" test/policy/play_invariants_test.dart` >= 1 (in the new test body).
    - `grep -c 'skip:' test/policy/phase_5_invariants_test.dart` == 0 (all invariants now real).
  </acceptance_criteria>
  <done>10th PLAY-02 invariant lands covering android/.../receiver/; all phase_5 invariants are real-and-green; pattern matches Plan 04-08 Task 2 exactly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Capture pre-overnight verification snapshot in 05-VERIFICATION.md (full test suite + analyze + APK build + JVM tests + invariant counts)</name>
  <files>.planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md</files>
  <read_first>
    .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md (lines 152-164 — Pre-Overnight Verification Snapshot table format; mirror exactly for Phase 5),
    .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md (Plan 05-01 skeleton — populate the snapshot section with REAL command outputs from this run),
    test/policy/play_invariants_test.dart (count invariants — should now be 10),
    test/policy/phase_5_invariants_test.dart (count invariants — should be 5+ real)
  </read_first>
  <action>
Run the full pre-overnight verification suite and record results in 05-VERIFICATION.md "Pre-Overnight Verification Snapshot" section (create if missing — mirror 04-VERIFICATION.md lines 152-164 format).

Commands to run + record:
- `flutter test` (full suite) → record pass/skip count and note any pre-existing flakes (dashboard_render_test.dart perf timeout is acceptable per Plan 04-08 precedent).
- `flutter analyze` → record errors/warnings/infos (target: 0 errors / 0 warnings).
- `flutter build apk --debug` → record path to generated APK (target: build/app/outputs/flutter-apk/app-debug.apk).
- `flutter test test/policy/play_invariants_test.dart` → 10/10 invariants green.
- `flutter test test/policy/phase_5_invariants_test.dart` → all real invariants green.
- `cd android && ./gradlew :app:testDebugUnitTest --tests "*.ScheduleWindowTest" --tests "*.StreakDayAnchoringTest"` → BUILD SUCCESSFUL.

If any command fails: STOP, file the failure as a blocker, do NOT proceed to Task 3 (manual gate). Task 3 requires a green snapshot.

Update 05-VERIFICATION.md status frontmatter: keep `status: draft` until Task 3 sign-off completes. Update wave_*_complete fields: wave_0_complete: true, wave_1_complete: true, wave_2_complete: true, wave_3_complete: true, wave_4_complete: false (until Task 3 + Task 4).
  </action>
  <verify>
    <automated>flutter test ; flutter analyze ; flutter build apk --debug ; pushd android ; ./gradlew :app:testDebugUnitTest --tests "*.ScheduleWindowTest" --tests "*.StreakDayAnchoringTest" ; popd</automated>
  </verify>
  <acceptance_criteria>
    - All 5 commands in the snapshot section run + their pass/fail recorded in 05-VERIFICATION.md.
    - flutter test pass count documented (only acceptable failure: pre-existing dashboard_render_test.dart perf flake per Plan 04-08 precedent).
    - flutter analyze 0 errors / 0 warnings.
    - APK builds.
    - JVM ScheduleWindowTest + StreakDayAnchoringTest BUILD SUCCESSFUL.
    - 05-VERIFICATION.md "Pre-Overnight Verification Snapshot" section populated with real evidence (no TBD/blank cells).
  </acceptance_criteria>
  <done>Software gates green; ready for the REL-05 manual overnight run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] REL-05 OEM-survival overnight gate — execute the 9-step protocol on real Samsung or Xiaomi device</name>
  <files>.planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md</files>
  <read_first>
    .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md (the REL-05 9-step protocol Plan 05-01 wrote — mirror Phase 4 REL-04),
    .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md (REL-04 PASS evidence + Run #1/Run #2 record format),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §5 + §10 R-2 + R-3 (OEM kill behavior to look for; canScheduleExactAlarms revoke; Xiaomi Autostart)
  </read_first>
  <what-built>
    Phase 5 software (Plans 05-01..05-08) is committed and the pre-overnight verification snapshot in Task 2 is green. The Phase 5 daily-reminder + streak engine is ready for the OEM-survival overnight gate that mirrors Phase 4 REL-04 (which PASSED on Samsung Galaxy S20 Ultra 5G on 2026-05-21 after ≥71 h unplugged Doze).
  </what-built>
  <how-to-verify>
This is the only manual-action task in Phase 5 (the rest of the human-required actions like granting Notifications + setting reminder are part of the test protocol itself).

REL-05 9-step protocol (Plan 05-01 wrote into 05-VERIFICATION.md — re-stated here for execution):

1. Install latest debug APK:
   `flutter build apk --debug && adb install -r build/app/outputs/flutter-apk/app-debug.apk`

2. Open the app, add one app entry to the not-to-do list (triggers the NOTF-06 earned prompt). Grant POST_NOTIFICATIONS in the system dialog. Complete onboarding if not already done.

3. Open the AppBar overflow → "Daily reminder" (or `/settings/reminder` directly). Set the reminder time to T+2 minutes from now (e.g., if it's 17:30, set 17:32). Verify the ListTile subtitle updates to show the new time in `DateFormat.jm()` format.

4. Force the device into idle Doze:
   `adb shell dumpsys deviceidle force-idle`

5. Unplug USB, lock the screen, and wait the 2 minutes. Observe whether the notification fires within 5 minutes of the scheduled time (NOTF-02 + NOTF-04). Record the actual fire delay in ms or seconds.

6. While device is locked, swipe down to see the notification:
   - Title MUST read exactly: "Daily check-in"
   - Body MUST read exactly: "How did today go?"
   - NO entry names appear in the notification (privacy lock D-10).

7. Tap the notification. The device unlocks (if needed) and the app launches showing the `/checkin` screen with the pending entry. (NOTF-03 deep-link.) Record success/failure.

8. Reboot the device:
   `adb reboot` (or hold power button).
   After reboot, observe whether the daily reminder is still scheduled. Use `adb shell dumpsys alarm | grep com.nottodo` to confirm — there MUST be at least one alarm entry for our package after reboot (NOTF-05 BOOT_COMPLETED re-arm). On Xiaomi: verify the app is in the Autostart list (RESEARCH §10 R-3 — Xiaomi-specific concern). Record outcome.

9. Optionally re-run steps 4-7 next morning after overnight idle (≥8 h unplugged) to verify the second alarm fires under deep Doze (mirrors Phase 4 REL-04 ≥71 h conditions).

**Pass criterion (REL-05):**
- Notification fires within 5 minutes of scheduled time (NOTF-04).
- Tap opens /checkin (NOTF-03).
- After reboot, the next reminder still fires the following day (NOTF-05).
- Notification body contains zero entry names (D-10 privacy lock).

**Fill `.planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md` REL-05 Device Record table** with:
- Device manufacturer + model (Samsung Galaxy S20 Ultra 5G recommended — already proven on Phase 4 REL-04; Xiaomi accepted)
- Android version + OEM skin version (e.g., Android 13 / OneUI 5.1)
- Idle duration (hours unplugged)
- Alarm fire delta (minutes from scheduled time)
- BOOT_COMPLETED re-arm outcome (PASS / FAIL)
- Pass date

**Update 05-VERIFICATION.md frontmatter** on PASS:
- `status: complete`
- `rel_05_status: pass`
- `wave_4_complete: true`
- `rel_05_pass_date: YYYY-MM-DD`

On FAIL: file the failure as a blocker in STATE.md Risks Being Tracked, file `rel_05_status: fail` + escalation path (try second device / file CD-equivalent disposition); pause Phase 5 close until resolved.
  </how-to-verify>
  <resume-signal>
Type "REL-05 PASS" to confirm the 9-step protocol completed successfully with all four pass criteria green (notification fired in time, /checkin opened on tap, BOOT_COMPLETED re-arm fired next day, no entry names in body) AND 05-VERIFICATION.md frontmatter is flipped to status=complete + rel_05_status=pass. Then proceed to Task 4 (bookkeeping flips).

Alternatively type "REL-05 PENDING_OVERNIGHT_RUN" if you've kicked off the overnight test but can't fully complete the 8h+ wait now — Plan 05-09 will close in pending state (matching Phase 4 Plan 04-08's interim state); commit the pre-overnight snapshot + 05-VERIFICATION.md status=pending_overnight_run; STATE.md Active Todo will track REL-05 follow-through with target date.

Alternatively type "REL-05 FAIL: <reason>" to record the failure and pause Phase 5 close.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Bookkeeping flips — REQUIREMENTS.md, ROADMAP.md, STATE.md, 05-VERIFICATION.md final</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md</files>
  <read_first>
    .planning/phases/04-pause-ux-the-wedge/04-08-PLAN.md (Task 6 — bookkeeping flips exact pattern),
    .planning/phases/04-pause-ux-the-wedge/04-08-SUMMARY.md (records the bookkeeping order),
    .planning/REQUIREMENTS.md (the 17 rows STRK-01..09 + NOTF-01..07 + REL-05 — current state Pending; flip to Complete with plan refs),
    .planning/ROADMAP.md (Phase 5 section — update plan list with [x], progress table, completion date),
    .planning/STATE.md (frontmatter + Current Position + Active Todos + Performance Metrics + Session Continuity)
  </read_first>
  <action>
Sequenced bookkeeping flips, matching Plan 04-08 Task 6 pattern exactly.

(a) .planning/REQUIREMENTS.md — locate each row in the traceability table:
- STRK-01..09: change `Pending` → `Complete (Plan 05-NN — <short note>)` per the plan that primarily addressed each:
  - STRK-01: Plan 05-02 (DAO upsert + getCurrent/getLongest) + Plan 05-07 (badge)
  - STRK-02: Plan 05-03 (rollover threshold logic)
  - STRK-03: Plan 05-02 (DAO unique key) + Plan 05-06 (checkin screen)
  - STRK-04: Plan 05-03 (source resolution matrix)
  - STRK-05: Plan 05-03 (soft-cache) + Plan 05-07 (lazy-on-resume trigger)
  - STRK-06: Plan 05-03 (clock-tamper detection)
  - STRK-07: Plan 05-07 (badge + history section)
  - STRK-08: Plan 05-03 (DST 23h/25h tests)
  - STRK-09: Plan 05-03 (schedule anchoring) + Plan 05-06 (checkin filter)
- NOTF-01..07: change `Pending` → `Complete (Plan 05-NN — <short note>)`:
  - NOTF-01: Plan 05-08 (reminder settings + showTimePicker)
  - NOTF-02: Plan 05-05 (AlarmManager.setExactAndAllowWhileIdle) — software gate; REL-05 real-device PASS adds runtime evidence
  - NOTF-03: Plan 05-05 (MainActivity.onNewIntent + deep_link_to) + Plan 05-06 (/checkin destination)
  - NOTF-04: Plan 05-05 (setExactAndAllowWhileIdle Doze guarantee) — verified by REL-05
  - NOTF-05: Plan 05-05 (BootReceiver) — verified by REL-05 reboot step
  - NOTF-06: Plan 05-08 (earned prompt + repo hook)
  - NOTF-07: Plan 05-07 (reminder-off banner three-state)
- REL-05: change `Pending` → `Complete YYYY-MM-DD on <device>` if REL-05 PASS; else `pending_overnight_run` (matching Plan 04-08's interim disposition).
- Update Phase 5 row count in the Coverage Summary table at the top (Phase 5 = 16 reqs becomes "16 Complete / 0 Pending").
- v1 requirements complete: previously 40 (Phase 4 PASS). Phase 5 adds 16 STRK/NOTF + 1 REL-05 = 17 → 40 + 17 = 57. Update the count in the "Coverage" footer block + "v1 requirements complete: 57".

(b) .planning/ROADMAP.md Phase 5 section:
- "**Plans**: 9 plans" (currently TBD).
- List all 9 plan files with [x] checkboxes (Plan 05-01 through 05-09 with one-line objective per Phase 4 Plan 04-08 style).
- Progress table: row 5 → "9/9", "✅ Complete", "YYYY-MM-DD" (or "Software-complete; REL-05 pending" if Task 3 returned pending).
- Coverage Summary table: Phase 5 row already shows STRK 9 + NOTF 7 = 16 — verify, no change needed.

(c) .planning/STATE.md:
- Frontmatter: `last_updated: YYYY-MM-DDTHH:MM:SSZ` (current time), `status: Phase 5 fully closed; all gates green` (or `Phase 5 software-complete; REL-05 pending` if Task 3 returned pending).
- progress.completed_phases: 4 → 5 (or stay 4 if REL-05 pending).
- progress.completed_plans: 29 → 38 (Phase 5 added 9 plans).
- progress.percent: recalc 38/(38 + Phase 6 plan count) — when Phase 6 plan count is unknown, set to a temp percent based on completed v1 requirements ratio (57/68 ≈ 84%) — match Plan 04-08's progress block style.
- Current Position: "Phase: 05 (Streak Engine & Daily Reminder) — COMPLETE YYYY-MM-DD" + "Next: Phase 6 (Polish & Play Store Submission) — `/gsd-plan-phase 6`".
- OEM-survival overnight tests passed: 1/3 → 2/3 (Samsung S20 Ultra REL-05 PASS) if PASS, else stay 1/3.
- Phase 5 Plan Inventory section: list all 9 plans with [x] + one-line objective (mirror Phase 4 Plan Inventory format).
- v1 requirements complete: 40 → 57.
- Key Decisions: append rows for Phase 5 key choices (e.g., "Phase 5: hand-rolled Pigeon NotificationApi, no flutter_local_notifications dep (RESEARCH §3 verdict)" / "Phase 5: lazy-on-resume rollover, no WorkManager periodic (STRK-05 lock)" / "Phase 5: notification body privacy — no entry names (D-10)" / "Phase 5: PLAY-02 invariant 9 → 10 (added android/.../receiver/ scope)").
- Performance Metrics Per Plan table: append 9 Phase 5 rows (P01..P09 with — duration columns filled by executor on actual run; Plan 04-08 left these — keep the same style).
- Active Todos: replace Phase 5 entry with "Phase 6 (Polish & Play Store Submission) — `/gsd-plan-phase 6`. Phase 5 fully closed; no upstream blockers" (if PASS) OR "REL-05 follow-through: <target date>" + Phase 6 (if pending).
- Closed Todos: append "✅ REL-05 overnight run on <device>: PASS YYYY-MM-DD." with one-line evidence.
- Session Continuity: Last Session entry "YYYY-MM-DD: Phase 5 complete. All 9 plans landed. Plan 05-NN evidence..."; Next Session: "Run /gsd-plan-phase 6."

(d) .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md — confirm Task 3 already flipped frontmatter; verify final state:
- On REL-05 PASS: `status: complete`, `rel_05_status: pass`, `wave_4_complete: true`, `rel_05_pass_date: YYYY-MM-DD`, `gsd_verifier_status: approved` (parallel to Phase 4).
- On REL-05 pending_overnight_run: `status: software_complete`, `rel_05_status: pending_overnight_run`, `wave_4_complete: false`, target date recorded.

Run the full validation suite once more to verify nothing regressed:
- `flutter test`
- `flutter analyze`
- `flutter build apk --debug`
- `cd android && ./gradlew :app:testDebugUnitTest`

Commit each bookkeeping file as a single commit with message `docs(phase-05): close Phase 5 — bookkeeping flips` (or `docs(phase-05): software-complete + REL-05 pending` interim).
  </action>
  <verify>
    <automated>flutter test ; flutter analyze ; grep -c '\[x\]' .planning/ROADMAP.md ; grep -c 'STRK-09 | Phase 5 | Complete' .planning/REQUIREMENTS.md ; grep -c 'Phase 5' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` rows for STRK-01..09 + NOTF-01..07 + REL-05 all show `Complete` (or `Complete` + 1 row `pending_overnight_run` for REL-05 if Task 3 returned pending).
    - `.planning/ROADMAP.md` Phase 5 row shows `9/9` and `Complete` (or `Software-complete; REL-05 pending`).
    - `.planning/STATE.md` `completed_phases` = 5 (PASS) or stays 4 (pending) — match Task 3 outcome.
    - `.planning/STATE.md` `completed_plans` = 38.
    - `.planning/STATE.md` v1 requirements complete = 57.
    - `.planning/STATE.md` Phase 5 Plan Inventory section lists 9 plans with `[x]`.
    - `.planning/STATE.md` Next Session reads `/gsd-plan-phase 6`.
    - `.planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md` frontmatter `status` matches Task 3 outcome.
    - `flutter test` still exits 0 / known-flake-only.
    - `flutter analyze` 0 errors / 0 warnings.
    - `flutter build apk --debug` succeeds.
  </acceptance_criteria>
  <done>Phase 5 fully closed (or interim software-complete) with full bookkeeping; Phase 6 ready to be planned with `/gsd-plan-phase 6`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cross-tree PLAY-02 invariant ↔ Future Kotlin additions | Receiver scope added; future phases extend per-phase per Plan 04-08 pattern |
| REL-05 manual gate ↔ real device behavior | Human-verified OEM-survival; only path to v1 trust |
| Bookkeeping artifacts ↔ Phase 6 planner | STATE.md is the input to /gsd-plan-phase 6; bookkeeping must be accurate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-36 | Tampering | Future receiver added under different package path bypasses the 10th PLAY-02 invariant | mitigate | Invariant directory scope is `android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver` — narrow but extensible per future phases (mirror Plan 04-08 expansion pattern) |
| T-05-37 | Repudiation | REL-05 PASS recorded without device evidence | mitigate | Task 3 requires Device Record table populated with device class/model/Android version/idle duration/fire delta — explicit fields, no soft-pass |
| T-05-38 | Information disclosure | STATE.md commits include user PII (device-specific identifiers) | accept | Phase 4 precedent — device model + Android version are acceptable; no IMEI, no serial; user's device is the same identity already in STATE.md from Phase 4 REL-04 PASS |
| T-05-39 | Denial of service | REL-05 FAIL blocks Phase 6 indefinitely | mitigate | Task 3 alternative resume signals include "REL-05 PENDING_OVERNIGHT_RUN" (matches Plan 04-08 interim model) and "REL-05 FAIL" with documented escalation path; Phase 6 can begin software work in parallel per Plan 04-08 precedent |
</threat_model>

<verification>
- 10th PLAY-02 invariant lands; all phase_5 invariants real-and-green.
- Pre-overnight snapshot captured with green commands.
- REL-05 manual gate executed with explicit human sign-off (PASS / PENDING / FAIL).
- Bookkeeping flips land per Plan 04-08 model.
</verification>

<success_criteria>
- Phase 5 closed (or interim software-complete with REL-05 follow-through tracked).
- Every Phase 5 REQ-ID flipped to Complete (or REL-05 pending with timeline).
- STATE.md Next Session reads `/gsd-plan-phase 6`.
- No PLAY invariant regression; 10/10 PLAY-02 + 5/5 phase_5 invariants green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-09-SUMMARY.md`
</output>
