---
phase: 05-streak-engine-daily-reminder
plan: 06
type: execute
wave: 5
depends_on: [05-02, 05-03, 05-05]
files_modified:
  - lib/features/checkin/pages/checkin_screen.dart
  - lib/features/checkin/providers/checkin_providers.dart
  - lib/features/checkin/controllers/checkin_controller.dart
  - lib/core/router/app_router.dart
  - test/features/checkin/checkin_screen_test.dart
autonomous: true
requirements:
  - STRK-03
  - NOTF-03
must_haves:
  truths:
    - "GoRoute(path: '/checkin', builder: (_, __) => const CheckinScreen()) is appended to app_router.dart after '/dashboard' — onboarding redirect at lines 23-33 is preserved byte-for-byte (T-2-10 lock from Phase 4)."
    - "CheckinScreen renders an AppBar titled 'Daily check-in' and a body listing all entries returned by pendingCheckinsTodayProvider, plus already-answered entries (shown locked) per D-01 idempotency."
    - "Each entry row contains a leading icon (AppIcon for apps, Icons.spa_outlined for habits at size 32), entry displayName (bodyMedium, maxLines:1, ellipsis), and a SegmentedButton<bool> with two segments {true: 'Yes', false: 'No'} — UI-SPEC §Screen 1 lines 156-166."
    - "Bottom of screen has a FilledButton with minimumSize Size.fromHeight(48); label 'Save check-in' when at least one entry is unanswered AND at least one is answered, 'All done' when every entry is already pre-answered, and onPressed: null (disabled) when no entry has been answered."
    - "Save handler writes ALL answered entries in ONE Drift transaction via `appDatabase.transaction(() async { ... })`; unanswered entries are NOT written (left pending until midnight per D-04). After write: invokes `streakRolloverServiceProvider.rollover()` to refresh badges, then `context.go('/')`."
    - "Idempotent re-entry: when /checkin is re-entered after partial submit, already-answered entries show their stored answer in the SegmentedButton (selected: {answer.avoided}) with onSelectionChanged: null (disabled)."
    - "Empty state (when no pending entries AND no recently-answered entries to display): centered Icon(Icons.check_circle_outline, size:48, color: colorScheme.primary) + Text('You\\'re all caught up', titleMedium) + Text('Check back tomorrow.', bodyMedium, color: onSurfaceVariant). UI-SPEC §Screen 1 lines 168-176."
    - "Write error: ScaffoldMessenger SnackBar 'Something went wrong — your check-in wasn\\'t saved. Try again.' (UI-SPEC copy lock — exact apostrophe + em dash); screen is not popped."
    - "Scheduled-entry weekday filter: only entries whose schedule weekdayMask includes today's weekday bit appear in pendingCheckinsTodayProvider (per RESEARCH §7 last paragraph + CONTEXT D-domain). Out-of-mask scheduled entries are dormant — no row written, no row displayed."
  artifacts:
    - path: "lib/features/checkin/pages/checkin_screen.dart"
      provides: "/checkin screen — AppBar, list, FilledButton, empty state, error SnackBar"
      contains: "class CheckinScreen"
    - path: "lib/features/checkin/providers/checkin_providers.dart"
      provides: "pendingCheckinsTodayProvider + checkinAnswersProvider"
      contains: "pendingCheckinsTodayProvider"
    - path: "lib/features/checkin/controllers/checkin_controller.dart"
      provides: "CheckinController — single Drift transaction submit + post-rollover trigger"
      contains: "class CheckinController"
    - path: "lib/core/router/app_router.dart"
      provides: "/checkin GoRoute (modified — append only)"
      contains: "GoRoute(path: '/checkin'"
  key_links:
    - from: "lib/features/checkin/pages/checkin_screen.dart"
      to: "lib/features/checkin/controllers/checkin_controller.dart"
      via: "ref.read(checkinControllerProvider.notifier).submit()"
      pattern: "checkinControllerProvider"
    - from: "lib/features/checkin/controllers/checkin_controller.dart"
      to: "lib/data/database/daos/daily_checkins_dao.dart + lib/domain/streak/streak_rollover_service.dart"
      via: "Drift transaction + streakRolloverServiceProvider.rollover()"
      pattern: "transaction|rollover"
---

# Plan 05-06 — Wave 5: /checkin screen + idempotent single-transaction submit + GoRouter wiring

<objective>
Ship the dedicated daily check-in surface per CONTEXT D-01..D-04: full-screen `/checkin` GoRoute, multi-entry list with per-row Yes/No SegmentedButton, single FilledButton submit that writes N rows to `daily_checkins` in one Drift transaction, then re-triggers streak rollover so badges update before returning to Home.

Idempotency is mandatory (D-01) — already-answered entries show locked SegmentedButton with their stored answer; the screen remains safe to re-enter from either reminder-tap or Home-trigger paths.

UI-SPEC §Screen 1 + §Copywriting Contract lock every visible string. Tests already exist as Wave 0 stubs and assert these literals verbatim (Plan 05-01 set the test contract); this plan flips them GREEN.

Purpose: deliver the trust-layer surface where the user records their daily honesty signal.
Output: 3 new feature files + 1 router edit + 1 GREEN test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md
@.planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md
@.planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md
@.planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md
@lib/core/router/app_router.dart
@lib/features/list/pages/edit_entry_screen.dart
@lib/features/pause/pages/pause_screen.dart
@lib/features/pause/controllers/pause_controller.dart
@lib/features/dashboard/widgets/dashboard_segmented.dart
@lib/features/dashboard/providers/avoided_today_provider.dart
@lib/data/database/daos/daily_checkins_dao.dart
@lib/data/database/daos/block_list_dao.dart
@lib/domain/streak/streak_rollover_service.dart
@lib/domain/streak/streak_rollover_service_providers.dart
@lib/domain/schedule/streak_day.dart
@lib/domain/schedule/schedule_window.dart
</context>

<interfaces>
<!-- Riverpod providers consumed + DAO contracts. Tests pre-locked the copy in Plan 05-01. -->

From lib/data/database/daos/daily_checkins_dao.dart (Plan 05-02):
- `Future<int> upsert({required int entryId, required DateTime day, required bool avoided, required DateTime answeredAt})`
- `Future<DailyCheckinData?> getFor(int entryId, DateTime day)`

From lib/domain/streak/streak_rollover_service_providers.dart (Plan 05-03):
- `final AsyncNotifierProvider<StreakRolloverService, void> streakRolloverServiceProvider`
- StreakRolloverService.rollover() — idempotent within 60s

From lib/features/dashboard/widgets/dashboard_segmented.dart (analog SegmentedButton pattern):
- `SegmentedButton<T>(segments: [ButtonSegment<T>(value: ..., label: Text(...))], selected: <T>{}, onSelectionChanged: (Set<T>) {...})`

From lib/features/pause/controllers/pause_controller.dart (analog Notifier pattern):
- `class PauseController extends Notifier<...>` with `submit()` method writing to DAO + navigating

UI-SPEC §Copywriting Contract — every literal MUST match exactly:
- AppBar title: "Daily check-in"
- Heading body: "How did today go?"
- SegmentedButton labels: "Yes", "No"
- Save button: "Save check-in" (default) or "All done" (when pre-answered)
- Empty state heading: "You're all caught up"
- Empty state body: "Check back tomorrow."
- Error: "Something went wrong — your check-in wasn't saved. Try again."
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add /checkin GoRoute + providers + controller + CheckinScreen with idempotent submit; flip Wave 0 stub to GREEN</name>
  <files>lib/core/router/app_router.dart, lib/features/checkin/providers/checkin_providers.dart, lib/features/checkin/controllers/checkin_controller.dart, lib/features/checkin/pages/checkin_screen.dart, test/features/checkin/checkin_screen_test.dart</files>
  <read_first>
    .planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md (D-01..D-04 — surface + trigger + multi-entry + answer-window decisions),
    .planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md §Screen 1 (lines 134-181 — full layout + entry row + empty state + idempotency rules),
    .planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md §Copywriting Contract (lines 314-337 — copy lock for all visible strings),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §7 (Riverpod providers + submit handler + idempotency + STRK-09 weekday filter),
    .planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md §"lib/features/checkin/pages/checkin_screen.dart" (composite analog: edit_entry_screen + pause_screen) + §"lib/features/checkin/providers" (analog: pause_providers) + §"lib/features/checkin/controllers" (analog: pause_controller),
    lib/core/router/app_router.dart (existing GoRoute appending pattern — line 44 is the last route added by Phase 3; Phase 5 appends after),
    lib/features/list/pages/edit_entry_screen.dart (Scaffold + AppBar + body pattern + the _save error-handling SnackBar at lines 41-66),
    lib/features/pause/pages/pause_screen.dart (Scaffold + Consumer*Widget pattern; idempotent navigation back to / pattern),
    lib/features/pause/controllers/pause_controller.dart (Notifier + DAO write + navigate pattern lines 1-100),
    lib/features/dashboard/widgets/dashboard_segmented.dart (SegmentedButton<bool>-style pattern — adapt for Yes/No tri-state),
    lib/features/dashboard/providers/avoided_today_provider.dart (multi-table-join inside provider pattern for pendingCheckinsTodayProvider),
    lib/domain/schedule/streak_day.dart (streakDayFor — used to compute "today" anchor),
    lib/domain/schedule/schedule_window.dart (isInScheduleWindow — used for STRK-09 weekday-mask filter),
    lib/data/database/daos/daily_checkins_dao.dart (upsert signature),
    lib/data/database/daos/block_list_dao.dart (BlockListData type, kind/packageName fields, schedule fields),
    lib/domain/streak/streak_rollover_service_providers.dart (streakRolloverServiceProvider — invoked post-submit)
  </read_first>
  <behavior>
    - /checkin renders an AppBar titled "Daily check-in" (centerTitle: false).
    - Body is a SingleChildScrollView with 16px horizontal padding.
    - Heading: Text("How did today go?", style: titleLarge, weight 400). SizedBox height 16.
    - For each entry returned by pendingCheckinsTodayProvider (sorted by entry.id asc for stable ordering): Row with leading 32px AppIcon for apps OR Icons.spa_outlined for habits, 8px SizedBox, Expanded Text(entry.displayName, bodyMedium, maxLines:1, ellipsis), SegmentedButton<bool> {ButtonSegment(value:true, label:Text("Yes")), ButtonSegment(value:false, label:Text("No"))} with selected derived from checkinAnswersProvider state for that entry (empty set = unselected), onSelectionChanged: updates checkinAnswersProvider Map<int, bool>. Divider between rows.
    - For already-answered entries (loaded as DailyCheckinData via dailyCheckinsDao.getFor(entryId, today)): same row shape but SegmentedButton selected = {data.avoided} and onSelectionChanged = null (disabled).
    - SizedBox height 24. FilledButton(onPressed: enabled ? () => controller.submit() : null, style: minimumSize Size.fromHeight(48), child: Text(label)). Label = "All done" when every entry is pre-answered (i.e., answeredCount == entries.length && newAnswers.isEmpty); else "Save check-in".
    - Enabled when newAnswers.isNotEmpty.
    - Empty state (entries.isEmpty AND no answered-today entries): centered Icon(Icons.check_circle_outline, size:48, color: primary), SizedBox 16, Text("You're all caught up", titleMedium), SizedBox 8, Text("Check back tomorrow.", bodyMedium, color: onSurfaceVariant).
    - CheckinController.submit() — single Drift transaction:
      ```
      await db.transaction(() async {
        for (var entry in answers.entries) {
          await dao.upsert(entryId: entry.key, day: streakDayFor(DateTime.now()), avoided: entry.value, answeredAt: DateTime.now());
        }
      });
      await ref.read(streakRolloverServiceProvider.notifier).rollover();
      if (mounted) context.go('/');
      ```
    - On exception in transaction: ScaffoldMessenger.showSnackBar with exact copy "Something went wrong — your check-in wasn't saved. Try again." Screen NOT popped.
    - pendingCheckinsTodayProvider implementation:
      - Read all block_list entries.
      - Today = streakDayFor(DateTime.now()).
      - For each entry: if `entry.scheduleStartMinutes != null && entry.scheduleWeekdayMask != null`, check `isInScheduleWindow(now: today + start - 1 minute, ...)` to determine if today's weekday is in-mask — if NOT, skip (entry dormant per STRK-09).
      - For included entries: check dao.getFor(entryId, today) — if null → pending; else → already-answered (display locked).
      - Return both lists in a single record `({List<BlockListData> pending, List<({BlockListData entry, DailyCheckinData answer})> answered})`.
    - checkinAnswersProvider = StateProvider<Map<int, bool>>((_) => {}) — tracks unsaved user input.
  </behavior>
  <action>
Implement /checkin surface and wire to router.

(a) lib/core/router/app_router.dart — surgical edit. After the line declaring `/dashboard` route (Phase 3, line 44), append:
```
GoRoute(path: '/checkin', builder: (_, __) => const CheckinScreen()),
```
Add `import 'package:not_to_do_list/features/checkin/pages/checkin_screen.dart';` to the imports list. Onboarding redirect (lines 23-33) unchanged — T-2-10 lock preserved.

(b) lib/features/checkin/providers/checkin_providers.dart — hand-written Riverpod:
- `final FutureProvider<({List<BlockListData> pending, List<({BlockListData entry, DailyCheckinData answer})> answered})> pendingCheckinsTodayProvider = FutureProvider<...>((ref) async { ... });` — implementation matches behavior section above: reads block_list, filters by STRK-09 schedule, splits by getFor null/non-null.
- `final StateProvider<Map<int, bool>> checkinAnswersProvider = StateProvider<Map<int, bool>>((_) => {});` — transient unsaved state.

(c) lib/features/checkin/controllers/checkin_controller.dart — class CheckinController extends Notifier<AsyncValue<void>>. Method submit() implements the Drift transaction + rollover trigger + navigation per behavior section. Errors caught in try/catch; on error sets state = AsyncError(...) and surfaces SnackBar via callback (Notifier doesn't have BuildContext; either pass context as parameter or expose error via state and let the screen react). Provider: `final NotifierProvider<CheckinController, AsyncValue<void>> checkinControllerProvider = NotifierProvider<CheckinController, AsyncValue<void>>(CheckinController.new);`.

(d) lib/features/checkin/pages/checkin_screen.dart — ConsumerStatefulWidget. build() consumes pendingCheckinsTodayProvider.maybeWhen(data: ..., orElse: CircularProgressIndicator). Renders the structure in the behavior section. Submit button reads checkinAnswersProvider; calls ref.read(checkinControllerProvider.notifier).submit() with the BuildContext for navigation/SnackBar. After successful submit, the controller invokes context.go('/'). On error, the controller signals via state; the screen wraps in `ref.listen(checkinControllerProvider, (prev, next) { next.whenOrNull(error: (e, st) => ScaffoldMessenger.of(context).showSnackBar(...)); });`.

(e) test/features/checkin/checkin_screen_test.dart — unskip; flip every Wave 0 stub to a real test using the ProviderScope + GoRouter scaffolding pattern from test/features/pause/pause_screen_test.dart. Cover:
- D-02 layout: renders AppBar "Daily check-in", heading "How did today go?", FilledButton "Save check-in".
- D-03 single transaction: with 3 pending entries, select Yes/Yes/No, tap Save — verify dao.upsert called 3 times AND wrapped in db.transaction (spy via mocktail captures).
- D-01 idempotency: pre-seed one DailyCheckinData via override; re-mount; verify that entry's SegmentedButton selected reflects stored answer AND is disabled.
- Empty state: with empty entries list, verify "You're all caught up" + "Check back tomorrow." text.
- Error: stub controller submit to throw; verify SnackBar text exact "Something went wrong — your check-in wasn't saved. Try again." appears AND no context.go fires.
- All test bodies pull literals from UI-SPEC §Copywriting Contract.
- Remove all `skip:` markers in this file.

Onboarding redirect contract: Phase 2's redirect at app_router.dart lines 23-33 redirects unfinished onboarding to /onboarding/welcome — this means /checkin is correctly gated behind onboarding completion (the user must finish onboarding before they can have a not-to-do entry, before they can have a streak, before /checkin is reachable). NO additional redirect logic in this plan. Verify by reading the existing redirect; ensure /checkin matches the `completed = true` branch (it does — only /onboarding/* is on the unfinished branch).
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs ; flutter test test/features/checkin/checkin_screen_test.dart ; flutter analyze lib/features/checkin/ lib/core/router/ ; flutter test test/policy/play_invariants_test.dart test/policy/phase_5_invariants_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/checkin/checkin_screen_test.dart` exits 0 with all named tests passing; no skip remaining.
    - `flutter analyze lib/features/checkin/ lib/core/router/` shows 0 errors / 0 warnings.
    - `grep -c "GoRoute(path: '/checkin'" lib/core/router/app_router.dart` >= 1.
    - `grep -c "import 'package:not_to_do_list/features/checkin/pages/checkin_screen.dart'" lib/core/router/app_router.dart` >= 1.
    - `grep -c 'completed && goingToOnboarding' lib/core/router/app_router.dart` >= 1 (redirect logic preserved byte-for-byte).
    - `grep -c "find.text('Daily check-in')" test/features/checkin/checkin_screen_test.dart` >= 1 (AppBar literal asserted).
    - `grep -c "find.text(\"How did today go?\")" test/features/checkin/checkin_screen_test.dart` >= 1 (heading literal asserted).
    - `grep -c 'find.text(.Save check-in.)' test/features/checkin/checkin_screen_test.dart` >= 1 (button literal asserted).
    - `grep -c 'You.re all caught up' test/features/checkin/checkin_screen_test.dart` >= 1 (empty state literal asserted).
    - `grep -c 'transaction' lib/features/checkin/controllers/checkin_controller.dart` >= 1 (single-tx pattern present).
    - `grep -c 'streakRolloverServiceProvider' lib/features/checkin/controllers/checkin_controller.dart` >= 1 (post-submit rollover trigger).
    - `flutter test test/policy/play_invariants_test.dart` 9/9 green.
    - `flutter test test/policy/phase_5_invariants_test.dart` all invariants green (no telemetry / no FCM in lib/features/checkin/).
  </acceptance_criteria>
  <done>/checkin route lands; idempotent single-transaction submit works; rollover re-triggers after write; UI-SPEC §Screen 1 copy locks verified by tests; empty state + error path covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (Yes/No SegmentedButton) ↔ Drift transaction | Bool only; Drift type-checks; no injection |
| Onboarding redirect ↔ /checkin route | Existing app_router redirect (T-2-10) preserved byte-for-byte |
| pendingCheckinsTodayProvider ↔ scheduled-entry STRK-09 filter | Pure Dart filter; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-23 | Tampering | Future edit weakens onboarding redirect, allowing /checkin to be reached without completing onboarding | mitigate | acceptance grep on the redirect condition `completed && goingToOnboarding` — fails today if changed |
| T-05-24 | Information disclosure | Entry names rendered in the screen are visible to shoulder-surfers | accept | This is the in-app surface, not the notification; UI-SPEC §A11Y allows screen-reader access to entry names; locked by D-02 surface choice |
| T-05-25 | Denial of service | Submit failure during transaction leaves partial writes | mitigate | Single Drift transaction — all-or-nothing semantics; on exception transaction rolls back; SnackBar surfaces error to user |
| T-05-26 | Repudiation | User claims they submitted but transaction failed silently | mitigate | controller surfaces error via state + SnackBar; user can re-submit (idempotent — upsert overwrites by (entryId, day) per Plan 05-02 DAO contract) |
</threat_model>

<verification>
- /checkin route appended without modifying redirect logic.
- Drift transaction wraps the multi-row upsert.
- Rollover service invoked post-submit (badge refresh).
- All UI-SPEC copy literals locked by tests.
</verification>

<success_criteria>
- /checkin is a real reachable surface.
- D-01..D-04 honored.
- STRK-03 GREEN.
- NOTF-03 deep-link target exists.
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-06-SUMMARY.md`
</output>
