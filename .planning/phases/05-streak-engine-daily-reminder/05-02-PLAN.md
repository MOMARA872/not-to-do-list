---
phase: 05-streak-engine-daily-reminder
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - lib/data/database/daos/daily_checkins_dao.dart
  - lib/data/database/daos/daily_streak_dao.dart
  - lib/data/database/app_database.dart
  - lib/features/streak/storage/streak_keys.dart
  - test/data/dao/daily_checkins_dao_test.dart
  - test/data/dao/daily_streak_dao_test.dart
autonomous: true
requirements:
  - STRK-01
  - STRK-03
  - STRK-07
must_haves:
  truths:
    - "DailyCheckinsDao.upsert is idempotent — second upsert with same (entryId, day) overwrites the row and row count stays 1."
    - "DailyStreakDao.upsert is idempotent on (entryId, day); upsertIfAbsent does NOT overwrite existing rows."
    - "DailyStreakDao.getCurrentStreakFor(entryId) returns the consecutive count of trailing days where status=0 (success) ending at today (exclusive of today's pending row); 0 when no successful days."
    - "DailyStreakDao.getLongestStreakFor(entryId) returns the maximum consecutive run of status=0 days in the table's full history; 0 when entry has no rows."
    - "DailyStreakDao.watchHistoryFor(entryId, days: 30) emits a reactive stream of DailyStreakData ordered by day asc, capped at the last 30 calendar days."
    - "StreakKeys constants are the single source of truth for prefs key names; reminder_hour_minute MUST match the Kotlin-side key the BootReceiver reads (no flutter. prefix in this file — that prefix is applied by the shared_preferences plugin)."
    - "app_database.dart @DriftDatabase(daos: ...) declares DailyCheckinsDao and DailyStreakDao; dart run build_runner build regenerates app_database.g.dart cleanly."
  artifacts:
    - path: "lib/data/database/daos/daily_checkins_dao.dart"
      provides: "DailyCheckinsDao with upsert, getFor"
      contains: "class DailyCheckinsDao"
    - path: "lib/data/database/daos/daily_streak_dao.dart"
      provides: "DailyStreakDao with upsert, upsertIfAbsent, getCurrentStreakFor, getLongestStreakFor, watchHistoryFor, watchForEntry"
      contains: "class DailyStreakDao"
    - path: "lib/features/streak/storage/streak_keys.dart"
      provides: "StreakKeys constants — streakThresholdMinutes, reminderHourMinute, lastWallClockMs, lastBootMonotonicNs, lastEvaluatedStreakDay, earnedPromptShown"
      contains: "abstract final class StreakKeys"
  key_links:
    - from: "lib/data/database/app_database.dart"
      to: "lib/data/database/daos/daily_checkins_dao.dart + lib/data/database/daos/daily_streak_dao.dart"
      via: "@DriftDatabase(daos: [...]) registration"
      pattern: "DailyCheckinsDao.*DailyStreakDao|DailyStreakDao.*DailyCheckinsDao"
    - from: "lib/features/streak/storage/streak_keys.dart"
      to: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/receiver/BootReceiver.kt"
      via: "shared key name 'reminder_hour_minute' (Kotlin reads 'flutter.reminder_hour_minute' from FlutterSharedPreferences file — RESEARCH §10 R-9)"
      pattern: "reminder_hour_minute"
---

# Plan 05-02 — Wave 1: DAOs (DailyCheckinsDao + DailyStreakDao) + StreakKeys constants

<objective>
Land the two DAOs that the Phase 5 rollover service and UI surfaces depend on, plus the StreakKeys constants file that is the single source of truth for every Phase 5 SharedPreferences key. Schema is locked from Phase 1 (Drift tables `DailyCheckins` and `DailyStreak` exist); this plan only adds typed Dart accessors + database registration.

Flip the corresponding Wave 0 RED stubs to GREEN: `test/data/dao/daily_checkins_dao_test.dart` and `test/data/dao/daily_streak_dao_test.dart` are unskipped and their assertions become real.

Purpose: provide the data layer Plan 05-03 (StreakRolloverService) and Plans 05-06/07 (UI) consume.
Output: 2 DAO files + 1 constants file + app_database.dart edit + 2 unskipped GREEN test files + regenerated *.g.dart artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md
@.planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md
@.planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md
@.planning/phases/05-streak-engine-daily-reminder/05-VALIDATION.md
@lib/data/database/app_database.dart
@lib/data/database/tables/daily_streak_table.dart
@lib/data/database/tables/daily_checkins_table.dart
@lib/data/database/daos/daily_usage_summary_dao.dart
@lib/data/database/daos/pause_event_dao.dart
@lib/data/database/daos/block_list_dao.dart
@lib/features/onboarding/storage_keys.dart
@test/data/repositories/pause_event_repository_test.dart
</context>

<interfaces>
<!-- DAO pattern extracted from existing daos/. Phase 5 DAOs mirror this surface exactly. -->

From lib/data/database/daos/daily_usage_summary_dao.dart (the analog):
- `@DriftAccessor(tables: [DailyUsageSummary])`
- `class DailyUsageSummaryDao extends DatabaseAccessor<AppDatabase> with _$DailyUsageSummaryDaoMixin`
- Constructor: `DailyUsageSummaryDao(super.attachedDatabase)`
- `Future<void> upsertDay({...}) { into(...).insert(companion, onConflict: DoUpdate((_) => companion, target: [...explicit unique-key cols])) }`
- `Future<DailyUsageSummaryData?> getTodayFor(String packageName, DateTime localMidnight)` — uses `getSingleOrNull()` on a `(select)..where(...)` chain
- `Stream<List<DailyUsageSummaryData>> watchRange(DateTime startDay, DateTime endDay)` — `isBetweenValues` is inclusive

From lib/data/database/app_database.dart line 22:
- Current: `daos: [BlockListDao, DailyUsageSummaryDao, PauseEventDao]`
- New: `daos: [BlockListDao, DailyUsageSummaryDao, PauseEventDao, DailyCheckinsDao, DailyStreakDao]`

From lib/data/database/tables/daily_streak_table.dart:
- Status enum (Dart literal ints): 0 success, 1 broken, 2 incomplete-data, 3 pending
- Source enum: 0 system-confirmed, 1 self-reported-only
- usageMinutesObserved has `withDefault(const Constant(0))`

From lib/features/onboarding/storage_keys.dart (analog for StreakKeys):
- `abstract final class OnboardingKeys` with `static const String cursor = 'onboarding_step';` etc.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StreakKeys constants + DailyCheckinsDao + DailyStreakDao + register in AppDatabase + regenerate *.g.dart</name>
  <files>lib/features/streak/storage/streak_keys.dart, lib/data/database/daos/daily_checkins_dao.dart, lib/data/database/daos/daily_streak_dao.dart, lib/data/database/app_database.dart</files>
  <read_first>
    lib/data/database/daos/daily_usage_summary_dao.dart (exact analog — copy file shape, swap target columns to (entryId, day)),
    lib/data/database/daos/pause_event_dao.dart (secondary analog — outcome enum-style int column patterns),
    lib/data/database/tables/daily_streak_table.dart (column names + enum ints),
    lib/data/database/tables/daily_checkins_table.dart (column names),
    lib/data/database/app_database.dart (only the @DriftDatabase declaration line — surgical change to daos: list),
    lib/features/onboarding/storage_keys.dart (StreakKeys class shape),
    .planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md (§DAOs — replication notes spell out method signatures),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §4 (algorithm consumes upsert + upsertIfAbsent + getCurrentStreakFor + getLongestStreakFor + watchHistoryFor — these are the contracts the service expects)
  </read_first>
  <behavior>
    - DailyCheckinsDao.upsert(entryId: A, day: D, avoided: true, answeredAt: t1) inserts row R1; subsequent upsert(entryId: A, day: D, avoided: false, answeredAt: t2) overwrites R1 → table contains exactly 1 row, with avoided=false and answeredAt=t2.
    - DailyCheckinsDao.upsert(entryId: A, day: D) followed by upsert(entryId: B, day: D) leaves 2 rows (different entries).
    - DailyCheckinsDao.getFor(entryId: A, day: D) returns the row when present, null when absent.
    - DailyStreakDao.upsert(entryId: A, day: D, status: 0, source: 0, usageMinutesObserved: 3, evaluatedAt: t1) inserts; second upsert with same (A,D) and status: 1 overwrites → row count 1, status=1.
    - DailyStreakDao.upsertIfAbsent(entryId: A, day: D, status: 3, ...) when row exists is a no-op; when no row exists, inserts the row. Implementation: select-where → if-null-then-insert (do NOT use Drift's plain `insertOnConflictUpdate`; that overwrites).
    - DailyStreakDao.getCurrentStreakFor(entryId: A) with rows {(A, today-1, status=0), (A, today-2, status=0), (A, today-3, status=1)} returns 2. With no rows returns 0. Excludes today's `status=3 pending` row.
    - DailyStreakDao.getLongestStreakFor(entryId: A) with rows showing two success runs of lengths 3 and 5, separated by a broken day, returns 5.
    - DailyStreakDao.watchHistoryFor(entryId: A, days: 30) emits List<DailyStreakData> ordered by day asc, containing only rows where day >= today - 29 (i.e., last 30 calendar days). New writes within range cause the stream to emit again.
    - StreakKeys constants are static const Strings exactly: `streak_threshold_minutes`, `reminder_hour_minute`, `streak_last_wall_clock_ms`, `streak_last_boot_monotonic_ns`, `streak_last_evaluated_day_ms`, `post_notifications_earned_prompt_shown`.
    - app_database.dart @DriftDatabase declaration lists `DailyCheckinsDao` and `DailyStreakDao` in the daos list; `dart run build_runner build --delete-conflicting-outputs` generates `app_database.g.dart`, `daily_checkins_dao.g.dart`, and `daily_streak_dao.g.dart` without errors.
  </behavior>
  <action>
Implement DAOs and StreakKeys per the PATTERNS.md replication notes. No schema changes — tables already exist from Phase 1.

(a) lib/features/streak/storage/streak_keys.dart — copy the OnboardingKeys shape verbatim. Body:
- `abstract final class StreakKeys` with 6 `static const String` members:
  - `streakThresholdMinutes = 'streak_threshold_minutes'` (default surfaced by Plan 05-08 settings; raw int stored)
  - `reminderHourMinute = 'reminder_hour_minute'` (raw int hour*60+minute; CRITICAL — matches Kotlin BootReceiver read of 'flutter.reminder_hour_minute' on the FlutterSharedPreferences file per RESEARCH §10 R-9)
  - `lastWallClockMs = 'streak_last_wall_clock_ms'`
  - `lastBootMonotonicNs = 'streak_last_boot_monotonic_ns'`
  - `lastEvaluatedStreakDay = 'streak_last_evaluated_day_ms'`
  - `earnedPromptShown = 'post_notifications_earned_prompt_shown'`
- Include the same doc-comment style as OnboardingKeys (one line per key explaining its purpose + the consuming surface).

(b) lib/data/database/daos/daily_checkins_dao.dart — copy daily_usage_summary_dao.dart structure verbatim. Swap:
- `@DriftAccessor(tables: [DailyCheckins])`
- `class DailyCheckinsDao extends DatabaseAccessor<AppDatabase> with _$DailyCheckinsDaoMixin`
- Imports: `'package:not_to_do_list/data/database/tables/daily_checkins_table.dart'`
- Methods:
  - `Future<int> upsert({required int entryId, required DateTime day, required bool avoided, required DateTime answeredAt})` — build `DailyCheckinsCompanion.insert(...)`, then `into(dailyCheckins).insert(companion, onConflict: DoUpdate((_) => companion, target: [dailyCheckins.entryId, dailyCheckins.day]))`. Returns Drift's autoincrement id (or 0 on update — caller doesn't depend on it but match analog signature shape).
  - `Future<DailyCheckinData?> getFor(int entryId, DateTime day)` — `(select(dailyCheckins)..where((t) => t.entryId.equals(entryId) & t.day.equals(day))).getSingleOrNull()`.
- Doc-comment header matches analog tone (hand-written, _$DailyCheckinsDaoMixin generated by drift_dev, unique key (entryId, day) drives the upsert target — Pitfall #5).

(c) lib/data/database/daos/daily_streak_dao.dart — same shape. Methods:
  - `Future<int> upsert({required int entryId, required DateTime day, required int status, required int source, required int usageMinutesObserved, required DateTime evaluatedAt})` — DoUpdate target `[dailyStreak.entryId, dailyStreak.day]`.
  - `Future<int> upsertIfAbsent({required int entryId, required DateTime day, required int status, required int source, required int usageMinutesObserved, required DateTime evaluatedAt})` — implement as: `final existing = await getFor(entryId, day); if (existing != null) return existing.id; return into(dailyStreak).insert(companion);`. Do NOT use Drift's onConflict for this method — semantics differ from upsert.
  - `Future<DailyStreakData?> getFor(int entryId, DateTime day)` — same shape as getFor on checkins DAO.
  - `Future<int> getCurrentStreakFor(int entryId)` — custom SQL: select day from daily_streak where entryId = :id AND status = 0 order by day desc; iterate rows; if first row's day < today-1, return 0 (gap from today means streak isn't current); else walk backwards counting consecutive days where each subsequent row's day = previous_day - 1 day. Stop at the first gap. Return the count. Use `customSelect(...)` with named params for portability — see avoided_today_provider.dart customSelect precedent in PATTERNS.md.
  - `Future<int> getLongestStreakFor(int entryId)` — custom SQL or Dart-side group-walk: select day from daily_streak where entryId = :id AND status = 0 order by day asc; walk rows, counting consecutive runs; return max(run_length). Returns 0 when no rows.
  - `Stream<List<DailyStreakData>> watchHistoryFor(int entryId, {int days = 30})` — `(select(dailyStreak)..where((t) => t.entryId.equals(entryId) & t.day.isBiggerOrEqualValue(DateTime.now().subtract(Duration(days: days - 1)))) ..orderBy([(t) => OrderingTerm.asc(t.day)])).watch()`. Use `DateTime` constructor day-stripping in the caller (the service) — DAO accepts the raw cutoff.
  - `Stream<List<DailyStreakData>> watchForEntry(int entryId)` — convenience all-time stream ordered desc for use by the badge provider if needed.

(d) lib/data/database/app_database.dart — surgical edit. Change line 22 from `daos: [BlockListDao, DailyUsageSummaryDao, PauseEventDao],` to `daos: [BlockListDao, DailyUsageSummaryDao, PauseEventDao, DailyCheckinsDao, DailyStreakDao],`. Add the two new imports near the existing DAO imports. NO schema bump — D-08 lock + tables exist from Phase 1 + 05-RESEARCH §4 verified.

(e) Regenerate codegen: run `dart run build_runner build --delete-conflicting-outputs`. Verify `lib/data/database/daos/daily_checkins_dao.g.dart` and `lib/data/database/daos/daily_streak_dao.g.dart` are created; verify `lib/data/database/app_database.g.dart` includes the new DAO accessors.

(f) Unskip + fill the two Wave 0 RED stubs:
- test/data/dao/daily_checkins_dao_test.dart — copy in-memory AppDatabase scaffold from test/data/repositories/pause_event_repository_test.dart (lines 14-39). Real tests covering STRK-03 (upsert idempotent per (entryId, day) — 1 row after 2 upserts of same key), getFor returns null on miss, avoided bool round-trip.
- test/data/dao/daily_streak_dao_test.dart — same scaffold. Real tests covering STRK-01 (upsert idempotent), upsertIfAbsent does NOT overwrite, getCurrentStreakFor with mixed status rows, getLongestStreakFor across two success runs, watchHistoryFor emits ordered + capped 30-day window.

CRITICAL: do NOT introduce a schemaVersion bump. Tables exist from Phase 1. Codegen must succeed against the current schema.

PLAY-02 invariants: this plan adds no Kotlin under android/.../service/ or android/.../not_to_do_list/ root. The 9 PLAY-02 invariants stay green by virtue of file-tree scope.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs 2>&amp;1 | tail -5 ; flutter test test/data/dao/daily_checkins_dao_test.dart test/data/dao/daily_streak_dao_test.dart 2>&amp;1 | tail -10 ; flutter analyze lib/data/database/daos/ lib/features/streak/ 2>&amp;1 | tail -5 ; flutter test test/policy/play_invariants_test.dart 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dart run build_runner build --delete-conflicting-outputs` exits 0; generates daily_checkins_dao.g.dart, daily_streak_dao.g.dart, app_database.g.dart with no errors.
    - `flutter test test/data/dao/daily_checkins_dao_test.dart test/data/dao/daily_streak_dao_test.dart` exits 0 with all behavior tests above PASSING (no `skip:` left in these two files).
    - `flutter analyze lib/data/database/daos/ lib/features/streak/` shows 0 errors / 0 warnings.
    - `flutter test test/policy/play_invariants_test.dart` 9/9 invariants green (no Kotlin under main/ added).
    - `grep -c 'DailyCheckinsDao' lib/data/database/app_database.dart` ≥ 1 (registration landed).
    - `grep -c 'DailyStreakDao' lib/data/database/app_database.dart` ≥ 1.
    - `grep -E "static const String (streakThresholdMinutes|reminderHourMinute|lastWallClockMs|lastBootMonotonicNs|lastEvaluatedStreakDay|earnedPromptShown) = '[a-z_]+';" lib/features/streak/storage/streak_keys.dart | wc -l` = 6 (all 6 keys present and lowercase_snake string values).
    - `grep "reminder_hour_minute" lib/features/streak/storage/streak_keys.dart` shows the exact literal `'reminder_hour_minute'` (no `flutter.` prefix in Dart code — the plugin adds it).
  </acceptance_criteria>
  <done>DAOs land, registration committed, codegen clean, two Wave 0 stubs flipped GREEN, StreakKeys constants ready for Plan 05-03 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drift DAO ↔ caller | Type-checked at compile time; no input parsing risk |
| StreakKeys ↔ Kotlin BootReceiver | Cross-process boundary on shared_preferences file; key collision risk if Dart key drifts from Kotlin reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Dart-side prefs key for `reminder_hour_minute` drifts from the Kotlin BootReceiver's expected `flutter.reminder_hour_minute` lookup, breaking NOTF-05 re-arm silently | mitigate | StreakKeys.reminderHourMinute = `'reminder_hour_minute'` (no flutter. prefix — plugin adds it); Plan 05-01 phase_5_invariants_test.dart + Plan 05-05 Kotlin reader both reference the same literal; manual key-parity test in Plan 05-09 REL-05 protocol |
| T-05-05 | Information disclosure | DAO methods leak internal status integers to consumers | accept | Status/source ints are documented enums per daily_streak_table.dart; consumers convert to UI labels per UI-SPEC §Color 4-state contract; no PII in DAO outputs |
| T-05-06 | Repudiation | DailyStreakDao.upsert overwrites prior evaluatedAt without audit trail | accept | evaluatedAt itself is the audit trail (timestamp of last evaluation); status changes are logged via the row's status field; full history retained in same row per day |
</threat_model>

<verification>
- DAOs compile and the two unskipped tests pass.
- StreakKeys exports 6 constants matching the names locked in RESEARCH §4 + the cross-process key lock.
- AppDatabase registration includes both new DAOs.
- No PLAY-02 invariant regression.
</verification>

<success_criteria>
- 4 source files land (3 new + 1 modified) + 3 *.g.dart artifacts regenerated.
- 2 Wave 0 RED stubs flipped GREEN with real behavior tests.
- All key contracts (RESEARCH §4 expects + Plan 05-03 consumes) are present.
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-02-SUMMARY.md`
</output>
