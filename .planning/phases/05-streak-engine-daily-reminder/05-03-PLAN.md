---
phase: 05-streak-engine-daily-reminder
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - lib/domain/streak/streak_rollover_service.dart
  - lib/domain/streak/streak_rollover_service_providers.dart
  - android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/StreakDay.kt
  - test/domain/streak/streak_rollover_service_test.dart
  - test/domain/streak/streak_rollover_dst_test.dart
  - android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/StreakDayAnchoringTest.kt
autonomous: true
requirements:
  - STRK-02
  - STRK-04
  - STRK-05
  - STRK-06
  - STRK-08
  - STRK-09
must_haves:
  truths:
    - "StreakRolloverService.rollover() reads (wallClockMs, bootMonotonicNs) every call; if |wallDelta - bootDeltaMs| > 24h (Duration(hours: 24).inMilliseconds) since the last persisted pair, all rolled days for this evaluation are written with status=2 (incomplete-data) — STRK-06."
    - "Day-step uses DateTime(cursor.year, cursor.month, cursor.day + 1); never cursor.add(Duration(days: 1)) — DST safety per RESEARCH §4 DST table."
    - "For each (entry, day) between lastEvaluatedDay+1 and today-1: _resolveDay(usageMinutes, threshold, checkinAvoided, a11yWasOn) returns (status, source) per the RESEARCH §4 2x2 matrix exactly — broken-by-system → (1, 0), broken-by-self-report → (1, a11yOn ? 0 : 1), success-checkin-yes → (0, a11yOn ? 0 : 1), no-checkin-a11yOn → (0, 0), no-checkin-no-a11yOn → (2, 0)."
    - "Today's row is upsertIfAbsent with status=3 pending — never overwrites a row already rolled to a final state."
    - "Backfill is capped at 30 days; if lastEvaluatedDay < today - 30, days beyond the cap are written status=2 incomplete-data (RESEARCH §10 R-7)."
    - "rollover() is idempotent within 60s — successive calls inside the soft-cache window return without re-evaluating."
    - "_usageMinutesForEntry returns 0 when entry.packageName is null (habit entries) per RESEARCH §10 R-8."
    - "Kotlin StreakDayAnchoringTest.kt passes — cross-midnight (22:00→06:00) windows anchored to the start day; mirrors Phase 4 ScheduleWindowTest pattern."
    - "streakBadgeProvider returns a 3-field record `({int current, int longest, bool breakDetectedToday})` — `breakDetectedToday` defaults to `false` in this plan (Plan 05-07 wires the real break-event source via the latest DailyStreakData row check). The 3-field shape is a contract guarantee from Wave 2 onward; downstream plans (05-07) consume but do not mutate it."
  artifacts:
    - path: "lib/domain/streak/streak_rollover_service.dart"
      provides: "StreakRolloverService AsyncNotifier — rollover(), _resolveDay 2x2 matrix, _usageMinutesForEntry, _a11yWasOnFor proxy, soft-cache, 30-day backfill cap"
      contains: "class StreakRolloverService"
    - path: "lib/domain/streak/streak_rollover_service_providers.dart"
      provides: "streakRolloverServiceProvider + streakBadgeProvider(entryId) returning 3-field record (current, longest, breakDetectedToday) + streakHistoryProvider(entryId) + dailyCheckinsDaoProvider + dailyStreakDaoProvider"
      contains: "streakBadgeProvider"
    - path: "android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/StreakDay.kt"
      provides: "Kotlin parity of streakDayFor for cross-process anchoring oracle"
      contains: "streakDayFor"
  key_links:
    - from: "lib/domain/streak/streak_rollover_service.dart"
      to: "lib/data/database/daos/daily_streak_dao.dart + daily_checkins_dao.dart + daily_usage_summary_dao.dart + block_list_dao.dart"
      via: "ref.read(dailyStreakDaoProvider) etc."
      pattern: "dailyStreakDaoProvider|dailyCheckinsDaoProvider"
    - from: "lib/domain/streak/streak_rollover_service.dart"
      to: "lib/domain/schedule/streak_day.dart"
      via: "streakDayFor(DateTime)"
      pattern: "streakDayFor"
    - from: "lib/domain/streak/streak_rollover_service.dart"
      to: "PermissionStatusApi.bootMonotonicNanos (added in Plan 05-04; mocked here in tests)"
      via: "Pigeon HostApi call"
      pattern: "bootMonotonicNanos"
---

# Plan 05-03 — Wave 2: StreakRolloverService (pure-Dart) + Kotlin parity oracle for STRK-09 day anchoring

<objective>
Implement the pure-Dart `StreakRolloverService` per RESEARCH §4 algorithm — the 2x2 status/source matrix, clock-tamper detection (>24h divergence), DST-safe day stepping via `DateTime` constructor, 30-day backfill cap, scheduled-entry STRK-09 anchoring, and the soft-cache idempotency guard.

This service IS the trust layer. It is called from `AppLifecycleState.resumed` (wired by Plan 05-07 in HealthLifecycleObserver) and on cold-open. It joins block_list × daily_usage_summary × daily_checkins and writes to daily_streak via Plan 05-02 DAOs.

Also lands the Kotlin `StreakDayAnchoringTest.kt` parity oracle — even though the engine runs Dart-side, cross-midnight day-anchoring is shared truth with Phase 4 `isInScheduleWindow` Kotlin port. The test prevents Kotlin/Dart drift if a future plan ever introduces a Kotlin streak path.

Sequencing: Plan 05-03 has a Pigeon dependency on `bootMonotonicNanos()` — Wave 1 ordering: 05-04 ships the Pigeon extension; this plan tests against the mock from `streak_fixture.dart`. Allowed ordering: 05-03 may land before 05-04 because tests mock the API; the real impl wiring is finished by 05-04.

Purpose: deliver the streak truth engine.
Output: service file + providers file + Kotlin helper + 2 GREEN unskipped tests + 1 GREEN Kotlin parity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md
@.planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md
@.planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md
@.planning/phases/05-streak-engine-daily-reminder/05-VALIDATION.md
@lib/data/database/daos/daily_streak_dao.dart
@lib/data/database/daos/daily_checkins_dao.dart
@lib/data/database/daos/daily_usage_summary_dao.dart
@lib/data/database/daos/block_list_dao.dart
@lib/domain/schedule/streak_day.dart
@lib/domain/schedule/schedule_window.dart
@lib/features/health/permission_health_provider.dart
@lib/features/dashboard/providers/avoided_today_provider.dart
@lib/features/pause/providers/pause_providers.dart
@lib/features/onboarding/providers/onboarding_cursor_provider.dart
@lib/features/streak/storage/streak_keys.dart
@android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindowTest.kt
@test/_fixtures/streak_fixture.dart
@test/_fixtures/permission_status_mock.dart
@test/domain/schedule/schedule_window_parity_test.dart
</context>

<interfaces>
<!-- Algorithm contract extracted from RESEARCH §4. Executor implements verbatim — no design freedom on the matrix. -->

From lib/domain/schedule/streak_day.dart (existing helper):
- DateTime streakDayFor(DateTime now) — local timezone, strips to DateTime(y, m, d) after subtracting 4h (the 04:00 day boundary)

From lib/domain/schedule/schedule_window.dart (existing helper):
- bool isInScheduleWindow({required DateTime now, required int? startMinutes, required int? endMinutes, required int? weekdayMask})

From lib/features/health/permission_health_provider.dart (analog AsyncNotifier shape):
- class PermissionHealthNotifier extends AsyncNotifier<PermissionHealth> with Future<X> build() + Future<void> refresh()

From lib/features/pause/providers/pause_providers.dart (analog hand-written family + repo provider):
- final Provider<X> someProvider = Provider<X>((ref) { ... });
- final FutureProviderFamily<T, K> someFamilyProvider = FutureProvider.autoDispose.family<T, K>((ref, k) { ... });

PermissionStatusApi extension (added in Plan 05-04, mocked here):
- Future<int> bootMonotonicNanos() — returns SystemClock.elapsedRealtimeNanos
- Tests inject buildClockMockApi(bootMonotonicNanos: N) from streak_fixture.dart

2x2 status/source matrix (verbatim RESEARCH §4):
- usage > threshold AND a11yOn → (1 broken, 0 system-confirmed)
- checkinAvoided == false → (1 broken, source = a11yOn ? 0 : 1)
- checkinAvoided == true → (0 success, source = a11yOn ? 0 : 1)
- checkinAvoided == null AND a11yOn → (0 success, 0 system-confirmed)
- checkinAvoided == null AND !a11yOn → (2 incomplete-data, 0)

streakBadgeProvider return record (locked in this plan; consumed by Plan 05-07):
- `({int current, int longest, bool breakDetectedToday})`
- `breakDetectedToday` is computed by reading the latest DailyStreakData row for `streakDayFor(DateTime.now())` (or the row immediately prior if today is still pending): if its `status == 1 broken` AND `evaluatedAt` is within the last 24h, return true; else false.
- Plan 05-03 ships this with the computation in place — the field is wired and returns the correct value from day one. Plan 05-07 only consumes the field for the strikethrough render path (D-05).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement StreakRolloverService + providers (3-field streakBadgeProvider record) + Kotlin parity helper; flip Wave 0 streak_rollover tests to GREEN; populate StreakDayAnchoringTest.kt</name>
  <files>lib/domain/streak/streak_rollover_service.dart, lib/domain/streak/streak_rollover_service_providers.dart, android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/StreakDay.kt, test/domain/streak/streak_rollover_service_test.dart, test/domain/streak/streak_rollover_dst_test.dart, android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/StreakDayAnchoringTest.kt</files>
  <read_first>
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §4 (full algorithm pseudocode — implement verbatim),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §10 R-1 (DST false-positive analysis), R-7 (30-day backfill cap), R-8 (habit entries have no usage rows),
    .planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md (D-04 answer window, D-08 schedule day-anchoring, D-07 status enum semantics),
    .planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md §"lib/domain/streak/streak_rollover_service.dart" (primary + composite analogs spelled out),
    lib/features/health/permission_health_provider.dart (AsyncNotifier + refresh() pattern lines 34-80),
    lib/features/dashboard/providers/avoided_today_provider.dart (multi-table join pattern lines 44-90),
    lib/domain/schedule/streak_day.dart (the streakDayFor helper — pure function, already exists),
    lib/domain/schedule/schedule_window.dart (isInScheduleWindow — consumed by STRK-09 anchor decision),
    lib/data/database/daos/daily_streak_dao.dart (upsert + upsertIfAbsent + getCurrentStreakFor + watchHistoryFor signatures from Plan 05-02),
    lib/data/database/daos/daily_checkins_dao.dart (getFor signature from Plan 05-02),
    lib/data/database/daos/daily_usage_summary_dao.dart (getTodayFor signature),
    lib/features/streak/storage/streak_keys.dart (key names),
    test/_fixtures/streak_fixture.dart (buildClockMockApi + 2x2 matrix tuples from Plan 05-01),
    test/_fixtures/permission_status_mock.dart (MockPermissionStatusApi shape),
    android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindow.kt (Phase 4 Kotlin parity model to mirror for StreakDay.kt),
    android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindowTest.kt (JUnit 4 ms() helper + tuple pattern — copy verbatim)
  </read_first>
  <behavior>
    - rollover() called twice within 60 seconds: second call returns immediately (no DAO writes). Verified by spy DAO insert count.
    - rollover() with lastEvaluatedDay == today's streakDayFor returns without per-day loop; today's pending row is refreshed via upsertIfAbsent only.
    - rollover() with lastEvaluatedDay = today - 1 day, one block_list entry packageName='com.instagram.android', daily_usage_summary row for yesterday foregroundSeconds=6*60 (6 min, threshold 5), no checkin, a11yWasOn=true → writes (status=1 broken, source=0 system-confirmed) for yesterday. STRK-02 PASS.
    - 2x2 matrix exhaustive: for every tuple in streak_fixture.matrixCases, _resolveDay returns the expected (status, source). STRK-04 PASS.
    - Clock tamper: nowBootNs - lastBootNs = 24h converted to ms, but nowWallMs - lastWallMs = 49h → divergence 25h > 24h threshold → every day rolled in this evaluation written status=2. STRK-06 PASS.
    - DST spring-forward DateTime(2026, 3, 8): rollover with lastEvaluatedDay = March 7 → cursor walks March 7 + 1 day = March 8 via DateTime(y, m, d+1); 23h wall delta vs 24h boot delta → divergence 1h < 24h → NO false-positive tamper. STRK-08 23h day PASS.
    - DST fall-back DateTime(2025, 11, 2): cursor walks Nov 1 + 1 day = Nov 2 → 25h wall vs 24h boot → divergence 1h < 24h → no false-positive. STRK-08 25h day PASS.
    - Scheduled entry scheduleStartMinutes=22*60, scheduleEndMinutes=6*60, weekdayMask covering Mon: daily_usage_summary for Mon = 12 min, threshold 5 → streak row anchored to Mon streakDay, status=1 broken. STRK-09 cross-midnight anchoring PASS.
    - Habit entry (kind=1, packageName=null) with no checkin and a11yWasOn=true → (status=2 incomplete-data, source=0) per RESEARCH §10 R-8.
    - Backfill cap: lastEvaluatedDay = today - 60 days → backfill processes only last 30 days; days [today-60 .. today-31] written status=2 incomplete-data. Verified by DAO upsert call count + status values.
    - After successful rollover, prefs contain updated lastWallClockMs / lastBootMonotonicNs / lastEvaluatedStreakDay. Verified by SharedPreferences reads in test teardown.
    - Kotlin StreakDayAnchoringTest.kt all @Test methods PASS — three assertions per the cross-midnight anchoring spec.
    - streakBadgeProvider(entryId) returns a 3-field record `({int current, int longest, bool breakDetectedToday})`. With no streak rows: returns `(current: 0, longest: 0, breakDetectedToday: false)`. With current=3, longest=12 and no recent break row: `(current: 3, longest: 12, breakDetectedToday: false)`. With a DailyStreakData row for today (or the most recent prior day) where `status == 1 broken` AND `evaluatedAt` within last 24h: `breakDetectedToday: true`. Otherwise `false`.
  </behavior>
  <action>
Implement the service in two Dart files plus tests, the Kotlin helper, and the Kotlin parity test.

(a) lib/domain/streak/streak_rollover_service.dart:
- Class `StreakRolloverService extends AsyncNotifier<void>` (analog: PermissionHealthNotifier).
- Add constructor-injected clock for testability: accept `DateTime Function()? clock` constructor param defaulting to `DateTime.now` (per CLAUDE.md Karpathy "Simplicity First" — avoid global mocking).
- `@override Future<void> build() async => null;` — this notifier does not auto-evaluate; it is invoked explicitly via .rollover().
- `Future<void> rollover() async { ... }` — implements RESEARCH §4 step 1..7 verbatim.
- Private helpers:
  - `(int status, int source) _resolveDay({required int usageMinutes, required int threshold, required bool? checkinAvoided, required bool a11yWasOn})` — exact 5-branch matrix per RESEARCH §4 pseudocode. Order matters: broken-by-system first, broken-by-self-report second, success branches, fallback no-checkin.
  - `Future<int> _usageMinutesForEntry(BlockListData entry, DateTime streakDayMidnight, int threshold)` — habit short-circuit (packageName null → 0); always-on returns dayTotal; scheduled returns dayTotal anchored to start-day per RESEARCH §4 simplification (sub-day bucketing deferred to v1.x per assumption A5).
  - `Future<bool> _a11yWasOnFor(DateTime day)` — conservative proxy: `return await ref.read(permissionStatusApiProvider).isAccessibilityServiceEnabled();`. Document in doc-comment that this is a current-state proxy, not historical; per-day accuracy deferred to v1.x with a `a11y_enabled_since` persisted timestamp.
- Soft-cache: instance field `DateTime? _lastCalled;`. At start of rollover: if `_lastCalled != null && DateTime.now().difference(_lastCalled!).inSeconds < 60` return; else set `_lastCalled = DateTime.now();`. Then proceed.
- Day stepping: `DateTime(cursor.year, cursor.month, cursor.day + 1)`. NEVER `cursor.add(Duration(days: 1))`. Inline-comment citing RESEARCH §4 DST table.
- Backfill cap (RESEARCH §10 R-7): if `today.difference(lastEvaluatedDay).inDays > 30`, write the gap days as status=2 incomplete-data in a single DAO batch, then start the per-day loop at `DateTime(today.year, today.month, today.day - 30)`.
- After per-day loop: for every entry, `dailyStreakDao.upsertIfAbsent(entryId, day: todayStreakDay, status: 3, source: 0, usageMinutesObserved: 0, evaluatedAt: now)`.
- Persist state: write all three StreakKeys ints to prefs.
- Imports: daily_streak_dao, daily_checkins_dao, daily_usage_summary_dao, block_list_dao, streak_day (helper), schedule_window (helper for STRK-09 future-proof), StreakKeys, permissionStatusApiProvider, Riverpod, shared_preferences.

(b) lib/domain/streak/streak_rollover_service_providers.dart — hand-written Riverpod (no codegen per pubspec.yaml):
- `final Provider<DailyCheckinsDao> dailyCheckinsDaoProvider` returning `ref.watch(databaseProvider).dailyCheckinsDao`.
- `final Provider<DailyStreakDao> dailyStreakDaoProvider` returning `ref.watch(databaseProvider).dailyStreakDao`.
- `final AsyncNotifierProvider<StreakRolloverService, void> streakRolloverServiceProvider = AsyncNotifierProvider<StreakRolloverService, void>(StreakRolloverService.new);`.
- `final FutureProviderFamily<({int current, int longest, bool breakDetectedToday}), int> streakBadgeProvider` — autoDispose. Implementation:
  - `final dao = ref.watch(dailyStreakDaoProvider);`
  - `final current = await dao.getCurrentStreakFor(entryId);`
  - `final longest = await dao.getLongestStreakFor(entryId);`
  - `final today = streakDayFor(DateTime.now());`
  - Read the most recent finalized row (status in {0,1,2}) for this entry via `dao.getFor(entryId, today)` first; if null, walk back via `dao.watchHistoryFor(entryId, days: 2)` first-emit or a dedicated `dao.getMostRecentFinalizedFor(entryId)` helper (use existing watchForEntry stream first-emit if no dedicated helper exists — pick whichever is cheapest given the DAO surface from Plan 05-02; no schema bump).
  - `final breakDetectedToday = (latestFinalized != null && latestFinalized.status == 1 && DateTime.now().difference(latestFinalized.evaluatedAt).inHours < 24);`
  - `return (current: current, longest: longest, breakDetectedToday: breakDetectedToday);`
- `final StreamProviderFamily<List<DailyStreakData>, int> streakHistoryProvider` returning `dao.watchHistoryFor(entryId, days: 30)`. autoDispose.

(c) android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/StreakDay.kt — Kotlin port of streakDayFor:
- `package com.nottodo.not_to_do_list.service`
- `fun streakDayFor(nowMs: Long): Long { ... }` — apply 4h subtraction then strip to local midnight via Calendar. Mirror the 4h shift logic from lib/domain/schedule/streak_day.dart byte-for-byte. Doc-comment header notes parity contract with Dart side; cited by StreakDayAnchoringTest.

(d) test/domain/streak/streak_rollover_service_test.dart — unskip; in-memory AppDatabase + buildClockMockApi from streak_fixture.dart. Tests named per RESEARCH §9 lock: `test_breaks_when_usage_exceeds_threshold`, `test_source_resolution_matrix`, `test_clock_tamper_flags_status_2`, `test_scheduled_window_anchoring`, plus `test_soft_cache_60s_idempotent`, `test_today_row_status_3_pending`, `test_backfill_cap_30_days_writes_incomplete`, `test_habit_entry_no_usage_returns_incomplete_when_no_checkin`. Inject the constructor clock for deterministic DateTime control.

In the SAME test file (streak_rollover_service_test.dart), add a group named "streakBadgeProvider 3-field record (D-05 break-detection wiring)" with at minimum these tests:
- `test_streak_badge_provider_returns_three_field_record_empty_state` — with no rows for the entry, `final r = await container.read(streakBadgeProvider(entryId).future); expect(r.current, 0); expect(r.longest, 0); expect(r.breakDetectedToday, false);`. Asserts the 3-field shape is contracted.
- `test_streak_badge_provider_returns_three_field_record_active_streak` — seed (today-1: status=0 source=0), (today-2: status=0 source=0), (today-3: status=0 source=0); expect `(current: 3, longest: 3, breakDetectedToday: false)`.
- `test_streak_badge_provider_break_detected_today_default_false_when_no_recent_break` — seed (today-2: status=1 broken evaluatedAt: 5 days ago); expect `breakDetectedToday: false` (too old).
- `test_streak_badge_provider_break_detected_today_true_when_recent_status_1` — seed (today's streakDay row: status=1 broken evaluatedAt: DateTime.now()); expect `breakDetectedToday: true`.

(e) test/domain/streak/streak_rollover_dst_test.dart — unskip with `test_spring_forward_23h_day` (DateTime(2026, 3, 8, 1, 30)) and `test_fall_back_25h_day` (DateTime(2025, 11, 2, 0, 30)) — mirror schedule_window_parity_test.dart GROUP 5 line 436 tuple shape.

(f) android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/StreakDayAnchoringTest.kt — unskip three @Test methods using JUnit 4 + Calendar.getInstance ms() helper from ScheduleWindowTest.kt:
- `crossMidnight_2200_to_0600_pointAt2300Mon_returnsMonMidnight()` — assert streakDayFor(ms-for-23:00-Mon) equals expected Mon midnight long.
- `crossMidnight_2200_to_0600_pointAt0300Tue_returnsMonMidnight()` — assert 03:00 Tue still anchors to Mon midnight via the 4h shift.
- `alwaysOnEntry_pointMidday_Tue_returnsTueMidnight()` — control case: midday returns its own calendar day midnight.

(g) Do NOT touch lib/data/database/app_database.dart — Plan 05-02 already registered DAOs; Drift's generated accessors (appDatabase.dailyCheckinsDao, appDatabase.dailyStreakDao) are consumed by the providers in (b).
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs ; flutter test test/domain/streak/streak_rollover_service_test.dart test/domain/streak/streak_rollover_dst_test.dart ; pushd android ; ./gradlew :app:testDebugUnitTest --tests "com.nottodo.not_to_do_list.service.StreakDayAnchoringTest" --tests "com.nottodo.not_to_do_list.service.ScheduleWindowTest" ; popd ; flutter test test/policy/play_invariants_test.dart ; flutter analyze lib/domain/streak/</automated>
  </verify>
  <acceptance_criteria>
    - `dart run build_runner build --delete-conflicting-outputs` exits 0 (no codegen regressions).
    - `flutter test test/domain/streak/streak_rollover_service_test.dart test/domain/streak/streak_rollover_dst_test.dart` exits 0; every behavior assertion above is a passing test; no skip remains in either file.
    - `cd android && ./gradlew :app:testDebugUnitTest --tests "*.StreakDayAnchoringTest"` BUILD SUCCESSFUL with all @Test methods passing (no @Ignore).
    - `cd android && ./gradlew :app:testDebugUnitTest --tests "*.ScheduleWindowTest"` still BUILD SUCCESSFUL (Phase 4 oracle untouched).
    - `flutter test test/policy/play_invariants_test.dart` 9/9 invariants green.
    - `flutter analyze lib/domain/streak/` shows 0 errors / 0 warnings.
    - `grep -c 'cursor.add(Duration' lib/domain/streak/streak_rollover_service.dart` == 0 (DST-unsafe pattern banned).
    - `grep -c 'DateTime(cursor.year, cursor.month, cursor.day + 1)' lib/domain/streak/streak_rollover_service.dart` >= 1 (DST-safe pattern present).
    - `grep -c '60' lib/domain/streak/streak_rollover_service.dart` >= 1 in soft-cache region (commented as `// soft-cache 60s`).
    - `grep -c '24' lib/domain/streak/streak_rollover_service.dart` >= 1 in clock-tamper region (Duration(hours: 24).inMilliseconds threshold).
    - File `android/app/src/main/kotlin/com/nottodo/not_to_do_list/service/StreakDay.kt` exists with `fun streakDayFor(` exported.
    - `grep -c 'breakDetectedToday' lib/domain/streak/streak_rollover_service_providers.dart` >= 1 (3-field record landed in this plan, not 05-07).
    - `grep -c 'int current, int longest, bool breakDetectedToday' lib/domain/streak/streak_rollover_service_providers.dart` >= 1 (exact record shape locked in source).
    - `grep -c 'breakDetectedToday' test/domain/streak/streak_rollover_service_test.dart` >= 4 (4 dedicated tests).
  </acceptance_criteria>
  <done>StreakRolloverService lands with the 2x2 matrix, DST safety, clock-tamper detection, soft-cache, and 30-day cap. Kotlin parity oracle PASSES. Wave 0 RED stubs flipped to GREEN. streakBadgeProvider exports the 3-field record `(current, longest, breakDetectedToday)` with the break-detection computation wired — Plan 05-07 consumes the field without mutating the provider. Plan 05-04 can extend Pigeon API; Plan 05-07 can wire resumed-trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart-side wall clock (DateTime.now) ↔ device wall clock | Trusted device API; user-mutable; STRK-06 detects via boot-monotonic divergence |
| Pigeon bootMonotonicNanos call ↔ Kotlin SystemClock | Trusted platform API; not user-mutable; STRK-06 baseline |
| Cross-process FlutterSharedPreferences ↔ Kotlin BootReceiver | NOT crossed by this plan — Plan 05-05 owns the receiver side; this plan only writes Dart-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | User advances wall clock by >24h to fake streak days | mitigate | STRK-06 — boot-monotonic vs wall-clock divergence > 24h → all rolled days flagged status=2 incomplete-data; tests `test_clock_tamper_flags_status_2` lock the behavior |
| T-05-08 | Tampering | Future plan adds `cursor.add(Duration(days: 1))` regressing DST safety | mitigate | acceptance grep-gate `grep -c 'cursor.add(Duration' == 0` + 23h/25h DST tests in streak_rollover_dst_test.dart |
| T-05-09 | Denial of service | User leaves app idle for 60 days; rollover spins on 1200 day-rows | mitigate | 30-day backfill cap (RESEARCH §10 R-7); gap days written status=2 incomplete-data in single batch |
| T-05-10 | Information disclosure | `_a11yWasOnFor` proxy uses current state, leaking false-positives that days were tracked when they weren't | accept | Documented v1.x followup; conservative fallback to status=2 incomplete-data when no checkin and no current a11y; honest framing per "incomplete-data" not "broken" |
| T-05-11 | Repudiation | Cross-process key drift between Dart `'reminder_hour_minute'` and Kotlin `'flutter.reminder_hour_minute'` | mitigate | This plan defers to Plan 05-05 which owns the Kotlin reader; key constant locked in StreakKeys (Plan 05-02); REL-05 manual test (Plan 05-09) verifies end-to-end |
</threat_model>

<verification>
- StreakRolloverService compiles, unit tests are GREEN, Kotlin parity oracle GREEN.
- No DST-unsafe `Duration(days: 1)` arithmetic in the service.
- 2x2 matrix exhaustive coverage in `test_source_resolution_matrix`.
- Soft-cache + 30-day cap both verified by their own tests.
- streakBadgeProvider's 3-field record contract `(current, longest, breakDetectedToday)` is locked in source + tests.
</verification>

<success_criteria>
- All 6 STRK requirements (STRK-02, 04, 05, 06, 08, 09) have at least one GREEN test.
- 4 RED Wave 0 stubs flip to GREEN (2 Dart streak tests + 1 Kotlin parity test + the in-line behavior assertions).
- streakBadgeProvider 3-field record shape ready for Plan 05-07 to consume without provider mutation.
- No PLAY-02 regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-03-SUMMARY.md`
</output>
