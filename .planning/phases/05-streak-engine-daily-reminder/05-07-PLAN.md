---
phase: 05-streak-engine-daily-reminder
plan: 07
type: execute
wave: 5
depends_on: [05-02, 05-03, 05-04]
files_modified:
  - lib/features/streak/widgets/streak_badge.dart
  - lib/features/streak/widgets/day_dot.dart
  - lib/features/streak/widgets/streak_history_section.dart
  - lib/features/home/widgets/block_list_row.dart
  - lib/features/list/pages/edit_entry_screen.dart
  - lib/features/reminder/widgets/reminder_off_banner.dart
  - lib/features/home/pages/home_screen.dart
  - lib/features/health/widgets/_health_lifecycle_observer.dart
  - test/features/streak/streak_badge_test.dart
  - test/features/streak/streak_history_section_test.dart
  - test/features/home/reminder_off_banner_test.dart
autonomous: true
requirements:
  - STRK-05
  - STRK-07
  - NOTF-07
must_haves:
  truths:
    - "StreakBadge consumes streakBadgeProvider(entryId) — the 3-field record `({int current, int longest, bool breakDetectedToday})` is ALREADY exported by Plan 05-03's providers file; this plan does NOT mutate the provider signature and does NOT list streak_rollover_service_providers.dart in files_modified."
    - "StreakBadge renders Text('🔥 {current} · best {longest}') with style labelSmall — pinned to 14sp/w400 per UI-SPEC §Typography line 76; color = onSurface when current > 0, else onSurfaceVariant."
    - "StreakBadge day-0 renders '🔥 0 · best 0' verbatim (D-15) — no CTA, no hidden badge."
    - "BlockListRow trailing slot replaces the existing Text('—') placeholder with StreakBadge(entryId: entry.id) — exactly that, no layout shift."
    - "DayDot widget renders the 4-state UI-SPEC §Color 4-State table tokens: green Color(0xFF2E7D32) / 0xFF66BB6A dark filled circle 8px, blue Color(0xFF1565C0) / 0xFF64B5F6 dark outlined 1.5px stroke, red Color(0xFFC62828) / 0xFFEF9A9A dark filled + × overlay, grey Color(0xFF757575) / 0xFF9E9E9E dark outlined + — overlay wrapped in Tooltip(message: 'Tracking was off this day') — MANDATORY tooltip on grey per D-07."
    - "StreakHistorySection renders Divider, SizedBox(16), Text('Streak history', titleMedium), SizedBox(12), GridView (NeverScrollableScrollPhysics, shrinkWrap, crossAxisCount: 7, childAspectRatio: 1, mainAxisSpacing: 4, crossAxisSpacing: 4) of last 30 days as DayDot widgets, SizedBox(12), Text('{current} days · best {longest}', bodyMedium, onSurfaceVariant), SizedBox(4), Text('Streak breaks if you use this app over 5 min/day (change in Settings)', labelSmall, onSurfaceVariant)."
    - "EditEntryScreen body inserts the StreakHistorySection between the existing ScheduleEditor (around line 179-184) and the final SizedBox/Save row — surgical splice."
    - "ReminderOffBanner copies the HealthCheckBanner Material+InkWell+amber-tokens shell verbatim (lines 28-101 analog) but swaps icon to Icons.notifications_off_outlined and copy to 'Reminder is off — tap to fix'. NO expand toggle, NO dismiss button. Tap behavior: if postNotificationsRationaleState == 'permanently_denied' → call openAppNotificationSettings (Pigeon); else → context.go('/onboarding/permissions/notifications') (route added by Plan 05-08)."
    - "HomeScreen body inserts a second AnimatedSwitcher BELOW the existing HealthCheckBanner that wraps ReminderOffBanner conditional on postNotificationsGrantedProvider being not-granted. Banner stack order: tracking-offline (Phase 3) ABOVE reminder-off (Phase 5) per D-12."
    - "HealthLifecycleObserver (existing Phase 2 file lib/features/health/widgets/_health_lifecycle_observer.dart) gains TWO new `unawaited(ref.read(...).rollover() / refresh())` calls in the AppLifecycleState.resumed branch: streakRolloverServiceProvider.rollover() AND postNotificationsGrantedProvider.notifier.refresh()."
    - "9 PLAY-02 invariants stay green; all Phase 5 widget tests now GREEN."
  artifacts:
    - path: "lib/features/streak/widgets/streak_badge.dart"
      provides: "Per-entry streak badge widget — consumes streakBadgeProvider's 3-field record (read-only)"
      contains: "class StreakBadge"
    - path: "lib/features/streak/widgets/day_dot.dart"
      provides: "4-state DayDot widget with mandatory tooltip on grey"
      contains: "class DayDot"
    - path: "lib/features/streak/widgets/streak_history_section.dart"
      provides: "30-day GridView + summary text + threshold help text"
      contains: "class StreakHistorySection"
    - path: "lib/features/reminder/widgets/reminder_off_banner.dart"
      provides: "NOTF-07 banner with three-state tap handler"
      contains: "class ReminderOffBanner"
  key_links:
    - from: "lib/features/streak/widgets/streak_badge.dart"
      to: "lib/domain/streak/streak_rollover_service_providers.dart"
      via: "ref.watch(streakBadgeProvider(entryId)) — consumes the 3-field record exported by Plan 05-03"
      pattern: "streakBadgeProvider"
    - from: "lib/features/home/pages/home_screen.dart"
      to: "lib/features/reminder/widgets/reminder_off_banner.dart + lib/features/onboarding/providers/post_notifications_provider.dart"
      via: "AnimatedSwitcher wrapping ReminderOffBanner gated on postNotificationsGrantedProvider"
      pattern: "postNotificationsGrantedProvider|ReminderOffBanner"
    - from: "lib/features/health/widgets/_health_lifecycle_observer.dart"
      to: "lib/domain/streak/streak_rollover_service_providers.dart"
      via: "unawaited(ref.read(streakRolloverServiceProvider.notifier).rollover()) on resumed"
      pattern: "streakRolloverServiceProvider.*rollover"
---

# Plan 05-07 — Wave 5: StreakBadge + StreakHistorySection + ReminderOffBanner + lazy-rollover hook on resume

<objective>
Land the Home + Entry-detail UI surfaces for the streak engine plus the NOTF-07 reminder-off banner, and wire the lazy-rollover trigger into the existing HealthLifecycleObserver so streak evaluation runs on every `AppLifecycleState.resumed` (STRK-05).

All 7 visible artifacts are pattern-mapped to existing analogs (PATTERNS.md §Widgets/Screens) — copy/paste with surgical swaps. Every visible string is in UI-SPEC §Copywriting Contract; the Wave 0 test stubs (Plan 05-01) already assert these literals.

This plan is parallel-safe with Plan 05-06 within Wave 5 (no file overlap). Plan 05-08 is sequentialized to Wave 6 because it shares app_router.dart with 05-06 and home_screen.dart with 05-07.

**Provider contract:** `streakBadgeProvider` exports a 3-field record `({int current, int longest, bool breakDetectedToday})` from Plan 05-03. This plan CONSUMES the third field for the D-05 strikethrough render path — it does NOT mutate the provider's signature. The file `lib/domain/streak/streak_rollover_service_providers.dart` is intentionally absent from this plan's `files_modified`.

Purpose: deliver the Home + Detail visible surfaces of the streak/reminder engine.
Output: 4 new widget files + 3 modified screen files + 1 modified lifecycle observer + 3 GREEN unskipped test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md
@.planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md
@.planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md
@.planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md
@lib/features/home/widgets/block_list_row.dart
@lib/features/home/pages/home_screen.dart
@lib/features/list/pages/edit_entry_screen.dart
@lib/features/health/widgets/health_check_banner.dart
@lib/features/health/widgets/_health_lifecycle_observer.dart
@lib/features/dashboard/widgets/dashboard_row.dart
@lib/domain/streak/streak_rollover_service.dart
@lib/domain/streak/streak_rollover_service_providers.dart
@lib/features/onboarding/providers/post_notifications_provider.dart
@lib/domain/providers/permission_status_api_provider.dart
@test/features/home/health_banner_test.dart
</context>

<interfaces>
<!-- Provider contracts + copy lock. Widget tests assert these literals exactly. -->

From lib/domain/streak/streak_rollover_service_providers.dart (Plan 05-03 — READ-ONLY in this plan):
- streakBadgeProvider(entryId) → ({int current, int longest, bool breakDetectedToday})
- streakHistoryProvider(entryId) → Stream<List<DailyStreakData>> (last 30 days, asc)
- streakRolloverServiceProvider → AsyncNotifier<void> with .rollover() method
- All three providers are exported by Plan 05-03; this plan only consumes them.

From lib/features/onboarding/providers/post_notifications_provider.dart (Plan 05-04):
- postNotificationsGrantedProvider → AsyncNotifierProvider<PostNotificationsGrantedNotifier, bool>

From lib/domain/providers/permission_status_api_provider.dart (Phase 2):
- permissionStatusApiProvider → Provider<PermissionStatusApi> (extended with postNotificationsRationaleState + openAppNotificationSettings in Plan 05-04)

From lib/features/health/widgets/health_check_banner.dart (analog — copy file shape):
- Material(color: bg)+InkWell(onTap: ...)+Padding+Row pattern lines 28-101
- amber tokens: _amberBgLight = Color(0xFFFFF3CD), _amberBgDark = Color(0xFF3D2E00), _amberFgLight = Color(0xFF856404), _amberFgDark = Color(0xFFFFD966)

From lib/features/home/widgets/block_list_row.dart:
- Existing line 53: `trailing: Text('—', ...)` — replaced verbatim by StreakBadge

UI-SPEC §Color 4-state day-dot tokens (UI-SPEC lines 109-114):
- green ✓: 0xFF2E7D32 / 0xFF66BB6A — filled circle 8px
- blue ○: 0xFF1565C0 / 0xFF64B5F6 — outlined 1.5px stroke
- red ×: 0xFFC62828 / 0xFFEF9A9A — filled + × overlay
- grey —: 0xFF757575 / 0xFF9E9E9E — outlined + — overlay + Tooltip MANDATORY

UI-SPEC §Copywriting Contract:
- Streak badge: "🔥 {current} · best {longest}" — exact characters (flame, middle dot U+00B7)
- Day-0: "🔥 0 · best 0"
- Section label: "Streak history"
- Summary text: "{N} days · best {M}"
- Threshold help: "Streak breaks if you use this app over 5 min/day (change in Settings)"
- Reminder banner: "Reminder is off — tap to fix"
- Grey tooltip: "Tracking was off this day"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land StreakBadge + DayDot + StreakHistorySection + ReminderOffBanner; wire BlockListRow + EditEntryScreen + HomeScreen + HealthLifecycleObserver; flip 3 Wave 0 widget tests to GREEN</name>
  <files>lib/features/streak/widgets/streak_badge.dart, lib/features/streak/widgets/day_dot.dart, lib/features/streak/widgets/streak_history_section.dart, lib/features/home/widgets/block_list_row.dart, lib/features/list/pages/edit_entry_screen.dart, lib/features/reminder/widgets/reminder_off_banner.dart, lib/features/home/pages/home_screen.dart, lib/features/health/widgets/_health_lifecycle_observer.dart, test/features/streak/streak_badge_test.dart, test/features/streak/streak_history_section_test.dart, test/features/home/reminder_off_banner_test.dart</files>
  <read_first>
    .planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md §Screen 2 (badge), §Screen 3 (history section), §Screen 5 (reminder banner), §Copywriting Contract (lines 314-337 — every literal),
    .planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md (D-05 strikethrough, D-07 4-state dots + grey tooltip mandatory, D-12 banner three-state, D-13..D-16 home badge + history detail),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §6 (banner three-state branch + openAppNotificationSettings path), §8 (Riverpod provider contracts),
    .planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md §"lib/features/streak/widgets/streak_badge.dart" + §"lib/features/streak/widgets/day_dot.dart" + §"streak_history_section.dart" + §"reminder_off_banner.dart" (analogs spelled out),
    lib/domain/streak/streak_rollover_service_providers.dart (Plan 05-03 — confirm the 3-field record is already exported; consume only, do NOT modify),
    lib/features/home/widgets/block_list_row.dart (existing trailing slot — line 53 replaced),
    lib/features/home/pages/home_screen.dart (existing AnimatedSwitcher around HealthCheckBanner lines 42-51 — second AnimatedSwitcher inserted just below per D-12),
    lib/features/list/pages/edit_entry_screen.dart (lines 179-184 around ScheduleEditor — insert section just after),
    lib/features/health/widgets/health_check_banner.dart (analog for ReminderOffBanner — copy lines 28-101 verbatim, swap icon + copy),
    lib/features/health/widgets/_health_lifecycle_observer.dart (lines 42-52 — existing resumed branch with 2 unawaited calls; add 2 more for streak + post_notifications),
    lib/features/onboarding/providers/post_notifications_provider.dart (Plan 05-04 provider),
    lib/domain/providers/permission_status_api_provider.dart (Phase 2 + Plan 05-04 extension),
    test/features/home/health_banner_test.dart (analog ProviderScope + GoRouter scaffold for the 3 widget tests)
  </read_first>
  <behavior>
    - StreakBadge ConsumerWidget rendering `Text('🔥 ${d.current} · best ${d.longest}', style: labelSmall, color: d.current > 0 ? onSurface : onSurfaceVariant)` from `streakBadgeProvider(entryId)`. Loading/error state falls back to `Text('—', labelSmall, onSurfaceVariant)`.
    - On D-05 break-detection day, when `streakBadgeProvider`'s `breakDetectedToday == true`, render `Text.rich` with two TextSpan children — first child has `TextDecoration.lineThrough` applied to the just-broken count visualization (e.g., the prior streak number); the rest of the badge renders plain. When `breakDetectedToday == false`, render the standard Text format. The `breakDetectedToday` field is ALREADY provided by Plan 05-03's `streakBadgeProvider`. This plan does NOT modify `lib/domain/streak/streak_rollover_service_providers.dart`.
    - BlockListRow.trailing — replace line 53 `Text('—', ...)` with `StreakBadge(entryId: entry.id)`. NO other change to BlockListRow.
    - DayDot widget: takes `({DateTime day, int status, int source})` plus theme brightness. Renders 24×24 container with centered 8px dot styled per the 4-state token table. For grey (status=2 incomplete-data), wrap in Tooltip(message: 'Tracking was off this day', child: ...). For today (pending, status=3) render only the date number — no circle. For empty (day before entry creation) render SizedBox(24, 24). Add Semantics(label: '{formatted-date} — {state}') per UI-SPEC §Accessibility.
    - StreakHistorySection ConsumerWidget: consumes streakHistoryProvider(entryId). Layout per UI-SPEC §Screen 3 lines 213-229 — Divider, SizedBox(16), titleMedium label "Streak history", SizedBox(12), GridView (NeverScrollableScrollPhysics, shrinkWrap, crossAxisCount: 7, childAspectRatio: 1, mainAxisSpacing/crossAxisSpacing: 4) of last 30 DayDot widgets, SizedBox(12), summary "{N} days · best {M}" (bodyMedium, onSurfaceVariant), SizedBox(4), threshold help text (labelSmall, onSurfaceVariant), SizedBox(16).
    - EditEntryScreen: surgical splice — find the line where ScheduleEditor block ends (around line 184); insert AFTER it: `const SizedBox(height: 24), const Divider(), const SizedBox(height: 16), StreakHistorySection(entryId: draft.id), const SizedBox(height: 16),`.
    - ReminderOffBanner StatelessWidget (or ConsumerWidget for the tap handler): copy HealthCheckBanner lines 28-101 verbatim, then swap:
      - Constant tokens unchanged.
      - Icon: Icons.notifications_off_outlined (UI-SPEC §Screen 5 line 292).
      - Copy: 'Reminder is off — tap to fix'.
      - Remove the expand toggle (HealthCheckBanner has it — Phase 5 banner omits per UI-SPEC line 299).
      - Tap handler: `final state = await ref.read(permissionStatusApiProvider).postNotificationsRationaleState(); if (state == 'permanently_denied') { await ref.read(permissionStatusApiProvider).openAppNotificationSettings(); } else { context.go('/onboarding/permissions/notifications'); }` (route added by Plan 05-08).
    - HomeScreen body: insert a second AnimatedSwitcher BELOW the existing HealthCheckBanner AnimatedSwitcher (lines 42-51). The new switcher's child reads `postNotificationsGrantedProvider.maybeWhen(data: (g) => g ? SizedBox.shrink() : const ReminderOffBanner(), orElse: () => SizedBox.shrink())`. Stack order: tracking-offline (Phase 3) above reminder-off (Phase 5) per D-12.
    - HealthLifecycleObserver.didChangeAppLifecycleState resumed branch (existing lines 42-52): add 2 new `unawaited(ref.read(streakRolloverServiceProvider.notifier).rollover());` AND `unawaited(ref.read(postNotificationsGrantedProvider.notifier).refresh());` alongside the existing 2 unawaited calls. Verify dispose path removes observer (existing).
    - test/features/streak/streak_badge_test.dart unskip: tests verifying badge format for (current=3, longest=12, breakDetectedToday=false) → finds '🔥 3 · best 12'; (current=0, longest=0, breakDetectedToday=false) → finds '🔥 0 · best 0'; on (current=0, longest=3, breakDetectedToday=true) → finds strikethrough TextDecoration via Finder.byWidgetPredicate(byTextDecoration: TextDecoration.lineThrough). Override `streakBadgeProvider(entryId).overrideWith((ref) async => (current: X, longest: Y, breakDetectedToday: Z))` in ProviderScope — this asserts the 3-field shape via the test's construction itself.
    - test/features/streak/streak_history_section_test.dart unskip: override streakHistoryProvider to a list of 30 DailyStreakData with mixed statuses (8 green, 5 blue, 3 red, 2 grey, rest pending); assert exactly 8 green dots, 5 blue, 3 red, 2 grey; tap a grey dot and `expect(find.byTooltip('Tracking was off this day'), findsOneWidget)`; assert summary text 'X days · best Y' (with X, Y derived from streak math).
    - test/features/home/reminder_off_banner_test.dart unskip: tests using ProviderScope + GoRouter scaffold; override postNotificationsGrantedProvider with AsyncValue.data(false) → expect find.text('Reminder is off — tap to fix') findsOneWidget; override with data(true) → expect find.byType(ReminderOffBanner) findsNothing; tap when rationale state = 'permanently_denied' → expect mock openAppNotificationSettings called; tap when state = 'rationale' or 'grantable' → expect router.location == '/onboarding/permissions/notifications'.
  </behavior>
  <action>
Implement 4 new widget files, modify 4 existing files, flip 3 tests GREEN. **Do NOT modify `lib/domain/streak/streak_rollover_service_providers.dart`** — Plan 05-03 already shipped the 3-field `streakBadgeProvider` record.

(a) lib/features/streak/widgets/streak_badge.dart — ConsumerWidget per PATTERNS.md lines 440-465. Read `final badge = ref.watch(streakBadgeProvider(entryId));` and unpack the 3 fields `badge.current`, `badge.longest`, `badge.breakDetectedToday`. Render Text with the literal format. Handle Loading/error gracefully (— placeholder). When `badge.breakDetectedToday == true`, render via `Text.rich` with the first TextSpan styled `TextStyle(decoration: TextDecoration.lineThrough)` (visualizing the just-broken streak) and subsequent spans plain.

(b) lib/features/streak/widgets/day_dot.dart — StatelessWidget. Constructor: `DayDot({required this.day, required this.status, required this.source})`. 24×24 outer Container, centered 8px inner. Switch on status: 0+source=0 → green filled, 0+source=1 → blue outlined, 1 → red filled with × overlay text, 2 → grey outlined with — overlay text WRAPPED in Tooltip, 3 → date number Text only, edge-case empty → SizedBox(24, 24). Wrap entire DayDot in Semantics(label: ...).

(c) lib/features/streak/widgets/streak_history_section.dart — ConsumerWidget. Consume streakHistoryProvider(entryId). Compute current + longest via the same DAO helpers (or read from streakBadgeProvider for consistency — ignore the `breakDetectedToday` field for the summary text; the strikethrough render is in the badge only). Render layout per UI-SPEC §Screen 3 lines 213-229.

(d) lib/features/home/widgets/block_list_row.dart — surgical edit. Replace exact `trailing: Text('—', ...)` at line 53 with `trailing: StreakBadge(entryId: entry.id)`. Add the import.

(e) lib/features/list/pages/edit_entry_screen.dart — surgical splice as in behavior section.

(f) lib/features/reminder/widgets/reminder_off_banner.dart — copy HealthCheckBanner shell + tap handler implementing the three-state branch.

(g) lib/features/home/pages/home_screen.dart — insert second AnimatedSwitcher per behavior section.

(h) lib/features/health/widgets/_health_lifecycle_observer.dart — append 2 unawaited calls in the resumed branch. Verify the existing imports include flutter_riverpod + streakRolloverServiceProvider + postNotificationsGrantedProvider; add them if not.

(i) Unskip + fill the 3 Wave 0 tests with assertions tied to UI-SPEC literals.

PLAY-02 lock: no new Kotlin in this plan; no risk.
NOTF-07 lock: banner is the only path to re-prompt after permanently_denied (RESEARCH §6 + R-5).
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs ; flutter test test/features/streak/streak_badge_test.dart test/features/streak/streak_history_section_test.dart test/features/home/reminder_off_banner_test.dart ; flutter analyze lib/features/streak/ lib/features/reminder/ lib/features/home/ lib/features/list/ lib/features/health/ ; flutter test test/policy/play_invariants_test.dart test/policy/phase_5_invariants_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/streak/streak_badge_test.dart test/features/streak/streak_history_section_test.dart test/features/home/reminder_off_banner_test.dart` exits 0; no skip remaining in any of the 3 files.
    - `flutter analyze lib/features/streak/ lib/features/reminder/ lib/features/home/ lib/features/list/ lib/features/health/` shows 0 errors / 0 warnings.
    - `grep -c "🔥" lib/features/streak/widgets/streak_badge.dart` >= 1 (literal flame in template).
    - `grep -c "best" lib/features/streak/widgets/streak_badge.dart` >= 1 (literal in template — UI-SPEC copy lock).
    - `grep -c "breakDetectedToday" lib/features/streak/widgets/streak_badge.dart` >= 1 (D-05 strikethrough field consumed from Plan 05-03's record).
    - `grep -c "Tracking was off this day" lib/features/streak/widgets/day_dot.dart` >= 1 (Tooltip message).
    - `grep -c "Streak history" lib/features/streak/widgets/streak_history_section.dart` >= 1 (section label).
    - `grep -c "Streak breaks if you use this app over 5 min/day" lib/features/streak/widgets/streak_history_section.dart` >= 1 (threshold help — exact copy).
    - `grep -c "Reminder is off — tap to fix" lib/features/reminder/widgets/reminder_off_banner.dart` >= 1 (banner copy with em dash).
    - `grep -c "Icons.notifications_off_outlined" lib/features/reminder/widgets/reminder_off_banner.dart` >= 1.
    - `grep -c "StreakBadge(entryId: entry.id)" lib/features/home/widgets/block_list_row.dart` >= 1 (trailing slot wired).
    - `grep -c "StreakHistorySection(entryId" lib/features/list/pages/edit_entry_screen.dart` >= 1 (section inserted).
    - `grep -c "streakRolloverServiceProvider" lib/features/health/widgets/_health_lifecycle_observer.dart` >= 1 (rollover trigger on resume — STRK-05 lock).
    - `grep -c "postNotificationsGrantedProvider" lib/features/health/widgets/_health_lifecycle_observer.dart` >= 1 (banner refresh on resume).
    - `grep -c "ReminderOffBanner" lib/features/home/pages/home_screen.dart` >= 1 (banner mounted on Home).
    - `git diff --name-only HEAD~1 -- lib/domain/streak/streak_rollover_service_providers.dart 2>/dev/null | wc -l` == 0 OR `git diff HEAD~1 -- lib/domain/streak/streak_rollover_service_providers.dart 2>/dev/null | wc -l` == 0 (Plan 05-07 does NOT touch the providers file — Plan 05-03 already owns the 3-field record).
    - `flutter test test/policy/play_invariants_test.dart` 9/9 green.
    - `flutter test test/policy/phase_5_invariants_test.dart` all invariants green.
  </acceptance_criteria>
  <done>Home + entry-detail surfaces ship; streak badge is the only reinforcement loop visible; reminder-off banner gates re-prompt path; lazy rollover triggers on every resume; UI-SPEC copy locks held by tests. streakBadgeProvider's 3-field record (locked by Plan 05-03) is consumed read-only — no provider mutation in this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streak badge text rendered to UI ↔ Lock-screen observers | Badge format contains only counts (numbers) + flame emoji; no entry names; no PII |
| Reminder banner tap ↔ ACTION_APP_NOTIFICATION_SETTINGS intent | Pigeon-backed openAppNotificationSettings (Plan 05-04) — uses launchSettingsOrFallback with resolveActivity guard |
| HealthLifecycleObserver ↔ streakRolloverServiceProvider | Lazy trigger only — service has internal 60s soft-cache to prevent storm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-27 | Information disclosure | Streak badge rendered on lock-screen via notification preview | accept | Badge appears only in-app; D-10 lock on notification body prevents leakage via notifications |
| T-05-28 | Tampering | Future plan adds dismiss button to reminder banner, bypassing the re-prompt-only re-grant path | mitigate | acceptance grep: file MUST NOT contain "InkWell(.*onTap.*dismiss" or "IconButton.*close" patterns — verified by Plan 05-09 phase exit grep |
| T-05-29 | Denial of service | Streak rollover fired on every resume causes UI jank | mitigate | StreakRolloverService 60s soft-cache (Plan 05-03) — no-op within window |
| T-05-30 | Spoofing | Grey dot tooltip displays attacker-controlled text via fake DailyStreakData status | accept | DailyStreakData is written only by StreakRolloverService — DAO + service code-path are project-controlled; no external write surface |
</threat_model>

<verification>
- Badge + History + Banner all render with UI-SPEC literal copy.
- BlockListRow trailing slot wired.
- HealthLifecycleObserver triggers rollover + post-notifications refresh on resume.
- All Wave 0 widget tests GREEN.
- streakBadgeProvider's 3-field record is consumed by the badge; the providers file is NOT modified by this plan.
</verification>

<success_criteria>
- STRK-05 lazy-on-resume verified via lifecycle observer.
- STRK-07 home badge + history rendered.
- NOTF-07 banner gates re-prompt path with three-state.
- No PLAY-02 regression.
- No contract drift on streakBadgeProvider (Plan 05-03 owns the signature; Plan 05-07 consumes only).
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-07-SUMMARY.md`
</output>
