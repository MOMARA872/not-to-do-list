---
phase: 05-streak-engine-daily-reminder
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/data/dao/daily_streak_dao_test.dart
  - test/data/dao/daily_checkins_dao_test.dart
  - test/domain/streak/streak_rollover_service_test.dart
  - test/domain/streak/streak_rollover_dst_test.dart
  - test/features/streak/streak_badge_test.dart
  - test/features/streak/streak_history_section_test.dart
  - test/features/checkin/checkin_screen_test.dart
  - test/features/reminder/reminder_settings_test.dart
  - test/features/reminder/deep_link_navigation_test.dart
  - test/features/onboarding/post_notifications_earned_test.dart
  - test/features/home/reminder_off_banner_test.dart
  - test/policy/phase_5_invariants_test.dart
  - test/_fixtures/streak_fixture.dart
  - android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/StreakDayAnchoringTest.kt
  - .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md
autonomous: true
requirements:
  - STRK-01
  - STRK-02
  - STRK-03
  - STRK-04
  - STRK-05
  - STRK-06
  - STRK-07
  - STRK-08
  - STRK-09
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04
  - NOTF-05
  - NOTF-06
  - NOTF-07
must_haves:
  truths:
    - "Every Phase 5 REQ-ID (STRK-01..09 + NOTF-01..07) has at least one stubbed test file with a named group declaration."
    - "test/_fixtures/streak_fixture.dart exposes buildClockMockApi({bootMonotonicNanos}) returning a MockPermissionStatusApi stub for STRK-06 tests."
    - "test/policy/phase_5_invariants_test.dart enforces NOTF-05 BootReceiver declaration, no USE_EXACT_ALARM, no FCM/firebase imports, no PeriodicWorkRequest in any android/.../streak/ Kotlin file."
    - "android/app/src/test/.../StreakDayAnchoringTest.kt stubs the cross-midnight (22:00–06:00) anchoring oracle, marked @Ignore until Plan 05-03 ships streakDayFor parity logic."
    - "05-VERIFICATION.md exists with REL-05 sign-off template (device manufacturer/model, Android version, idle duration, alarm-fire delta, BOOT_COMPLETED re-arm outcome — fields blank until Plan 05-09)."
    - "flutter test exits 0 — every new test file is either skip: 'Plan 05-NN fills' or contains only fixture/grep code that already passes."
    - "test/policy/play_invariants_test.dart 9/9 PLAY-02 invariants stay green (Wave 0 adds no Dart source under lib/ except fixtures already exempted, and no Kotlin under android/app/src/main/)."
  artifacts:
    - path: "test/_fixtures/streak_fixture.dart"
      provides: "Shared mock clock api + 2x2 (usageMinutes, checkin, a11yWasOn) → (status, source) tuples"
      contains: "buildClockMockApi"
    - path: "test/policy/phase_5_invariants_test.dart"
      provides: "Source-tree absence-greps locking STRK-05 (no WorkManager periodic for streak), NOTF-05 (BootReceiver declared), PLAY (no USE_EXACT_ALARM), zero-telemetry (no FCM, no firebase)"
      contains: "phase_5_invariants_test"
    - path: ".planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md"
      provides: "REL-05 OEM-survival overnight gate sign-off template — populated by Plan 05-09"
      contains: "REL-05"
    - path: "android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/StreakDayAnchoringTest.kt"
      provides: "JVM oracle for cross-midnight day-anchoring; @Ignore until Plan 05-03 lands"
  key_links:
    - from: "test/_fixtures/streak_fixture.dart"
      to: "test/domain/streak/* + test/features/streak/* + test/features/checkin/*"
      via: "import '../_fixtures/streak_fixture.dart'"
      pattern: "import.*_fixtures/streak_fixture.dart"
    - from: "test/policy/phase_5_invariants_test.dart"
      to: "android/app/src/main/AndroidManifest.xml + lib/ + android/app/src/main/kotlin/"
      via: "File.readAsStringSync() + Directory.listSync(recursive)"
      pattern: "phase_5_invariants_test"
---

# Plan 05-01 — Wave 0: RED stubs + REL-05 protocol skeleton + Kotlin parity oracle + policy invariants

<objective>
Stub all 14 Phase 5 test files (the full Wave 0 list in 05-VALIDATION.md) plus the shared streak fixture, the Phase 5 policy invariants test, the JVM cross-midnight anchoring oracle, and the 05-VERIFICATION.md REL-05 sign-off template — so the testable surface is locked before any production code lands. Mirrors Phase 4 Plan 04-01 exactly.

Per Karpathy "Simplicity First": each stub MUST minimally compile. NO speculative test bodies. Use `skip: 'Plan 05-NN fills'` with the exact downstream plan number. The Wave 0 invariant is "the suite stays green" + "every Phase 5 REQ-ID has a named `group(...)` somewhere in tests/".

Per CLAUDE.md "Goal-Driven Execution": every subsequent wave becomes "make these tests pass," not "build a feature."

Purpose: lock the test contract for Phase 5; create REL-05 sign-off file consumed by Plan 05-09.
Output: 14 test stubs + 1 verification skeleton, all referenced in 05-VALIDATION.md "Wave 0 Requirements" checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md
@.planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md
@.planning/phases/05-streak-engine-daily-reminder/05-VALIDATION.md
@.planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md
@.planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md
@.planning/phases/04-pause-ux-the-wedge/04-01-PLAN.md
@.planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md
@test/policy/play_invariants_test.dart
@test/policy/phase_4_invariants_test.dart
@test/_fixtures/permission_status_mock.dart
@test/features/home/health_banner_test.dart
@test/data/repositories/pause_event_repository_test.dart
@test/domain/schedule/schedule_window_parity_test.dart
@android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindowTest.kt
@lib/data/database/tables/daily_streak_table.dart
@lib/data/database/tables/daily_checkins_table.dart
</context>

<interfaces>
<!-- Schemas extracted from lib/data/database/tables/ — every Wave 0 test references these column names. -->

From lib/data/database/tables/daily_streak_table.dart:
- Table DailyStreak with columns: id (int autoInc PK), entryId (int FK→BlockList, onDelete cascade), day (DateTime), status (int: 0=success / 1=broken / 2=incomplete-data / 3=pending), source (int: 0=system-confirmed / 1=self-reported-only), usageMinutesObserved (int default 0), evaluatedAt (DateTime)
- Unique key: {entryId, day}

From lib/data/database/tables/daily_checkins_table.dart:
- Table DailyCheckins with columns: id (int autoInc PK), entryId (int FK→BlockList, onDelete cascade), day (DateTime), avoided (bool), answeredAt (DateTime)
- Unique key: {entryId, day}

From lib/domain/schedule/streak_day.dart (existing helper consumed by every streak test):
- DateTime streakDayFor(DateTime now) — strips to local midnight after 04:00 shift; pure function

From test/_fixtures/permission_status_mock.dart (existing Phase 2 fixture):
- MockPermissionStatusApi extends Mock implements PermissionStatusApi
- buildMockPermissionStatusApi({usageAccess, accessibility, batteryOpt, fingerprint}) factory
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Stub all 14 Phase 5 RED test files + shared fixture + REL-05 verification skeleton</name>
  <files>test/data/dao/daily_streak_dao_test.dart, test/data/dao/daily_checkins_dao_test.dart, test/domain/streak/streak_rollover_service_test.dart, test/domain/streak/streak_rollover_dst_test.dart, test/features/streak/streak_badge_test.dart, test/features/streak/streak_history_section_test.dart, test/features/checkin/checkin_screen_test.dart, test/features/reminder/reminder_settings_test.dart, test/features/reminder/deep_link_navigation_test.dart, test/features/onboarding/post_notifications_earned_test.dart, test/features/home/reminder_off_banner_test.dart, test/policy/phase_5_invariants_test.dart, test/_fixtures/streak_fixture.dart, android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/StreakDayAnchoringTest.kt, .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md</files>
  <read_first>
    .planning/phases/05-streak-engine-daily-reminder/05-VALIDATION.md (Wave 0 file list + Per-Task Verification Map),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §9 (test name lock for every REQ-ID),
    .planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md (every analog file listed for tests),
    .planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md §Copywriting Contract (all locked copy that widget tests must assert),
    .planning/phases/04-pause-ux-the-wedge/04-01-PLAN.md (Wave 0 RED stub template — mirror exactly),
    .planning/phases/04-pause-ux-the-wedge/04-VERIFICATION.md (REL-04 template — REL-05 mirrors structure),
    test/policy/phase_4_invariants_test.dart (analog for phase_5_invariants_test.dart — same File.readAsStringSync + Directory.listSync pattern),
    test/policy/play_invariants_test.dart (verify it stays at 9/9 invariants — Wave 0 must not break it),
    test/_fixtures/permission_status_mock.dart (analog for streak_fixture.dart — copy MockPermissionStatusApi factory shape),
    test/features/home/health_banner_test.dart (analog for every widget test — ProviderScope + GoRouter scaffold pattern),
    test/data/repositories/pause_event_repository_test.dart (analog for both DAO tests — in-memory AppDatabase(NativeDatabase.memory()) + FK seed),
    test/domain/schedule/schedule_window_parity_test.dart (analog for streak_rollover_dst_test.dart — GROUP 5 DST tuples are the pattern),
    android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/ScheduleWindowTest.kt (analog for StreakDayAnchoringTest.kt — JUnit 4 + Calendar.getInstance ms helper),
    lib/data/database/tables/daily_streak_table.dart (column names referenced by daily_streak_dao_test.dart),
    lib/data/database/tables/daily_checkins_table.dart (column names referenced by daily_checkins_dao_test.dart)
  </read_first>
  <action>
Create each test file as a minimal compiling skeleton with named `group(...)` declarations that map 1:1 to the Phase 5 REQ-IDs listed in 05-VALIDATION.md "Per Requirements → Test Map" and "Wave 0 Requirements". Use `skip: 'Plan 05-NN fills'` on every test body so `flutter test` exits 0 today.

File-by-file specification:

(a) test/data/dao/daily_streak_dao_test.dart — copy analog test/data/repositories/pause_event_repository_test.dart imports + setUp scaffolding (AppDatabase(NativeDatabase.memory()), block_list FK seed). Stub groups: 'DailyStreakDao.upsert idempotent per (entryId, day) (STRK-01)', 'DailyStreakDao.upsertIfAbsent does not overwrite (STRK-05 today=pending)', 'DailyStreakDao.getCurrentStreakFor + getLongestStreakFor (STRK-07)', 'DailyStreakDao.watchHistoryFor(entryId, days: 30) (D-16 history)'. All tests `skip: 'Plan 05-02 fills'`.

(b) test/data/dao/daily_checkins_dao_test.dart — same scaffold. Stub groups: 'DailyCheckinsDao.upsert idempotent per (entryId, day) (STRK-03)', 'DailyCheckinsDao.getFor(entryId, day) returns null on miss', 'DailyCheckinsDao.upsert roundtrips avoided bool'. All tests `skip: 'Plan 05-02 fills'`.

(c) test/domain/streak/streak_rollover_service_test.dart — copy test/data/repositories/pause_event_repository_test.dart in-memory Drift scaffold; import test/_fixtures/streak_fixture.dart. Stub groups: 'StreakRolloverService breaks when usage exceeds threshold (STRK-02) — test_breaks_when_usage_exceeds_threshold', 'StreakRolloverService 2x2 source resolution matrix (STRK-04) — test_source_resolution_matrix', 'StreakRolloverService clock-tamper > 24h flags status=2 (STRK-06) — test_clock_tamper_flags_status_2', 'StreakRolloverService scheduled-entry window anchoring (STRK-09) — test_scheduled_window_anchoring', 'StreakRolloverService idempotent under repeated rollover() in <60s (D-12 soft-cache)'. All tests `skip: 'Plan 05-03 fills'`. Test names MUST match RESEARCH §9 verbatim because Plan 05-09 greps them.

(d) test/domain/streak/streak_rollover_dst_test.dart — copy GROUP 5 _Tuple harness from test/domain/schedule/schedule_window_parity_test.dart lines 436-447. Stub groups: 'StreakRolloverService DST spring-forward 23h day (STRK-08) — test_spring_forward_23h_day', 'StreakRolloverService DST fall-back 25h day (STRK-08) — test_fall_back_25h_day'. Use DateTime(2026, 3, 8) (spring forward) and DateTime(2025, 11, 2) (fall back). All tests `skip: 'Plan 05-03 fills'`.

(e) test/features/streak/streak_badge_test.dart — copy ProviderScope + MaterialApp + ConsumerWidget harness from test/features/home/health_banner_test.dart. Stub groups: 'StreakBadge renders 🔥 3 · best 12 when current=3, longest=12 (STRK-07 + D-14)', 'StreakBadge renders 🔥 0 · best 0 on day-0 (D-15)', 'StreakBadge renders strikethrough on break-detection day (D-05)'. Assert copy literals exactly per UI-SPEC §Copywriting Contract. All tests `skip: 'Plan 05-07 fills'`.

(f) test/features/streak/streak_history_section_test.dart — same harness. Stub groups: 'StreakHistorySection renders 30-day 7-col GridView (D-16)', 'StreakHistorySection renders 4 dot states (D-07 — green/blue/red/grey)', 'StreakHistorySection grey dot has Tooltip "Tracking was off this day" (D-07 mandatory)', 'StreakHistorySection summary text "{N} days · best {M}" (UI-SPEC copy)'. All tests `skip: 'Plan 05-07 fills'`.

(g) test/features/checkin/checkin_screen_test.dart — copy Scaffold + Notifier harness from test/features/pause/pause_screen_test.dart. Stub groups: 'CheckinScreen lists pending entries with Yes/No SegmentedButton (D-02 / D-03)', 'CheckinScreen Save check-in writes N rows in one Drift transaction (D-03)', 'CheckinScreen idempotent — already-answered entries show locked SegmentedButton (D-01)', 'CheckinScreen empty state heading "You\'re all caught up" + body "Check back tomorrow." (UI-SPEC copy)', 'CheckinScreen error shows SnackBar "Something went wrong — your check-in wasn\'t saved. Try again." (UI-SPEC copy)'. All tests `skip: 'Plan 05-06 fills'`.

(h) test/features/reminder/reminder_settings_test.dart — same harness. Stub groups: 'ReminderSettingsScreen shows ListTile "Daily reminder" subtitle in DateFormat.jm() (NOTF-01)', 'ReminderSettingsScreen showTimePicker persists hour*60+minute to prefs key reminder_hour_minute (NOTF-01)', 'ReminderSettingsScreen onPick calls NotificationApi.scheduleDailyReminder(h, m)'. All tests `skip: 'Plan 05-08 fills'`.

(i) test/features/reminder/deep_link_navigation_test.dart — same harness with GoRouter scaffold. Stub groups: 'pending_deep_link=/checkin in SharedPreferences routes to /checkin on Home initState (NOTF-03)', 'deep_link is cleared after consumption (idempotent, no re-route loop)'. All tests `skip: 'Plan 05-05 fills'`.

(j) test/features/onboarding/post_notifications_earned_test.dart — same harness. Stub groups: 'earned prompt fires on first block_list insert (NOTF-06) — test_prompt_fires_on_first_insert', 'earned prompt suppressed after StreakKeys.earnedPromptShown=true (NOTF-06 fire-once)', 'rationale screen body matches RESEARCH §6 copy verbatim'. All tests `skip: 'Plan 05-08 fills'`.

(k) test/features/home/reminder_off_banner_test.dart — copy from test/features/home/health_banner_test.dart. Stub groups: 'ReminderOffBanner visible when isPostNotificationsGranted=false (NOTF-07) — test_banner_visible_when_denied', 'ReminderOffBanner hidden when isPostNotificationsGranted=true (NOTF-07) — test_banner_hidden_when_granted', 'ReminderOffBanner copy exact "Reminder is off — tap to fix" (UI-SPEC copy lock)', 'ReminderOffBanner tap on permanently_denied → openAppNotificationSettings; on rationale → /onboarding/permissions/notifications'. All tests `skip: 'Plan 05-07 fills'`.

(l) test/policy/phase_5_invariants_test.dart — copy test/policy/phase_4_invariants_test.dart shell. Implement REAL (non-skipped) tests now because the targets are existing artifacts (AndroidManifest + zero-source absence): 'NOTF-05: BootReceiver declared with BOOT_COMPLETED intent-filter' — File.readAsStringSync('android/app/src/main/AndroidManifest.xml'), assert contains 'android:name=".receiver.BootReceiver"' OR document-but-skip until Plan 05-05 adds the receiver (mark skip: 'Plan 05-05 fills receiver declaration'). 'PLAY-Phase5: no USE_EXACT_ALARM permission' — assert manifest does NOT contain 'USE_EXACT_ALARM' (REAL test — runs today, passes today). 'no FCM / firebase_messaging imports under lib/' — Directory.listSync(recursive) over lib/, skip .g.dart, assert no 'firebase_messaging' or 'FirebaseMessaging' (REAL — passes today). 'STRK-05: no WorkManager periodic for streak rollover' — Directory.listSync(recursive) over android/app/src/main/kotlin, for any file under streak/ path assert no 'PeriodicWorkRequest' (REAL — passes today, no streak/ path exists yet). 'PLAY-Phase5: notification body must not contain entry names' — grep lib/ for hardcoded entry-name patterns in notification builder calls (mark skip: 'Plan 05-05 fills notification body — privacy check applied then').

(m) test/_fixtures/streak_fixture.dart — copy MockPermissionStatusApi factory shape from test/_fixtures/permission_status_mock.dart. Export: buildClockMockApi({int bootMonotonicNanos = 0}) returning MockPermissionStatusApi with when(() => m.bootMonotonicNanos()).thenAnswer((_) async => bootMonotonicNanos). Add the 6-cell 2x2 source-resolution matrix as a top-level const List<({int usageMinutes, bool? checkinAvoided, bool a11yWasOn, int expectedStatus, int expectedSource})> with the cells from RESEARCH §4 _resolveDay docstring (broken-by-system, broken-by-self-report, success-system-confirmed, success-self-reported, fallback-no-checkin-a11y-on, incomplete-data-no-checkin-no-a11y).

(n) android/app/src/test/kotlin/com/nottodo/not_to_do_list/service/StreakDayAnchoringTest.kt — copy ScheduleWindowTest.kt JUnit 4 scaffold + ms(year, month, day, hour, minute) helper. Stub @Test methods: crossMidnightStartDayBelongsToStartStreakRow_2200_to_0600(), sameDayWindowBelongsToOwnStreakRow(), allwaysOnEntryBelongsToCalendarDay() — every method body is `// @Ignore until Plan 05-03 ships streakDayFor parity — see RESEARCH §4 STRK-09` plus throw NotImplementedError(). Use @Ignore("Plan 05-03 fills") on each method so ./gradlew :app:testDebugUnitTest passes today.

(o) .planning/phases/05-streak-engine-daily-reminder/05-VERIFICATION.md — copy 04-VERIFICATION.md skeleton: YAML frontmatter (phase: 05-streak-engine-daily-reminder, status: draft, nyquist_compliant: false, wave_0_complete: false, rel_05_status: pending_overnight_run, rel_05_device_class: TBD, rel_05_device_model: TBD, rel_05_alarm_fire_delta_minutes: TBD, rel_05_boot_completed_re_arm: TBD, rel_05_pass_date: TBD). Body: Phase 5 Verification Map table with one row per STRK-01..09 + NOTF-01..07 + REL-05 referencing the Plan-05-NN that fills each (matches RESEARCH §11 ordering). REL-05 OEM-Survival Overnight Gate section with the 9-step protocol mirroring 04-VERIFICATION.md lines 51-67 but updated for REL-05: (1) flutter build apk + adb install -r, (2) add 1 entry + grant POST_NOTIFICATIONS via earned prompt + open Settings → set reminder for T+2 min, (3) adb shell dumpsys deviceidle force-idle, (4) unplug + lock + idle ≥8h overnight, (5) plug back in next morning, (6) confirm notification fires within 5 min of scheduled time (NOTF-04), (7) tap notification → assert /checkin appears (NOTF-03), (8) reboot device, (9) wait one full day → confirm next-day reminder fires (NOTF-05 BOOT_COMPLETED re-arm). Pass criterion: alarm fires within 5 min of scheduled time, /checkin opens on tap, BOOT_COMPLETED re-arm fires next day. Device record table with same fields as REL-04 plus alarm-fire-delta and re-arm outcome.

Naming + style notes: every test file ends with `_test.dart`. Use `import 'package:flutter_test/flutter_test.dart';` and `mocktail ^1.0.5` for mocks. Drift in-memory uses `import 'package:drift/native.dart';` + `AppDatabase(NativeDatabase.memory())`. JUnit 4 file: `import org.junit.Test; import org.junit.Ignore; import org.junit.Assert.*`.

Verbatim copy lock: every widget test that asserts a string MUST pull it from 05-UI-SPEC.md §Copywriting Contract — NO paraphrase. The test is the lock.
  </action>
  <verify>
    <automated>flutter test test/policy/phase_5_invariants_test.dart test/policy/play_invariants_test.dart 2>&amp;1 | tail -20 ; flutter test 2>&amp;1 | tail -5 ; cd android &amp;&amp; ./gradlew :app:testDebugUnitTest --tests "com.nottodo.not_to_do_list.service.StreakDayAnchoringTest" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at every path in `files_modified`.
    - `flutter test` exits 0 with no compile errors; skip count increases by ~30+ (one per stubbed group); pass count stays ≥ Phase 4 baseline.
    - `flutter test test/policy/play_invariants_test.dart` still shows 9 invariants passing (no regression).
    - `flutter test test/policy/phase_5_invariants_test.dart` runs and shows ≥3 REAL (non-skipped) PASSING invariants today: "PLAY-Phase5: no USE_EXACT_ALARM", "no FCM / firebase_messaging imports", "STRK-05: no WorkManager periodic for streak rollover". Remaining 2 invariants (BootReceiver declared, notification body privacy) are marked `skip: 'Plan 05-05 fills'`.
    - `cd android && ./gradlew :app:testDebugUnitTest --tests "com.nottodo.not_to_do_list.service.StreakDayAnchoringTest"` BUILD SUCCESSFUL with all @Test methods @Ignore'd.
    - Every Phase 5 REQ-ID appears in at least one test file as a literal substring in a group name — verifiable by: `for id in STRK-01 STRK-02 STRK-03 STRK-04 STRK-05 STRK-06 STRK-07 STRK-08 STRK-09 NOTF-01 NOTF-02 NOTF-03 NOTF-04 NOTF-05 NOTF-06 NOTF-07; do grep -r "$id" test/ | head -1; done` — every line non-empty.
    - 05-VERIFICATION.md contains the literal string "REL-05" at least 5 times (header, frontmatter field, gate section title, status line, sign-off section).
    - `test/_fixtures/streak_fixture.dart` exports `buildClockMockApi` — verified by `grep -c 'buildClockMockApi' test/_fixtures/streak_fixture.dart` ≥ 1.
    - No file in `lib/` is added or modified (Wave 0 is test-only).
  </acceptance_criteria>
  <done>15 RED stubs landed; flutter test green; gradle test green; phase_5 invariants test in place with ≥3 real-and-passing absence-greps; 05-VERIFICATION.md REL-05 protocol skeleton ready for Plan 05-09 sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test/ ↔ lib/ | Wave 0 test files reference (but do not depend on) lib/ source that does not exist yet — every reference is behind `skip:` |
| test/ ↔ android/app/src/main/AndroidManifest.xml | phase_5_invariants_test.dart reads manifest as text; manifest is trusted source-controlled artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | phase_5_invariants_test.dart absence-grep on `grep -c` of unfiltered file would count comment occurrences as failures | mitigate | Apply Nyquist rule "use `grep -v '^#'` before `grep -c token`" — every absence-grep filters comments first per CLAUDE.md absence-grep hygiene |
| T-05-02 | Information disclosure | Test fixture `streak_fixture.dart` references mocked entry names | accept | All entry names in tests are synthetic ("Instagram", "Doomscroll") per Phase 4 convention; no PII; no telemetry stack present |
| T-05-03 | Denial of service | Stubbed test names drift from REQ-IDs over time, breaking the test-locks-the-contract guarantee | mitigate | acceptance criterion grep-checks every REQ-ID appears at least once in test/ — fails today if names drift |
</threat_model>

<verification>
- `flutter test` exits 0
- `flutter test test/policy/phase_5_invariants_test.dart` exits 0 with ≥3 real-and-passing invariants
- `flutter test test/policy/play_invariants_test.dart` 9/9 green
- `cd android && ./gradlew :app:testDebugUnitTest --tests "*.StreakDayAnchoringTest"` BUILD SUCCESSFUL
- Every Phase 5 REQ-ID grep-positive in test/
</verification>

<success_criteria>
- 14 test files + 1 fixture file + 1 JVM oracle + 1 verification skeleton land.
- REL-05 sign-off template exists, ready for Plan 05-09 to populate.
- No production code under lib/ or android/app/src/main/ touched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-01-SUMMARY.md`
</output>
