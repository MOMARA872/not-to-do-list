---
phase: 05-streak-engine-daily-reminder
plan: 08
type: execute
wave: 6
depends_on: [05-02, 05-04, 05-05]
files_modified:
  - lib/features/reminder/pages/reminder_settings_screen.dart
  - lib/features/reminder/providers/reminder_providers.dart
  - lib/features/streak/providers/streak_threshold_provider.dart
  - lib/features/onboarding/pages/post_notifications_earned_step.dart
  - lib/data/repositories/block_list_repository.dart
  - lib/core/router/app_router.dart
  - lib/features/home/pages/home_screen.dart
  - test/features/reminder/reminder_settings_test.dart
  - test/features/onboarding/post_notifications_earned_test.dart
autonomous: true
requirements:
  - NOTF-01
  - NOTF-06
  - STRK-02
must_haves:
  truths:
    - "ReminderSettingsScreen exposes a ListTile titled 'Daily reminder' with subtitle showing the saved time formatted via DateFormat.jm() (e.g., '9:00 PM' in en-US, '21:00' in 24h locales)."
    - "Tapping the ListTile launches showTimePicker(initialTime: TimeOfDay(savedHour, savedMinute)); on non-null pick: reminderTimeProvider.set(hour*60+minute) writes to SharedPreferences key 'reminder_hour_minute' AND calls notificationApi.cancelDailyReminder() AND notificationApi.scheduleDailyReminder(hour, minute) atomically."
    - "Default reminder time is 21:00 — stored as 1260 under SharedPreferences key 'reminder_hour_minute' (D-09 lock)."
    - "ReminderSettingsScreen exposes a SECOND ListTile titled 'Streak threshold' (D-08) — placed directly below the 'Daily reminder' ListTile, mirroring its visual pattern. Subtitle shows the current value as 'Streak breaks after {N} min/day' where N reads from SharedPreferences key 'streak_threshold_minutes' (StreakKeys.streakThresholdMinutes, declared in Plan 05-02). Default is 5 (STRK-02 lock + D-08)."
    - "Tapping the 'Streak threshold' ListTile opens a numeric value selector (NumberPicker or a Dialog with a SpinBox-style int field) allowing the user to choose an integer in range [1, 60]. On select: streakThresholdProvider.set(newValue) writes to SharedPreferences key 'streak_threshold_minutes' atomically. No alarm-re-arm side effect (threshold is read by the streak rollover service on every lazy evaluation — no scheduled component to cancel)."
    - "Streak threshold input validation: values < 1 or > 60 are clamped before write — defensive against rogue input despite the picker UI's own bounds. Tested at the provider boundary via a `clampStreakThreshold(int)` helper."
    - "PostNotificationsEarnedStep is a ConsumerStatefulWidget with WidgetsBindingObserver pattern — reuses RationaleScreen shell (Phase 2 ONBD-02 carry-forward)."
    - "Rationale screen body copy is verbatim: 'Get a daily reminder to confirm your not-to-do list. Without notifications, you\\'ll need to open the app to record each day.' (RESEARCH §6 line 621)."
    - "Primary CTA label is 'Continue'; onPressed calls await ref.read(permissionStatusApiProvider).requestPostNotifications(); on grant: writes StreakKeys.earnedPromptShown=true; pops to /."
    - "Earned prompt fires ONCE: BlockListRepository.add() (or insertEntry) post-write hook checks if (a) StreakKeys.earnedPromptShown == false AND (b) postNotificationsGrantedProvider == false AND (c) the just-inserted entry was the first row (block_list count transitioned 0→1); if all three, navigates to '/onboarding/permissions/notifications'. After fire (regardless of grant outcome): writes earnedPromptShown=true."
    - "Route GoRoute(path: '/onboarding/permissions/notifications', builder: (_, __) => const PostNotificationsEarnedStep()) added to app_router.dart — this is the destination for both the earned-prompt fire-once path AND the NOTF-07 banner non-permanently-denied re-prompt path (Plan 05-07 banner)."
    - "HomeScreen exposes the reminder settings as an AppBar action OR overflow menu item routing to '/settings/reminder' (RESEARCH §14 OQ #2 — Plan 05-08 ships the minimal route; Phase 6 reorganizes into a proper Settings screen)."
    - "9 PLAY-02 invariants stay green; phase_5_invariants_test.dart all invariants stay green."
  artifacts:
    - path: "lib/features/reminder/pages/reminder_settings_screen.dart"
      provides: "Minimal /settings/reminder screen — Scaffold + AppBar + reminder ListTile + streak threshold ListTile (D-08)"
      contains: "class ReminderSettingsScreen"
    - path: "lib/features/reminder/providers/reminder_providers.dart"
      provides: "ReminderTimeNotifier AsyncNotifier<int> with set(int hm) writes prefs + re-schedules alarm"
      contains: "ReminderTimeNotifier"
    - path: "lib/features/streak/providers/streak_threshold_provider.dart"
      provides: "StreakThresholdNotifier AsyncNotifier<int> with set(int minutes) writes prefs (no alarm side effect) + clampStreakThreshold helper"
      contains: "StreakThresholdNotifier"
    - path: "lib/features/onboarding/pages/post_notifications_earned_step.dart"
      provides: "Earned POST_NOTIFICATIONS rationale + system-prompt flow"
      contains: "class PostNotificationsEarnedStep"
    - path: "lib/data/repositories/block_list_repository.dart"
      provides: "Post-insert hook for earned-prompt fire-once trigger"
      contains: "earnedPromptShown"
  key_links:
    - from: "lib/data/repositories/block_list_repository.dart"
      to: "lib/features/onboarding/pages/post_notifications_earned_step.dart"
      via: "Riverpod listener on insertEntry success + count==1 + earnedPromptShown==false + isPostNotificationsGranted==false → context.go('/onboarding/permissions/notifications')"
      pattern: "earnedPromptShown.*go.*notifications"
    - from: "lib/features/reminder/pages/reminder_settings_screen.dart"
      to: "lib/domain/providers/notification_api_provider.dart"
      via: "ref.read(notificationApiProvider).scheduleDailyReminder + cancelDailyReminder"
      pattern: "notificationApiProvider"
    - from: "lib/features/reminder/pages/reminder_settings_screen.dart"
      to: "lib/features/streak/providers/streak_threshold_provider.dart"
      via: "ref.read(streakThresholdProvider.notifier).set(int) on numeric picker confirm"
      pattern: "streakThresholdProvider"
---

# Plan 05-08 — Wave 6: Reminder time picker (NOTF-01) + Streak threshold picker (D-08) + earned POST_NOTIFICATIONS prompt (NOTF-06)

<objective>
Ship the three remaining Phase 5 UI surfaces: (1) a minimal `/settings/reminder` route with a ListTile time picker that persists `reminder_hour_minute` AND re-schedules the AlarmManager alarm in one atomic flow, (2) a second ListTile on the same screen for the streak threshold (D-08) that lets the user change `streak_threshold_minutes` in the range [1, 60] with default 5, and (3) the earned POST_NOTIFICATIONS prompt route that fires ONCE after the user's first not-to-do entry insertion (NOTF-06).

This plan runs in Wave 6 — sequentialized after Plans 05-06 (/checkin, also edits app_router.dart) and 05-07 (Home banner, also edits home_screen.dart). Both files are shared, so 05-08 cannot run parallel-safe with them.

The reminder settings screen is intentionally minimal — Phase 6 (Polish & Play Store Submission) reorganizes into a proper Settings screen. Plan 05-08 ships the minimum two pickers + persist + re-arm flow + an AppBar entry on Home to reach it (RESEARCH §14 OQ #2).

Purpose: deliver NOTF-01 user-configurable time + D-08 user-configurable streak threshold + NOTF-06 earned-prompt + complete the Phase 5 UI surface.
Output: 4 new feature files (reminder screen + 2 providers + earned-prompt page) + 1 modified repository + 1 router edit + 1 HomeScreen edit + 2 GREEN unskipped Wave 0 test files (the reminder_settings_test now also covers D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md
@.planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md
@.planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md
@.planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md
@lib/core/router/app_router.dart
@lib/features/home/pages/home_screen.dart
@lib/features/list/widgets/schedule_editor.dart
@lib/features/onboarding/pages/usage_access_step.dart
@lib/features/onboarding/widgets/rationale_screen.dart
@lib/features/onboarding/providers/onboarding_cursor_provider.dart
@lib/features/onboarding/storage_keys.dart
@lib/data/repositories/block_list_repository.dart
@lib/features/streak/storage/streak_keys.dart
@lib/domain/providers/notification_api_provider.dart
@lib/features/onboarding/providers/post_notifications_provider.dart
@lib/domain/providers/permission_status_api_provider.dart
</context>

<interfaces>
<!-- Riverpod + Pigeon contracts. RationaleScreen shell exists from Phase 2; reused verbatim. -->

From lib/domain/providers/notification_api_provider.dart (Plan 05-04):
- notificationApiProvider → Provider<NotificationApi>
- NotificationApi.scheduleDailyReminder(int hour, int minute) Future<void>
- NotificationApi.cancelDailyReminder() Future<void>

From lib/features/streak/storage/streak_keys.dart (Plan 05-02 — ALL three keys already declared):
- StreakKeys.reminderHourMinute = 'reminder_hour_minute'
- StreakKeys.streakThresholdMinutes = 'streak_threshold_minutes'   // D-08 — consumed by this plan's threshold picker
- StreakKeys.earnedPromptShown = 'post_notifications_earned_prompt_shown'

From lib/features/onboarding/widgets/rationale_screen.dart (Phase 2 — analog shell, copy verbatim):
- RationaleScreen(headline, body, primaryCtaLabel, onPrimaryCta) — full-screen ConsumerStatefulWidget

From lib/features/onboarding/pages/usage_access_step.dart (Phase 2 — analog for resumed-detection pattern):
- ConsumerStatefulWidget with WidgetsBindingObserver
- initState calls _checkAndMaybeAdvance(); didChangeAppLifecycleState resumed re-checks

From lib/features/list/widgets/schedule_editor.dart (Phase 2 — analog for showTimePicker):
- `final picked = await showTimePicker(context: ctx, initialTime: TimeOfDay(...));`
- `if (picked == null) return;`
- Pre-existing pattern lines 49-61

From lib/data/repositories/block_list_repository.dart (existing Phase 2 repo):
- insertEntry / insertMany / add — these are the hooks for the post-write trigger

D-08 streak threshold contract (from 05-CONTEXT.md):
- Single global `streak_threshold_minutes` setting (no per-entry override in v1).
- Default 5 (STRK-02 lock).
- Surfaced in Settings (this plan ships the minimum picker; Phase 6 reorganizes into a "Streak" section).
- Help text already in entry-edit screen: "Streak breaks if you use this app over 5 min/day (change in Settings)" — Plan 05-07 ships this; this plan ships the Settings UI it points at.
- Valid range chosen for v1: [1, 60] minutes. Below 1 makes no sense; above 60 (1 hour) is past the point where "streak" semantically applies to mindful avoidance.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land reminder settings (time picker + D-08 streak threshold picker) + earned POST_NOTIFICATIONS prompt; flip 2 Wave 0 stubs to GREEN; wire post-insert hook + 2 new routes + Home AppBar action</name>
  <files>lib/features/reminder/providers/reminder_providers.dart, lib/features/streak/providers/streak_threshold_provider.dart, lib/features/reminder/pages/reminder_settings_screen.dart, lib/features/onboarding/pages/post_notifications_earned_step.dart, lib/data/repositories/block_list_repository.dart, lib/core/router/app_router.dart, lib/features/home/pages/home_screen.dart, test/features/reminder/reminder_settings_test.dart, test/features/onboarding/post_notifications_earned_test.dart</files>
  <read_first>
    .planning/phases/05-streak-engine-daily-reminder/05-CONTEXT.md (D-08 streak threshold settings-only global default 5, D-09 reminder default 21:00 + key name lock, NOTF-06 earned-prompt-after-first-entry rule),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §6 (POST_NOTIFICATIONS earned-prompt 3-state UX + rationale screen contract + fire-once trigger pattern),
    .planning/phases/05-streak-engine-daily-reminder/05-RESEARCH.md §14 OQ #2 (Plan 05-08 ships minimal /settings/reminder; Phase 6 reorganizes),
    .planning/phases/05-streak-engine-daily-reminder/05-UI-SPEC.md §Screen 4 (Reminder picker contract — DateFormat.jm() display, showTimePicker, default hour:21 minute:0),
    .planning/phases/05-streak-engine-daily-reminder/05-PATTERNS.md §"reminder_settings_screen.dart" + §"post_notifications_earned_step.dart" + §"reminder_providers.dart" (analogs spelled out),
    lib/core/router/app_router.dart (append 2 new routes after Plan 05-06's /checkin),
    lib/features/home/pages/home_screen.dart (AppBar — add an action linking to /settings/reminder),
    lib/features/list/widgets/schedule_editor.dart (showTimePicker pattern lines 49-61),
    lib/features/onboarding/pages/usage_access_step.dart (WidgetsBindingObserver pattern lines 20-86 — _checkAndMaybeAdvance on resumed),
    lib/features/onboarding/widgets/rationale_screen.dart (the full shell to reuse for the earned prompt),
    lib/features/onboarding/providers/onboarding_cursor_provider.dart (AsyncNotifier over single prefs int — exact analog for ReminderTimeNotifier AND StreakThresholdNotifier — lines 10-33),
    lib/features/onboarding/storage_keys.dart (constants pattern),
    lib/features/streak/storage/streak_keys.dart (the key constants — StreakKeys.streakThresholdMinutes is the D-08 key, already declared by Plan 05-02),
    lib/data/repositories/block_list_repository.dart (existing add/insertEntry surface — add the post-write hook),
    lib/domain/providers/notification_api_provider.dart (Plan 05-04),
    lib/features/onboarding/providers/post_notifications_provider.dart (Plan 05-04 — postNotificationsGrantedProvider),
    lib/domain/providers/permission_status_api_provider.dart (Phase 2 + Plan 05-04 — for requestPostNotifications + isPostNotificationsGranted)
  </read_first>
  <behavior>
    - ReminderTimeNotifier extends AsyncNotifier<int>: build() reads prefs[reminder_hour_minute] ?? 1260; set(int hm) writes prefs[reminder_hour_minute] = hm, calls notificationApi.cancelDailyReminder(); notificationApi.scheduleDailyReminder(hm ~/ 60, hm % 60); state = AsyncData(hm).
    - StreakThresholdNotifier extends AsyncNotifier<int>: build() reads prefs[StreakKeys.streakThresholdMinutes] ?? 5; set(int minutes) clamps via `clampStreakThreshold(minutes)` then writes prefs[StreakKeys.streakThresholdMinutes] = clamped; state = AsyncData(clamped). No alarm-re-arm side effect — the streak rollover service reads the threshold on every lazy evaluation, so the next AppLifecycleState.resumed rollover picks up the new value naturally.
    - Top-level helper `int clampStreakThreshold(int raw) => raw.clamp(1, 60);` exposed from streak_threshold_provider.dart for direct unit testing.
    - ReminderSettingsScreen: Scaffold(appBar: AppBar(title: Text('Daily reminder')), body: ListView with TWO ListTiles in this order:
      1. ListTile #1 (existing time picker) — leading Icon(Icons.notifications_outlined), title 'Daily reminder', subtitle Text(DateFormat.jm().format(DateTime(2000, 1, 1, h, m))) (any date — only h/m matters for jm), trailing Icon(Icons.chevron_right), onTap: showTimePicker → on non-null pick → reminderTimeProvider.notifier.set(picked.hour * 60 + picked.minute)).
      2. ListTile #2 (NEW — D-08 threshold picker) — leading Icon(Icons.timer_outlined), title 'Streak threshold', subtitle Text('Streak breaks after $N min/day') where N is the current value from streakThresholdProvider, trailing Icon(Icons.chevron_right), onTap: opens a numeric value selector (use Flutter's `showDialog` with a NumberPicker widget, OR a SimpleDialog containing a Slider with discrete labels 1..60, OR an AlertDialog with a TextField configured for int input + done button — choose the approach that fits the existing app's dialog conventions, see PATTERNS.md). On confirm with a value in [1, 60]: `ref.read(streakThresholdProvider.notifier).set(value)`. On dialog cancel: no-op. On invalid input (out of range): the clamp helper enforces bounds before write.
    - PostNotificationsEarnedStep: full-screen ConsumerStatefulWidget reusing RationaleScreen shell. Body copy = the RESEARCH §6 line 621 verbatim. CTA 'Continue' triggers `final granted = await api.requestPostNotifications(); await prefs.setBool(StreakKeys.earnedPromptShown, true); if (granted && context.mounted) { await ref.read(reminderTimeProvider.notifier).set(prefs.getInt(StreakKeys.reminderHourMinute) ?? 1260); ref.read(postNotificationsGrantedProvider.notifier).refresh(); } if (context.mounted) context.go('/');`. WidgetsBindingObserver: on resumed re-check isPostNotificationsGranted; if granted, write earnedPromptShown=true and pop. This resumed re-check is the mitigation for T-05-16 (semantic gap in requestPostNotifications — see Plan 05-04 threat register).
    - BlockListRepository.add (or insertEntry): after a successful insert, check `final count = await dao.count()` (or equivalent). If `count == 1` AND `await prefs.getBool(StreakKeys.earnedPromptShown) != true` AND `await api.isPostNotificationsGranted() == false`: write `prefs.setBool(StreakKeys.earnedPromptShown, true)` AND publish a Riverpod signal that triggers navigation. Pattern options:
      - Option A (cleaner Riverpod): expose a StreamController/StateProvider `postFirstEntryNavRequest` set to '/onboarding/permissions/notifications'; the Home screen has a `ref.listen` that consumes once + clears.
      - Option B (direct nav from repo): pass BuildContext via a callback — couples repo to context, less clean.
      Choose Option A. Implementation: add `final StateProvider<String?> pendingNavRequestProvider = StateProvider<String?>((_) => null);` in lib/core/router/ or a dedicated nav-signal file. Repo writes to it via ref.read after successful first-insert. HomeScreen subscribes via ref.listen and calls context.go on non-null value, then resets to null. On any second/third insert the count == 1 condition is false so the trigger does not re-fire.
    - app_router.dart additions: append after Plan 05-06's /checkin GoRoute:
      ```
      GoRoute(path: '/settings/reminder', builder: (_, __) => const ReminderSettingsScreen()),
      GoRoute(path: '/onboarding/permissions/notifications', builder: (_, __) => const PostNotificationsEarnedStep()),
      ```
      Preserve the onboarding redirect (T-2-10 lock) exactly — `/onboarding/*` is in the unfinished branch, but PostNotificationsEarnedStep is reached AFTER onboarding completion AND first entry. The redirect currently routes `completed && goingToOnboarding` → '/' which would prevent reaching '/onboarding/permissions/notifications' after onboarding completes. FIX: in the redirect logic, add a narrow exception — `if (completed && state.matchedLocation == '/onboarding/permissions/notifications') return null;`. This preserves T-2-10 for every other /onboarding/* path while enabling the earned-prompt route post-onboarding. Document the exception as a 1-liner comment.
    - HomeScreen AppBar: add an `actions: [IconButton(icon: Icons.notifications_outlined, tooltip: 'Daily reminder', onPressed: () => context.go('/settings/reminder'))]`. Use semanticLabel for a11y.
    - test/features/reminder/reminder_settings_test.dart unskip: tests covering NOTF-01 + D-08:
      - NOTF-01: initial subtitle reflects default 1260 → 'AM/PM' format; tap → showTimePicker invoked (mock testHooks); after pick → prefs.getInt(reminder_hour_minute) == new value AND notificationApi.scheduleDailyReminder called with the new (h, m) AND notificationApi.cancelDailyReminder called first.
      - D-08: `test_streak_threshold_tile_renders` — pumping the screen finds an exact `find.text('Streak threshold')` widget AND finds the subtitle `find.textContaining('Streak breaks after')` AND `find.textContaining('min/day')`.
      - D-08: `test_streak_threshold_default_is_5` — without seeding prefs, the subtitle text is 'Streak breaks after 5 min/day'.
      - D-08: `test_streak_threshold_picker_writes_prefs` — tap the tile → confirm picker dialog opens → select 10 → confirm → assert `prefs.getInt('streak_threshold_minutes') == 10` AND the subtitle updates to 'Streak breaks after 10 min/day'.
      - D-08: `test_streak_threshold_clamp_below_1` — `expect(clampStreakThreshold(0), 1); expect(clampStreakThreshold(-5), 1);`.
      - D-08: `test_streak_threshold_clamp_above_60` — `expect(clampStreakThreshold(61), 60); expect(clampStreakThreshold(120), 60);`.
      - D-08: `test_streak_threshold_clamp_valid_range_is_identity` — `for (var i = 1; i <= 60; i++) expect(clampStreakThreshold(i), i);`.
    - test/features/onboarding/post_notifications_earned_test.dart unskip: tests covering NOTF-06 — `test_prompt_fires_on_first_insert`: with StreakKeys.earnedPromptShown=false and isPostNotificationsGranted=false, inserting the FIRST block_list entry triggers pendingNavRequest = '/onboarding/permissions/notifications'; second-insert does NOT re-fire. `test_rationale_body_copy_exact`: pump PostNotificationsEarnedStep, assert find.text containing the exact body copy. `test_continue_button_triggers_request_and_writes_flag`: tap Continue → mock api.requestPostNotifications called, prefs.earnedPromptShown=true. `test_resumed_re_check_handles_late_grant` (T-05-16 mitigation): with permission initially denied, simulate AppLifecycleState.resumed after granting → assert the widget re-polls isPostNotificationsGranted and advances.
  </behavior>
  <action>
Implement the reminder + threshold + earned-prompt flow.

(a) lib/features/reminder/providers/reminder_providers.dart — new file. Pattern from onboarding_cursor_provider.dart (analog). Class ReminderTimeNotifier extends AsyncNotifier<int> with build() reading prefs default 1260 and set(int hm) that (1) writes prefs, (2) calls notificationApi.cancelDailyReminder, (3) calls scheduleDailyReminder, (4) updates state. Export `final AsyncNotifierProvider<ReminderTimeNotifier, int> reminderTimeProvider`.

(b) lib/features/streak/providers/streak_threshold_provider.dart — NEW file (D-08). Pattern from onboarding_cursor_provider.dart (analog). Implementation:
- Top-level pure helper `int clampStreakThreshold(int raw) => raw.clamp(1, 60);` — exported for direct unit test.
- Class StreakThresholdNotifier extends AsyncNotifier<int>:
  - `Future<int> build() async { final prefs = await SharedPreferences.getInstance(); return prefs.getInt(StreakKeys.streakThresholdMinutes) ?? 5; }`
  - `Future<void> set(int minutes) async { final clamped = clampStreakThreshold(minutes); final prefs = await SharedPreferences.getInstance(); await prefs.setInt(StreakKeys.streakThresholdMinutes, clamped); state = AsyncValue.data(clamped); }`
- Export `final AsyncNotifierProvider<StreakThresholdNotifier, int> streakThresholdProvider = AsyncNotifierProvider<StreakThresholdNotifier, int>(StreakThresholdNotifier.new);`.
- File-header doc-comment cites D-08 + STRK-02 (default 5) + the [1, 60] range rationale.

(c) lib/features/reminder/pages/reminder_settings_screen.dart — new file. Scaffold + AppBar 'Daily reminder' + body ListView with two ListTiles per UI-SPEC §Screen 4 + the new D-08 tile.
- ListTile #1 (Daily reminder): showTimePicker → ref.read(reminderTimeProvider.notifier).set(...).
- ListTile #2 (Streak threshold): on tap call `_showThresholdPicker(context, ref)` helper. The helper builds an `AlertDialog` (or equivalent dialog form fitting the app's existing dialog conventions) containing a numeric picker UI for [1, 60] with the current value pre-selected. On confirm: `ref.read(streakThresholdProvider.notifier).set(picked)`. On cancel: pop without write. Mirror the ListTile #1 visual pattern (same leading/trailing/title/subtitle layout). Use the current streakThresholdProvider value to render the subtitle as `Text('Streak breaks after $value min/day')`.

(d) lib/features/onboarding/pages/post_notifications_earned_step.dart — new file. ConsumerStatefulWidget mirroring usage_access_step.dart. Reuse RationaleScreen shell — pass headline + body + CTA + onPrimaryCta. WidgetsBindingObserver auto-advances on resumed if permission is granted (after the user grants in system Settings). This re-check is the mitigation for T-05-16 (Plan 05-04 threat) — document inline with a comment referencing T-05-16.

(e) lib/data/repositories/block_list_repository.dart — surgical post-insert hook. Locate the existing `add` or `insertEntry` method. After a successful insert returns, query count; if count == 1 AND earnedPromptShown==false AND isPostNotificationsGranted==false: write earnedPromptShown=true to prefs + write pendingNavRequest='/onboarding/permissions/notifications' via the StateProvider.

(f) lib/core/router/app_router.dart — append 2 GoRoutes; add narrow exception to redirect for /onboarding/permissions/notifications. Add imports.

(g) lib/features/home/pages/home_screen.dart — append AppBar action button routing to /settings/reminder. Add ref.listen on pendingNavRequestProvider firing context.go + clearing the request.

(h) test/features/reminder/reminder_settings_test.dart + test/features/onboarding/post_notifications_earned_test.dart — unskip; fill with tests asserting NOTF-01 + D-08 + NOTF-06 behaviors per behavior section.

Verify the redirect change does not regress T-2-10: write a single test (or extend an existing onboarding test) asserting that `/onboarding/welcome` still redirects when completed=true, but `/onboarding/permissions/notifications` does NOT.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs ; flutter test test/features/reminder/reminder_settings_test.dart test/features/onboarding/post_notifications_earned_test.dart ; flutter analyze lib/features/reminder/ lib/features/streak/providers/ lib/features/onboarding/pages/post_notifications_earned_step.dart lib/data/repositories/block_list_repository.dart lib/core/router/ lib/features/home/ ; flutter test test/policy/play_invariants_test.dart test/policy/phase_5_invariants_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/features/reminder/reminder_settings_test.dart test/features/onboarding/post_notifications_earned_test.dart` exits 0; no skip remaining in either file.
    - `flutter analyze lib/features/reminder/ lib/features/streak/providers/ lib/features/onboarding/pages/post_notifications_earned_step.dart lib/data/repositories/block_list_repository.dart lib/core/router/ lib/features/home/` shows 0 errors / 0 warnings.
    - `grep -c "GoRoute(path: '/settings/reminder'" lib/core/router/app_router.dart` >= 1.
    - `grep -c "GoRoute(path: '/onboarding/permissions/notifications'" lib/core/router/app_router.dart` >= 1.
    - `grep -c "matchedLocation == '/onboarding/permissions/notifications'" lib/core/router/app_router.dart` >= 1 (narrow redirect exception).
    - `grep -c "completed && goingToOnboarding" lib/core/router/app_router.dart` >= 1 (T-2-10 preserved alongside the exception).
    - `grep -c "Daily reminder" lib/features/reminder/pages/reminder_settings_screen.dart` >= 1 (AppBar + ListTile title).
    - `grep -c "DateFormat.jm" lib/features/reminder/pages/reminder_settings_screen.dart` >= 1.
    - `grep -c "showTimePicker" lib/features/reminder/pages/reminder_settings_screen.dart` >= 1.
    - `grep -c "'Streak threshold'" lib/features/reminder/pages/reminder_settings_screen.dart` >= 1 (D-08 literal title — exact copy).
    - `grep -c "Streak breaks after" lib/features/reminder/pages/reminder_settings_screen.dart` >= 1 (D-08 subtitle template).
    - `grep -c "streakThresholdProvider" lib/features/reminder/pages/reminder_settings_screen.dart` >= 1 (provider consumed).
    - `grep -c "clampStreakThreshold" lib/features/streak/providers/streak_threshold_provider.dart` >= 1 (helper exported).
    - `grep -c "raw.clamp(1, 60)" lib/features/streak/providers/streak_threshold_provider.dart` >= 1 (range enforced).
    - `grep -c "StreakKeys.streakThresholdMinutes" lib/features/streak/providers/streak_threshold_provider.dart` >= 1 (key from Plan 05-02 consumed).
    - `grep -c "cancelDailyReminder" lib/features/reminder/providers/reminder_providers.dart` >= 1 (cancel before schedule).
    - `grep -c "scheduleDailyReminder" lib/features/reminder/providers/reminder_providers.dart` >= 1.
    - `grep -c "Get a daily reminder to confirm your not-to-do list" lib/features/onboarding/pages/post_notifications_earned_step.dart` >= 1 (RESEARCH §6 line 621 exact copy).
    - `grep -c "Continue" lib/features/onboarding/pages/post_notifications_earned_step.dart` >= 1 (CTA label).
    - `grep -c "T-05-16" lib/features/onboarding/pages/post_notifications_earned_step.dart` >= 1 (resumed re-check is the mitigation — documented inline).
    - `grep -c "earnedPromptShown" lib/data/repositories/block_list_repository.dart` >= 1 (fire-once flag wired).
    - `grep -c "isPostNotificationsGranted" lib/data/repositories/block_list_repository.dart` >= 1 (gate check).
    - `grep -c "settings/reminder" lib/features/home/pages/home_screen.dart` >= 1 (AppBar action wired).
    - `flutter test test/policy/play_invariants_test.dart` 9/9 green.
    - `flutter test test/policy/phase_5_invariants_test.dart` all invariants green.
  </acceptance_criteria>
  <done>Reminder time picker persists + re-arms; streak threshold picker writes prefs in [1, 60] with default 5 (D-08); earned prompt fires once after first entry insertion + writes fire-once flag; both routes added with narrow redirect exception preserving T-2-10; UI-SPEC + RESEARCH §6 copy locked by tests; T-05-16 semantic-gap mitigation lives in the resumed re-check with inline cross-reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User taps reminder time picker ↔ AlarmManager re-arm | Bounded by Pigeon API; system dialog handles input validation |
| User taps streak threshold picker ↔ prefs write | In-process; clampStreakThreshold enforces [1, 60] at the provider boundary even if the dialog UI is bypassed |
| BlockListRepository.add ↔ Riverpod nav-signal | In-process pure-Dart; trusted |
| Router redirect ↔ /onboarding/permissions/notifications | Narrow allow-list exception; documented; tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-31 | Tampering | Future plan removes the narrow redirect exception, breaking the earned-prompt route post-onboarding | mitigate | acceptance grep on `matchedLocation == '/onboarding/permissions/notifications'` — fails today if removed; test asserts route is reachable when completed=true |
| T-05-32 | Tampering | Earned prompt fires multiple times across reinstall (user reflexively denies, app reinstalled, prompt fires again) | accept | Per CLAUDE.md "uninstall-resets-streak is by design" — same applies to earned-prompt flag; reinstall is a deliberate user signal |
| T-05-33 | Information disclosure | Reminder time stored in prefs reveals user's habit window | accept | Local-only; no telemetry; on-device only |
| T-05-34 | Denial of service | scheduleDailyReminder Result.failure("EXACT_ALARM_DENIED") on canScheduleExactAlarms revoke | mitigate | Caller catches and surfaces via NOTF-07 banner (Plan 05-07); reminder picker shows the error inline with retry; fall back to inexact alarm logic in Plan 05-05 |
| T-05-35 | Elevation of privilege | Earned-prompt navigation hijacked by a 3rd party intent | mitigate | All navigation is in-process via context.go; no Intent surface; no external entrypoint |
| T-05-36 | Tampering | Streak threshold value injected from a future programmatic path (e.g., a debug-only setter, a config import, or a malformed prefs file) lies outside the user-meaningful [1, 60] minute range — e.g., 0 (immediate break on any usage), -5 (nonsensical), 9999 (effectively disables the streak engine) | mitigate | Defense in depth: (a) the picker UI in this plan only exposes values in [1, 60], (b) `clampStreakThreshold(int)` enforces the range at the provider boundary on every write — any caller (including future ones) that bypasses the UI still gets clamped, (c) dedicated unit tests `test_streak_threshold_clamp_below_1` / `test_streak_threshold_clamp_above_60` / `test_streak_threshold_clamp_valid_range_is_identity` lock the behavior, (d) the rollover service in Plan 05-03 reads the prefs value directly, so the clamp is the single chokepoint — additional defensive clamps in the rollover service are not needed because the writer is the only path. |
</threat_model>

<verification>
- Reminder picker writes prefs + re-arms alarm in one atomic flow.
- Streak threshold picker writes prefs in [1, 60]; clamp enforced at the provider boundary (D-08).
- Earned prompt fires once on first insert, writes the flag, requests permission.
- Two new routes added; redirect exception narrow + tested.
- T-05-16 semantic-gap mitigation is wired via WidgetsBindingObserver resumed re-check with inline cross-reference.
</verification>

<success_criteria>
- NOTF-01 GREEN — user can change reminder time.
- D-08 GREEN — user can change streak threshold from Settings (range [1, 60], default 5).
- NOTF-06 GREEN — earned prompt fires after first entry.
- T-2-10 onboarding redirect lock preserved with narrow exception.
- T-05-16 semantic-gap (Plan 05-04) is mitigated end-to-end via the resumed re-check in this plan's PostNotificationsEarnedStep.
- Phase 5 UI surface complete; only REL-05 manual gate remains (Plan 05-09).
</success_criteria>

<output>
After completion, create `.planning/phases/05-streak-engine-daily-reminder/05-08-SUMMARY.md`
</output>
